# Upgrade sheet copy — Pro Plus / Projects

For issue #20011 (GTM-CMO), feeding the shared upgrade-sheet component
`#20010` is building (Claude.ai upgrade pattern: locked tabs inside a
Project carry a "Pro Plus" pill, one click opens this sheet). Price is a
token, resolved live from checkout config — never a literal.

## Title
Run the whole project, not just the bid.

## 6-line checklist
Each line is one capability in the Rehab-to-Exit suite, locked behind Pro
Plus for an Investor/Pro viewer; `#20010`'s component renders the tier
state (its job, not this file's) — this is the copy for the six rows.

1. Budget & scopes of work
2. Schedule
3. Ledger — receipts and budget-vs-actual
4. Lender draws
5. Funding pack from your SIGNAL$ Property Report
6. Exit pack

## CTA label (primary)
Upgrade to Pro Plus — `{{pricing.pro_plus.monthly}}`/mo
Links to `/subscribe?tier=proplus`.

## CTA label (secondary)
See a project
Opens the sample project, read-only (per `#20010` item 2).
