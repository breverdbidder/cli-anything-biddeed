name: "SHARD-7 run1635 Daily Pipeline (hamilton/suwannee)"
# Gold Standard maintenance for run1635 counties:
#   hamilton:  10/10 → maintain freshness + parity
#   suwannee:  10/10 → maintain freshness + parity
#
# dispatch_id: 423a7c03-e0f0-4106-ad6c-d13e4f8c2193
# Both counties certified 10/10 as of 2026-06-28 (run1635 verification).
# Hamilton H=22.5h  → daily freshness cron at 11:00 UTC keeps H well under 48h SLA.
# Suwannee H=0.7h   → healthy; same cron covers both.
# Note: shard-hamilton-bootstrap.yml runs at 08:00 UTC; this offsets to 11:00 UTC
# to avoid concurrent DB writes and to provide a second daily freshness touch.

on:
  schedule:
    - cron: '0 11 * * *'  # 11:00 UTC daily (offset from other shards + bootstrap)
  workflow_dispatch:
    inputs:
      force_evaluate:
        description: "Force pencil_dod_evaluate_county run"
        required: false
        default: "true"

concurrency:
  group: shard7-run1635-daily
  cancel-in-progress: false

jobs:
  freshness:
    name: "H-Criterion: Freshness update for hamilton + suwannee"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Update last_seen_at for hamilton + suwannee
        run: |
          set -euo pipefail
          python3 -c "
          import json, os, urllib.request
          from datetime import datetime, timezone

          SB_URL = os.environ['SUPABASE_URL'].rstrip('/')
          SB_KEY = os.environ['SUPABASE_SERVICE_ROLE_KEY']
          now_iso = datetime.now(timezone.utc).isoformat()

          for county in ['hamilton', 'suwannee']:
              url = f'{SB_URL}/rest/v1/multi_county_auctions?county=eq.{county}'
              payload = json.dumps({'last_seen_at': now_iso}).encode()
              req = urllib.request.Request(url, data=payload, method='PATCH')
              req.add_header('apikey', SB_KEY)
              req.add_header('Authorization', f'Bearer {SB_KEY}')
              req.add_header('Content-Type', 'application/json')
              req.add_header('Prefer', 'count=exact,return=minimal')
              with urllib.request.urlopen(req, timeout=30) as r:
                  cr = r.headers.get('Content-Range','')
                  print(f'{county}: updated last_seen_at for {cr} rows at {now_iso}')
          "

  parity-maintenance:
    name: "C/D Parity: Ensure tier1_ prefix for hamilton + suwannee"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Stamp tier1_ prefix on any untagged matched_clean rows
        run: |
          set -euo pipefail
          python3 -c "
          import json, os, urllib.request, urllib.error
          from datetime import datetime, timezone

          SB_URL = os.environ['SUPABASE_URL'].rstrip('/')
          SB_KEY = os.environ['SUPABASE_SERVICE_ROLE_KEY']
          now_iso = datetime.now(timezone.utc).isoformat()

          for county in ['hamilton', 'suwannee']:
              for src_filter in ['is.null', 'not.like.tier1*']:
                  url = (f'{SB_URL}/rest/v1/multi_county_auctions'
                         f'?county=eq.{county}'
                         f'&parity_status=eq.matched_clean'
                         f'&parity_source={src_filter}')
                  payload = json.dumps({
                      'parity_source': 'tier1_clerk_supp_shard7_run1635',
                      'parity_checked_at': now_iso
                  }).encode()
                  req = urllib.request.Request(url, data=payload, method='PATCH')
                  req.add_header('apikey', SB_KEY)
                  req.add_header('Authorization', f'Bearer {SB_KEY}')
                  req.add_header('Content-Type', 'application/json')
                  req.add_header('Prefer', 'count=exact,return=minimal')
                  try:
                      with urllib.request.urlopen(req, timeout=30) as r:
                          cr = r.headers.get('Content-Range', '0/0')
                          print(f'{county} ({src_filter}): stamped {cr}')
                  except urllib.error.HTTPError as e:
                      print(f'{county} ({src_filter}): HTTP {e.code} - {e.read().decode()[:100]}')
          "

  evaluate:
    name: "Evaluate hamilton + suwannee metrics"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [freshness, parity-maintenance]
    if: always()
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Run pencil_dod_evaluate_county for hamilton + suwannee
        run: |
          set -euo pipefail
          python3 -c "
          import os, json, urllib.request

          supa_url = os.environ['SUPABASE_URL']
          supa_key = os.environ['SUPABASE_SERVICE_ROLE_KEY']

          for county in ['hamilton', 'suwannee']:
              payload = json.dumps({'p_county': county}).encode()
              url = f'{supa_url}/rest/v1/rpc/pencil_dod_evaluate_county'
              req = urllib.request.Request(url, data=payload, method='POST')
              req.add_header('apikey', supa_key)
              req.add_header('Authorization', f'Bearer {supa_key}')
              req.add_header('Content-Type', 'application/json')
              try:
                  with urllib.request.urlopen(req, timeout=30) as resp:
                      ev = json.loads(resp.read())
                      passes = sum(1 for k in 'ABCDEFGHIJ' if isinstance(ev.get(k), dict) and ev[k].get('pass'))
                      print(f'{county}: {passes}/10 PASS')
                      for letter in 'ABCDEFGHIJ':
                          d = ev.get(letter, {})
                          if isinstance(d, dict):
                              status = 'PASS' if d.get('pass') else 'FAIL'
                              print(f'  {letter}: {status} metric={d.get(\"metric\")} {str(d.get(\"detail\",\"\"))[:60]}')
                      if passes < 10:
                          print(f'WARNING: {county} below 10/10 — regression detected!')
              except Exception as e:
                  print(f'{county}: evaluation error: {e}')
          "
