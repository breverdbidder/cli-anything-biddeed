name: "Test Agency Insurance (DISPOSABLE) Web CI"

on:
  push:
    paths: ['sites/test-agency-web/**']
    branches: [main]
  pull_request:
    paths: ['sites/test-agency-web/**']

defaults:
  run:
    working-directory: sites/test-agency-web

jobs:
  build:
    name: "Build"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: sites/test-agency-web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          PUBLIC_SITE_DOMAIN: test-agency-web.pages.dev
