name: CC Runner — GHA-only (no Hetzner)
on:
  workflow_dispatch:
    inputs:
      issues:
        description: 'Comma-separated issue numbers (REQUIRED)'
        required: true
        type: string
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Reject empty issues input
        run: |
          if [ -z "${{ inputs.issues }}" ]; then echo "::error::Empty issues input"; exit 1; fi
          echo "::notice::GHA-only CC for issues: ${{ inputs.issues }}"
  run-cc:
    needs: validate
    runs-on: ubuntu-latest
    # 330min = confirmed legitimate worst case (~300min weekly-quota-reset wait in the
    # retry loop below, see MAX_WAIT, historically hit exactly at 08:19-08:20 2026 run
    # 32230550433 which exited at the 300min mark) + ~30min buffer for setup/commit/retry
    # overhead. Historical p99 successful runtime is 89min, max 106min (129-run sample,
    # 2026-08-25..27) — the 300min+ tail is real and by-design, not padding: do not shrink
    # this to "30-60min" without first killing the weekly-limit sleep-and-retry behavior.
    timeout-minutes: 330
    concurrency:
      group: cc-ghonly-issue-${{ matrix.issue }}
      cancel-in-progress: false
    strategy:
      matrix:
        issue: ${{ fromJson(format('[{0}]', inputs.issues)) }}
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT_FULL }}
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Python + Playwright dependencies
        run: |
          pip install --quiet -r requirements.txt playwright
          playwright install --with-deps chromium
      - name: Install Claude Code
        run: |
          npm install -g @anthropic-ai/claude-code
          claude --version
      - name: Fetch issue brief
        env:
          GH_TOKEN: ${{ secrets.GH_PAT_FULL }}
        run: |
          {
            echo "NON-INTERACTIVE HEADLESS SESSION: this runs via \`claude -p\` with no human attached — nobody will read or answer any question you print. You already hold full standing authorization per this repo's CLAUDE.md (zero-HITL, execute-first, ship-to-main mandate, STANDING AUTHORIZATIONS section) — do not stop to ask for confirmation; there is no one to confirm with, and doing so wastes this attempt. If SUPABASE_DB_PASSWORD / direct psql fails (a known, long-standing, already-documented constraint — see decision_log ids 169/205/287 — not something to re-diagnose or wait on), that is expected: fall back to Supabase PostgREST (SUPABASE_SERVICE_ROLE_KEY) and RPC calls for all reads/writes, which is the established working pattern this pipeline runs on daily. Make the maximum safe, verified progress against the DoD below within this session and report exact evidence (before/after metrics). Only stop early for a genuine data ceiling (not a credential/authorization question) and document it inline in your final output — do not end the session by asking a question."
            echo
            gh issue view ${{ matrix.issue }} --repo ${{ github.repository }} --json title,body \
              -q '"# " + .title + "\n\n" + .body'
          } > /tmp/prompt.md
          echo "----- BRIEF -----"; cat /tmp/prompt.md
      - name: Run Claude Code (in-runner, OAuth token, NO Hetzner, retry on 5xx)
        # 315min budget, but MAX_WAIT below is capped at 200min (not 315) — see incident
        # 2026-08-27 issue #19511, run 33052164053: the step's timeout-minutes is measured
        # from STEP START, not from wake-up. A MAX_WAIT of 300min let the reset-sleep alone
        # consume ~298min of this 315min budget, leaving only ~16min for the actual claude -p
        # retry before this same timeout cancelled it mid-attempt with ZERO work done —
        # the entire 5.5h window wasted on a sleep that by design should have produced a
        # working session afterward. 200min MAX_WAIT guarantees >=110min of real working
        # time after any capped sleep (well above the 89min p99 / 106min max successful
        # runtime, 129-run sample 2026-08-25..27). If the real reset is >200min away, fail
        # fast instead of sleeping the whole window away — cc_redispatch_guard/tick already
        # retries blocked issues on a later wave, so a fast non-transient failure recovers
        # sooner than a multi-hour sleep that ends in the same zero-progress cancellation.
        timeout-minutes: 315
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          GH_TOKEN: ${{ secrets.GH_PAT_FULL }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
          REALFORECLOSE_EMAIL: ${{ secrets.REALFORECLOSE_EMAIL }}
          REALFORECLOSE_USERNAME: ${{ secrets.REALFORECLOSE_USERNAME }}
          REALFORECLOSE_PASSWORD: ${{ secrets.REALFORECLOSE_PASSWORD }}
          BRIGHTDATA_API_KEY: ${{ secrets.BRIGHTDATA_API_KEY }}
          BRIGHTDATA_BROWSER_WSS: ${{ secrets.BRIGHTDATA_BROWSER_WSS }}
          TRACERFY_API_KEY: ${{ secrets.TRACERFY_API_KEY }}
          TRACERFY_MCP_URL: ${{ secrets.TRACERFY_MCP_URL }}
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
          EXA_API_KEY: ${{ secrets.EXA_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          APIFY_API_KEY: ${{ secrets.APIFY_API_KEY }}
          HUNTER_API_KEY: ${{ secrets.HUNTER_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS: "0"
        run: |
          set +e
          unset ANTHROPIC_API_KEY
          ISSUE_NUM="${{ matrix.issue }}"
          ATTEMPT_NUM=0
          push_tail() {
            [ -f /tmp/cc.log ] || return 0
            local chunk bytes
            chunk="$(tail -c 4000 /tmp/cc.log 2>/dev/null | python3 -c 'import json,sys; print(json.dumps(sys.stdin.read()))' 2>/dev/null)"
            [ -z "$chunk" ] && return 0
            bytes="$(wc -c < /tmp/cc.log 2>/dev/null || echo 0)"
            curl -s -o /dev/null --max-time 10 -X POST "${SUPABASE_URL}/rest/v1/cc_live_log" \
              -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
              -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
              -H "Content-Type: application/json" \
              -H "Prefer: return=minimal" \
              -d "{\"run_id\": ${GITHUB_RUN_ID}, \"issue_number\": ${ISSUE_NUM}, \"attempt\": ${ATTEMPT_NUM}, \"tail_chunk\": ${chunk}, \"bytes_so_far\": ${bytes}}"
          }
          ( while true; do push_tail; sleep 20; done ) &
          TAIL_PID=$!
          trap 'kill "$TAIL_PID" 2>/dev/null' EXIT
          PROMPT="$(cat /tmp/prompt.md)"
          for attempt in 1 2 3; do
            ATTEMPT_NUM=$attempt
            echo "=== claude -p attempt $attempt ==="
            claude -p "$PROMPT" --dangerously-skip-permissions --output-format text > /tmp/cc.log 2>&1
            CC_EXIT=$?
            tail -c 2000 /tmp/cc.log
            BYTES=$(wc -c < /tmp/cc.log)
            echo "attempt=$attempt exit=$CC_EXIT bytes=$BYTES"
            echo "$attempt" > /tmp/cc_attempt.txt
            if [ "$CC_EXIT" -eq 0 ] && [ "$BYTES" -ge 50 ]; then
              # A clean exit with output is not necessarily progress: the session may have
              # asked for human confirmation instead of acting (impossible to answer in -p
              # mode, see issue #19074) and exited 0 anyway. Treat that as a failed attempt
              # so it retries with the reinforced non-interactive framing above, instead of
              # silently reporting "success" while zero work happened.
              if grep -qiE "haven.t made any changes yet|before i go further|can you confirm|let me know how you.d like to proceed|awaiting (your |)confirmation|do you want me to (actually )?(execute|proceed)" /tmp/cc.log; then
                echo "session asked for confirmation instead of acting (non-interactive run, nobody can answer) — treating as failed attempt, retrying"
                continue
              fi
              echo "CC OK"; exit 0
            fi
            if grep -qiE 'API Error: 5[0-9][0-9]|server error|overloaded|rate.?limit|timeout' /tmp/cc.log; then
              echo "transient error; retrying in 25s"; sleep 25; continue
            fi
            # Weekly/session usage-cap messages ("You've hit your weekly limit · resets 1pm (UTC)",
            # "Spending cap reached resets 1pm") are transient on a known clock, not a hard failure.
            # If the printed reset time falls within this job's 360-minute ceiling, sleep to it and retry
            # instead of burning the whole 6h window on a same-second exit (see issue #18065).
            if grep -qiE "weekly limit|spending cap|usage limit|session limit" /tmp/cc.log; then
              RESET_STR=$(grep -ioE '[0-9]{1,2}(:[0-9]{2})?\s*(am|pm)' /tmp/cc.log | head -1)
              RESET_HOUR=$(echo "$RESET_STR" | grep -oE '^[0-9]{1,2}')
              RESET_AMPM=$(echo "$RESET_STR" | grep -oiE 'am|pm' | tr '[:upper:]' '[:lower:]')
              if [ -n "$RESET_HOUR" ] && [ -n "$RESET_AMPM" ]; then
                H24=$RESET_HOUR
                if [ "$RESET_AMPM" = "pm" ] && [ "$RESET_HOUR" -ne 12 ]; then H24=$((RESET_HOUR + 12)); fi
                if [ "$RESET_AMPM" = "am" ] && [ "$RESET_HOUR" -eq 12 ]; then H24=0; fi
                NOW_EPOCH=$(date -u +%s)
                TARGET_EPOCH=$(date -u -d "today ${H24}:00" +%s)
                if [ "$TARGET_EPOCH" -le "$NOW_EPOCH" ]; then TARGET_EPOCH=$(date -u -d "tomorrow ${H24}:00" +%s); fi
                WAIT=$((TARGET_EPOCH - NOW_EPOCH + 60))
                MAX_WAIT=$((200 * 60))
                if [ "$WAIT" -le "$MAX_WAIT" ]; then
                  echo "usage limit hit, reset ~${H24}:00 UTC — sleeping ${WAIT}s then retrying"
                  sleep "$WAIT"
                  continue
                fi
                echo "::error::usage-limit reset (${RESET_STR} UTC) is more than 200min away — cannot wait and still leave real working time within this step's budget"; exit 1
              fi
            fi
            echo "::error::claude -p failed (exit=$CC_EXIT, non-transient)"; exit 1
          done
          echo "::error::claude -p failed after 3 attempts (transient)"; exit 1
      - name: Commit changes
        if: always()
        run: |
          git config user.name "cc-ghonly-bot"
          git config user.email "cc@biddeed.ai"
          git add -A
          if git diff --cached --quiet; then echo "No file changes"; exit 0; fi
          git commit -m "CC GHA-only: issue ${{ matrix.issue }} (run ${{ github.run_id }})"
          for attempt in 1 2 3 4 5; do
            if git push; then echo "push OK on attempt $attempt"; exit 0; fi
            echo "push rejected (attempt $attempt) — likely concurrent shard push to main; rebasing and retrying"
            git fetch origin main
            if ! git rebase origin/main; then
              echo "::error::rebase conflict — aborting rebase, work stays committed locally only (will be lost when runner is torn down)"
              git rebase --abort
              exit 1
            fi
            sleep $((attempt * 5))
          done
          echo "::error::push failed after 5 attempts — commit not persisted to origin/main"
          exit 1
      - name: Post run breadcrumb to issue
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GH_PAT_FULL }}
        run: |
          ATTEMPT="$(cat /tmp/cc_attempt.txt 2>/dev/null)"
          ATTEMPT="${ATTEMPT:-unknown}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          gh issue comment ${{ matrix.issue }} --repo ${{ github.repository }} \
            --body "CC GHA-only run finished — attempt ${ATTEMPT}, conclusion **${{ job.status }}**, run: ${RUN_URL}"
