# Protection Partners — website

Generated by `scripts/new-agency-site.mjs` from `sites/_template-agency-web/`
and `agency-configs/protectionpartners.config.json`. **Do not hand-edit
structural files here** (layouts, components, page shells, `functions/`,
`scripts/audit-site.mjs`) — edit the template or the config and regenerate
with `--force`. Copy/content that lives only in `agency.config.json` is safe
to hand-edit directly, since it's this site's own data, not shared structure.

Hand-copying an existing `sites/<slug>-web/` directory to start a new agency
site is the anti-pattern this factory exists to eliminate — it silently
forks the structure, so a fix to (say) the Canopy Connect flow never reaches
sites that were copy-pasted instead of generated. This file is the full
operator guide, present in the template itself and copied into every
generated site so it travels with the code.

## Generate (or regenerate) a site from this template

```bash
cp agency-configs/test-agency.config.json agency-configs/<slug>.config.json
# edit agency-configs/<slug>.config.json -- see field guide below
node scripts/new-agency-site.mjs --config agency-configs/<slug>.config.json --slug <slug>
cd sites/<slug>-web && npm ci && npm run build
```

Add `--force` to regenerate an existing `sites/<slug>-web/` from an updated
template or config — this is how `sites/protectionpartners-web/` itself is
kept in sync with the template rather than hand-maintained as a special case.

## Config field guide (for a non-technical operator)

Full machine-readable contract: `sites/_template-agency-web/agency.config.schema.json`.
Plain-language version of the fields an operator (not an engineer) is
expected to fill in:

| Field | What it is | Example |
|---|---|---|
| `slug` | URL-safe site id. Becomes `sites/<slug>-web/` and the Cloudflare Pages project name. | `"protectionpartners"` |
| `agency_name` | Legal/brand name shown in the header, footer, and page titles. | `"Protection Partners"` |
| `tagline` | Homepage headline. | `"AI-native independent insurance, human-bound."` |
| `hero_description` | Paragraph under the headline. | — |
| `meta_description` | Homepage `<meta description>` for search/social previews. | — |
| `domain.default` | Placeholder `*.pages.dev` domain until a real domain is bound (manual step, see below). | `"protectionpartners.pages.dev"` |
| `brand` | Optional color/font overrides. Omit to use the default ink/brass palette. | — |
| `phone.display` / `phone.tel` | Human-readable and `tel:` phone number. Also the Vapi voice widget's fallback when Vapi isn't configured. | `"(321) 555-0100"` / `"tel:+13215550100"` |
| `city`, `license_line`, `licensed_states` | Footer/compliance copy — where the agency operates and its license line. | — |
| `ams_vendor` | `"ezlynx"` or `"other"`. Only changes README/UI copy — there is no code-level AMS API integration for any vendor (see Manual steps below). | `"ezlynx"` |
| `client_center` | Optional existing-client self-service login link (e.g. EZLynx Client Center URL). Omit if the agency doesn't have one yet. | — |
| `carriers_note` | Homepage carrier-appointments row copy. | — |
| `why_points`, `proof_points` | Homepage bullet/proof-point copy. | — |
| `lines_of_business` | One entry per line-of-business page (commercial, personal lines, etc.) — rendered by the generic `src/pages/[line].astro` template, no per-line page files needed. | — |
| `team` | Team bio cards on `/about`. | — |
| `consent.version` / `consent.text_template` | TCPA consent copy and its version id. Bump the version any time the text changes. | — |
| `canopy_connect.public_alias_env` | Name of the env var holding the Canopy Connect `publicAlias`. Defaults to `PUBLIC_CANOPY_CONNECT_PUBLIC_ALIAS` — omit this field entirely unless you have a reason to rename the var. | — |
| `vapi.public_key_env` / `vapi.assistant_id_env` | Same pattern for Vapi's two env vars. | — |
| `supabase_table` | Intake table name. Defaults to `<slug_with_underscores>_intake`. Must be created via a migration before go-live — the generator does not create tables. | `"protection_partners_intake"` |
| `supabase_storage_bucket` | Storage bucket for the dec-page upload fallback. Defaults to `<slug>-dec-uploads`. Must exist before go-live if `intake.fallbacks.decUpload` is enabled — the generator does not create buckets. | `"protection-partners-dec-uploads"` |
| `canopy_connect.dec_upload_url_env` | Env var name for a Canopy-hosted "Document Upload" sharing-path link, if that plan tier is enabled. Omit unless you have a reason to rename it — defaults to `PUBLIC_CANOPY_DEC_UPLOAD_URL`. | — |
| `intake.fallbacks.decUpload` / `.formEntry` / `.callback` / `.firstTimeBuyer` | Per-path on/off switches for the secondary intake paths below the Canopy CTA (see next section). All default to `true` — omit `intake` entirely to keep every path on. | `{ "decUpload": false }` |
| `posthog.enabled` | Whether the analytics snippet renders when `PUBLIC_POSTHOG_KEY` is set. | — |

## Canopy Connect — the core mechanic

The 1-click carrier-authorization **pull** flow (`src/components/CanopyConnectWidget.astro`)
is the primary CTA on `/` and `/get-a-quote` by default in every generated
site, not an opt-in feature. A prospect clicks once, authenticates with
their current carrier, and Canopy pulls their policy dec pages, driver
history, and vehicle/policy details — no manual data entry. The manual
multi-step quote form is the fallback, deliberately demoted below it in both
page hierarchy (form comes second, smaller heading, muted styling) and
markup (`#canopy-primary-cta` precedes `#quote-form` on `/get-a-quote` —
enforced by the `CANOPY DOMINANCE` check in `scripts/audit-site.mjs`).

When `PUBLIC_CANOPY_CONNECT_PUBLIC_ALIAS` is unset (no Canopy account yet),
the widget still renders a same-size, same-position **disabled "coming
soon"** button rather than disappearing — so the page's visual hierarchy
already centers Canopy before the account exists. It does not silently swap
in the plain form as the primary element.

Two write paths exist and both upsert on `pull_id` (idempotent — whichever
fires first creates the row, the other just updates it):

- `functions/api/canopy-complete.ts` — client-side, fired from the widget's
  `authenticationSuccess` event the moment a prospect finishes connecting.
- `functions/api/canopy-webhook.ts` — server-side, fired by Canopy's own
  webhook once registered on Canopy's dashboard (manual step below). Canopy's
  public docs don't specify a webhook signing scheme — treat this endpoint as
  best-effort, not the source of truth, until Canopy support confirms one.

**Canopy → EZLynx is not code in this repo.** EZLynx has no public write API.
The actual data push happens entirely inside Canopy's own EZLynx Marketplace
connector, configured on Canopy's dashboard (manual step below). This
codebase's job ends at keeping its own parallel record in
`<supabase_table>` with `source='canopy_connect'`.

## Intake safety net — secondary sharing paths (issue #19602)

Canopy Connect's one-click pull is the primary CTA (above), but some
prospects can't or won't use it — they don't know their carrier login,
they're switching from a carrier Canopy doesn't support, or they're shopping
their *first* policy and have nothing to connect. Without a fallback those
prospects bounce with zero capture. `/get-a-quote` renders an
`#intake-fallback-paths` section directly beneath the Canopy CTA — smaller
type, no accent background, structurally and visually subordinate (enforced
by the `FALLBACK SUBORDINATION` check in `scripts/audit-site.mjs`, same
discipline as the existing `CANOPY DOMINANCE` check). Every path writes to
`<supabase_table>` with its own `source` value so an operator can filter
leads by which path produced them:

| Path | Config flag | `source` value | What it does |
|---|---|---|---|
| Upload your declarations page | `intake.fallbacks.decUpload` | `dec_upload` | `DecUploadWidget.astro` posts a PDF/JPG/PNG (10MB cap, validated server-side) to `functions/api/dec-upload.ts`, which stores the file in Supabase Storage (`<supabase_storage_bucket>`) and writes an intake row flagged `payload.needs_manual_review=true` — this codebase does not parse or OCR the file. If `PUBLIC_CANOPY_DEC_UPLOAD_URL` is set (Canopy's own hosted "Document Upload" sharing path, plan-tier dependent), the widget links out to Canopy's flow instead and this endpoint isn't used. |
| Answer a few questions instead | `intake.fallbacks.formEntry` | `form_entry` | The existing multi-step quote form (`#quote-form`), reachable via a small "Jump to the form" link in the fallback section — same form, same `functions/api/quote.ts`, just a different `source` tag than the Canopy path. |
| Have an agent call me | `intake.fallbacks.callback` | `callback_request` | `CallbackWidget.astro` captures name, phone, best time to call, and TCPA consent, and posts to `functions/api/callback-request.ts`. Missing consent is rejected with 4xx and zero DB write, same as the main quote form. |
| Buying your first policy? Start here | `intake.fallbacks.firstTimeBuyer` | `first_time_buyer` | A prospect with no current policy to connect has nothing to gain from the Canopy CTA. This link (in the fallback section and near the homepage's manual-quote link) sends them to `/get-a-quote?path=first_time_buyer#quote-form`, which shows a banner, skips straight to the form, and tags the submission `first_time_buyer` instead of `form_entry`. |

Toggle any path off per agency by setting it `false` under `intake.fallbacks`
in that agency's config and regenerating with `--force` — a disabled path is
absent from the rendered HTML entirely, not just hidden by CSS.
`agency-configs/test-agency.config.json` sets `decUpload: false` as the
living proof of this.

## Vapi voice widget

`src/components/VoiceWidget.astro` loads the real `@vapi-ai/web` SDK when
`PUBLIC_VAPI_PUBLIC_KEY` and `PUBLIC_VAPI_ASSISTANT_ID` are both set at build
time, and falls back to a plain `tel:` link (using `phone.tel` from the
config) when either is unset. RingCentral is out of scope — the `tel:`
fallback is the only non-Vapi calling path.

## Manual steps vs. fully automated

| Step | Automated? | Notes |
|---|---|---|
| Generate site structure from config | **Automated** | `node scripts/new-agency-site.mjs --config ... --slug ...` |
| Build + design-bar audit (320/393/768/1440px, no overflow, no JS errors, Canopy CTA dominance) | **Automated** | `npm run build && npm run preview -- --port 4321 & npm run audit` |
| Canopy Connect account signup | **Manual — human only** | Third-party paid signup on Canopy's own dashboard. No API to automate account creation. |
| Linking the EZLynx account inside Canopy's EZLynx Marketplace connector | **Manual — human only** | Behind-login step on Canopy's dashboard. This is the entire Canopy→EZLynx data path; nothing in this repo touches EZLynx. |
| Registering the `canopy-webhook.ts` URL in Canopy's dashboard | **Manual — human only** | Settings → Webhooks (or Canopy's Webhooks API) once the site is deployed and the URL is known. |
| Vapi assistant creation | **Manual — human only** | Third-party paid signup + assistant configuration on Vapi's dashboard. |
| Setting secrets (`SUPABASE_SERVICE_ROLE`, `PUBLIC_CANOPY_CONNECT_PUBLIC_ALIAS`, `PUBLIC_VAPI_PUBLIC_KEY`, `PUBLIC_VAPI_ASSISTANT_ID`) | **Manual — human only** | Cloudflare Pages dashboard → Settings → Environment variables. Never commit these; `.dev.vars.example` documents the names only. |
| Creating the Supabase intake table (`<supabase_table>`) | **Manual — human only** | Write and apply a migration before go-live. The generator does not create tables. |
| Creating the Supabase Storage bucket (`<supabase_storage_bucket>`) | **Manual — human only** | Required only if `intake.fallbacks.decUpload` is enabled. Create via the Storage REST API or a migration before go-live. The generator does not create buckets. |
| Enabling Canopy's own "Document Upload" sharing path (optional) | **Manual — human only** | Plan-tier dependent, configured on Canopy's dashboard. If enabled, set `PUBLIC_CANOPY_DEC_UPLOAD_URL` so `DecUploadWidget` links to it instead of this site's own upload form. |
| Domain DNS / Cloudflare Pages project creation | **Manual — human only** | Bind a real domain and create the Pages project (or connect the repo via Git integration) on Cloudflare's dashboard. Until then the site builds and serves on the `*.pages.dev` preview domain from `domain.default`. |
| CI build workflow (`.github/workflows/<slug>-web-ci.yml`) | **Automated** | Stamped from `_ci-template.yml` by the generator — one file per generated site, path-filtered to it. |

## Local development

```bash
cd sites/protectionpartners-web
npm ci
npm run dev        # Astro dev server, pages only -- functions/api/* is NOT served
```

To exercise `functions/api/*.ts` locally (the actual deploy artifact), use
Wrangler's Pages dev server against a built `dist/`:

```bash
npm run build
cp .dev.vars.example .dev.vars   # fill in real values, never commit .dev.vars
npx wrangler pages dev dist --port 8788
```

## Design bar / audit

```bash
npm run build && npm run preview -- --port 4321 &
npm run audit
```

Checks every page (home, get-a-quote, one per `lines_of_business` entry,
about, client-service) at 320/393/768/1440px for horizontal overflow and
console/page JS errors, plus a `/get-a-quote` **CANOPY DOMINANCE** check that
the Canopy Connect CTA (`#canopy-primary-cta`) renders above the fold at
1440px and precedes the fallback form (`#quote-form`) in vertical position.
Requires Playwright's Chromium browser (`npx playwright install chromium`
once).

## Deploy

```bash
npm run build
npx wrangler pages deploy dist --project-name=protectionpartners-web
```

Or connect this directory (`sites/protectionpartners-web`) as the
Cloudflare Pages project root via Git integration.
