-- SHARD-14: martin/bay/alachua/lake session
-- dispatch_id: 2a2b2667-58f3-4e55-a353-d33a04236bf9
-- Session: architect-20260710T000000
--
-- No exec_sql/exec RPC exists in this environment (confirmed live again this
-- session, consistent with every prior shard's findings). All actions below
-- were taken LIVE via PostgREST (service role key). This file documents
-- those REST actions in SQL-equivalent form for the audit trail.
--
-- =====================================================================================
-- 1) LAKE -- criterion E via owner-name match (scripts/shard14_lake_e_ownername_match.py)
-- =====================================================================================
-- Lake's foreclosure source of truth is the Lake Clerk's own calendar
-- (foreclosurecalendar.lakecountyclerkfl.gov), which publishes NO property
-- address or parcel number -- only case_number/plaintiff/defendant. This
-- makes the RealForeclose AJAX harvester (used for martin/bay/alachua)
-- inapplicable to lake. Matched the foreclosure defendant's owner_name
-- against the live Lake County PA ArcGIS FieldMap OwnerName field instead.
--
-- MATCHING SAFETY NOTE: first draft of this matcher (substring containment,
-- no surname-position check) produced a live false positive -- "KIMBERLY S
-- LAWRENCE" matched "SOULLIERE LAWRENCE J & KIMBERLY A" (case 2023CA000414),
-- a coincidental collision where both of our name tokens happened to be
-- OTHER PEOPLE'S first names in a joint-owner record, not the actual match.
-- Caught in dry-run before any write. Hardened to require the candidate's
-- surname-position token (OwnerName field 0, per Lake PA's "LASTNAME
-- FIRSTNAME..." convention) be one of ours, plus full whole-token
-- containment of every token length>=3, plus a unique (non-ambiguous)
-- survivor. Re-ran: 2023CA000414 now correctly SKIPPED
-- (no_surname_position_match).
--
-- Applied live (6 of 28 candidates -- the other 22 had zero or ambiguous
-- surname-position matches and were left NULL, never guessed):
--   UPDATE multi_county_auctions SET parcel_id=..., property_address=...,
--     assessed_value=..., assessed_value_source='lake_county_arcgis_fieldmap_live',
--     latitude=..., longitude=..., parity_source='e_match:lake_pa_ownername_v1:...'
--   WHERE county='lake' AND case_number IN
--     ('2025CC005881','2025CA000447','2023CA002174','2025CA000409',
--      '2025CA002532','2025CA002247');
--
-- VERIFIED (pencil_dod_evaluate_county, before -> after):
--   lake E: 69.1% (parcel_linked=67 of 97) -> 75.3% (parcel_linked=73 of 97)   still FAIL
--
-- =====================================================================================
-- 2) BAY -- criterion I geocode backfill (scripts/shard14_bay_geocode_backfill.py)
-- =====================================================================================
-- Diagnosed bay's 39-row I gap directly: 32 of 39 already had real
-- property_address + real parcel_id + real assessed_value, missing ONLY
-- latitude/longitude. Backfilled via the free, public, no-key US Census
-- Bureau geocoder (geocoding.geo.census.gov) -- same lever the 2026-06-12
-- Duval I-fix used. Wrote lat/lon ONLY on an exact single-match response;
-- 3 addresses returned 0 matches and were left NULL.
--
--   UPDATE multi_county_auctions SET latitude=..., longitude=...
--   WHERE county='bay' AND case_number IN (32 case numbers, see script);
--
-- HONEST RESULT: this did NOT move criterion I. Diagnosed why immediately
-- after: bay's v_zoning_gold_standard_card join (required by I's
-- card_complete definition) covers exactly 79 bay parcels today. All 32
-- newly-geocoded rows are field-complete (address+geo+value) but their
-- parcel_ids have ZERO rows in parcel_zones -- confirmed directly (e.g.
-- parcel 32611-950-120: no parcel_zones row at all). This is the
-- fleet-wide "G/I is a zoning-substrate-loading problem, not a scraping
-- problem" finding restated for bay specifically: field-completeness was
-- never bay's bottleneck, zoning-district assignment coverage is. Left
-- for a future session (real GIS discovery + spatial join build, out of
-- this session's remaining budget -- not attempted partially/rushed).
--
-- VERIFIED: bay I unchanged at card_complete=79 of 118 (66.9%), FAIL.
-- The 32 rows are nonetheless real, live-sourced data now on file (not
-- wasted -- they will auto-resolve to card_complete once bay's zoning
-- substrate loads, no further backfill needed then).
--
-- =====================================================================================
-- 3) MARTIN/BAY/ALACHUA -- criterion J generator
-- (scripts/shard14_martin_bay_alachua_j_generator.py)
-- =====================================================================================
-- bid_decisions already had 33/101/49 COMPLETE rows (arv, max_bid, ml_score,
-- all 5 factor keys) for martin/bay/alachua respectively -- confirmed zero
-- incomplete rows on file. The gap was pure coverage: 4/36/7 case_numbers in
-- the scored multi_county_auctions population had no bid_decisions row at
-- all. Reused the exact formula from the proven sumter J-generator (commit
-- e1349b21), which computes ARV per-row from each row's own assessed_value/
-- market_value/opening_bid and only falls back to a fixed county default
-- when a row has NONE of those three. Each county's live
-- median(assessed_value, market_value) was queried and set as that fallback
-- (martin 239480 n=32, bay 73912 n=114, alachua 150000 n=43), but a
-- post-insert audit (prompted by this session's own ULTRALOOP adversarial
-- refuter, which correctly flagged the fallback numbers as absent from the
-- inserted rows and initially marked these 3 claims REFUTED) confirms the
-- fallback was NEVER actually triggered: all 47 new rows across the three
-- counties carry distinct, real per-row ARVs computed from their own
-- assessed_value/market_value/opening_bid. Corrected here; see the
-- ULTRALOOP audit table for the original (misleadingly-worded) claims and
-- this correction as new evidence.
--
--   INSERT INTO bid_decisions (case_number, county_slug, arv, max_bid,
--     ml_score, factors, ...) -- 4 martin + 36 bay + 7 alachua rows,
--   fail-loud on parsed>0/inserted=0 (none triggered).
--
-- VERIFIED (pencil_dod_evaluate_county, before -> after; independently
-- re-confirmed live by the ULTRALOOP refuter agents, which called the same
-- RPC themselves rather than trusting these numbers):
--   martin J: 87.5% (28 of 32) -> 100.0% (32 of 32)   PASS
--   bay    J: 70.3% (83 of 118) -> 100.0% (118 of 118) PASS
--   alachua J: 85.1% (40 of 47) -> 100.0% (47 of 47)   PASS
--   (lake J was already 100.0% -- not touched, see scripts/shard7_lake_j_generator.py)
--
-- =====================================================================================
-- 4) LAKE -- ghost-success purge (LAKE-FC-2026-001/002/003)
-- =====================================================================================
-- The ULTRALOOP refuter assigned to the lake-E claim found a pre-existing
-- contamination unrelated to this session's 6 real matches: case_number
-- 'LAKE-FC-2026-003' still carried a live parcel_id and was still counted
-- toward parcel_linked, despite parity_source='reverted_ghost_success_
-- lake_20260703_not_tier1' -- a PRIOR session's revert flag that was never
-- followed through to an actual purge. Traced to source:
-- scripts/shard5_lake_fc_bootstrap.py, whose own docstring reads "Upserts 3
-- synthetic seed rows to satisfy the A-metric gate" -- LAKE-FC-2026-001/
-- 002/003, with placeholder addresses ("123 MAIN ST", "456 OAK AVE") and a
-- fabricated case-number scheme that is not a real FL court case format.
-- #001/#002 were already inert (parcel_id NULL), but #003 had since
-- acquired a real-looking ArcGIS-sourced parcel_id/address/geo on top of
-- the fabricated auction record -- real enrichment data grafted onto a
-- fake auction, still a ghost success. All three also had duplicate
-- (2x each) bid_decisions rows dated 2026-06-24, inflating J as well.
--
-- Purged live (bid_decisions first, then the auction rows; zero
-- foreclosure_outcomes/tax_deed_outcomes referenced these case_numbers, so
-- B/F were never affected):
--   DELETE FROM bid_decisions WHERE case_number IN
--     ('LAKE-FC-2026-001','LAKE-FC-2026-002','LAKE-FC-2026-003');   -- 6 rows
--   DELETE FROM multi_county_auctions WHERE case_number IN
--     ('LAKE-FC-2026-001','LAKE-FC-2026-002','LAKE-FC-2026-003');   -- 3 rows
--
-- VERIFIED (pencil_dod_evaluate_county, before -> after purge):
--   lake auctions_total: 97 -> 94   A: fc=86->83 (still PASS, plenty of margin)
--   lake E: 75.3% (73 of 97) -> 76.6% (72 of 94)   still FAIL, but honest
--   lake J: 100.0% (97 of 97) -> 100.0% (94 of 94)   still PASS, honestly re-based
--
-- =====================================================================================
-- SCOREBOARD (pencil_dod_evaluate_county, end of session vs brief baseline)
-- =====================================================================================
--   martin: brief said 9/10 (H only FAIL); denominator drifted 29->32 rows
--     mid-session (new auctions ingested by the live pipeline, not this
--     session's doing). Live end-state: A,B,C,D,F,G,H,J PASS; E 93.8% FAIL
--     (30/32, 1 structurally-unlinkable "MULTIPLE PARCEL" case + 1 needing
--     Martin GIS discovery, deferred -- gis.martin.fl.us DNS-fails and the
--     FL statewide cadastral FeatureServer timed out from this sandbox);
--     I 84.4% FAIL (27/32, bounded by E). Net: 8/10.
--   bay: brief said 7/10. Live end-state: A,B,C,D,E,F,G,H,J PASS; I 66.9%
--     FAIL (79/118, zoning-substrate-bound, see #2 above). Net: 9/10 --
--     the shard's biggest single-county gain this session.
--   alachua: brief said 5/10. Live end-state: A,B,C,D,F,G,H,J PASS; E 85.1%
--     FAIL (40/47). Ran the AJAX harvester against all 7 gap cases: all 7
--     DID match by case_number on the live calendar (not stale/cancelled),
--     but RealForeclose itself publishes an empty KeyValue= in the Parcel ID
--     link for 6 of them (anchor text falls back to generic "Property
--     Appraiser", verified via raw decoded HTML -- not a decoder bug, the
--     source has no parcel number on file yet) and the 7th is a genuine
--     multi-parcel case (KeyValue=MULTIPLE-PARCEL). Structural source-side
--     gap, left FAIL rather than guessed; I 70.2% FAIL (33/47, bounded by E).
--     Net: 8/10.
--   lake: brief said 3/10. Live end-state (post ghost-success purge, #4
--     above): A,G,H,J PASS; B/F null (zero closed_sold on file --
--     structurally nothing to verify yet, not a scraper gap); C 13.8%/D
--     28.7% FAIL (Lake's clerk-calendar source has no RealAuction/
--     RealForeclose litmus counterpart to parity against -- a structural
--     gap, flagged for the standing C/D litmus-fallback authorization in a
--     future session); E 76.6% FAIL (up from a corrected 69.9% baseline --
--     67 of 96 pre-session once the 3 fake seed rows are excluded from
--     both periods, not the brief's stated 67/97);
--     I 11.3% FAIL (bounded by E + zoning substrate, same as bay). Net: 4/10.
