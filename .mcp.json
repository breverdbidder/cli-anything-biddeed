{
  "mcpServers": {
    "tracerfy": {
      "type": "http",
      "url": "${TRACERFY_MCP_URL}"
    },
    "brightdata": {
      "command": "npx",
      "args": ["-y", "@brightdata/mcp"],
      "env": { "API_TOKEN": "${BRIGHTDATA_API_KEY}" }
    }
  }
}
