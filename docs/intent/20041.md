# Intent — #20041 LAUNCH-A2: rate limiting must be provably effective
Precedence: this file > docs/spec/20041.md > issue body > comments. Supersedes the rate-limit part of #20035 only (its edge cache + security headers are VERIFIED live and must be preserved).
Intent: every limited route on biddeed-mcp-production, worker-damp-snowflake-cead and biddeed-web-production returns 429 + Retry-After to an EXTERNAL burst that exceeds its limit. Evidence is raw curl output from the runner after deploy — never a claim from code reading. VERIFIED 16:13 UTC Sep 5: 25 unauth POSTs to /api/mcp -> 25x401 (no 429); 8 POSTs to /chat/lead -> 8x400 (no 429). The caches.default "counter" from #20035 does not work and must be removed (keep the caches.default response cache).
Hard constraints: use the Workers Rate Limiting binding (`ratelimits` in wrangler.jsonc/toml, period 10 or 60, available on the Free plan); if wrangler rejects it paste the error and try `unsafe.bindings`; if both fail use a SQLite Durable Object counter. Never Cache API or KV for counting. Never limit /api/health, /.well-known/*, Stripe webhooks. Deploy only via the existing production workflows; fix their post-deploy grep for the workers.dev URL where it would fail. No WAF/dashboard, no plan change, no winnerdata-* Workers, no product logic.
Evidence table in the issue body must be pasted verbatim; a valid API key at 2 req/s for 30 s must see zero 429; one request after the 60 s cooldown must succeed again.
