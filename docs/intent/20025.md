# Intent — #20025 EXIT VERCEL A: mcp.biddeed.ai → Cloudflare Worker biddeed-mcp-production
Precedence: this file > docs/spec/20025.md > issue body > comments. Ariel's go for a same-day cutover was given 2026-09-05 ~07:40 EDT ("everything out of vercel by the end of the day"). A red gate still means NO cutover — post BLOCKED/READY FOR CUTOVER instead.

## Hard guardrails (each one has cost a prior run)
1. SECRETS: never echo, cat, or log a secret value. `vercel pull` output and `wrangler secret bulk` input live in a temp dir that is `rm -rf`'d in the same step. Logs list KEY NAMES only. Check the log for leaks before posting any comment.
2. PROCESSES: never `pkill node`, `killall node`, `pkill -f` on a generic name — Claude Code IS a Node process and kills itself (exit 143, two dead runs on #19678). Start `wrangler dev` in the background, capture `$!`, kill that PID (or `fuser -k <port>/tcp`). Never start `next`.
3. DNS: before the first DNS change, docs/spec/20025-rollback.md must already be committed with record id 44c5c371327762c2eeb4a27eea937217 / `mcp.biddeed.ai CNAME cname.vercel-dns.com`. No rollback file → no DNS change.
4. TOKEN SCOPE: if the step-0 DNS/Routes probe fails, do not retry with tricks — deploy to workers.dev, run all gates, comment `READY FOR CUTOVER …` and stop. Chat holds a DNS-scoped token and will finish it.
5. MODEL: no Anthropic API key anywhere in the Worker or workflow. Pipeline LLM calls (if any exist in http.js) keep going through the Smart Router exactly as on Vercel.
6. SSOT: `packages/biddeed-mcp/src/report/*` is the S5 engine — import-compat shims only, zero logic changes. `multi_county_auctions`, `gold_standard_*`, `taxi_meter_*`, `insights` stay read-only.
7. SPEND: `"limits": {"cpu_ms": 30000}` is the plan probe. If the deploy rejects it, STOP before cutover and report "Workers Paid ($5/mo) needed" — Ariel decides spend, not the runner.
8. EVIDENCE: Honesty V3. A 200 is evidence only with the body/header that proves it (PDF bytes start `%PDF-`, tools/list count, `server: cloudflare`). "It should work" is not a gate result.
9. Stripe: `api/stripe/webhook.js` is part of this surface — same path, same signing secret, same table write, proven with a signed synthetic event before cutover.
