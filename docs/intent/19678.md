# Intent — #19678 Migrate biddeed-web Vercel → Cloudflare Workers (OpenNext)

Outranks the issue body. Read this first.

## Why this file exists
Runs 33513246050 and 33598436899 both died identically: `claude -p` exit 143 (SIGTERM) after 5-7 minutes with a `next-server (v16.1.6)` orphan still alive at cleanup. INFERRED root cause (no session log survives in text mode): the session started a Next.js test server, then tried to stop it with a broad process kill (`pkill node`, `killall node`, `pkill -f node`, or similar). Claude Code itself runs on Node — that command kills the session. The `next-server` process renames its title so it does NOT match "node" and survives, which is exactly the orphan pattern observed both times.

## HARD RULES (non-negotiable for this session)
1. NEVER run `pkill node`, `killall node`, `pkill -f node`, `pkill -f next`, `kill -9 -1`, or any broad process-name kill. You are a Node process; it kills you.
2. Start any test server in the background and capture its PID: `npx wrangler dev --port 8787 > /tmp/wrangler.log 2>&1 & echo $! > /tmp/server.pid`. Stop it ONLY with `kill "$(cat /tmp/server.pid)"`, or by port: `kill $(lsof -ti:8787)`.
3. Do not use `next start` at all for verification — the target is the Workers runtime, so verify with `npx wrangler dev` (OpenNext build output), not the Node server. `next build` is fine.
4. Before doing anything else, post a comment on issue #19678 saying "intent file read, starting" so a live signal exists even though -p text mode buffers output until exit.

## Scope reminder (unchanged from body)
biddeed-web only. OpenNext only. Preview deploy before prod. Vercel stays live as rollback. Stop and report if evidence shows the $100 bill comes from biddeed-mcp or zonewise-web instead.

## Known blocker you will hit
You cannot read biddeed-web's Vercel env var VALUES (sealed via API; only the dashboard shows them — Ariel must supply). You CAN discover the NAMES from the code (`grep -rhoE "process\.env\.[A-Z0-9_]+" app lib | sort -u`). Do the full build + `wrangler dev` smoke test with the names known and placeholder/test values where safe, get as far as a preview deploy with everything except real secrets, then post a comment listing the exact secret names Ariel needs to `wrangler secret put`. That is PARTIAL with a clear handoff, not a failure.
