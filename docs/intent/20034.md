# Intent — #20034 GTM-5 daily-digest consent compliance
Precedence: this file > docs/spec/20034.md > issue body > comments.

## Hard guardrails
1. ZERO sends to non-consented addresses, including tests. The only permitted live recipient during this issue is the internal recipient the script already supports. Workflow 321410535 stays disabled until the DoD is met; re-enable is the last step and must post the verified recipient count.
2. Consent = `email_consent = true OR marketing_consent = true`, AND not opted out. `unsubscribe_link`-sourced rows are opted out. Skip-traced and scraped rows (skip_trace_pipeline, auction_llc_expansion, exa_tam_expansion) are never recipients regardless of flags.
3. Never delete lead_profiles rows; never print an email address in a comment, log excerpt, or doc — counts and sources only. Names only for secrets.
4. Suppress every past non-consented recipient in Resend; do not "clean up" by removing evidence.
5. Additive schema only (an opt-out column/table with RLS on) — state it explicitly; no other DDL.
6. Schedule 22:00 UTC; content per docs/gtm/GTM_SOP_v1.md §3; List-Unsubscribe header + one-click link mandatory.
7. No Anthropic API; no generic pkill; kill by PID/port.
8. Honesty V3 on every audit number; if Resend history is unavailable for a day, say "unknown" rather than estimating.
