# Intent — #20027 EXIT VERCEL C: decommission + dispute evidence
Precedence: this file > docs/spec/20027.md > issue body > comments.

## Hard guardrails
1. NEVER DELETE a Vercel project or a Vercel domain before the matching hostname is live on Cloudflare (`server: cloudflare`, zero x-vercel headers, checked by you, now). The Sep 3 deletion of biddeed-mcp took mcp.biddeed.ai down for hours — that instruction is SUSPENDED; pause instead.
2. Part 2 (domain detach + pausing biddeed-mcp/zonewise-web) runs only after BOTH #20025 and #20026 carry a `CUTOVER VERIFIED` comment. Poll at most every 10 min, 3 h max, then PARTIAL.
3. Pausing must not take a live site down: verify one paused project's production URL still serves before pausing anything that still has traffic.
4. No DNS changes in this issue at all.
5. SECRETS: names only, never values. Do not remove VERCEL_TOKEN secrets — Ariel revokes them at account closure.
6. zonewise-web changes go on branch `cf-exit-decommission` → PR (main protected). Other repos via the clone pattern; commit+push inside the clone before exiting.
7. Numbers in the dispute pack carry Honesty V3 tags and come from the Vercel API / GitHub API responses you observed, never from memory or issue text. The customer-facing section of docs/finance/vercel-dispute-evidence-2026-09.md names no internal issue numbers or vendors.
8. No Anthropic API.
