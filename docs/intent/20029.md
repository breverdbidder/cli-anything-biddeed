# Intent — #20029 GTM-0 launch-night readiness
Precedence: this file > docs/spec/20029.md > issue body > comments. Operating document: docs/gtm/GTM_SOP_v1.md.

## Hard guardrails
1. NOTHING PUBLISHES. Dial < 3 (M8). Public upload, public post, newsletter send to real recipients, Typefully publish = forbidden. Queue rows, drafts, private-upload-then-delete smoke tests only.
2. `reel_variant_review` is Ariel's ground truth — never write rows to it. `spi_gates` rows are closed by chat on Ariel's word — never touch them.
3. Never-list in every asset: no person names except Ariel (M7), no vendor/model names, no unverified $ figures (each must trace to a DB column), no foreclosure-relief language, no homeowner contact. Public copy says US county foreclosure and tax deed auctions; signature "For Everyone. Everywhere."
4. SECRETS: names only, never values. Platform inventory = `select name from vault.secrets` + GH secret names.
5. PROCESSES: never `pkill node` / `killall node` / `pkill -f` generic names (self-kill, exit 143). Background servers by PID/port. Never `next start`.
6. The LMS review screen must work on a phone (Playwright 390 px, zero console errors) — Ariel approves from mobile Tuesday night. If it must be built, build the minimal version; do not gold-plate.
7. The 14 queue picks follow Ariel's cadence rule: slot A foreclosure + slot B tax deed per day; never two variants of one property on the same day; tax deed is the dependable slot (absolute auctions).
8. EVIDENCE: Honesty V3 — counts come from queries you ran, URLs you fetched, screenshots you took. No "should work".
9. If something needs Ariel (accounts, credentials), write the runbook and stop — do not create accounts in his name.
