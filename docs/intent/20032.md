# Intent — #20032 GTM-3 make the launch reels publishable
Precedence: this file > docs/spec/20032.md > issue body > comments. Operating document: docs/gtm/GTM_SOP_v1.md. Read docs/spec/20029.md + docs/spec/20030.md first.

## Hard guardrails
1. NOTHING PUBLISHES (M8, dial < 3). The goal is a FULL `youtube_publish_queue` view, not an upload.
2. Scope is finishing, not producing: the 14 launch picks + 2 spares per slot type = 18 reels, top 2 variants each = 36 finals. The volume rule does not block finishing these; it does block anything beyond them.
3. Never write to `reel_variant_review` or `spi_gates`.
4. Voice: eleven_v3 TX3LPaxmHKxFdv7VOQHJ for EN finals (log characters used; the Creator plan is $22/mo — stay inside it, report if the 36 finals would exceed the remaining quota); kokoro for ES/PT-BR.
5. Every $ figure traces to `multi_county_auctions` / `zw_parcels`; never-list scan on title, caption, on-screen captions AND transcript; REJECT with the matched token.
6. Render path = the deterministic pipeline/scripts; fix them, do not hand-craft. Check row counts after every run (Sep 3 lesson: exit 0 with zero effect = failure).
7. LLM: OpenRouter GLM-5.3-flash → DeepSeek → router t2. Never Anthropic API. Gemini keys are 429.
8. SECRETS names only; PROCESSES no generic pkill; kill by PID/port; never `next start`.
9. Deal-page verification is a real HTTP fetch of the live page (Street View img present, capture form present), not a DB flag flip.
