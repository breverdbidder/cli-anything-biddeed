# Intent — #20038 LAUNCH-D: winnerdata-ff Worker lockdown
Precedence: this file > docs/spec/20038.md > issue body > comments.
HARD MANDATE (Ariel, Sep 1 2026): NOTHING goes out to Mariam or any producer — no FF batch, no email, no send of any kind. Auth plumbing only; producer-facing HTML byte-identical (except prepared_date).
Intent: remove the two public side doors on the Fact Finder Worker — (1) workers.dev hostname disabled so only ff.winnerdataai.com remains (ready for Cloudflare Access, Ariel's action), (2) the hardcoded public anon key replaced by a Worker secret (service role) and, ONLY after that deploy is verified live, anon/authenticated EXECUTE revoked on ff_portal_leads/ff_get_lead/ff_record_bind/ff_upsert_response/ff_healthz as a tracked migration.
Hard constraints: order matters (deploy with secret -> verify -> revoke). The CC step has no CF secrets; secrets are set from the Worker's deploy workflow; if that workflow lacks the CF token, post BLOCKED with exact secret names and stop. No template changes; do not create the missing wd_* functions; no LMS changes.
Evidence: workers.dev /portal no longer 200; ff.winnerdataai.com/portal row count unchanged; /ff/<uuid> 200; anon RPC ff_portal_leads -> 401/403; bogus uuid -> 404; ff_digest_log row count unchanged before/after.
