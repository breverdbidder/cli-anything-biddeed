# Intent — issue #19715 — GHA-only skill eval loop

## Originator
Ariel Shapira (founder), 2026-09-02: "Rebuild the eval loop GHA-only, off Hetzner, and gate it on quota_gate_check once the user:profile token blocker clears. Until that runs, any claim about skill effectiveness stays UNTESTED under Honesty Protocol V3." Plus, same message: "Delete everything related to Hetzner — we disabled Hetzner and it should be deleted permanently."

## Problem, in the originator's words
The nightly skill-evolution loop ran on an external SSH box that is now retired. Its replacement never existed, so nothing measures whether the ~42 skills in .claude/skills/ actually fire during a CC dispatch. Every claim about skill effectiveness is currently unfalsifiable.

## Outcome that counts as done (DoD)
1. .github/workflows/skill-eval.yml exists, ubuntu-latest, workflow_dispatch + weekly cron, contains ZERO ssh / *_SSH_KEY / host-IP references.
2. Step 1 calls public.quota_gate_check('engineering') and records the payload in skill_eval_runs.quota_gate. NO_READING → proceed capped at 15 skills; any other deny → exit 0 without evaluating; allow=true → uncapped.
3. >=1 row in public.skill_eval_runs with real git_sha and real quota payload.
4. >=1 row in public.skill_eval_results per skill directory found (skills without eval.json get outcome='skipped', never omitted).
5. public.v_skill_effectiveness returns >=1 row.
6. autoloop.yml deleted; zero workflow files match ssh|HETZNER|87\.99\.129\.125; remaining repo hits enumerated in the spec.
7. docs/spec/19715.md with a per-skill invoked/not-invoked table and an explicit list of skills still UNTESTED.

## Non-goals
No cc-runner-ghonly.yml edits. No attempt to fix the anthropic_oauth_bearer user:profile scope. No new Supabase tables. No sends. No edits to skill CONTENT — this run measures, it does not improve.

## SSOT this task must use
- public.skill_eval_runs / public.skill_eval_results / public.v_skill_effectiveness (migration skill_eval_tables, applied 2026-09-02, RLS on, no anon policy). These already exist — write into them, do not create alternatives.
- public.quota_gate_check(category) — returns {allow, reason}. Current live value for 'engineering': {allow:false, reason:'NO_READING', note:'Telemetry missing. Denying by design.'}. Root cause is the vault secret anthropic_oauth_bearer being an inference-scoped token without user:profile. Documented, out of scope here.
- scripts/eval_runner.py — makes zero network/Supabase calls by design; reuse standalone.
- PostgREST + SUPABASE_SERVICE_ROLE_KEY for all writes. No direct psql.

## Task-specific mandates
- skill_invoked is the entire point. If it cannot be determined reliably from the installed claude version's output, write NULL and say so in evidence. Never guess true.
- A skill with no eval.json gets a 'skipped' row so it is visibly untested, never silently absent.
- Retiring the SSH path is part of THIS issue, not a follow-up. A disabled-but-present SSH workflow does not satisfy the DoD.

## Negative test
public.rls_gate_check('skill-eval session') returns passed=true, new_violations=0.

## Constraints
The Supabase instance restarted 3x in 25 minutes on 2026-09-02 (public.db_restart_log). Every PostgREST write must be retryable and idempotent; a mid-run restart must not leave a skill_eval_runs row with finished_at NULL and no results.
