# Intent — #20035 LAUNCH-A: in-Worker rate limiting + edge cache + security headers
Precedence: this file > docs/spec/20035.md > issue body > comments.
Intent: every public Worker (biddeed-mcp-production, worker-damp-snowflake-cead, biddeed-web-production, zonewise-web-production) enforces per-IP/per-key rate limits in code (Workers Rate Limiting binding — Cloudflare account is on Workers FREE), edge-caches /auctions JSON and /county/:slug for 60s, and sends HSTS/nosniff/Referrer-Policy. Zero product-logic changes; checkout/Stripe paths untouched.
Hard constraints: deploy only through each repo's existing production workflow (deploy-worker.yml, cloudflare-mcp-production.yml, cloudflare-production.yml); never raw CF API pushes (Aug 7 outage). zonewise-web main is protected — PR only. No WAF/dashboard actions, no plan changes (Ariel). Never print secrets; use vault RPC helpers for an authenticated MCP smoke test.
Evidence: pasted curl showing 429 + Retry-After after the unauth threshold; cf-cache-status: HIT on second /auctions hit; headers present; authed tools/list still returns tools; per-key 5 req/s for 20s NOT limited.
Non-goals: winnerdata-ff/lms/cfo Workers; schema; Chatwoot; plan upgrade.
Rollback: revert commit, redeploy via same workflow.
