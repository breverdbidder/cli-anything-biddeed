# Intent — #20026 EXIT VERCEL B: zonewise.ai (+www, +mcp) → Cloudflare Worker zonewise-web-production
Precedence: this file > docs/spec/20026.md > issue body > comments. Ariel's go for a same-day cutover was given 2026-09-05 ~07:40 EDT. A red gate still means NO cutover.

## Hard guardrails
1. REPO: work in `/tmp/zonewise-web` (clone of breverdbidder/zonewise-web), branch `cf-exit`, never main (protected; Ariel merges). Commit+push from inside the clone BEFORE exiting; the wrapper's post-run commit only sees cli-anything-biddeed. Specs/rollback files go in THIS repo under docs/spec/.
2. SECRETS: never print values. `vercel pull` and `wrangler secret bulk` use a temp dir removed in the same step. Key NAMES only in logs. The zonewise Clerk keys come from the Vercel production env pull — verify the pk_ is the zonewise app, never biddeed's.
3. PROCESSES: never `pkill node` / `killall node` / `pkill -f` on a generic name (self-kill, exit 143 — #19678). Background `wrangler dev`, keep `$!`, kill by PID/port. Never `next start`.
4. INSTALL: `--legacy-peer-deps` is mandatory (vercel.json installCommand). Node 22.
5. DNS: no DNS change without docs/spec/20026-rollback.md committed first (ids dd1d4f1d75e2e5db26a9bf3cd2fa52e7 `A 216.150.1.1`, bb6230a10d845c516bb0a91a2a7f079d `www CNAME b655eaa023f33e94.vercel-dns-017.com`). Clerk/Google/MX/TXT records in the zone are untouchable.
6. TOKEN SCOPE: if the CF token cannot do DNS/Routes, finish everything up to workers.dev gates and comment `READY FOR CUTOVER …`; chat performs DNS and re-dispatches with `bind_domains=true`.
7. SPEND: `"limits": {"cpu_ms": 30000}` is the plan probe; rejected → STOP before cutover, report "Workers Paid ($5/mo) needed". No Cloudflare Images, no paid cache. R2 only within free tier and only if ISR is actually used.
8. NEXT VERSION: Next 16.1.6 — confirm @opennextjs/cloudflare supports it before building; unsupported → STOP and report options (do not downgrade Next on your own).
9. mcp.zonewise.ai: bind only if the app really serves an MCP route (grep app/api for it and ZONEWISE_MCP_SECRET). Otherwise report "no route" — do not invent one.
10. EVIDENCE: Honesty V3. Record Vercel's current responses for /api/zoning-chat and /report BEFORE cutover and compare; screenshots from Playwright at 390 + 1440; `server: cloudflare` and zero `x-vercel-*` on the real hostnames.
