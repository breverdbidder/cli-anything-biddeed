# Intent — #20036 LAUNCH-B: status.biddeed.ai via Upptime
Precedence: this file > docs/spec/20036.md > issue body > comments.
Intent: public status page + 5-minute checks with zero servers (GHA-ONLY mandate, $0): breverdbidder/status from the upptime/upptime template, GitHub Pages, CNAME status.biddeed.ai (DNS-only). Monitors listed in the issue; Supabase check uses the PUBLIC anon key only.
Hard constraints: no paid uptime vendor; no Kuma/Gatus/Grafana; incidents = GitHub Issues in breverdbidder/status only (no Telegram/SMS/Slack — Ariel's no-external-channels rule). DNS record only from a workflow step that holds CF_API_TOKEN; if scope is missing post "READY FOR DNS" with the exact record and stop.
Evidence: curl -sI https://status.biddeed.ai -> 200 (or READY FOR DNS comment); one green Upptime run; negative test with a deliberate 404 monitor detected then removed; SSOT §1 row added in the same commit.
