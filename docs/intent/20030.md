# Intent — #20030 GTM-1 production run
Precedence: this file > docs/spec/20030.md > issue body > comments. Operating document: docs/gtm/GTM_SOP_v1.md.

## Hard guardrails
1. NOTHING PUBLISHES (M8, dial < 3). Renders, variants, verdicts, queue-ready assets only.
2. Render path = biddeed-reels-daily.yml / Variant Studio scripts (deterministic). Do not hand-produce MP4s in the agent session; fix the pipeline instead. The Sep 3 lesson: "success" runs with zero DB effect are failures — check row counts after every run.
3. Every $ figure in a title/caption/voiceover traces to `multi_county_auctions` or `zw_parcels`; no assessed-value-as-market-value claims; tax deed clears ~40% of assessed vs foreclosure ~61% only if recomputed live.
4. 32.000 s exactly; parcel outline in every aerial frame; Street View mandatory; eleven_v3 voice TX3LPaxmHKxFdv7VOQHJ for EN; kokoro allowed for non-EN.
5. Never-list scan is mandatory on title, caption, on-screen captions AND voiceover transcript. REJECT on any hit with the matched token in the reason.
6. Never write to `reel_variant_review` (Ariel's ground truth) or `spi_gates`.
7. LLM calls: OpenRouter GLM-5.3-flash first (vault openrouter_api_key, $10/wk cap), DeepSeek fallback, claude-router t2 last. Never the Anthropic API. Gemini vault keys are 429 — do not retry them.
8. SECRETS: names only. PROCESSES: no generic `pkill`; kill by PID/port.
9. Volume rule: >3 days of slots pending → stop and report; do not add agents.
