# Intent — #20040 GTM-5 scoped deal-page + imagery backfill

Outranks the issue body. Read docs/spec/20032.md first.

1. Scope is a LIST OF 10 CASE NUMBERS, written to docs/spec/20040.md BEFORE any data write. If the pipeline entrypoint cannot take a case-number list, add that argument. A run by date, county, or "pending_approval" set is a violation — stop and report BLOCKED instead.
2. Zero LLM calls to OpenRouter, Gemini, ElevenLabs, or any Anthropic API / Max OAuth router. Deal pages are templated from auction data; if any step needs an LLM, skip that step, mark UNKNOWN, and say so.
3. Never write winnerdata.reel_variant_review or public.spi_gates.
4. No YouTube upload, no social publish, no email/SMS send, no Resend call.
5. Secrets: names only in comments/spec. Never print values. Never echo env.
6. No generic `pkill`/`kill` of runner processes; no changes to .github/workflows other than adding an argument passthrough if the pipeline runs via a workflow.
7. Street View / Maps calls: only for the 10 case numbers, one image set each. Report the exact API call count.
8. Honesty V3: every line VERIFIED / INFERRED / ASSUMED / UNKNOWN. "Nothing is SHIPPED until the HTTP checks in issue §3 pass live."
9. Before/after row counts on biddeed_reels, reel_links, and the deal-page store are mandatory in the final comment.
10. If Supabase drops the connection (known: project reboots every 10–15 min), retry the same statement up to 3× with 30 s backoff; do not re-run a whole pipeline stage that may have partially written.
