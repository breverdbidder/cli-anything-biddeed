# Intent — issue #19711 — register the artifact-chain convention in SSOT docs

## Originator
Ariel Shapira (founder), via chat-Claude, 2026-09-02. First dispatch under the new wrapper (cc-runner commit 0fd0aa19).

## Problem, in the originator's words
On 2026-09-01 an FF batch went to producers without approval because the mandate lived only in an issue comment CC never read. The fix (docs/intent/, docs/spec/, mandates injected by the wrapper) is committed but not yet registered in BIDDEED_SSOT.md or CC_META_PROMPT.md, so the next architect session or CC run has no SSOT pointer to it.

## Outcome that counts as done (DoD)
1. BIDDEED_SSOT.md gains "### Artifact chain (2026-09-02)" under "## CANONICAL CC DISPATCH".
2. CC_META_PROMPT.md gains one "Artifact chain:" rule line.
3. docs/spec/19711.md exists, DoD table filled, and quotes the brief's section headers as evidence the wrapper injected mandates + this intent.
4. One public.agent_ops_log row, task='artifact_chain_first_dispatch'.

## Non-goals
No edits to .github/workflows/*, docs/intent/MANDATES.md, or any Supabase table except agent_ops_log. No restructuring of BIDDEED_SSOT.md beyond the one subsection.

## SSOT this task must use
- docs/intent/README.md and docs/spec/README.md (the convention text — quote, don't paraphrase)
- public.unified_context key artifact_chain_convention_v1 (decision row, commit shas)
- public.agent_ops_log columns: dispatch_id, task, status, evidence, severity

## Task-specific mandates
Keep the SSOT edit additive. If BIDDEED_SSOT.md has no "## CANONICAL CC DISPATCH" heading, add the subsection at the top of the file and say so in the spec.

## Negative test
cc-runner-ghonly.yml blob sha 7ad7536629f5a6d92742bd252d2966d51d06a110 unchanged after your commit.

## Constraints
Single session. Small task — should finish well under 30 min of agent time.
