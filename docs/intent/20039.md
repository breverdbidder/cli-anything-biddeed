# Intent — #20039 LAUNCH-E: DB stampede diet (GHA side)
Precedence: this file > docs/spec/20039.md > issue body > comments.
Intent: cut scheduled GitHub Actions load on Supabase before launch — cap fl-parcel-centroids-all to 2 workers with a restart-aware pre-flight, slow centroid-watchdog to 2h until Sep 10, consolidate three sentinel workflows to one schedule (7,37 * * * *) and two SUMMIT verifiers to :41, move every DB-touching schedule off :00/:15/:30/:45, and disable-with-comment the two chronically failing schedules after reading their logs (gtm_factory_halt OPEN is by design — never "fix" the gate).
Hard constraints: schedules/matrix/timeouts only — zero business logic; no table drops (fl_parcels_stage is Ariel's call); no pg_cron edits (done from chat: cron_stagger_launch_hardening_20260905); no compute upgrade; leave cc-runner, cc-oauth-keepalive and winnerdata-ff-send-approved schedules alone; never self-redispatch.
Evidence: before/after hourly table from public.db_restart_log; scheduled-run rate 2 hours after merge; docs/ops/GHA_SCHEDULE_MAP.md; negative test = pre-flight skip observed. If restarts do not drop, state it plainly — that is the finding.
