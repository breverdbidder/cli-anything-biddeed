# Issue 19740 — Gold Standard shard-2: broward, okaloosa, walton, taylor, lake

dispatch_id: `10f34bfe-2344-4548-b2a2-9de92f46b885` · loop run 16395 · chat_session `architect-20260902T160000`

## What this session actually built

Ran the ULTRALOOP protocol as a background Workflow (7 fix agents fanned out per failing
letter, each followed by an independent adversarial-refuter agent). No human in the loop
(non-interactive `claude -p` dispatch); all baselines and final scores below were queried
live from `public.pencil_dod_evaluate_county` via PostgREST (direct psql is a known-broken
constraint in this environment — every read/write in this session went through PostgREST
using `SUPABASE_SERVICE_ROLE_KEY`, per CLAUDE.md guidance).

## Scoreboard — before vs after (VERIFIED, live RPC calls pasted below)

| County | Before | After | Letters fixed | Letters still failing |
|---|---|---|---|---|
| broward | 9/10 | **10/10** | I (93.8%→96.7%) | none |
| walton | 9/10 | **10/10** | I (94.9%→95.6%) | none |
| lake | 6/10 | **8/10** | E (93.9%→96.6%), I (91.2%→95.9%) | C (86.4%→87.8%, still FAIL), J (93.2%, unchanged — downstream of a fleet-wide generator gap, out of this session's scope) |
| okaloosa | 9/10 | 9/10 (unchanged) | none | I (92.9%, confirmed genuine ceiling — see below) |
| taylor | 8/10 | 8/10 (unchanged) | none | B/F (null/null, confirmed genuine ceiling, 6th session on this pair) |

### BEFORE (pasted from live RPC at session start)
```json
broward: I FAIL 93.8 [card_complete=819 of 873] — A,B,C,D,E,F,G,H,J all PASS
walton:  I FAIL 94.9 [card_complete=150 of 158] — A,B,C,D,E,F,G,H,J all PASS
lake:    C FAIL 86.4 [matched_clean=127] E FAIL 93.9 [parcel_linked=138] I FAIL 91.2 [card_complete=134 of 147] J FAIL 93.2 [deal_complete=137]
okaloosa: I FAIL 92.9 [card_complete=79 of 85] — rest PASS
taylor:  B FAIL null [verified=0 closed_sold=0] F FAIL null [tier1_sold=0 closed_sold=0] — rest PASS
```

### AFTER (pasted from live RPC, re-run independently post-close-out, 2026-09-02 ~16:45Z)
```json
broward: {"A":36✓,"B":100.0✓,"C":98.7✓,"D":98.9✓,"E":99.3✓,"F":100.0✓,"G":98.8✓,"H":0.1✓,"I":96.7✓ [card_complete=844 of 873],"J":98.1✓} auctions_total=873
walton:  {"A":49✓,"B":100.0✓,"C":96.2✓,"D":96.2✓,"E":98.1✓,"F":100.0✓,"G":97.2✓,"H":0.1✓,"I":95.6✓ [card_complete=151 of 158],"J":100.0✓} auctions_total=158
lake:    {"A":11✓,"B":100.0✓,"C":87.8✗ [matched_clean=129],"D":100.0✓,"E":96.6✓ [parcel_linked=142],"F":100.0✓,"G":96.2✓,"H":0.3✓,"I":95.9✓ [card_complete=141 of 147],"J":93.2✗ [deal_complete=137]} auctions_total=147
okaloosa:{"A":28✓,"B":100.0✓,"C":97.6✓,"D":97.6✓,"E":97.6✓,"F":100.0✓,"G":100.0✓,"H":0.3✓,"I":92.9✗ [card_complete=79 of 85],"J":100.0✓} auctions_total=85
taylor:  {"A":4✓,"B":null✗,"C":100.0✓,"D":100.0✓,"E":100.0✓,"F":null✗,"G":100.0✓,"H":1.5✓,"I":100.0✓,"J":100.0✓} auctions_total=14
```

## Per-letter detail

### broward I — FIXED, PASS (VERIFIED)
BCPA (Broward County Property Appraiser, web.bcpa.net) folio lookup for
assessed_value/market_value/situs address on 27 lookupable folios (of a 54-row fresh gap);
BCPA ArcGIS Parcels layer (`gisweb-adapters.bcpa.net` MapServer/16) polygon centroid for
lat/lon, US Census geocoder fallback for alt-key condo/timeshare folios. 25 of 27 rows
written (one PATCH silently no-op'd on a client timeout — caught by a fail-loud GET
re-check and retried). Remaining 29-row residual gap is a genuine ceiling: 10 truncated
6-digit folio stubs with no BCPA record, 11 placeholder parcel_ids, 6 NULL parcel_id, 2
alt-key folios that failed Census geocoding.

Files: `scripts/gold_standard_broward_i_batch_20260902.py`,
`supabase/migrations/20260902m_gold_standard_broward_i_bcpa_backfill.sql`

Adversarial verify: **SURVIVED**. Refuter independently rebuilt the 844-count from raw
`multi_county_auctions` columns (not the RPC) and landed on exactly 844; confirmed
zero PropertyOnion/`%promote%` contamination on spot-checked rows; confirmed no
regression on A/B/C/D/E/F/G/H/J. Also flagged and independently reproduced a real
PostgREST filter-semantics trap in the fresh-gap discovery query (`NOT ILIKE` against a
NULL `data_source` column evaluates to NULL, silently dropping rows) — worth carrying
forward into future sessions' query templates.

### walton I — FIXED, PASS (VERIFIED)
Single fresh row (case 25CA000404, added same-day by `calendar_sweep_mca_v3`) already had
address/value/parcel_id but was missing geo coordinates. Geocoded and PATCHed.

Adversarial verify: **SURVIVED**, exact match on independent live re-query.

### lake E — FIXED, PASS (VERIFIED)
Owner-name-match re-run (same method as `scripts/shard14_lake_e_ownername_match.py`) on 9
fresh unlinked candidates; 4 matched to a unique Lake County parcel and linked.

Adversarial verify: **SURVIVED**.

### lake I — FIXED, PASS (VERIFIED)
7 GIS-sourced `parcel_zones` inserts covering unincorporated Lake County
(MapServer/50), Eustis (CityFLU/MapServer/2), Leesburg (Planning_Zoning GIS), Clermont
(LocalGov/CityZoning MapServer/1), and a newly-discovered Lady Lake Zoning FeatureServer
(`services5.arcgis.com/WSrmy5ECedUbsQ39/.../Zoning/FeatureServer/0`, live HTTP 200,
not used by any prior session). Of the 3 previously-documented "permanently GIS-blocked"
parcels, 2 were already resolved by a prior (unrelated) session; only
`011926060000202200` (case 2024CA002034) remains a genuine ceiling after checking every
reachable Lake zoning layer. 5 rows with no `parcel_id` at all were correctly left for
the E lane rather than faked.

**Self-caught regression**: the Clermont insert briefly used the raw ArcGIS attribute
string `'PUD PLANNED UNIT DEVELOPMENT'` instead of the county's normalized `'PUD'` code,
which regressed letter G from 96.0%→66.7% for one session-internal window (visible as a
transient dip in the lake-C item's "after" snapshot, captured mid-flight by a
concurrently-running pipeline stage). The same agent caught and corrected it before
returning its claim; the live final state confirms G=96.2% PASS. Flagging this
explicitly per the Honesty Protocol rather than letting the transient number stand
uncommented — no regression persists in production.

Files: `lake_i_7row_zone_link_gpass_2026-09-02.sql`

Adversarial verify: **SURVIVED**. Refuter independently confirmed all 7 parcel_zones
rows exist live with clean non-promote/non-PropertyOnion `data_source` values, confirmed
the Clermont correction landed (single row, no orphan), confirmed the Lady Lake
FeatureServer is real and reachable, and reconciled the residual math (147−141=6 =
5 null-parcel + 1 genuinely-blocked) exactly.

### lake C — PARTIAL, still FAIL (VERIFIED)
2 of 20 non-matched rows were genuinely fixable (a stale reschedule and a
stuck-at-scheduled-actually-sold row); C moved 86.4%→87.8%, still short of the 95% bar.
The remaining 18 rows were re-checked fresh via chronological docket review and confirmed
genuinely `CLERK_SSOT_CANCELLED` — this is a real data ceiling, not a matcher bug. This
narrows (does not overturn) the earlier fleet-wide "policy question" framing: 2/20 were
fixable, 18/20 are a genuine ceiling.

Files: `lake_c_2row_docket_reschedule_and_stale_sold_fix_11262.sql`

Adversarial verify: **SURVIVED**.

### lake J — unchanged, still FAIL
Not directly targeted this session (deal_complete depends on the fleet-wide
`bid_decisions` generator, which per the campaign's own diagnosis is empty/unmatched
fleet-wide — a separate, larger build called out in "FLEET PRIORITY CORRECTION" and
"BREVARD SPRINT ORDER" item 2, not a lake-specific gap). Deferred; flagging for the next
session rather than spending this shard's budget re-diagnosing a fleet-wide root cause.

### okaloosa I — BLOCKED, confirmed genuine (VERIFIED)
The prior sessions' stated mechanism (Cloudflare Turnstile blocking the Okaloosa PA) was
**refuted** — brightdata hit zero bot-protection on any Okaloosa GIS/bid4assets endpoint
this session. The real root cause of all 6 failing rows: 2 are pre-existing stale
placeholder rows with no `parcel_id`; 4 belong to one consolidated multi-parcel
foreclosure judgment (case 2025-CA-002286, sub-cases F/F2/F3/F4/F5) where bid4assets sold
collateral parcels that are physically in **Walton County** (CO_NO=76, confirmed against
`fl_counties`), not Okaloosa (CO_NO=56) — no Okaloosa zone_code can exist for them without
fabrication. Attempting to correct sub-case F's `parcel_id` to its true value was
**rejected by the database itself**: the unique constraint
`uq_mca_county_sale_date_parcel (county, sale_type, auction_date, parcel_id)` already
holds that exact key on sibling row F2. Zero writes made; zero fabricated data.

Adversarial verify: **SURVIVED** — refuter reproduced the byte-identical unchanged metric
and independently confirmed the CO_NO/fl_counties cross-check.

### taylor B/F — BLOCKED, confirmed genuine (VERIFIED)
6th documented session on this exact pair (prior dispatches ab46d459, c5a8b2c7,
2a942b32). Tested brightdata's Cloudflare-bypass — a capability none of the prior 5
sessions had — against every previously-blocked source (taylorclerk.com,
pubrecords.taylorclerk.com, Schneider Corp QPublic) plus 6 newly-discovered independent
leads, for all 5 closed/past-due cases (25-218 CA, 25-196 CA, 25-217 CA, TDA 26-026, TDA
26-028). No sold-amount data is reachable through any read-only channel. Zero writes
made. Per the brief's own honesty rules this is a legitimate data ceiling, not a thing to
keep re-attempting without a new lever (a phone call to the Clerk's tax-deed department,
850-838-3506 ext 103, remains the only undocumented next step, and that's a human task).

## Close-out artifacts (mandatory per issue body)

- `gold_standard_campaign` row (dispatch_id `10f34bfe-...`) updated with final
  `criteria_passed` JSON, `criteria_total=10`, `exit_reason`, `session_end_at` — VERIFIED,
  PATCH returned HTTP 200 with the row echoed back.
- `gold_standard_ultraloop_audit`: 8 rows inserted (one per county+letter claim:
  broward-I, walton-I, lake-E, lake-I, lake-C, okaloosa-I, taylor-B, taylor-F), all
  `survived=true`, `ultraloop_mode='fallback'` (native `/effort ultracode` menu wasn't
  invoked directly — this ran as an explicit Workflow-tool fan-out per the user's
  ultracode opt-in, which is the documented fallback path).
- Did **not** run `gold_standard_loop()` / `gold_standard_certify()` per the
  PARALLEL-FLEET RULES (other shards may be mid-flight); per-county
  `pencil_dod_evaluate_county` was used for all verification instead.
- Did **not** touch cron jobs 109/111/115 or the gold-standard-loop-* scoring jobs.
- No PropertyOnion data was ever written as a `data_source` — verified per-item by each
  refuter.

## Deferred / next-session priorities

1. **lake C** — 18-row genuine ceiling confirmed; not worth further scraping attempts
   without a new external lever (e.g. a Lake Clerk case-status API not yet discovered).
2. **lake J** — depends on the fleet-wide `bid_decisions`/Shapira-formula generator gap;
   should be picked up by whichever session builds that generator (see campaign's
   "BREVARD SPRINT ORDER" item 2 — same generator, county-agnostic).
3. **okaloosa I** — hard ceiling (cross-county collateral parcels + a real DB unique
   constraint). Only remaining lever: a schema/canon discussion about whether
   sub-case F should even be counted as an Okaloosa row given its true situs is Walton —
   that's a canon-definition question, not a data-fixing task, and is out of this
   session's scope to decide unilaterally.
4. **taylor B/F** — needs a human phone call to the Taylor Clerk tax-deed department;
   6 consecutive automated sessions have exhausted the read-only-scraping search space.

All claims above tagged VERIFIED are backed by a live `pencil_dod_evaluate_county` call
made independently by an adversarial refuter agent, not just by the fixer's own
self-report — full refuter transcripts are in the Workflow task output
(`wbwdwvt7x`, run `wf_406460ae-dbb`).
