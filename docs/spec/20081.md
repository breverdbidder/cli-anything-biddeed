# Spec — issue #20081 — CMO Factory full-site content + consent audit

## Intent reference
No `docs/intent/20081.md` existed — issue body was the only brief, per the dispatch's own artifact-chain note.

## What was built

Files touched (this repo, `cli-anything-biddeed`):
- `docs/gtm/CONTENT_AUDIT_2026-09-07.md` — new. 30 routes/route-groups (23 Worker, 7 biddeed-web) judged against canon, one row per route/finding, citations to file:line or live query. Includes §3a addendum on the PR #20082 fabricated-citation finding (see below).
- `docs/gtm/CONSENT_AUDIT_2026-09-07.md` — new. 8 ranked findings (F1-F8) across every lead-capture point + every automated-send workflow touching `lead_profiles`/consent columns, each with live PostgREST evidence.
- `src/worker.js` — 2-line fix, `/section18-teaser` OG tags (stale "$431K Naples" → correct on-page $329K/$50K/~$335K figures).
- `docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md` — reverted a fabricated clerk citation merged by a concurrent PR (#20082) mid-session, restoring $20,100. See "Critical mid-session finding" below.
- `docs/spec/20081.md` — this file.

Files touched (other repo, `breverdbidder/biddeed-web`, per M5's "clone to /tmp, commit, push from inside the clone" rule):
- `components/deed-home/LandingSections.tsx:142` — corrected Lakewood entry price $28,100 → $20,100 and recomputed per-door ($1,756 → $1,256).

PRs opened (none merged — "PR only, never a push to main" per the issue's own rule):
- cli-anything-biddeed #20083 — Section 18 OG fix
- cli-anything-biddeed #20085 — both audit docs
- cli-anything-biddeed #20086 — reverts PR #20082's fabricated Lakewood citation
- biddeed-web #55 — Lakewood figure fix

PR merged by another actor during this session, under this same issue's lane, and found to contain a fabricated citation (see below): cli-anything-biddeed #20082.

Issue comment posted on #20081 with the two requested decisions (PLANS headline replacement, status-hue question) and a summary of the other outcomes.

`agent_ops_log` row inserted (`dispatch_id='20081'`, `severity='blocker'`) summarizing the three live consent findings with an active/unattended trigger (F1, F2, F4). `honesty_violations` row inserted (id `bc98d02f-9792-4f59-a25b-4490c59cc452`, `severity='CRITICAL'`) for PR #20082's fabricated citation.

## SSOT bindings used

Read-only, live queries this session (no writes to any protected object per M2; the two disposable probe rows created during the consent audit — one `lead_profiles` test address, one `upsert_lead_consent` probe call — were both deleted in the same session, `lead_profiles` count re-verified back to 365 afterward):

- `unified_context` — `biddeed_content_voice_v2`, `m7_founder_carveout_sep3`, `biddeed_tagline_v1`, `cmo_factory_meta_v1`, `cc_standing_mandates_v1`, `artifact_chain_convention_v1` (canon source for every verdict in the content audit).
- `public.lead_profiles` — 365 total rows, 61 with email, 2 with real consent (`email_consent.eq.true OR marketing_consent.eq.true`), 0 with `stage='unsubscribed'`; source breakdown of the 61 (skip_trace_pipeline 32, auction_llc_expansion 14, unsubscribe_link 6, allowed_emails 3, voice_gate 3, test 1, support_escalation 1, reel 1).
- `winnerdata.email_opt_outs` — 6 rows, all one 2026-09-05 backfill.
- `winnerdata.email_suppressions` — 21 rows, `resend_synced=true` on all 21.
- `public.sms_consent_events` — confirmed to exist (schema-error probes), 0 live rows.
- RPC `list_published_content_slugs` / `get_published_content` — 8 live `/answers/:slug` rows, scanned for banned terms (zero hits).
- Live fetch of `everestcapitalusa.com` — used only to independently corroborate the Lakewood $20,100 figure against canon.

## DoD status

| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| A1 | Content audit doc, one row per route, verdict + citation | VERIFIED | `docs/gtm/CONTENT_AUDIT_2026-09-07.md`, PR #20085 |
| A2 | Two open decisions returned with recommendation + rationale | VERIFIED | Issue comment + audit doc §4-5 |
| A3 | Lakewood canon conflict settled with clerk-record citation | VERIFIED, with a mid-session correction | Audit doc §3 — canon + independent live fetch of everestcapitalusa.com, both agree on $20,100; instrument number for this specific parcel not published anywhere and explicitly flagged as unverified-at-that-level. §3a: a concurrent PR (#20082) merged the opposite conclusion with a citation that does not appear on its own cited source — reverted in PR #20086, logged as a CRITICAL honesty violation. |
| A4 | PR per repo with unambiguous copy fixes only | VERIFIED | #20083 (this repo), biddeed-web#55 — both are canon-numeric corrections, no taste calls bundled in |
| B1 | Every capture point: fields/table, consent copy, audit-trail row, unsubscribe-honored, non-consented-send risk, TCPA/CAN-SPAM, B2B-only line | VERIFIED | `docs/gtm/CONSENT_AUDIT_2026-09-07.md`, all 8 capture points + 8 workflows covered with live query or file:line evidence per row |
| B2 | Prove the #20034 defect class is gone everywhere, not just the one workflow | **NOT MET — findings F1/F2/F4** | The digest fix itself holds (verified at HEAD), but the same defect class is live in 3 other places: `insert_reel_lead` hardcodes consent=true (F1), `/unsubscribe`'s RPC doesn't feed the new suppression tables (F2), a second live daily cron doesn't check them (F4). Logged to `agent_ops_log`, not patched — see Deviations below. |
| C | Comment on issue with the two decisions | VERIFIED | https://github.com/breverdbidder/cli-anything-biddeed/issues/20081#issuecomment-5564195994 |

## Negative test result

Repo-wide grep, both repos, 2026-09-07: zero occurrences of "Shapira Max Bid" (exact phrase), "Know your number before the gavel," "14 patents," "enterprise grade"/"enterprise-grade," or "Mariam." These retired/forbidden terms are fully purged, not merely absent from new copy — checked and confirmed unchanged by this session's edits (the 2 fixes made were numeric/OG-tag corrections, not touching any of these terms).

No SMS/Twilio send code exists anywhere in either repo (checked as part of the TCPA scan) — unchanged by this session.

## Critical mid-session finding: a concurrent PR under this same issue merged a fabricated citation

While reconciling the Lakewood canon conflict, `git fetch origin main` surfaced PR #20082 (merged 2026-09-07T02:05:59Z, filed under this issue's own lane per its PR description — "Filed under the CONTENT + CONSENT audit lane, #20081") which had already changed `docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md` from $20,100 to $28,100, citing "recorded tax deed file 160654, OR Book 7806 / Page 1987, Brevard County, Jan 27 2017." A raw `curl` fetch of `everestcapitalusa.com`'s HTML (deliberately bypassing any LLM-summarization layer, to eliminate hallucination risk as the explanation) proved that citation does not appear anywhere on the page it names as its source — the page's raw text says $20,100, January 20 2022, and contains zero occurrences of "28,100", "160654", or "7806" anywhere. Logged as a CRITICAL honesty-protocol violation (`public.honesty_violations` id `bc98d02f-9792-4f59-a25b-4490c59cc452`) and reverted in PR #20086. Full evidence trail in `docs/gtm/CONTENT_AUDIT_2026-09-07.md` §3a.

Also discovered via the same `git fetch`: PR #20084 (a `/deal/:county/:case` visual rebuild, merged 2026-09-07T02:25:32Z) landed on `main` during this session, on top of #20080 (palette/harness canon, already merged before this session started — the `697f6be7` HEAD this dispatch began from). #20084's diff was checked against this session's consent-audit finding F1 (the deal-page lead form has no consent UI) — confirmed the rebuild is CSS/markup only and does not touch the lead form, `ctaBlock`, or the `insert_reel_lead` call, so F1 stands unaffected. Flagging this concurrency explicitly rather than silently treating this session's snapshot of `main` as static: **any session picking up the handoff items below should re-fetch `main` first**, since more concurrent activity may have landed since this session's last fetch.

This session has no visibility into what produced PR #20082 or #20084 — flagging only the verifiable facts (the merged diffs, timestamps, and in #20082's case, a citation that fails to verify against its own named source).

## Deviations from intent

- **Did not fix consent findings F1/F2/F4 (or F3/F5/F6/F8) in-session**, despite the issue asking this lane to "prove that class of defect is gone everywhere." Reasoning: each finding requires an RPC signature change, a schema/migration change, or a live cron's filter logic change — not a copy edit — and M5 scope discipline plus the precedent in `docs/spec/20029.md` (which found the *original* digest bug and explicitly declined to bundle a same-session fix into an unrelated issue) both point toward escalation over an unreviewed same-session patch to production consent-gating logic. Logged to `agent_ops_log` with full evidence and a recommended follow-up-issue priority order instead.
- **Did not pursue a Brevard County Clerk Official Records lookup** for the Lakewood parcel's raw instrument number, despite the issue asking to "establish which is the clerk record, cite it." Two independent canonical sources (internal `unified_context` canon + a live fetch of Everest Capital's own public track-record page, which itself claims to be sourced from "recorded clerk instrument numbers") already agree on $20,100, and the instrument number specifically for this one parcel isn't published on either source (unlike the other 12 track-record entries, which do list instrument numbers). Flagged explicitly in the audit doc as the one piece of this reconciliation not independently verified to instrument-number precision, rather than asserting VERIFIED beyond what was actually checked.
- **Did not treat "Cheaper than one bad bid." or the status-hue question as things to fix** — per the issue's own instruction, these are taste calls that go in the audit doc as recommendations, not PRs. Both are answered with 3 options + a recommendation in `docs/gtm/CONTENT_AUDIT_2026-09-07.md` §4-5 and restated in the issue comment.
- **Did not audit `/answers/:slug`'s 8 live pages word-for-word** the way the legal pages and hero copy were — instead live-queried the RPC, confirmed the slug list matches canon's C3 checklist, and ran a banned-term scan across all 8 bodies (zero hits). Full paragraph-by-paragraph review of AEO answer-asset prose was judged out of proportion to the session's cost/time budget given zero banned-term hits and correct slug-to-canon-checklist alignment; flagged here rather than silently treated as fully audited.

## Open items / handoff

Ranked, per `docs/gtm/CONSENT_AUDIT_2026-09-07.md`'s own recommended order:

1. **F1** (CRITICAL) — `public.insert_reel_lead` (`supabase/migrations/20260903g_reel_landing_sticky_wiring.sql:74-131`) needs a real consent parameter; both `/deal/:county/:case` templates (`src/worker.js:5510-5518`, `:5680-5689`) need real consent UI — currently zero consent language on either.
2. **F2** (CRITICAL) — `public.upsert_lead_consent` (called from `src/worker.js:3176-3180`, no migration file exists for it anywhere in this repo) needs to also write `winnerdata.email_opt_outs`/`email_suppressions`, or every consent-gated sender needs to check `lead_profiles.marketing_consent` directly instead of the suppression tables.
3. **F4** (HIGH) — `scripts/countdown_reengagement_send.py:266` needs the same `email_opt_outs`/`email_suppressions` check `biddeed-daily-digest.cjs` already has.
4. **F3** (HIGH) — blog (`src/worker.js:6309`) and county-page (`:9358`) `/chat/lead` callers need a real consent checkbox instead of a hardcoded `email_consent:true` literal; the `activate@biddeed.ai` template (`:4236-4292`) needs a `List-Unsubscribe` header/footer link.
5. **F6** (MEDIUM) — extend the `sms_consent_events` audit-trail pattern to email consent, starting with `/free-report`'s real checkbox.
6. **F5, F8** (LOW) — `acquisition_cold_email.py`'s unused `email_consent` selection; Pioneer waitlist's hardcoded `marketing_consent=true` (mitigated by being a transactional-style confirmation, still worth a real checkbox for consistency).
7. **Technical, non-content finding**: `/report-success` is referenced by `EXIT_EXCLUDED_PATHS` (`src/worker.js:10186`) but has no live route handler, and `REPORT_SUCCESS_HTML` is dead code — recommend a small separate engineering ticket to either wire the route or remove the dead reference.
8. **Two decisions await Ariel's click**: PLANS headline replacement (recommend option A, "One bad bid costs more than a year of this."), and the status-hue question (recommend confirming words-only, or a shape/weight alternative if `/radar`'s table view's scan-cost is judged a real problem) — see issue comment and audit doc §4-5.
9. **Merge PR #20086** (reverts PR #20082's fabricated Lakewood citation) — until merged, `main`'s `docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md` still carries the wrong $28,100 figure and the fabricated instrument citation. This is the single highest-priority open item from this session — it's a false claim currently live in the canon doc that public copy is generated from.
