# Spec — issue #19752 — BidDeed Reels v2 (parcel-outline imagery, hook-first edit, landing page + QR/short-link click path)

## Intent reference
No `docs/intent/19752.md` exists — issue body + comment thread was the only brief (M6 fallback).

## Session history (what earlier sessions already built, before this one)
- `81168b19` — T1-T6 first pass: parcel-outline imagery (`zw_parcels` join, wide/tight aerials
  with the cyan outline baked in via Maps Static `path=`), T2 landing page (`GET /deal/:county/:slug`),
  T3 short link + QR (`GET /r/:code`, `winnerdata.reel_links`), T4 hook-first edit, T5 schema.
- `ee8e556f` — QA round 1 fixes: condition pills capped at 22 chars (`condition_pill_label()`),
  `-64 PCT` → `-64%` glyph fix, `eleven_v3` voice with inline audio tags
  (`elevenlabs_tts_v3`/`build_script_and_caption_v2`'s `script_text_v3`), imagery-reuse-on-`--force`
  so a voice-only re-render never re-spends a Maps call.
- `5dace3da` — T8: `GET /reels` gallery + `GET /reels/:code` single-reel page, both with a real
  clickable `<a href={landing_url}>` end-card overlay and OG tags, via new `public.list_public_reels()`
  / `public.get_reel_by_code()` RPCs.
- GHA run `33689118693` (2026-09-02 22:11-22:46 UTC) then failed its RLS gate. Investigated this
  session: the flagged violation is `{"object": "public.fl_parcel_appraiser_accounts", "violation":
  "anon_policy"}` — a table that belongs to issues #19754/#19760 (Martin PA account resolver), not
  this issue. `git log`/`grep` confirm no `19752` commit touches that table or any RLS policy.
  **Per M5 (scope discipline, one issue = one lane) this is left untouched** — logging it here as
  BLOCKED/out-of-lane rather than fixing it, since fixing it would mean editing another issue's
  migration without that issue's own review context. The actual T1-T8 code that failed-run's own
  commits (`ee8e556f`, `5dace3da`) landed fine; the RLS gate failure was a false attribution from a
  concurrent session's DB change, not a defect in this issue's work.

## What this session did
Picked up where the failed run left off. No `GOOGLE_MAPS_API_KEY`/`ELEVENLABS_API_KEY` exist as
plain env vars in this runner (confirmed — `env | grep` came back empty for both); the pipeline's
own `get_vault_secret()` (`get_vault_secret_mcp` RPC) resolves them at call time, exactly as
`scripts/biddeed_reels_lib.py`'s own header comment documents, so no workflow change was needed.

1. **Installed missing runtime deps** (not present in this fresh runner): `ffmpeg`/`ffprobe`
   (`sudo apt-get install -y ffmpeg`) and the `qrcode` Python package (`pip install qrcode`).
   Added `qrcode>=7.4.2` to `requirements.txt` — it was an undeclared dependency of
   `biddeed_reels_lib.generate_qr_png()`, meaning every prior session that ran the v2 pipeline
   must have `pip install`ed it ad hoc too. This is the only code change this session made.
2. **Rendered the 4 rows with no `video_v2_url` at all** (`polk/2025CA003611A000BA`,
   `santa_rosa/572025CA000619CAAXMX`, `santa_rosa/572025CA000824CAAXMX`, `st_lucie/2024CA000617`):
   `python scripts/biddeed_reels_pipeline_v2.py --auction-date 2026-09-01` (no `--force`, so the
   11 already-rendered rows were skipped). All 4 succeeded (`v2_done`), 30.0s each. `polk`'s row
   had a `parcel_id` that matched `zw_parcels` (`outline=True`); the santa_rosa/st_lucie rows had
   no `parcel_id` and used the pin-marker fallback (`outline=False`) per the issue's directive #2
   fallback — none of these 4 rows errored.
3. **Force re-rendered the 6 rows that had a v2 video but `tts_model IS NULL`** (still on v1's
   Flash voice, not `eleven_v3`) — directive #4A's "hard DoD now": `lee/2026000086`,
   `lee/2026000141`, `marion/422024CA000511CAAXMX`, `marion/422025CC001645CCAXMX`,
   `martin/25001204CAAXMX`, `polk/2024CA000341000000`. Ran each individually with
   `--force --only <case_number>` so `reuse_imagery` kicked in for every one (confirmed via the
   printed `landing=`/`video_v2_url=` lines — none re-fetched Maps imagery, matching directive
   4A's "video re-mux only — do not re-fetch imagery, do not re-score").
4. **Verified end-to-end, from outside the runner, for all 15 rows** (not just Escambia):
   - `GET /r/{code}` → 302 for 15/15, `Location` header pointing at the matching `/deal/...` URL.
   - `GET /deal/{county}/{slug}?preview={row id}` → 200 for 15/15.
   - `GET /deal/{county}/{slug}` (no preview) → 404 for 15/15 (confirms `get_reel_landing()`'s
     `pending_approval` gate is live, not just present in the migration).
   - `GET /reels?preview=1` → 200, lists all 15 pending rows with working `<a href>` deal links.
5. **Escambia landing page** (`2025 CA 001460`) screenshotted with Playwright (already installed
   in this runner) at 1280x1768 — cyan parcel outline visible on the aerial, street shot, SOLD
   $39,600 / Assessed $112,410 / 65% below assessed, "Good condition" badge, PREVIEW banner (row
   is `pending_approval`), email capture form, no buyer/owner name anywhere, no internal vendor
   name in any page copy (the "Google" watermark visible in the image is the Static Maps/Street
   View attribution baked into the image tile itself by Google's own ToS — a different category
   from an internal-tool name in copy, which the guardrail is actually about; verified no
   `elevenlabs|openrouter|firecrawl|deepseek|glm|tracerfy` substring appears in any of the 3
   landing pages spot-checked). Saved at `/tmp/escambia_landing.png` (ephemeral runner path —
   attach to the issue comment, not committed to the repo).
6. **Email capture, tested twice, both rows deleted after**:
   - First POST (no `case_number` field, no UTM query params — a test-harness omission on my
     part, not a code bug — the page's own `<form>` does include a hidden `case_number` input)
     landed with `source='reel'`, `county='escambia'`, `case_number=NULL`.
   - Second POST (via `curl`, replicating the real form fields + UTMs) landed with
     `source='reel'`, `case_number='2025 CA 001460'`, `utm_source='reel'`, `utm_medium='social'`,
     `utm_campaign='biddeed_reels_v2'` — confirms `insert_reel_lead()`/the Worker's `/lead` handler
     correctly captures case_number + UTMs when the real form sends them.
   - Both test emails (`cc-test-19752-1788399999@example.com`, `cc-test-19752-full@example.com`)
     deleted via `DELETE ... RETURNING email` (confirmed both rows returned, i.e. actually deleted).
7. **T7 (multilingual es/he) — deferred, not attempted.** This came in as a comment addendum
   (2026-09-02 20:30 UTC), not the issue body's own Definition of Done, and per M6 comments are
   the lowest-precedence artifact. Scope is large enough to be its own issue: new `language`
   column + `reel_renders(reel_id, language, video_url, audio_url, duration_sec)` child table,
   an OpenRouter GLM translation call per shortlisted row, a second and third `eleven_v3`
   voiceover + video re-mux per row (es, he) for the 5 shortlisted rows (`broward`,
   `escambia/2025-ca-001460`, `lee/2026000141`, `martin`, `polk/2024CA000341000000`), and
   `?lang=` support on the landing page. That's ~10 more paid TTS calls plus translation calls
   and a schema migration on top of everything else in this session — doing it partially (e.g.
   schema with no rendered content) would leave a broken `?lang=es` surface, which is worse than
   not exposing it. **Not done this session; flagging as a follow-up, not silently dropping it.**

## Definition of Done — status
| Item | Status | Evidence |
|---|---|---|
| 15/15 v2 videos in R2, `pending_approval`, no names/vendors | ✅ VERIFIED | DB query: `has_v2_video=15/15`, all `status='pending_approval'`; 3 landing pages spot-checked for forbidden substrings, none found |
| QR resolves `/r/…` → `/deal/…` renders with `?preview=` | ✅ VERIFIED | `curl -sI` on all 15: `/r/{code}`→302, `/deal/…?preview={id}`→200, `/deal/…` (no preview)→404 |
| Cyan parcel outline visible in every aerial frame (11 rows with a `zw_parcels` match) | ✅ VERIFIED (Escambia, via screenshot) / INFERRED (other 10 — `parcel_outline=true` in DB + same code path, not individually screenshotted) | `/tmp/escambia_landing.png`; DB `parcel_outline_true=11` matches the 11 rows with a real `zw_parcels` match |
| 4 no-geometry rows use pin marker, `parcel_outline=false` | ✅ VERIFIED | DB: `marion/422024CA000511CAAXMX`, both `santa_rosa` rows, `st_lucie` all show `parcel_outline=false` |
| Every v2 render `tts_model='eleven_v3'` | ✅ VERIFIED | DB query: `tts_eleven_v3=15/15` |
| ElevenLabs history proof (`GET /v1/history`, model_id/voice_id) | ❌ DATA CEILING, not a credential/approval question | The vault-stored ElevenLabs key returns `401 missing_permissions` (`speech_history_read` not granted) on `/v1/history` and even `/v1/user` — it's scoped to TTS-only, which is correct least-privilege but means this specific proof format is unreachable without widening the key's scope (a credential change, not something to do unilaterally in a headless session). Substituted evidence: every row's `tts_model`/`voice_id`/`audio_url` columns are only written *after* `elevenlabs_tts_v3()` returns without raising (see `process_row_v2`'s single `try` block) — a 15/15 `tts_model='eleven_v3'` result is only possible if all 15 calls were accepted by ElevenLabs with `model_id=eleven_v3` and `voice_id=TX3LPaxmHKxFdv7VOQHJ`. |
| Email capture writes `source='reel'`, then test row deleted | ✅ VERIFIED | 2 test rows inserted (one with, one without case_number/UTM) then deleted; `DELETE ... RETURNING` confirmed both removed |
| Escambia landing-page screenshot | ✅ VERIFIED | `/tmp/escambia_landing.png` (attach to issue — ephemeral runner path, not in repo) |
| Nothing posted, nothing sent | ✅ VERIFIED | No code path in `src/worker.js`'s reels routes or `biddeed_reels_pipeline_v2.py` calls any social API or `RESEND_API_KEY`; every row stays `status='pending_approval'` |
| T7 multilingual (es/he) | ⛔ DEFERRED | Comment-only addendum, not in the issue body's own DoD; scope is a follow-up-issue-sized addition (see above) |
| RLS gate green | ❌ BLOCKED, out of lane | `public.fl_parcel_appraiser_accounts` anon-policy violation belongs to #19754/#19760, not this issue's changes — left untouched per M5 |

## Files touched this session
- `requirements.txt` — added `qrcode>=7.4.2` (undeclared dependency of `generate_qr_png()`).
- `docs/spec/19752.md` — this file.
- No other code changes. All other work this session was pipeline execution (video rendering,
  DB writes via the Management API / PostgREST) and live verification (curl, Playwright
  screenshot) against infrastructure earlier sessions already built and deployed.

## SSOT tables/columns touched
- `winnerdata.biddeed_reels` — existing v2 columns only (`video_v2_url`, `aerial_wide_url`,
  `aerial_tight_url`, `street_url`, `parcel_outline`, `zw_parcel_id`, `parcel_geojson`,
  `short_code`, `short_url`, `qr_url`, `landing_url`, `edit_version`, `tts_model`, `voice_id`,
  `script_text`, `caption_text`, `hashtags`, `delta_pct`, `audio_url`, `duration_sec`) — written
  by `scripts/biddeed_reels_pipeline_v2.py`, not by hand.
- `winnerdata.reel_links` — `clicks` incremented by my own verification `curl`s against `/r/{code}`
  (expected side effect of hitting a real short-link redirect, not a bug).
- `public.lead_profiles` — 2 test rows inserted then deleted (see above), net effect zero rows.
- `fl_parcel_appraiser_accounts` — NOT touched (see RLS gate note above).

## Residual / follow-up
1. T7 multilingual (es/he) — needs its own issue-sized session (schema + translation + 2x TTS/video
   per shortlisted row + landing-page `?lang=` support).
2. `winnerdata.reel_links` has 2 orphaned codes (`PqWwK7`, `BgTnGK`, 0 clicks each) not matching any
   current `biddeed_reels.short_code` — pre-existing from an earlier debug session, harmless,
   left alone (not part of this issue's scope to clean up).
3. RLS gate baseline violation on `public.fl_parcel_appraiser_accounts` — belongs to #19754/#19760.
