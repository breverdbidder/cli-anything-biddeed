# Spec — issue #<N> — <short title>

<!-- Written by CC at the END of the session (mandate M6), or by chat-Claude
     before dispatch for large tasks. This is the machine-actionable record of
     what was actually built, so the next agent starts cold with zero chat history. -->

## Intent reference
docs/intent/<N>.md @ <git sha>

## What was built
Files touched (path — one-line purpose). Migrations applied (name). RPCs/views created.

## SSOT bindings used
table.column / RPC / view actually read or written, with one sample row or count.

## DoD status
| # | DoD item | Status (VERIFIED/UNTESTED/BLOCKED) | Evidence |
|---|----------|------------------------------------|----------|

## Negative test result
What was checked, and that it is still unchanged.

## Deviations from intent
Anything done differently and why. Empty = none.

## Open items / handoff
What the next session must pick up, with exact object names.
