# Spec — issue #19798 — Architect triage of blocked issue #19773

<!-- Written by CC at the END of the session (mandate M6). -->

## Intent reference
None at docs/intent/19798.md — issue body was the only brief.

## DoD under triage
`SELECT EXISTS (SELECT 1 FROM public.gold_standard_certifications WHERE county_slug = ANY('{lafayette,st_lucie,martin,lee,seminole}'::text[]) AND certified)` — last_error: none logged.

## Diagnosis (VERIFIED live, 2026-09-03)

`gold_standard_certifications` for all 5 counties: `certified=false` (queried directly). None of the 5 counties are at 10/10 in `gold_standard_county_status` (loop_run 16698, 2026-09-03T13:30Z) — the DoD is **legitimately unmet**, not a system malfunction, for 4 of 5 gap classes:

| County | Score | Blocking letter(s) | Class |
|---|---|---|---|
| lafayette | 9/10 | C 75.0% | **Structural, permanent** — 1 of 4 auctions is a confirmed `CLERK_SSOT_CANCELLED` sale; a cancelled sale can never be `matched_clean`. Cannot improve without a 5th auction entering the calendar. |
| st_lucie | 9/10 | C 78.5%→78.9% | **Structural** — 51-row `CLERK_SSOT_CANCELLED` block, independently re-confirmed live this session's predecessor. |
| martin | 9/10 | I 94.3% | **Cross-pipeline dependency** — needs fleet-wide zoning-crosswalk ingestion (tracked separately in the campaign's G/I workstream), not more auction-data work. |
| lee | 6/10 | C/D/E/I | **Real backlog, in progress** — issue #19773's own session shipped +150 rows via live harvest this morning (C/D 50.7%→72.8%, E 73.8%→89.6%); I additionally needs the same zoning-crosswalk dependency as martin. |
| seminole | 5/10 | C/D/E/I/J | **Real backlog + one genuine evaluator bug** (fixed this session, see below). E capped by a duplicate-stub unique-index conflict needing a per-row dedup decision (not attempted — see Deferred). |

Traced the actual #19773 GHA run (33731083915, 08:01–08:32Z) and its committed `docs/spec/19773.md`: that session did honest, independently-verified work and correctly declined to force-fix genuine structural ceilings (hard rule #5). It explicitly surfaced two architect-level decisions as out of scope for a per-county data-fix session.

## Fix shipped (within autonomous authority — function patch, non-destructive, precedent-following)

**Migration:** `supabase/migrations/20260903h_pencil_dod_matched_any_redeemed_cancelled_gap.sql`, applied live via the Supabase Management API `database/query` endpoint (documented working channel — direct psql/`db push` unavailable in this sandbox, matches prior documented constraint).

**What:** `public.pencil_dod_evaluate_county()`'s letter-D `matched_any` allow-list accepted `CLERK_SSOT_CANCELLED` (a clerk-confirmed cancelled sale — no litmus match is possible because the sale never happened) but not `REALTDM_REDEEMED` / `REALTDM_CANCELLED`, the structurally identical terminal states from the RealTaxDeed platform lane. Verified via `pg_get_functiondef` that neither string appeared in the live function body before this migration — an omission, not an intentional exclusion. `C` (`matched_clean`) is untouched by design; a redeemed/cancelled sale still can never be a genuine litmus match.

**Blast radius checked before shipping** (live query, whole fleet, old vs new D):

| County | D before | D after | Flips PASS? |
|---|---|---|---|
| brevard | 94.7% FAIL | 95.4% PASS | **yes** |
| lee | 72.8% FAIL | 91.0% FAIL | no |
| santa_rosa | 35.8% FAIL | 46.3% FAIL | no |
| sarasota | 68.5% FAIL | 90.4% FAIL | no |
| seminole | 79.6% FAIL | 84.7% FAIL | no |

B/F structurally unaffected: 0 of 295 `REALTDM_REDEEMED` rows and 0 of 3 `REALTDM_CANCELLED` rows carry `sold_amount`. No county outside this list is touched.

**Re-verified live post-migration** (`pencil_dod_evaluate_county` called fresh for all 5 shard counties + brevard/sarasota/santa_rosa): exact predicted deltas observed, zero regressions on any of the 10 letters for any of the 5 shard counties.

## Deferred (not fixed this session — judgment call, not human-approval-gated)

**Seminole E** (83.7%, capped): 32 rows blocked from getting `parcel_id` by unique index `uq_mca_county_sale_date_parcel (county, sale_type, auction_date, parcel_id) WHERE parcel_id IS NOT NULL` — a sibling row already holds the same key. Confirmed live: exactly 32 seminole rows with `parcel_id IS NULL` among tier1 rows. Resolving this requires a per-row canonical-row decision on a protected table (`multi_county_auctions`) — not a mechanical fix, and forcing an automated merge risks silently discarding a real auction record. Left for a dedicated data-investigation session, as the prior engineer correctly judged.

This is **not** a human-approval-gated blocker per the triage protocol (no credentials/dashboard/spend/protected-schema action needed) — it's substantive engineering work already covered by the campaign's existing 3x-daily shard cadence.

## SSOT bindings used
- `public.gold_standard_certifications` — read-only (DoD check).
- `public.gold_standard_county_status` — read-only (live per-letter metrics, trend history).
- `public.pencil_dod_evaluate_county(text, timestamptz)` — **function patch** (this session's only write besides logging).
- `public.pencil_dod_evaluate_county_rows` / `public.gold_standard_loop()` — read-only, confirmed both call through the patched function (sole RPC source, no divergent copy to fix separately).
- `public.multi_county_auctions` — read-only this session.
- `public.decision_log` — 1 insert (id=3145, decision_type=triage).
- `summit_chat_dispatch` (row `f10e941c-...`, issue 19773) — read-only. Noted but not touched: `state='dispatched'`, `completed_at=null`, `attempt_number=2` despite `max_attempts=1` and the GHA run itself completing successfully — a dispatch-tracking discrepancy outside this triage issue's scope (INFERRED, not investigated further).

## Verification evidence
- `SELECT county_slug, certified, revocation_reason FROM gold_standard_certifications WHERE county_slug = ANY('{lafayette,st_lucie,martin,lee,seminole}')` → all `certified=false` (pre- and post-fix; unchanged, correctly).
- `SELECT pencil_dod_evaluate_county('<county>')` run live for all 5 shard counties + brevard/sarasota/santa_rosa, before and after the migration — deltas match the predicted blast-radius table exactly.
- `pg_get_functiondef` diffed before/after to confirm only the two enum values were added, nothing else changed.

## Deviations from intent
None — followed the ARCHITECT TRIAGE MANDATE protocol as specified: read the #19773 thread, queried the DB directly, diagnosed before writing code, fixed the one item within autonomous authority, left the rest honestly documented as either structural/permanent or requiring a follow-up engineering session (not a human-approval blocker), logged to `decision_log`, commented findings on #19773.

## Open items / handoff
- **lafayette**: permanently capped at 9/10 unless a new auction enters the calendar. No further action possible.
- **st_lucie**: C capped by 51-row structural cancellation block; no further auction-data action expected to move it.
- **martin, lee**: I blocked on the fleet-wide zoning-crosswalk ingestion workstream (already tracked in the campaign's G/I diagnosis); lee additionally needs +151 C/D rows and +37 E rows via continued harvest (in progress).
- **seminole**: E needs the 32-row dedup decision (flagged above); C/D has residual gap after this session's fix; J needs continued `bid_decisions` generation.
- The `summit_chat_dispatch` `attempt_number=2 > max_attempts=1` / `completed_at=null` discrepancy for issue 19773 may be why architect triage fired even though the session's own run completed successfully and made real progress — flagged for whoever owns dispatch-completion tracking, not fixed here (out of this issue's named scope).
