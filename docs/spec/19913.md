# 19913 — Architect triage: blocked issue #19838 (Gold Standard shard-3)

## Role
Architect triage (not engineer) on issue #19913, dispatched to diagnose why issue #19838's DoD keeps failing.

## DoD under triage (from #19838)
```sql
SELECT EXISTS (
  SELECT 1 FROM public.gold_standard_certifications
  WHERE county_slug = ANY('{gilchrist,st_lucie,madison,union,sumter}'::text[])
    AND certified
);
```
**Result at end of this session: FALSE** (re-verified live).

## Diagnosis

### 1. The prior GHA "failure" was a false alarm, not a bug
Run `33851387054` reported `rls_gate=failed`, but the captured log shows the RPC call to
`rls_gate_check()` hit a **Cloudflare 520** ("Web server is returning an unknown error") from
Supabase, not an actual RLS/anon violation. The gate fails closed on an unreachable check by
design. Re-running `rls_gate_check()` in this session returns:
```json
{"new": [], "passed": true, "baseline_size": 307, "new_violations": 0, "total_violations": 307}
```
— identical to the grandfathered baseline, 0 new violations. **No RLS regression was ever introduced.**
The prior session's real work was not lost: commit `7e9b6fd4` ("gilchrist 10/10, union 9/10") is on
`main`, and the checkpoint is in `gold_standard_campaign` id=5692 (dispatch_id matches #19838's brief,
`c77dbc8d-f2f1-4ea8-a61f-340bf78381ba`, session_end_at 2026-09-04T08:17Z, exit_reason=timeout).

### 2. Live state of the 5 shard counties (VERIFIED via `pencil_dod_evaluate_county`)

| county | A-J live | blocking letters | certification status |
|---|---|---|---|
| gilchrist | **10/10 PASS** | none | `certified=false`, `consecutive_gold` 0→1 this session |
| st_lucie | 9/10 | C=78.1% (matched_clean 196/251) | `certified=false`, `consecutive_non_gold`=318 |
| madison | 8/10 | B/F=null (closed_sold=0) | `certified=false`, `consecutive_non_gold`=608 |
| union | 6/10 | C/E/I/J ~80% (4-5 auctions total, tiny denominator) | `certified=false`, `consecutive_non_gold`=608 |
| sumter | 6/10 | C/E/I/J 88-94% | `certified=false`, `consecutive_non_gold`=195 |

Every failing letter above is already independently diagnosed with live evidence in prior sessions
(`decision_log` / `gold_standard_campaign` history): dead leads with zero address on any reachable
source, Cloudflare-Turnstile-blocked lookups, `CLERK_SSOT_CANCELLED` by-design exclusions from
`matched_clean`, or auctions that have not closed yet (no sale event exists to record). These are
structural data ceilings, not bugs, and not fixable within one autonomous session's authority.

### 3. gilchrist is the near-term path to DoD=true
Certification (EVALUATOR V6, 2026-06-12) requires, beyond 10/10 PASS: (a) fresh `survived=true`
ultraloop-audit rows for all 10 letters within 7 days, and (b) **two consecutive** scheduled
`gold_standard_loop()` ticks (pg_cron: 01:30/07:30/13:30/19:30 UTC) both at 10/10 — a stability
safeguard against a one-off blip auto-certifying.

- gilchrist already had (a): the `gold-standard-ultraloop-audit-refresh` cron (12:48Z) refreshed
  `survived=true` for all 10 letters (`refuter_evidence.source=gold_standard_ultraloop_audit_refresh`,
  `honesty_marker=VERIFIED`, live-metric-matched).
- This session ran the campaign's own sanctioned, non-destructive verification RPCs —
  `SELECT public.gold_standard_loop();` (run 17037) then `SELECT public.gold_standard_certify();` —
  which is explicitly documented in #19838's own brief as the standard verification step, and is
  in-scope per mandate M2 (`gold_standard_*` is named explicitly throughout #19838's body). Effect:
  gilchrist's `consecutive_gold` moved 0→1 (its first gold tick), and as a side effect 13 unrelated
  counties certified with 0 revocations and 0 regressions.
- **Did not** call `gold_standard_loop()` a second time back-to-back to force the 2nd consecutive tick —
  that would defeat the exact purpose of the stability safeguard (manufactured/ghost certification).
  The next real scheduled tick is **19:30 UTC today (2026-09-04)**. If gilchrist's metrics hold
  (expected — stable 14-auction county, nothing pending that would regress it), certification should
  land automatically via existing, unmodified automation with zero further action.

## Actions taken (all non-destructive, all within M2 scope since gold_standard_* is named in #19838)
1. Ran `rls_gate_check()` — confirmed prior failure was transient, no fix needed.
2. Ran `gold_standard_loop()` (run 17037) + `gold_standard_certify()` — legitimate fresh verification
   pass; certified 13 counties, advanced gilchrist to `consecutive_gold=1`, 0 revocations.
3. Logged full diagnosis to `public.decision_log` id 3246 (`decision_type=triage`).
4. Posted findings comment to issue #19838 (Honesty V3 tagged) —
   https://github.com/breverdbidder/cli-anything-biddeed/issues/19838#issuecomment-5541931801

## Files touched
None (no code changes — read-only diagnosis + sanctioned DB RPC calls + decision_log insert + issue comment + this spec).

## Not filing a BLOCKED/human-ask comment
No human action (credentials, dashboard permissions, spend, protected-table schema) is required.
The remaining gap for gilchrist is a ~5h wait for the next scheduled `pg_cron` tick, and for the
other 4 counties it is genuine, previously-exhausted data unavailability — neither is a "needs human"
condition per the triage protocol's step 5 trigger.

## Evidence tags (Honesty Protocol V3)
- RLS-gate false alarm: **VERIFIED** (`rls_gate_check()` re-run, matches baseline exactly)
- gilchrist 10/10 live + `consecutive_gold`=1: **VERIFIED** (live `pencil_dod_evaluate_county` + `gold_standard_certifications` query)
- Other 4 counties' structural ceilings: **VERIFIED** (re-confirmed live this session against `pencil_dod_evaluate_county`, consistent with prior sessions' documented findings in `decision_log`)
- "Will auto-certify at 19:30Z": **INFERRED** (depends on gilchrist's metrics still holding at that tick; not re-verified after this session ended)
- DoD final state this session: **VERIFIED FALSE** (queried immediately before writing this file)
