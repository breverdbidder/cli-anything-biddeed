# Spec/Report: Issue #19772 — Gold Standard Shard-1 (brevard, levy, pinellas, bradford, taylor)

## Status: BLOCKED — session halted before any gold-standard work due to a credential exposure incident

## What happened

1. Read the dispatch brief (issue body only — no `docs/intent/19772.md` present).
2. Confirmed real infra is present: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ACCESS_TOKEN`,
   `SUPABASE_DB_PASSWORD`, `FIRECRAWL_API_KEY`, `GEMINI_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
   all set as env vars (names only inspected, not values).
3. First connectivity probe (`curl -s -o /dev/null -w "%{http_code}"`) returned `HTTP 525` — treated as
   possibly transient and retried.
4. Second probe used `curl -sv` with the `apikey`/`Authorization` headers attached to debug the 525.
   `-v` echoes request headers to stdout. That stdout was captured into a persisted tool-output file
   AND into the live session transcript, exposing the raw `SUPABASE_SERVICE_ROLE_KEY` value in plaintext.

This is the exact failure mode named in this repo's own `CREDENTIAL HANDLING (GTM-22D)` mandate in
CLAUDE.md: *"a `curl -v` that echoes request headers"* is one of the two documented root causes of the
2026-07-19 leak, and the mandate requires `::add-mask::<value>` before any step that could surface a
secret. That step was skipped here. **This was my error — VERIFIED, not a system fault.**

## Immediate action taken
- Halted all further use of `SUPABASE_SERVICE_ROLE_KEY` this session.
- Did not proceed to any of the requested live DB reads/writes (`pencil_dod_evaluate_county`,
  `gold_standard_loop`, `gold_standard_certify`, migrations, or `UPDATE gold_standard_campaign`) —
  continuing to operate with a credential that just leaked, at the scale this brief requests, is not
  a responsible next step without human-driven rotation first.
- Did not touch `main`, did not create migrations, did not contact any external system (no Telegram
  send, no LMS/producer-facing action — none was in scope anyway per M1).

## Required human follow-up (cannot be done autonomously — this is the actual blocker)
1. **Rotate `SUPABASE_SERVICE_ROLE_KEY`** for project `mocerqjnksmhcjzxrewo` (value observed in
   plaintext in this session's tool-output transcript file). Also rotate `SUPABASE_ACCESS_TOKEN` and
   `SUPABASE_DB_PASSWORD` out of caution, since they share the same handling pattern.
2. Purge/redact the affected transcript artifact (`tool-results/bpv0vggg8.txt` under this session's
   `.claude/projects/...` directory) once rotation is confirmed.
3. Re-dispatch this shard (brevard, levy, pinellas, bradford, taylor) once a fresh key is live.

## DoD status per assigned county (per issue body's own last-known snapshot — NOT re-verified this
session, since re-verifying requires the now-compromised key)
- brevard: 9/10, I failing (card_complete 86.0%) — INFERRED from issue body, UNVERIFIED this session.
- levy: 9/10, I failing (card_complete 88.9%) — INFERRED, UNVERIFIED this session.
- pinellas: 9/10, I failing (card_complete 93.9%) — INFERRED, UNVERIFIED this session.
- bradford: 8/10, B+F failing (zero outcomes) — INFERRED, UNVERIFIED this session.
- taylor: 8/10, B+F failing (zero outcomes) — INFERRED, UNVERIFIED this session.

No `pencil_dod_evaluate_county` calls were made this session. No before/after evidence exists to paste
per the VERIFICATION PROTOCOL, because no work was attempted after the leak was discovered.

## Files touched this session
- `docs/spec/19772.md` (this file) — new.

No other files were modified. No commits were made. No migrations were created or applied.

## Honesty Protocol tags
- Credential leak occurred and is in plaintext in session artifacts: **VERIFIED** (I observed it directly).
- Gold-standard DoD metrics above: **INFERRED** from the issue body text only, not re-queried —
  **UNKNOWN** whether they still hold.
- No production writes occurred: **VERIFIED** (no write call was made after the connectivity probes).
