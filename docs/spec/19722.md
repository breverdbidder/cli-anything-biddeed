# Gold Standard shard-4: manatee, nassau (dispatch `b556ca84-9cba-418f-894f-8e9dd7f6bc89`)

Session: 2026-09-02 08:00Z wave, headless (`claude -p`, no human in loop). Executed via ultracode
Workflow orchestration (1 diagnose+fix agent + 1 independent adversarial verifier per target,
4 targets, 8 agents total, all claims logged to `gold_standard_ultraloop_audit`).

## SCOREBOARD DELTA (live `pencil_dod_evaluate_county`, re-run fresh before and after)

| County | Before | After | Letters changed |
|---|---|---|---|
| manatee | 9/10 (C fail) | 9/10 (C fail) | none — C structural ceiling reconfirmed |
| nassau | 8/10 (C, D fail) | **9/10 (C fail)** | **D: 94.6% -> 96.4% (FAIL -> PASS)** |

### SQL VERIFICATION

Timestamp: 2026-09-02T08:1X-08:2X UTC. All runs via `python3 mgmt_sql.py "SELECT public.pencil_dod_evaluate_county('<county>')"` (Supabase Management API — direct `psql`/pooler auth confirmed broken again this session, per the known long-standing documented constraint; PostgREST RPC also hit transient `55P03` lock-timeouts and a `520` under concurrent-fleet load before stabilizing).

**manatee BEFORE == AFTER (unchanged):**
```json
{"A":{"pass":true,"metric":12,"detail":"fc=161 td=12"},"B":{"pass":true,"metric":100.0,"detail":"verified=12 closed_sold=12"},
 "C":{"pass":false,"metric":91.3,"detail":"matched_clean=158"},"D":{"pass":true,"metric":99.4,"detail":"matched_any=172"},
 "E":{"pass":true,"metric":97.7,"detail":"parcel_linked=169"},"F":{"pass":true,"metric":100.0,"detail":"tier1_sold=12 closed_sold=12"},
 "G":{"pass":true,"metric":95.8,"detail":"density=95.8 far=100.0 pk1000=100.0"},"H":{"pass":true,"metric":0.1},
 "I":{"pass":true,"metric":97.7,"detail":"card_complete=169 of 173"},"J":{"pass":true,"metric":100.0,"detail":"deal_complete=173"},
 "auctions_total":173}
```

**nassau BEFORE:**
```json
{"A":{"pass":true,"metric":19},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":94.6,"detail":"matched_clean=53"},
 "D":{"pass":false,"metric":94.6,"detail":"matched_any=53"},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},
 "G":{"pass":true,"metric":97.7},"H":{"pass":true,"metric":0.1},"I":{"pass":true,"metric":100.0},"J":{"pass":true,"metric":100.0},
 "auctions_total":56}
```

**nassau AFTER — D flipped PASS:**
```json
{"A":{"pass":true,"metric":19},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":94.6,"detail":"matched_clean=53"},
 "D":{"pass":true,"metric":96.4,"detail":"matched_any=54"},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},
 "G":{"pass":true,"metric":97.7},"H":{"pass":true,"metric":0.1},"I":{"pass":true,"metric":100.0},"J":{"pass":true,"metric":100.0},
 "auctions_total":56}
```

## WHAT SHIPPED

1. **`supabase/migrations/20260902_gold_standard_shard4_nassau_d_redeemed_reclassify.sql`** (applied live via `mgmt_sql.py` during the session, file committed for repo history) — nassau tax-deed case `452026XX000010TDAXYX` reclassified `PHANTOM_NOT_ON_CLERK` -> `CLERK_SSOT_CANCELLED`. Independently re-verified by an adversarial refuter agent (own headless-browser fetch, not trusting the fix agent's quote) against RealTaxDeed's **date-specific** auction page (`zaction=AUCTION&zmethod=PREVIEW&AuctionDate=09/01/2026`, not the default calendar/preview view): `Auction Status = "Redeemed"`, Certificate #1436, Opening Bid $238,579.73, parcel `00-00-31-101G-0001-2169`, address `2169 HIBISCUS CT FERNANDINA BE` — byte-for-byte match. `survived=true`.

2. **Root-cause finding (regression, not new work)**: this exact case, plus a sibling case `26TD000009AXYX`, had already been correctly verified `PARITY_OK` by an *earlier same-day* session (dispatch `6284f4fc`, ~08:20Z 2026-09-01) — see `GOLD_STANDARD_SHARD4_STJOHNS_NASSAU_DISPATCH_6284F4FC_SESSION_REPORT.md`. Both were reverted back to `PHANTOM_NOT_ON_CLERK` by an unidentified process at **2026-09-01 16:42:30 UTC** (identical `updated_at` timestamp on both rows — a single batch UPDATE, not the routine `*/5 * * * *` parity crons, whose source functions (`refresh_parity_chunk`, `gold_calendar_parity_cycle`) were checked live and do not write the `PHANTOM_NOT_ON_CLERK` status string at all; no matching `gha_dispatch_log`/`pipeline.scrape_runs`/`agent_ops_log` row was found for nassau in that window either). Likely another concurrent fleet session re-ran a preview-page-only recheck and hit the same known false-phantom bug (redeemed cases drop off the default RealTaxDeed calendar/preview view — already documented for gadsden/suwannee/sumter/charlotte). **This is now the second time these exact two rows have flipped PARITY_OK -> PHANTOM within 24h purely from methodology (preview-page vs date-specific-page), not real data change** — flagged below as a next-session priority (harden the check, not just re-fix the symptom again).

3. **Case `26TD000009AXYX` (the sibling redeemed case) was NOT fixed this session** — `nassau.realtaxdeed.com` returned HTTP 403 to every `WebFetch` attempt this session (site-level bot-blocking, confirmed independently by both the fix agent and the refuter), and the Nassau Clerk's official-records search (`myfloridacounty.com/orisearch/45`) is a JS SPA whose real query goes through an AJAX POST not reachable via `WebFetch`/blind `curl` GETs. The headless-Playwright workaround that worked for `452026XX000010TDAXYX` was not retried on this case within session budget. Left `PHANTOM_NOT_ON_CLERK`, unchanged, no write — correctly reported `UNTESTED`, not fabricated. D already passes (54/56) without this row; fixing it would move D to 55/56 (98.2%) but not change the PASS/FAIL outcome.

4. **nassau C — case `26TD000013AXYX`** (future auction, 2026-10-13, `auction_status='scheduled'`): live-confirmed **day-level** presence on RealTaxDeed's October calendar (`aria-label='October-13-2026'`, "1 active / 2 scheduled TD" that date), but the date-specific preview page for 10/13 only exposes internal numeric IDs (`1514630`, `1514768`) with no case-number/parcel mapping visible in the static HTML — **case-level identity could not be confirmed**. Correctly left as no-write/inconclusive rather than assuming the day-level match implies this specific case. C remains FAIL at 94.6% (53/56) — this is the sole remaining gap letter for nassau.

5. **manatee C — structural ceiling reconfirmed, no letter movement possible this session.** 14 of 173 rows are genuinely `CLERK_SSOT_CANCELLED` (2 freshly re-verified live against `records.manateeclerk.com/CourtRecords/Search/ForeclosureSales` — both still show `CANCELLED ONLINE` / `Sale Cancelled Reason: JUDICIAL ORDER` today). Max reachable even with every other gap row fixed = `(173-14)/173 = 91.9%`, below the 95% threshold **by construction** — cannot pass without a canon-level change to how C treats cancellations (see `GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md`; manatee is now an 8th confirming county for this cross-county finding).

6. **manatee C — candidate phantom row `412025CA003113CAAXMA` investigated, correctly NOT written, and a real duplicate-row bug surfaced.** Live Clerk fetch shows this case (`caseId=2619771`, $390,462.87 judgment) is real and `PENDING ONLINE`, but scheduled `2026-10-07`, not `2026-09-02` as our DB's `auction_date` has it — the fix agent's first read was "our date is wrong, backfill it." **The adversarial refuter went one step further and found this diagnosis incomplete**: a second row already exists in `multi_county_auctions` — `case_number='2025CA003113AX'` (short form of the same case), **same `parcel_id=401646309`**, already carrying the correct `auction_date=2026-10-07` and `parity_status='PARITY_OK'`. So `412025CA003113CAAXMA` is a **duplicate row of an already-fixed case**, not a simple date-backfill target — blindly "correcting" its date as the fix agent's own stated follow-up recommendation would have created a second live row for one real auction. **No write was made** (verdict `survived=false` on the fix agent's claim specifically because its narrative omitted this duplicate, even though its no-write decision was tactically correct); flagged below for a dedup-focused follow-up. Note this candidate would not have flipped C to PASS even if resolved cleanly (structural ceiling still caps at 91.9%).

## HONESTY-PROTOCOL / AUDIT NOTES

All 4 claims logged to `gold_standard_ultraloop_audit` (dispatch_id `b556ca84-9cba-418f-894f-8e9dd7f6bc89`, `ultraloop_mode='fallback'` — ran via the Workflow-tool manual fan-out path, not a native `/effort ultracode` toggle):

- `nassau/D` (`26TD000009AXYX`, UNTESTED/no-write): **survived=true** — refuter independently reproduced the 403/JS-SPA dead ends and confirmed no silent write occurred.
- `nassau/D` (`452026XX000010TDAXYX`, VERIFIED/write): **survived=true** — refuter independently re-fetched the cited RealTaxDeed page via its own headless-browser session and confirmed the quoted HTML byte-for-byte, confirmed the DB write landed exactly as claimed, and confirmed `CLERK_SSOT_CANCELLED` is the canonically correct classification for a redeemed case.
- `nassau/C` (`26TD000013AXYX`, UNTESTED/no-write): **survived=true** — refuter independently re-fetched both the calendar and preview URLs, confirmed the day-level evidence is real but genuinely does not establish case-level identity; the no-write decision was correct.
- `manatee/C` (`412025CA003113CAAXMA`, VERIFIED core facts but incomplete diagnosis): **survived=false** — refuter confirmed every cited fact was real (byte-for-byte HTML match, no write occurred) but found the fix agent failed to run a trivial case-number search that would have surfaced the pre-existing duplicate `PARITY_OK` row, and its stated follow-up recommendation (backfill the date on this row) would have created a duplicate. Logged as a false-positive-diagnosis, not retried without new evidence per protocol; the underlying no-write action stands (it just wasn't fixed, and isn't described as "just a date bug" for future sessions).

## Fleet coordination

Per PARALLEL-FLEET RULES: touched only `manatee` (read-only this session — no writes landed, structural ceiling + 1 investigated-not-written phantom) and `nassau` (`multi_county_auctions.parity_status`/`parity_source`/`updated_at` for exactly 1 row). Did not run `gold_standard_loop()` or `gold_standard_certify()` (other shards were mid-flight per standing parallel-fleet assumption; used `pencil_dod_evaluate_county('<county>')` per-county as instructed). Did not touch cron jobs 109/111/115 or any gold-standard-loop-* scoring job. Multiple transient `55P03` lock-timeout / `520` / `ECONNREFUSED` / `57P03 hot standby` errors were hit and retried against both PostgREST and the Management API during the session — consistent with concurrent-fleet load, not a code defect; all writes were confirmed to have actually landed via fresh `SELECT`s after each retry succeeded.

## Migration files

- `supabase/migrations/20260902_gold_standard_shard4_nassau_d_redeemed_reclassify.sql` (this session — documents the 1 live write; applied via `mgmt_sql.py` Management API since direct `psql`/pooler auth is broken, per the standing documented constraint referenced in this dispatch's brief).

## Next-session priorities

1. **nassau D->10/10 hardening**: `26TD000009AXYX` is the last true gap row for D (currently UNTESTED, not written). Retry with a headless-Playwright session against `nassau.realtaxdeed.com`'s date-specific preview URL for `2026-09-01` (the same technique that worked for its sibling case this session) rather than `WebFetch`, which is 403'd site-wide on this domain.
2. **Investigate the 16:42 UTC 2026-09-01 regression source**: two already-fixed nassau rows flipped back to `PHANTOM_NOT_ON_CLERK` from an unidentified process (no matching cron, `gha_dispatch_log`, `pipeline.scrape_runs`, or `agent_ops_log` row found). If this is a routine nightly recheck using the default preview/calendar view instead of the date-specific view, it will silently re-break this fix and any future one for redeemed nassau cases — worth finding and hardening the checker itself rather than re-fixing the same 1-2 rows every session.
3. **nassau C (`26TD000013AXYX`)**: the day-level match (1 active/2 scheduled TD items on 10/13) is real; a session with parcel-level lookup tooling (county PA parcel search by `00-00-31-1800-0161-0080`, cross-referenced against the two internal RealTaxDeed IDs `1514630`/`1514768` via a rendered-JS fetch rather than static HTML) could resolve case-level identity and potentially flip C to 54/56=96.4% PASS.
4. **manatee `412025CA003113CAAXMA` / `2025CA003113AX` duplicate-row bug**: confirmed duplicate MCA rows for the same real-world case (same `parcel_id=401646309`, different `case_number` formatting, one stale `auction_date=2026-09-02`/`PHANTOM_NOT_ON_CLERK`, one correct `auction_date=2026-10-07`/`PARITY_OK`). Needs a dedup-focused session (likely affects `auctions_total` denominators fleet-wide wherever the same ingestion produces both a long-form and short-form case number for one sale) — out of scope to fix unilaterally this session per the standing protected-table caution on `multi_county_auctions`, and would not itself flip manatee C to PASS given the structural ceiling (item 5 below).
5. **manatee C remains structurally capped at 91.9% max** — no further session time should be spent trying to force this letter past 95% without a canon-level decision on how C treats `CLERK_SSOT_CANCELLED`/redeemed rows (now an 8-county cross-confirmed finding). Continue routine freshness spot-checks only (~weekly), not full re-diagnosis.
