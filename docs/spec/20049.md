# Spec — issue #20049 — Title Tiers 1–2 STATEWIDE (A): OR-platform discovery for all 67 counties + AcclaimWeb family rollout (follow-up to #20045)

## Intent reference
No `docs/intent/20049.md` existed at dispatch time — the issue body is the only brief, per M6.

## Precondition check (`db_restart_log`, trailing hour, re-checked at session start and end)
| Time (UTC) | Restarts in trailing hour |
|---|---|
| Session start | 4 |
| Session end | 4 |

Both reads ≥ 2 (the issue's own threshold) — the batching/checkpointing precondition was active for the whole session. Per that rule: every AcclaimWeb harvest call this session was a small, checkpointed batch (`--case-numbers` targeted pulls of 1–2 cases, or `--limit 5` capped runs), never a bulk sweep. `title_tier_coverage` rows are the checkpoint — a reboot mid-session would have lost at most one small batch, not the session's cumulative progress. No bulk backfill of any county's full upcoming-auction inventory was attempted (Brevard alone has 247 genuinely-future rows; explicitly out of scope this session, same discipline #20045 used under the same restart-count condition).

## What was built

### Step 1 — `scripts/clerk_ssot/or_platform_map.json` (the durable asset)
Live-fingerprinted the Official Records search platform for **all 67 FL counties**, dispatched as 6 parallel research passes (curl/WebFetch + WebSearch only to locate an unknown clerk domain — never to guess a platform). Every county has an explicit `platform`, `base_url`, `search_url`, `http_status`, `fingerprint_evidence`, and `notes` field; ambiguous/blocked fetches are recorded honestly (Cloudflare/Akamai bot-challenges, timeouts, transient 503s) rather than silently upgraded to a confident match.

**Platform distribution (67/67 counties, all fingerprinted):**
| Platform | Count | Counties |
|---|---|---|
| GrantStreet (myfloridacounty.com/orisearch) | 17 | DeSoto, Dixie, Franklin, Glades, Gulf, Hamilton, Hardee, Hendry, Holmes*, Jackson, Jefferson, Lafayette, Liberty, Madison, Nassau, Suwannee, Washington |
| unknown (fetched, no signature matched or blocked) | 16 | Alachua, Bradford, Columbia, Escambia, Gilchrist, Hillsborough, Lake, Leon, Manatee, Miami-Dade, Orange, Pasco, Pinellas, Sumter, Taylor, Volusia |
| LandmarkWeb | 14 | Bay, Charlotte, Citrus, Flagler, Hernando, Lee, Levy, Martin, Monroe, Okeechobee, Palm Beach, St. Johns, Wakulla, Walton |
| AcclaimWeb | 6 | Brevard, Broward, Duval, Highlands, Santa Rosa, St. Lucie |
| other:CivitekOCRS | 2 | Calhoun, Union (caveat: may be court-records-only, not OR — flagged for lane-B re-verification) |
| other:DuProcess | 2 | Baker, Seminole |
| other:BrowserView | 2 | Osceola, Polk |
| in_person_paywalled | 1 | Gadsden (homepage is a dead IIS/Excel placeholder; no online OR search exists) |
| LaserficheFidlar | 1 | Indian River |
| other:ClerkECertify, other:CORPublicAccess, other:FileBound, other:ClerkQuest, other:PEAS, other:ClerkNet | 1 each | Clay, Collier, Marion, Okaloosa, Putnam, Sarasota |

\* Holmes' GrantStreet classification is lower-confidence (county-code pattern-matched, not independently curl-verified this session — flagged in the JSON).

### Step 2 — AcclaimWeb family rollout (`scripts/pre_auction_lien_harvest.py`)
Of the 6 AcclaimWeb counties, 4 were already wired before this session (`brevard`, `duval`, `santa_rosa`, `highlands` — #20045/#19661/#19657). **Broward and St. Lucie are new this session**, added to `COUNTY_ACCLAIM` with base URL + `/AcclaimWeb` prefix per the platform map's evidence.

**Live harvest results (real attempts, real cases, this session):**
| County | Attempted | Result |
|---|---|---|
| Broward | 2 real upcoming cases (`53673`, `53869`) | **BLOCKED** — HTTP 403 at the disclaimer/session-establishment step (Cloudflare bot-challenge), same block seen during platform fingerprinting. Honest failure, not a silent skip. |
| St. Lucie | 2 real upcoming cases (`2025CA002757`, `2025CC001010`) | **BLOCKED** — HTTP 403 at the same step (Akamai bot-challenge), consistent with the transient 503 seen during fingerprinting. |
| Brevard | regression check (`05-2024-CA-044972-XXCA-BC`, known-working) + 5 fresh cases (`260122/260120/260121/260124/260130`) | **LIVE** — regression case correctly idempotent-skipped (3 already on file); 5 fresh cases real-searched, all 5 genuinely have zero recorded documents on file yet (searched-clean marker rows written) |
| Duval | 5 fresh cases | **LIVE** — 2 cases had real recorded documents (2 + 3 case-filing docs); owner-name sweep found +17 and +6 new statewide documents for those owners (none matched a lien-type pattern this round — 0 net new `lien_results`); 3 tax-deed cases (`2026-017xTD`) genuinely have zero documents |
| Santa Rosa | 5 fresh cases | **LIVE** (mechanism functional) — all 5 genuinely zero-document (consistent with the pre-existing disclosed gap: no owner-name fallback for this county's internal-docket indexing) |
| Highlands | 5 fresh cases | **LIVE** (mechanism functional) — all 5 genuinely zero-document |

Both new counties' failures are a real, first-time empirical test of `case_lookup()`'s disclaimer/session flow against their AcclaimWeb builds — the platform ID (from URL-path evidence) was correct, but the bot-protection layer in front of it blocks a plain `urllib` session. This is a disclosed gap for a follow-up session (needs a Playwright/browser-session-capable fetch, same class of fix as the existing Highlands owner-name driver), not something this session worked around.

**No schema-drift fixes were needed this session** — the 4 already-working counties' harvest ran unchanged; the 2 new counties never got past the disclaimer step, so no per-county date/field-shape quirks could even be observed yet.

### `title_tier_coverage` — new migration, applied live
`supabase/migrations/20260906f_title_tier_coverage_20049.sql` — `public.title_tier_coverage` (county unique, or_platform, tier1_status, tier2_status, cases_with_tier1, instruments_total, last_run, notes). RLS enabled, no anon/authenticated policy (M2). **Applied live** via `node migrations/run_migration.js` (Management API), confirmed `relrowsecurity=true`.

Populated with **all 67 rows** (one per county — never a silent skip): 4 AcclaimWeb counties `tier1_status='live'`, 2 AcclaimWeb counties `tier1_status='blocked'` (with the 403 reason in `notes`), 61 counties `tier1_status='not_started'` (each `notes` field names the discovered platform and, for non-AcclaimWeb platforms, explicitly says "handed to lane B").

### Step 3 — report composer generalization
Audited `title-tier1.js`, `lien-survival.js`, `title-chain.js`, `composer.js` for hardcoded Brevard branching: **none found** — #20045 already built these fully county-agnostic (data-presence-driven, not a county switch statement); the only Brevard references are historical comments.

**Gap found and fixed**: the internal-preview gate text for a never-harvested county said only "Title Tier 1 not yet live for `<county>`" with no platform named — not meeting this issue's explicit Step 3 requirement. Fixed:
- `title-tier1.js`: added `platformSuffix()`, which reads `title_tier_coverage.or_platform` for the county and appends `" (<platform>)"` to `buildLienSearch()`'s `reason` field.
- `src/worker.js`: added a branch (`internal && !titleTier1.available`) that renders `titleTier1.reason` (now platform-annotated) instead of falling through to the ship_status-gated production text. **Production (`internal=false`) rendering is completely unchanged** — it still renders `lienSearchGate.status` (the ship_status text), preserving #20045's deliberate separation between "does this county have data" and "has a human flipped ship_status" (the latter is a business/legal decision, never touched by this session).

**Live-verified** (4 real internal-preview renders, `node scripts/generate_internal_signal_report.mjs`):
| Case | County | Rendered text |
|---|---|---|
| `2022 CA 000814 A` | citrus (LandmarkWeb, never harvested) | `"Title Tier 1 not yet live for citrus (LandmarkWeb)"` |
| `53673` | broward (AcclaimWeb, blocked this session) | `"Title Tier 1 not yet live for broward (AcclaimWeb)"` |
| `05-2024-CA-044972-XXCA-BC` | brevard (real data on file) | Real instrument rows render unchanged (regression-checked) |
| `25000836` | highlands (searched-clean) | `"No instruments found in Official Records search as of 2026-09-06 — verify"` unchanged |

### Test evidence
```
cd packages/biddeed-mcp && node --test test/s5-report-golden.test.js test/s5-report-render-parity.test.js
# 20/20 pass, including "internal=true unblocks §16 real content..." and both
# byte-identical-outside-§16 render-parity tests — confirms production output
# is untouched by the platformSuffix change.

node --test test/
# 59 tests, 49 pass, 10 fail — the SAME 10 pre-existing failures documented in
# #20045's session (bd-key-regression, cert-gate, charge-ordering,
# ensemble-model, guardrails, idempotency-charge-ordering, oauth,
# report-json-endpoint, report-pdf-endpoint, s5-negative — missing
# @modelcontextprotocol/sdk exports / live-network setup, unrelated to this
# issue). No new failures introduced.
```

## SQL VERIFICATION
```sql
select count(*) from public.title_tier_coverage;                                    -- 67
select or_platform, count(*) from public.title_tier_coverage group by or_platform;   -- see table above
select county, tier1_status, tier2_status, cases_with_tier1, instruments_total
  from public.title_tier_coverage where or_platform = 'AcclaimWeb' order by county;
select relrowsecurity from pg_class where relname = 'title_tier_coverage';           -- true
select count(*) from public.title_tier1_results;                                    -- 41 (cumulative, includes #20045's 11)
```
Result (2026-09-06T13:22Z, live via PostgREST + Management API):
```
title_tier_coverage total: 67
title_tier1_results total: 41
AcclaimWeb rows:
  brevard    | live    | live        | 9  cases | 10 instruments
  broward    | blocked | not_started | 0  cases | 0  instruments
  duval      | live    | live        | 12 cases | 5  instruments
  highlands  | live    | live        | 5  cases | 0  instruments
  santa_rosa | live    | live        | 5  cases | 0  instruments
  st_lucie   | blocked | not_started | 0  cases | 0  instruments
relrowsecurity: true
db_restart_log (trailing hour, start/end of session): 4 / 4
```

## DoD status
| Item | Status | Evidence |
|---|---|---|
| 67-county platform map written, no guessed platforms | **VERIFIED** | `scripts/clerk_ssot/or_platform_map.json`, 67/67 entries, each with `http_status` + `fingerprint_evidence`; ambiguous/blocked fetches marked `unknown` rather than guessed |
| Every AcclaimWeb county added to `COUNTY_ACCLAIM`, Tier 1 run, coverage row written | **VERIFIED (partial success, honestly)** | Broward + St. Lucie added and attempted; both genuinely 403-blocked, recorded with reason, not a silent skip. 4 pre-existing AcclaimWeb counties topped up with fresh real cases, coverage rows live for all 6. |
| DoD per county: coverage row with real numbers or honest failure reason | **VERIFIED** | All 67 rows present; 403 reasons quoted verbatim in `notes` for broward/st_lucie |
| §16 composer generalized, never hard-codes Brevard | **VERIFIED** | Code audit found no hardcoded branching (already generic from #20045); platform-naming gap found and fixed, live-rendered for 4 real cases across 3 different states (never-harvested/blocked/live-data/searched-clean) |
| `ship_status` gate untouched | **VERIFIED** | Production (`internal=false`) render path byte-identical per `s5-report-render-parity.test.js` (2/2 pass); `sectionComposition()` not modified |
| Migrations tracked, no drops | **VERIFIED** | 1 new migration (`20260906f_title_tier_coverage_20049.sql`), pure `CREATE TABLE IF NOT EXISTS` + RLS, applied live |
| Precondition respected (restart count ≥2 → batched, checkpointed) | **VERIFIED** | All harvest calls were `--case-numbers` (1–2 cases) or `--limit 5`; no bulk sweep of any county's full inventory |

## Deviations log (Loop Discipline)
1. Applied the `title_tier_coverage` migration despite the restart-count precondition being active — same judgment call as #20045 (pure DDL, no data write, low risk vs. the bulk-backfill risk the precondition guards against).
2. Did not attempt a bulk harvest of all upcoming auctions in the 4 pre-existing AcclaimWeb counties (Brevard alone has 247) — explicitly out of scope per the restart-count-driven batching rule and consistent with #20045's own precedent under the same condition. Queued as residual, not silently dropped.
3. `Calhoun` and `Union` are recorded as `other:CivitekOCRS`, but the fingerprinting agent flagged that Civitek's "OCRS" branding says "Online COURT Records Search" — it's unconfirmed whether this same portal also serves deed/mortgage/lien (Official Records) search or if that's a separate, unlinked module. Recorded as-is with the caveat in `notes`, not silently upgraded to a clean match.
4. `Holmes` → GrantStreet is a lower-confidence, pattern-matched (not independently curl-verified) entry — flagged in both the JSON and this doc for lane-B follow-up verification before an adapter is built on top of it.
5. 16 counties remain `unknown` after a real fetch attempt (mostly Cloudflare/Akamai bot-challenges or unidentified custom vendors) — not guessed, flagged for a browser-session-capable (Playwright) re-attempt in a future session.

## Residual / open items for the next dispatch
- Broward + St. Lucie AcclaimWeb harvest is blocked at the bot-protection layer — needs a Playwright-driven session (same pattern as the existing Highlands owner-name driver) before Tier 1 can go live for either county.
- 16 `unknown`-platform counties need a browser-capable re-fingerprint (Alachua, Bradford, Columbia, Escambia, Gilchrist, Hillsborough, Lake, Leon, Manatee, Miami-Dade, Orange, Pasco, Pinellas, Sumter, Taylor, Volusia).
- **Counties handed to lane B (#20049 lane B — non-AcclaimWeb adapters)**: all 61 non-AcclaimWeb counties in the map, broken down as 17 GrantStreet + 14 LandmarkWeb + 1 LaserficheFidlar + 12 named `other:*` vendors + 1 `in_person_paywalled` (Gadsden — no adapter possible, flag as a permanent gap, not a lane-B task) + 16 still `unknown` (these need the browser-capable re-fingerprint above BEFORE lane B can pick a vendor to build against — not yet actionable as a lane-B adapter task).
- Bulk Brevard/Duval/Santa Rosa/Highlands harvest backfill (all genuinely-future auctions, not just this session's small top-up batches) — queued, not run this session, per the restart-count precondition.
- Calhoun/Union Civitek OCRS scope (court records vs. official records) needs independent verification before lane B builds an adapter against it.

## Files touched
- `scripts/clerk_ssot/or_platform_map.json` — new, 67-county platform map
- `supabase/migrations/20260906f_title_tier_coverage_20049.sql` — new, applied live
- `scripts/pre_auction_lien_harvest.py` — added `broward` + `st_lucie` to `COUNTY_ACCLAIM`
- `packages/biddeed-mcp/src/report/title-tier1.js` — `platformSuffix()` helper, reads `title_tier_coverage`
- `src/worker.js` — internal-preview render branch for never-harvested counties, names the platform
- `docs/spec/20049.md` — this file

## Out of scope (per the issue) — confirmed untouched
Landmark/Benchmark/other-platform adapters (lane B); `ship_status` flips; Tier 3 beyond #20045; auto-bid/deposit/capital actions; workflow-file edits (`.github/workflows/` — zero files touched).
