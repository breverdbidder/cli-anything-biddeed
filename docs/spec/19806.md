# docs/spec/19806.md — GOLD STANDARD SHARD-1: brevard, lafayette, okaloosa, suwannee, madison

Issue: [breverdbidder/cli-anything-biddeed#19806](https://github.com/breverdbidder/cli-anything-biddeed/issues/19806)
Intent file: none at `docs/intent/19806.md` — issue body is the brief, per M6.
dispatch_id: `0f702775-8a80-4c1f-bb08-8340bfd4103e`

## Headline

**PARTIAL — one real fix shipped, three ceilings re-confirmed, one prior "ceiling" claim REFUTED and left open.**
No county reached 10/10 this session. Connection method: PostgREST (`SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`) for all reads/writes — direct psql against the pooler was tested fresh this session and confirmed still broken (`FATAL: password authentication failed for user "postgres"` at both `aws-0-us-west-2.pooler.supabase.com:5432` and `:6543` with the tenant-qualified username), consistent with the documented long-standing constraint. No generic `exec_sql`-style RPC exists on this project (probed 5 common names, all `PGRST202`) — all writes went through targeted `multi_county_auctions` PATCH requests, matching the established pattern used by the 100+ prior `scripts/gold_standard_*.py` sessions already in this repo.

Work was run via a `ultracode` Workflow: 5 parallel diagnose agents (one per failing county+letter) → pipelined fix agents → pipelined adversarial-refuter verify agents, per this repo's ULTRALOOP PROTOCOL. One claim (suwannee C) did not survive its refuter and is reported as an open, unresolved discrepancy rather than a pass.

## Live baseline vs. session-end (independently re-verified by me, not just trusted from the workflow)

Fetched fresh via `POST rpc/pencil_dod_evaluate_county` for all 5 counties both before dispatching the workflow and again at close-out.

| County | Letter targeted | Baseline (issue body, loop_run 16731) | Session-end (live) | Result |
|---|---|---|---|---|
| brevard | I | 86.0% (6106/7099) | 86.0% (6444/7491) | **Real progress, still FAIL.** 122 rows genuinely fixed; denominator grew 7099→7491 concurrently (other fleet activity), masking the % gain. |
| okaloosa | I | 92.9% (79/85) | 92.9% (79/85) | **Ceiling confirmed**, no write. |
| lafayette | C | 75.0% (3/4) | 75.0% (3/4) | **Ceiling confirmed** (mathematical: needs n≥20), no write. |
| suwannee | C | 80.0% (28/35) | 80.0% (28/35) | **Unresolved — prior "ceiling" claim REFUTED**, no write either direction. |
| madison | B, F | null (0/8 closed) | null (0/8 closed) | **Ceiling confirmed**, no write. New evidence: Cloudflare Turnstile blocks the one untried lever. |

Also newly observed (not this session's target, not touched): brevard **C flipped PASS→FAIL** between the issue-body snapshot (97.7%, matched_clean=6938) and session-end (94.4%, matched_clean=7075) — auctions_total grew 7099→7491 during the session window faster than clean-parity matching kept up. This happened concurrently with (not caused by) this session's I-only work; flagged for the next brevard session, not fixed here (M5 scope discipline — this issue targeted I, not C).

## What was actually run

1. Verified live DB reachability (PostgREST reachable; direct psql pooler auth confirmed broken fresh this session) before writing anything.
2. Pulled live `pencil_dod_evaluate_county` for all 5 shard counties — confirmed the issue body's numbers were current (same `loop_run_id`=16731 rows in `gold_standard_county_status`).
3. Ran a `Workflow` (`ultracode`, per user directive) with 3 phases:
   - **Diagnose** (5 parallel agents, one per county+letter): re-derived the current failing-row set fresh from `multi_county_auctions` (never trusted prior sessions' cached breakdowns), classified each failing row as fixable vs. structural ceiling.
   - **Fix** (pipelined): wrote and *executed* a real fix where a genuine lever existed; where the diagnosis showed zero fixable rows, explicitly declined to write a script (anti-fabrication rule) and instead re-confirmed the ceiling with one more fresh query.
   - **Verify** (pipelined, adversarial): an independent refuter agent re-derived every claim from scratch against the live DB/external sources, defaulting to `refuted=true` on any doubt.
4. Independently re-verified brevard's before/after metric myself (outside the workflow) via 3 direct `curl` calls to the RPC before trusting it.
5. Wrote 6 rows to `public.gold_standard_ultraloop_audit` (one per verified letter claim; madison split into B and F rows since the `letter` column rejects a combined `"B_F"` value — confirmed via a `400` on the first insert attempt, corrected).
6. Updated `public.gold_standard_campaign` for `dispatch_id=0f702775-8a80-4c1f-bb08-8340bfd4103e` (scoped by the literal dispatch_id from the issue footer, **not** the brief's `WHERE state='processing' ORDER BY updated_at DESC LIMIT 1` subquery — with multiple shards running concurrently per PARALLEL-FLEET RULES, that subquery could silently patch a different shard's row; scoping to the known dispatch_id avoids that).
7. Did **not** run `gold_standard_loop()` / `gold_standard_certify()` — per PARALLEL-FLEET RULES, other shards were running concurrently this session window.

## Files touched

- `scripts/gold_standard_brevard_i_shard19806_fix.py` (new) — brevard I fix, executed live with `--apply`. Two levers: (1) 2 rows with existing address/geo/parcel_id but null value, backfilled `assessed_value` = `LAND_VALUE + BLDG_VALUE` from Brevard's own GIS (`Base_Map/Parcel_New_WKID2881/MapServer/5`) — a field never pulled by any prior brevard-I session; (2) 159 true "skeleton" rows (only `case_number`/`sale_type`/`county`/`auction_date` populated, `bcpao_enriched=false`) resolved via `brevard.realtdm.com` (tax-deed) / AcclaimWeb (foreclosure) case lookup → parcel_id → same GIS layer for address/geo/value. 125/125 PATCHes returned HTTP 204; 122 flipped `card_complete` (3 remain zone-link-pending, a separate lever). 36 candidates correctly left untouched (no real upstream match — fabrication guard).
- `supabase/migrations/20260903j_gold_standard_brevard_i_shard19806_skeleton_enrichment_fix.sql` (new) — audit-trail documentation migration (no DDL/DML; all writes were live PostgREST PATCHes, not SQL, consistent with the psql-broken constraint).
- `docs/spec/19806.md` (this file).

No files were touched for okaloosa, lafayette, suwannee, or madison — every one of those four sessions concluded "nothing legitimately fixable this session" (three confirmed ceilings, one refuted claim requiring further diagnosis, not a blind retry).

## DoD status (canon A–J, this shard's 5 counties)

Live, independently reproduced 3× per RPC call (or fresh row-level queries where the RPC itself was transiently 5xx-ing under concurrent fleet load — retried with backoff, never assumed):

- **brevard**: 9/10 → 9/10 (A,B,D,E,F,G,H,J pass; **C now FAIL** 94.4% — new, unrelated to this session; **I FAIL** 86.0% — real progress, not yet passing)
- **lafayette**: 9/10 → 9/10 (C FAIL 75.0%, confirmed structural ceiling, mathematically capped until auctions_total≥20)
- **okaloosa**: 9/10 → 9/10 (I FAIL 92.9%, confirmed structural ceiling — 2 dead placeholders + 4 rows in an ArcGIS parcel range that doesn't exist for Okaloosa)
- **suwannee**: 9/10 → 9/10 (C FAIL 80.0% — **prior ceiling claim refuted this session**, ground truth unresolved, do not retry without new evidence)
- **madison**: 8/10 → 8/10 (B,F FAIL, both null — confirmed structural ceiling, 0 of 8 auctions have closed; new: the one untried lever, civitekflorida.com OCRS, is Cloudflare-Turnstile-gated, confirmed via live Playwright browser + screenshot)

## Verification evidence (Honesty Protocol V3 tags)

- brevard I before/after (84.4%→86.0%, 122 rows): **VERIFIED** — 3/3 identical fresh RPC calls; 2 of the written rows spot-checked against a live re-query of Brevard's external GIS endpoint (not our own DB), exact `LAND_VALUE+BLDG_VALUE` and address match; no PropertyOnion sourcing found in the fix script.
- okaloosa I ceiling: **VERIFIED** — RPC reproduced 3×; ArcGIS range-21 absence independently re-queried (refuter caught and corrected its own initial encoding bug before concluding, then confirmed 0 features in range 21 vs. tens of thousands in ranges 22–25).
- lafayette C ceiling: **VERIFIED** — RPC reproduced; evaluator SQL read directly (`supabase/migrations/20260903h_pencil_dod_matched_any_redeemed_cancelled_gap.sql` lines 53–54) confirms `CLERK_SSOT_CANCELLED` is genuinely excluded from the `matched_clean` allow-list by design.
- madison B/F ceiling: **VERIFIED** — RPC reproduced 2× bracketing the session; `foreclosure_outcomes` and clerk calendar/WP-REST/site-search all independently re-checked live; new Cloudflare Turnstile evidence captured via screenshot from an actual Playwright browser session, not inferred.
- suwannee C: **UNVERIFIED / OPEN** — the 80.0% metric itself is VERIFIED accurate (RPC + row-level parity_status distribution both independently reproduced), but the claim that the 7 non-matched rows are correctly excluded (CLERK_SSOT_CANCELLED) is **REFUTED**: the cited corroborating source (`clerk_ssot_sale_rows` cron table) was independently re-queried live and shows the opposite — all 7 flagged case_numbers present as active/non-cancelled with normal opening-bid data. This is logged as `survived=false` in `gold_standard_ultraloop_audit` (id=21015) and must not be retried without new evidence per the ULTRALOOP PROTOCOL's false-positive ledger rule. No write was made in either direction, so there is no data-integrity regression — only an open question about which of two sources (the 2026-08-30 clerk-verified write vs. the live cron feed) reflects ground truth.

## Residual / next-session hand-off

1. **suwannee C** (highest-priority open item from this session): re-derive ground truth directly from Suwannee Clerk's live Schedule PDF for the 7 disputed case_numbers (4672, 4676, 4681, 4693, 4694, 4741, 4744) — do not trust either the 2026-08-30 `suwannee_clerk_verified` write or the `clerk_ssot_sale_rows` cron table without a fresh independent read of the source document.
2. **brevard C regression** (new finding, not this session's scope): re-verify live; if still FAIL, this needs its own diagnose/fix pass separate from I.
3. **brevard I**: ~1006-row residual is a repeatedly-reconfirmed structural ceiling (vacant land / STREET_NAME=UNKNOWN in Brevard's own GIS, retired TaxAccts with no GIS feature, `brevard_clerk`-source rows with no parcel_id, 4 permanently-blocked zoning cases). The 3 zone-link-pending rows from this session (TaxAcct 2317094/2443033/2314974) are a distinct, smaller, likely-tractable lever (parcel_zones backfill) for a future session.
4. **okaloosa I**: one of the 4 range-21 rows (`2025-CA-002286-F5`) has property_address text literally reading "WALTON COUNTY, FLORIDA" — worth a future session checking whether this case was mis-attributed to okaloosa at ingestion time.
5. **madison B/F**: genuinely blocked pending either (a) Madison's future-dated auctions actually closing, or (b) a non-Cloudflare-gated source for the 2 past-dated cases that dropped off every checked channel.

## Guardrail compliance

- M1 (approval gate): not applicable — no producer-facing send touched.
- M2 (protected objects): `multi_county_auctions` writes were explicitly authorized by the issue body's own I/C playbooks ("I additionally needs address/geo/value enrichment on multi_county_auctions"); no DROP/TRUNCATE/bulk-DELETE; targeted single/few-row PATCHes only, matching the established pattern.
- M5 (scope discipline): only this shard's 5 counties touched; brevard's C regression was observed and reported, not fixed (out of this issue's I-only scope); `gold_standard_campaign` PATCH scoped to this exact `dispatch_id`, not a sibling shard's row.
- Hard guardrail #1 (PropertyOnion litmus-only): confirmed absent from all sources used this session (RealTDM, AcclaimWeb, Brevard/Okaloosa GIS, Madison clerk/WP-REST, Suwannee clerk cron).
- Hard guardrail #2 (fail-loud): brevard fix script logs and raises on any PATCH that doesn't return 2xx (`FAIL-LOUD` print on partial-apply), confirmed 125/125 succeeded this run.
