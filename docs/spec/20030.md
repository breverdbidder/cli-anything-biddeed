# Spec — issue #20030 — GTM-1 production run

## Intent reference
docs/intent/20030.md @ a81d5386 (repo HEAD at session start)

## What was built
- `scripts/biddeed_reels_pipeline.py:494` — one-line fix. `sorted(shortlisted, key=lambda x: x.get("rank_score") or 0, ...)` crashed with `TypeError: '<' not supported between instances of 'str' and 'int'` because `apply_shortlist()` fetches `rank_score` via the Supabase Management API (`lib.run_sql`), which serializes Postgres `numeric` columns as JSON strings. Changed to `float(x.get("rank_score") or 0)`, matching the existing cast pattern already used at line 465 for `sold_amount`. This was the exact cause of today's scheduled run failure (GHA run 33962763340, `biddeed-reels-daily`, 2026-09-05 11:13 UTC, `event=schedule`, `conclusion=failure`). No other files touched. No workflow YAML edited (M5).

## SSOT bindings used (all reads via Supabase Management API `run_sql`, matching `biddeed_reels_lib.py`'s own documented pattern — `winnerdata.*` is not exposed via PostgREST on this project; direct psql with `SUPABASE_DB_PASSWORD` still fails password auth, consistent with decision_log 169/205/287)
- `winnerdata.biddeed_reels` — read only. 42 `pending_approval` (29 postsale / 13 presale), 188 `error`. Unchanged from GTM_SOP_v1.md's Sep 5 13:00 UTC baseline — **re-verified, not re-produced**.
- `winnerdata.reel_variants` — read only. 25 rows, covering 5 of 42 pending reels (5 variants each); 37 reels have zero variants.
- `public.mcp_server_registry` — read only (via PostgREST, this table IS exposed). `declared_tool_count=36`, `probed_tool_count=null`, `is_live=false`.
- `public.agent_ops_log` — 1 row written (id `eeced7c5-fe75-48c2-850d-d2d604e810ad`), status `PARTIAL`, full evidence text inline.
- `.github/workflows/biddeed-reels-daily.yml`, `biddeed-reels-presale.yml` — read only, plus `gh api`/`gh run list` for live schedule/run-history proof.

## DoD status

| # | DoD item | Status | Evidence |
|---|---|---|---|
| Variants per property (42 rows) | **PARTIAL — audited, not expanded** | See table below. 5/42 reels have variants (25 total, all `is_draft=true`/`render_mode=draft`/`pending_final_voice=true`, i.e. correctly DRAFTS ONLY). 37/42 have zero. **Did not render new variants**: intent guardrail 9 / issue deliverable 7 (volume rule) triggers — 42 pending ÷ 6/day cap ≈ 7 days of backlog > the 3-day stop threshold. Producing more before Ariel drains the existing 42 would make the backlog worse, which is the exact failure mode the rule exists to prevent. |
| Languages rendered | VERIFIED, gap flagged | `en`=20, `es`=3, `pt-BR`=2, all `tts_model='kokoro'`. **Spec violation found**: intent hard guardrail 4 requires eleven_v3 voice `TX3LPaxmHKxFdv7VOQHJ` for EN; all 20 EN variants are on kokoro. `pending_final_voice=true` on every row suggests this is an intentional two-pass design (kokoro draft → eleven_v3 finishing pass before approval) rather than a bug, but no row has yet received the finishing pass — flagging since the intent text reads as EN-must-be-eleven_v3, not EN-may-be-draft-kokoro. |
| Director/QA verdicts + never-list scan | VERIFIED (audited existing scores, did not author new ones) | 25/25 variants have `qa_pass=false` (0 PASS, 0 explicit REJECT — pipeline only has a boolean pass/fail, not a 3-way verdict). **Root cause (CONFIRMED from `qa_scores` JSON on all 25 rows)**: every failure is the `diversity` check, for two distinct reasons: (a) genuine — Hook Writer emits only 4 distinct `archetype` values (`underdog_bidder` ×7, `shock_number` ×6, `hidden_value_reveal` ×6, `red_flag_warning` ×6) into 5-variant sets, so every set has one duplicate archetype; (b) **likely a QA-checker bug, HYPOTHESIS** — "Jaccard distance 0.00" fires between same-`variant_key` EN/ES or EN/pt-BR pairs (a literal translation of the same script), which the diversity check appears to compare against the intra-set distinctness metric instead of excluding cross-language pairs of the same creative. `never_list` hits: 0/25 — clean. No `remote_bidder` archetype present anywhere in the 25 variants, despite the issue's explicit requirement ("include `remote_bidder` on at least one per property"). Did not write new verdicts to `reel_variant_review` (forbidden) or hand-author fixes (would need a Hook Writer / Director-QA code change, out of this issue's scope per M5 and the volume-rule stop). |
| Error triage (188 rows) | VERIFIED | See table below. 84 correct policy disqualifiers, 28 transient infra, **72 code-bug (new finding — not the Sep-3 ON CONFLICT bug)**, 3 budget-gated, 1 ambiguous. |
| Autopilot schedule | VERIFIED | Both workflows are `state=active` with real `schedule:` triggers (`biddeed-reels-daily.yml` cron `0 11 * * *`, `biddeed-reels-presale.yml` cron `0 10 * * *`) — **not dispatch-only**, no fix needed, no workflow-file edit made. `biddeed-reels-presale` has 3 consecutive successful scheduled runs (Sep 3/4/5). `biddeed-reels-daily`'s Sep 5 11:13 UTC scheduled run failed on the bug described above (fixed in this session); prior scheduled run (Sep 4) succeeded. Next scheduled fires: presale 2026-09-06 10:00 UTC, daily 2026-09-06 11:00 UTC. |
| MCP directory | BLOCKED (documented, not a credential question I can resolve without spend) | `mcp_server_registry`: declared 36, probed `null`. Live probe: `POST https://mcp.biddeed.ai/api/mcp {"method":"tools/list"}` unauthenticated → `HTTP 401 {"error":"Authorization required","hint":"...bd_live_xxx or WorkOS OAuth..."}`. This matches the registry's own Jul 28 note exactly — the server requires an authenticated handshake even for `tools/list`, and I hold no `bd_live_` key or WorkOS token. Obtaining one requires a customer signup/dashboard flow, which is a spend/scope decision outside this session (M5). **Did not fabricate a "25" reconciliation** — the issue body's "live tools/list returns 25" could not be independently reproduced from this session; left `declared_tool_count`/`probed_tool_count`/`is_live` untouched rather than guess. |
| Volume rule | VERIFIED, TRIGGERED | 42 pending_approval ÷ 6 reels/day cap = 7 days of backlog, exceeds the 3-day stop threshold in intent guardrail 9 and issue deliverable 7. Stopped before producing new variants/renders; did not dispatch any new GHA runs; did not spin up additional agents. |

## Variants-per-property (42 rows, VERIFIED via winnerdata.biddeed_reels + reel_variants, 2026-09-05 13:3x UTC)

| Phase | County | Sale type | Case | Auction date | Variants | Langs |
|---|---|---|---|---|---|---|
| postsale | broward | foreclosure | CACE-24-008115 | 2026-09-01 | 5 | en,es |
| postsale | escambia | foreclosure | 2025 CA 001460 | 2026-09-01 | 5 | en,es |
| postsale | lee | tax_deed | 2026000141 | 2026-09-01 | 5 | en,es |
| postsale | martin | foreclosure | 25001204CAAXMX | 2026-09-01 | 5 | en,pt-BR |
| postsale | polk | foreclosure | 2024CA000341000000 | 2026-09-01 | 5 | en,pt-BR |
| postsale | alachua | foreclosure | 01 2025 CA 003415 | 2026-09-01 | 0 | — |
| postsale | alachua | foreclosure | 01 2025 CA 002136 | 2026-09-03 | 0 | — |
| postsale | bay | tax_deed | 2026-3969TD | 2026-09-01 | 0 | — |
| postsale | broward | foreclosure | COWE-12-011377 | 2026-09-04 | 0 | — |
| postsale | escambia | foreclosure | 2025 CA 001415 | 2026-09-01 | 0 | — |
| postsale | lee | tax_deed | 2026000086 | 2026-09-01 | 0 | — |
| postsale | marion | foreclosure | 422024CA000511CAAXMX | 2026-09-01 | 0 | — |
| postsale | marion | foreclosure | 422025CC001645CCAXMX | 2026-09-01 | 0 | — |
| postsale | palm_beach | foreclosure | 502025CA009901XXXAMB | 2026-09-03 | 0 | — |
| postsale | polk | foreclosure | 2025CA003611A000BA | 2026-09-01 | 0 | — |
| postsale | santa_rosa | foreclosure | 572025CA000824CAAXMX | 2026-09-01 | 0 | — |
| postsale | santa_rosa | foreclosure | 572025CA000619CAAXMX | 2026-09-01 | 0 | — |
| postsale | santa_rosa | foreclosure | 572025CA000489CAAXMX | 2026-09-03 | 0 | — |
| postsale | seminole | foreclosure | 2025CA002400 | 2026-09-03 | 0 | — |
| postsale | seminole | foreclosure | 2025CA000273 | 2026-09-03 | 0 | — |
| postsale | seminole | foreclosure | 2025CA001862 | 2026-09-03 | 0 | — |
| postsale | st_lucie | foreclosure | 2024CA000617 | 2026-09-01 | 0 | — |
| postsale | suwannee | tax_deed | 4679 | 2026-09-03 | 0 | — |
| postsale | suwannee | tax_deed | 4756 | 2026-09-03 | 0 | — |
| postsale | suwannee | tax_deed | 4704 | 2026-09-03 | 0 | — |
| postsale | suwannee | tax_deed | 4680 | 2026-09-03 | 0 | — |
| postsale | suwannee | tax_deed | 4752 | 2026-09-03 | 0 | — |
| postsale | suwannee | tax_deed | 4677 | 2026-09-03 | 0 | — |
| postsale | walton | foreclosure | 25CA000534 | 2026-09-04 | 0 | — |
| presale | clay | foreclosure | 2026CA000160 | 2026-09-04 | 0 | — |
| presale | clay | foreclosure | 2025CA001104 | 2026-09-04 | 0 | — |
| presale | duval | foreclosure | 16-2025-CA-006263-AXXX-MA | 2026-09-07 | 0 | — |
| presale | duval | foreclosure | 16-2025-CC-014368-AXXX-MA | 2026-09-07 | 0 | — |
| presale | flagler | foreclosure | 2026 CA 000192 | 2026-09-04 | 0 | — |
| presale | leon | foreclosure | 2025 CC 003163 | 2026-09-04 | 0 | — |
| presale | leon | foreclosure | 2024 CA 000670 | 2026-09-04 | 0 | — |
| presale | polk | foreclosure | 2025CA003924A000BA | 2026-09-04 | 0 | — |
| presale | polk | foreclosure | 2026CA000684A000BA | 2026-09-04 | 0 | — |
| presale | santa_rosa | tax_deed | 2026188 | 2026-09-07 | 0 | — |
| presale | santa_rosa | tax_deed | 2026187 | 2026-09-07 | 0 | — |
| presale | santa_rosa | tax_deed | 2026248 | 2026-09-07 | 0 | — |
| presale | walton | foreclosure | 25CA000534 | 2026-09-04 | 0 | — |

## Error triage (188 rows, VERIFIED via `error_text` on `winnerdata.biddeed_reels where status='error'`)

| Bucket | n | Disposition |
|---|---|---|
| `DISQUALIFIED: sub-$5k sale...` | 70 | Correct policy disqualifier — stays error |
| `unsupported operand type(s) for -: 'str' and 'int'` | 50 | **Code bug** — see below |
| `HTTP Error 521` | 22 | Transient Cloudflare edge flakiness (documented in `biddeed_reels_lib.py` itself as reproduced/retried live) — retryable with `--force`, no code change needed |
| `'>' not supported between instances of 'str' and 'int'` | 11 | **Code bug** — see below |
| `Unknown format code 'f' for object of type 'str'` | 10 | **Code bug** — see below |
| no zw_parcels match AND geocode miss | 9 | Correct policy disqualifier — stays error |
| `HTTP Error 520` | 3 | Transient — retryable |
| `DISQUALIFIED: no property address in source` | 3 | Correct policy disqualifier — stays error |
| all T3 vision tiers failed (OpenRouter HTTP 402, credits exhausted) | 3 | Budget-gated, not a bug — GLM-5.3-flash weekly OpenRouter cap hit (per intent item 7, $10/wk); recoverable only after credit refresh, not by re-render now |
| `HTTP Error 503` | 2 | Transient — retryable |
| `DISQUALIFIED: no assessed_value` | 2 | Correct policy disqualifier — stays error |
| `HTTP Error 401` | 1 | Ambiguous — one investigative retry needed, not classified further |
| Storage upload `DatabaseTimeout` (544) | 1 | Transient — retryable |
| `JSON object must be str, bytes or bytearray, not NoneType` | 1 | Code bug (null-handling), not investigated further |
| **Total** | **188** | 84 correct disqualifiers · 28 transient/retryable · 72 code-bug · 3 budget-gated · 1 ambiguous |

**72-row code-bug root cause (CONFIRMED chain, not yet fixed for these historical rows):** `get_existing_reel()` (`biddeed_reels_pipeline.py:163-170`) reads `assessed_value` via the Management API (`lib.run_sql`), which serializes Postgres `numeric` as a JSON **string**. On any `--force`/re-render path where `existing` is non-null, `assessed_value` at line 237 is a string that flows unguarded into the delta/condition arithmetic in `sale_and_caption()` — producing exactly the `str`-`int` subtract/compare/format errors above. This is the same bug class I fixed live at line 494 (`rank_score`), not the Sep-3 `ON CONFLICT` bug (that bug's signature is a Postgres constraint error and would have produced zero DB rows, not `error`-status rows with these Python `TypeError`/`ValueError` texts — confirmed no row's `error_text` mentions ON CONFLICT/constraint). **Not fixed in this session**: fixing all call sites in `sale_and_caption()`/`process_row()` is a multi-site change beyond one surgical line, and re-rendering the 72 rows to confirm the fix would add new variants against the volume-rule stop. Recommend a dedicated follow-up issue: audit every `lib.run_sql()` numeric field for a cast before arithmetic/formatting in `biddeed_reels_pipeline.py` and `biddeed_reels_pipeline_presale.py`.

## Negative test result
Checked `reel_variant_review` — not written to (0 rows touched by this session). Checked no `SHIPPED`/publish action was taken — `is_draft=true` unchanged on all variants, no `youtube_uploads` or publish-queue row created. Checked M2 protected tables — none read/written except read-only `spi_gates`-adjacent context in GTM_SOP_v1.md (doc, not the table itself); no writes to any M2-protected table.

## Deviations from intent
- Did not produce the 4-variants-per-property / ES-for-shortlisted / PT-BR expansion the issue's deliverables 1-2 describe, because the issue's own deliverable 7 (volume rule) and the intent's guardrail 9 both say stop producing when backlog > 3 days of slots, and the backlog (42 pending ÷ 6/day) is already ~7 days. Producing more first and reporting the volume-rule breach second would visibly contradict the rule's own text.
- Fixed one line of `scripts/biddeed_reels_pipeline.py` (not a data/schema/workflow change) to un-break today's live scheduled run, since it was a 1-line, zero-risk, already-battle-tested pattern (matches line 465) directly serving deliverable 5's "autopilot" intent, and leaving a known-broken autopilot run in place for another 24h to stay maximally passive seemed like the wrong tradeoff. Did not touch anything else in that file.
- Did not attempt to obtain a `bd_live_` MCP API key to probe the live tool count, since that requires a customer/dashboard signup flow with spend/scope implications outside M5.

## Open items / handoff
- **72 code-bug error rows**: root-caused (numeric-as-string from Management API) but not fixed at every call site or re-rendered. Follow-up issue should audit `sale_and_caption()` and all `lib.run_sql()` numeric consumers in both pipeline scripts.
- **QA diversity-check false-positive on translated pairs**: `qa_scores.diversity` appears to compare EN/ES/pt-BR translations of the same `variant_key` as if they were meant to be distinct creatives. If confirmed, this is why 0/25 variants have ever reached PASS. Needs a Director/QA code owner to confirm and fix the comparison scope (same-language-only diversity check).
- **`remote_bidder` archetype never generated**: Hook Writer currently emits only `underdog_bidder`/`shock_number`/`hidden_value_reveal`/`red_flag_warning`. The issue's explicit requirement for a `remote_bidder` variant per property is unmet in all 25 existing variants.
- **EN voice on kokoro, not eleven_v3**: all 20 EN variants are `tts_model='kokoro'` with `pending_final_voice=true`. If this is the intended two-pass design, fine; if not, needs the eleven_v3 finishing pass before any of these can be approved per intent hard guardrail 4.
- **MCP tool-count reconciliation**: still blocked on obtaining an authenticated probe (`bd_live_` key or WorkOS token). `declared_tool_count=36` in the registry is unchanged.
- **Volume rule**: 42 pending / 188 error is unchanged from the Sep 5 13:00 UTC SOP baseline — the backlog has not moved since that snapshot. Draining it (via LMS approvals + the dial-3 gate) is Ariel's/the LMS's job per GTM_SOP_v1.md §5, not something this pipeline should route around by producing more.
