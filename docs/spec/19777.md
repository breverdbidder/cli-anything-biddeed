# docs/spec/19777.md — CMO FACTORY CP0 install

Issue: #19777 ("CMO FACTORY CP0 — install GTM software-factory skeleton").
Branch: `main`. Session: headless `claude -p`, no human attached this run.
Mandates followed: `docs/intent/MANDATES.md` M1-M6 (see notes inline below);
M8 (no publish step) held throughout -- nothing in this session sends
marketing content anywhere; the two GTM PRs referenced by
`docs/gtm/verdicts/` do not exist yet (CP0 ships the merge machinery, not
real GTM PRs).

## What was built

| Deliverable | File(s) | Status |
|---|---|---|
| 1. META/MISSION/journeys | `docs/gtm/META.md`, `docs/gtm/MISSION.md`, `harness/gtm/END-TO-END.md` | VERIFIED |
| 2. Holdout stub + gitattributes + negative test | `.factory/gtm/holdout/HOLDOUT.md`, `.gitattributes`, `factory/gtm/builder_checkout.sh`, `factory/gtm/test_holdout_negative.sh` | VERIFIED |
| 3. floor.json + STOP semantics | `.factory/gtm/locks/floor.json`, `.factory/gtm/STOP.md` | VERIFIED |
| 4. gate.py / merge.py / doctor.py / watchdog_sql.sql | `factory/gtm/*.py`, `factory/gtm/watchdog_sql.sql` | VERIFIED |
| 5. gtm-validate.yml / gtm-merge.yml | `.github/workflows/gtm-*.yml` | PARTIAL — see below |
| 6. Labels + control issue | 8 `factory:*` labels, issue #19780 (pinned) | VERIFIED |
| 7. `cmo_factory_watchdog` migration | `supabase/migrations/20260903a_cmo_factory_watchdog.sql` | VERIFIED — applied live |
| 8. unified_context upserts | `cmo_factory_meta_v1`, `cmo_factory_external_gate_sep3` | VERIFIED |
| 9. This file | `docs/spec/19777.md` | VERIFIED |

## Per-item evidence

### 1. META.md / MISSION.md / END-TO-END.md — VERIFIED
- `docs/gtm/META.md` is byte-identical to `unified_context.cmo_factory_meta_v1.content.markdown`
  as authored by `claude_chat` earlier today (fetched live via PostgREST,
  written with `python3`/`json`, not hand-retyped — avoids transcription
  drift).
- `docs/gtm/MISSION.md` is META §2 plus a compliance-check mapping table I
  added so `factory/gtm/gate.py`'s 6 checks trace back to specific
  NEVER-list items, per the issue's explicit ask ("banned terms list from
  docs/gtm/MISSION.md #3/#4/#7").
- `harness/gtm/END-TO-END.md`: each of the 5 journeys was checked against
  live code/DB this session, not assumed from META's prose. Findings:
  - J1 (win→reel): PARTIAL — 23/23 `biddeed_reels` rows verified
    `pending_approval` live via `list_public_reels(true)` RPC; frame-level
    rendering claims are INFERRED from commit history, not re-observed.
  - J2 (`/r/{code}` click resolve): LIVE by code-path (`resolve_reel_link`
    SECURITY DEFINER function + its call site in `src/worker.js`), not by a
    live HTTP round trip (no deploy to curl from this sandbox).
  - J3 (digest certified count + banned terms): **NOT YET** —
    `scripts/biddeed-daily-digest.cjs:65-69` sources its gold badge from
    `gold_standard_scoreboard`, which `CC_META_PROMPT.md` §2.2 calls
    explicitly untrustworthy, and has zero banned-terms enforcement. This is
    a real compliance gap the factory should flag, not a false pass.
  - J4 (county SEO buy-CTA gating): LIVE — `src/worker.js` correctly
    re-queries `v_certified_counties` (the correct SSOT) and cross-checks
    `gold_standard_certifications.certified` per-county.
  - J5 (Chatwoot token-gated widget): LIVE by code-path —
    `injectChatwootWidget()` no-ops without `CHATWOOT_WEBSITE_TOKEN_BIDDEED`;
    not verified by a live HTTP 200 from this sandbox.

### 2. Holdout + negative test (a builder-role read fails) — VERIFIED
- `.gitattributes` carries `gtm-checkout=validator-only` on
  `.factory/gtm/holdout/**` only (narrower than META §5's full protected-path
  list — MISSION.md/harness/gtm stay builder-readable, since builders must
  read them to comply; only the holdout answer key is checkout-blocked).
- `factory/gtm/builder_checkout.sh` applies a non-cone `git sparse-checkout`
  exclude derived from that attribute.
- `factory/gtm/test_holdout_negative.sh` builds a real (dangling, unpushed)
  commit via `git write-tree`/`commit-tree`, checks it out into two
  `git worktree`s, and proves: validator-role worktree **can** read
  `HOLDOUT.md`; builder-role worktree (after running
  `builder_checkout.sh`) **cannot** — file absent, `cat` errors. Live run
  this session: `NEGATIVE TEST RESULT: PASS`.

### 3. floor.json / STOP.md — VERIFIED
- `.factory/gtm/locks/floor.json`: `journey_assertions=11` (the count of
  individually evidence-backed CONFIRMED/FINDING statements in
  `harness/gtm/END-TO-END.md`'s summary table, enumerated in that file),
  `compliance_checks=6` (exact, per the issue body).
- `.factory/gtm/STOP.md` documents the 3-signal OR-logic halt (file +
  label + `spi_gates` row), fails-closed on any unreadable signal, and who
  may set/clear each signal.

### 4. gate.py / merge.py / doctor.py / watchdog_sql.sql — VERIFIED, all 3 negative tests pass live
- **Negative test (a)** — `factory/gtm/test_gate_negative.sh`: planting
  "Tracerfy" in `factory/gtm/fixtures/caption_with_tracerfy.txt` makes
  `gate.py` FAIL (`COMPLIANCE FAIL: banned_terms, vendor_name_detector`,
  exit 1); the clean fixture passes (`GTM_COMPLIANCE_PASSED`, exit 0). Also
  live-tested the `certified_county_count_match` check against the real
  `v_certified_counties` count (12 counties with `consecutive_gold>=1`,
  confirmed via `content-range: 0-11/12`) — a correct claim passes, a wrong
  one (50) fails.
- **Negative test (b)** — `factory/gtm/test_merge_negative.sh`: a verdict
  file with `gates_green:false` makes `merge.py` refuse
  (`decision: REFUSE -- gates_green=False, not true`); a fully-green
  fixture decides `MERGE`; a missing verdict file refuses. `merge.py`'s
  merge/no-merge logic is a single pure function (`decide()`) with no LLM
  call in the path.
- **Negative test (c)** — see item 2 above (holdout).
- `factory/gtm/doctor.py`: live run this session —
  **`CURRENT EARNED DIAL: 1`**, 8 of 11 checks PASS, 3 FAIL
  (`dial2_holdout_real`, `dial3_mutation_set`, `dial3_kill_switch_drilled`
  — each correctly deferred to CP4/CP5 per META §8, not silently passed).
  `exit=1` as required by the DoD ("MUST fail on first run and print the
  remaining todo list"). Fixed one real bug found while building this: `gh
  label list --json` silently truncated/omitted results on this repo's
  100+-label set — swapped to `gh api ... --paginate` in
  `check_dial1_labels_exist()`.
- `factory/gtm/watchdog_sql.sql` is a browsable reference copy; the applied
  version lives in the migration (item 7).

### 5. gtm-validate.yml / gtm-merge.yml — PARTIAL
- Both workflows: `workflow_dispatch` + `schedule`, halt-check (3 signals,
  fails closed) before any real work, `quota_gate_check('engineering')`
  gate with `NO_READING` → capped-but-not-skipped mode (explicit `mode=cap`
  branch, never a silent skip), and `ref: main` on checkout so protected
  paths are always read from `main`.
  `gtm-validate.yml` runs `factory/gtm/gate.py` against its own fixture
  (`caption_clean.txt`) by default, since CP0 has no builder-produced
  artifacts yet to validate — this is what proves the
  `GTM_COMPLIANCE_PASSED` marker prints in a real Actions log.
- **[NOT YET]**: I did **not** confirm the marker appears in an actual GHA
  run log this session. `workflow_dispatch` only becomes callable once a
  workflow file lands on the repo's default branch — this file is
  committed in this same session, so I could not `gh workflow run` it and
  observe a completed log before writing this spec (a dispatch immediately
  after push would race the branch-registration step, and this is a
  headless session with a hard wall-clock budget — I chose to spend the
  remaining budget on live-testing every DB/negative-test path I *could*
  test now rather than gambling on a race). YAML syntax was validated with
  `python3 -c "import yaml; yaml.safe_load(...)"` (both files: OK) and both
  scripts they call (`gate.py`, `merge.py`, `doctor.py`) were separately
  proven correct via the negative tests above and the live `doctor.py` run
  — so the failure surface still open is narrowly "does GHA accept and run
  this exact YAML," not "is the underlying logic correct." Flagging this
  honestly per Honesty Protocol V3 rather than claiming VERIFIED.
  **Recommended immediate follow-up** (does not require new code): once
  this commit is confirmed on `main`, run
  `gh workflow run gtm-validate.yml --repo breverdbidder/cli-anything-biddeed`
  and paste the run URL + `GTM_COMPLIANCE_PASSED` log line into the control
  issue #19780 as a comment.

### 6. Labels + control issue — VERIFIED
All 8 labels confirmed via `gh api repos/.../labels --paginate` (not
`gh label list`, which silently omitted them on this repo — see the
`doctor.py` bugfix note above): `factory:queued`, `factory:building`,
`factory:validating`, `factory:pass`, `factory:fail`, `factory:merged`,
`factory:needs-human`, `factory:halt`. Control issue **#19780** ("CMO
FACTORY — control") created and pinned
(`gh issue view 19780 --json isPinned` → `true`).

### 7. `cmo_factory_watchdog` migration — VERIFIED, applied live
- Applied via the Supabase Management API (`/v1/projects/{ref}/database/query`
  with `SUPABASE_ACCESS_TOKEN`) — the sanctioned fallback per
  `CC_META_PROMPT.md` §4; `SUPABASE_DB_PASSWORD`/direct psql was not
  attempted since it's documented as a known, already-diagnosed constraint.
- **Real bug found and fixed during live testing**: the first apply used
  `v_tripped_names || 'literal'` for `text[]` append, which Postgres
  resolved to the `anyarray || anyarray` operator overload instead of
  `anyarray || anyelement` for the unknown-typed literal, raising
  `22P02 malformed array literal`. Fixed with `array_append()` (unambiguous)
  in both the migration and the reference copy, re-applied, re-tested clean.
- **Live evidence gtm_watchdog() executes without error**:
  `curl .../rpc/gtm_watchdog` → HTTP 200, returned a well-formed JSON
  result on the first clean call.
- **Live evidence it trips organically**: this environment's
  `quota_gate_check('engineering')` genuinely returns `reason=NO_READING`
  (real finding, not staged) — the D7 detector correctly tripped on real
  data (`tripped_detectors: ["quota_no_reading"]`), opened
  `spi_gates.gtm_factory_halt` (`opened_at` set, `verified_at` null),
  and logged `GTM_WATCHDOG_TRIP` to `agent_ops_log`.
- **Live evidence it trips on synthetic history** (DoD's explicit ask):
  inserted one synthetic `campaign_agent_log` row
  (`action='gtm_publish_synthetic_test', success=true, details.approved_at=null`),
  re-ran `gtm_watchdog()`, confirmed `tripped_detectors` now contained both
  `publish_attempted_approval_null` and `quota_no_reading`, then **deleted**
  the synthetic row immediately after (row id
  `d5b97053-20f7-48e6-a363-ae952cde1ce1`, confirmed deleted via the DELETE
  response echo).
- **Security advisor re-run** (M2 requirement): queried
  `/v1/projects/{ref}/advisors/security` before and after — zero findings
  reference `gtm_watchdog` (the one pre-existing `gtm22p0_leg1_checkpoint`
  RLS/GraphQL-exposure finding predates this session and is unrelated — no
  table was created by this migration, only a function).
- **[NOT YET]**: the control-issue comment path (`net.http_post` inside
  `gtm_watchdog()`) is best-effort and will almost certainly 401 in
  practice — Postgres has no `GH_TOKEN`. Documented explicitly in the
  migration's own comments and in `.factory/gtm/STOP.md` as a CP1+
  follow-up; the reliable notification path today is
  `gtm-validate.yml`/`gtm-merge.yml` checking `spi_gates.gtm_factory_halt`
  themselves with authenticated `gh`.

### 8. unified_context upserts — VERIFIED
- `cmo_factory_meta_v1`: PATCHed (row already existed from `claude_chat`'s
  earlier authoring of the meta prompt) — `content` replaced with a
  summary + full `file_paths` map of everything this CP installed,
  `last_updated_by` → `cc`. Confirmed via the PATCH response echo.
- `cmo_factory_external_gate_sep3`: also already existed (same prior
  authoring pass) — PATCHed to merge in fuller reasoning detail while
  preserving the original `never_relitigate: true` flag, `last_updated_by`
  → `cc`. Confirmed via the PATCH response echo.

## Deviations from the issue body

1. The issue's literal instruction was to create these two
   `unified_context` keys; both already existed (written by an earlier
   `claude_chat` session the same day, per `created_at` timestamps
   identical to `cmo_factory_meta_v1`'s original `updated_at`). I PATCHed
   rather than INSERTed, and merged rather than overwrote, to preserve that
   prior authoring rather than discard it.
2. `gtm-validate.yml`/`gtm-merge.yml` are committed and YAML-validated but
   not dispatch-tested in this session (see item 5's PARTIAL note above) —
   flagged, not hidden.
3. Journey 3 in `harness/gtm/END-TO-END.md` is marked `[NOT YET]` rather
   than the fully-live characterization META §6's prose implies, because
   the digest script's actual data source
   (`gold_standard_scoreboard`) doesn't match what META describes
   (re-queried `v_certified_counties`). This is exactly the kind of drift
   this factory exists to catch, so it's recorded as a finding rather than
   smoothed over.

## Findings (not part of the task, but worth surfacing)

- `scripts/biddeed-daily-digest.cjs` sources its "Gold Standard" badge from
  `gold_standard_scoreboard`, which `CC_META_PROMPT.md` explicitly calls
  untrustworthy. `src/worker.js`'s county-page logic already does this
  correctly (`v_certified_counties` + a live `gold_standard_certifications`
  cross-check) — the digest should be brought in line with that pattern.
  This is pre-existing drift, not something introduced this session.
- `quota_gate_check('engineering')` returns `NO_READING` in this live
  environment right now — real telemetry gap, not a test artifact. Every
  GTM workflow this CP installs is built to treat that as "run capped, log
  it" rather than silently skip, precisely because of this observation.

## Cache / cost telemetry

Per-session token/cache metrics were not captured via `/cost` in this
headless run; per `CC_META_PROMPT.md`'s own instruction, 0 is reported for
unavailable fields rather than a guess. `log_cc_session_cost` /
`record_session_cache_metrics` calls are deferred to whichever wrapper
process holds the actual usage numbers for this dispatch.
