# Spec — issue #19724 — Gold Standard shard-5: st_lucie, union (dispatch 3fbcb0c2)

## Intent reference
None at docs/intent/19724.md — issue body was the only brief (per M6, intent > body > comments; no intent file existed for this dispatch).

## What was built
- No new files, scripts, or migrations. This session enriched existing rows via PostgREST (Supabase REST/RPC), consistent with the SUPABASE_DB_PASSWORD/psql-fails-fall-back-to-PostgREST pattern already documented in decision_log ids 169/205/287.
- Ran the existing `scripts/shard9_union_clerk_realdata_ingest.py` live (idempotent re-scrape of unionclerk.com) — it errored on a transient `net::ERR_CONNECTION_CLOSED` fetching the tax-deed page before any write, so a standalone Playwright probe (`/tmp/union_probe.py`, not committed — throwaway) was used instead to read both pages without writing, which is what surfaced that case `63-2026-CA-0022` had newly appeared on the FC calendar.
- One ad hoc `/tmp/gold_standard_19724_stlucie_union.js` Workflow (not committed — throwaway) ran a 3-way fresh-angle fan-out + adversarial verify on Union's sole E/I/J-blocking row. It correctly refuted its own most promising finding (a GIS-details-pane URL that doesn't reproduce statically) and separately reconfirmed both counties' C-ceiling status via a clean DB-only recheck.
- Independently (outside the workflow, after its refuter killed the first lead), found and verified a real evidentiary chain via WebSearch + WebFetch + a US Census Bureau geocode, then had a second, independent Agent adversarially re-verify that chain before writing anything.

## SSOT bindings used
- `multi_county_auctions` (id `af29ad63-3b4e-4ec8-8ec5-b6bbfb0a63b2`, county=union, case_number=`63-2026-CA-0022`): wrote `parcel_id`, `property_address`, `owner_name`, `latitude`, `longitude`, `lot_size`, `year_built`, `legal_description`, `property_type`, `judgment_amount`, `judgment_amount_usd`, `plaintiff`, `parity_source`, `scraped_at`/`last_seen_at`. Left `assessed_value`/`market_value` NULL (genuinely unavailable, see Negative test result).
- `parcel_zones` (new row id 877040): parcel_id `06-06-20-00-000-0600-0`, jurisdiction_id 1187 ("Union County"), zone_code `R-1`, source suffixed `_ZONECODE_UNVERIFIED` — matches the exact pattern of the 3 pre-existing union rows in this table (same jurisdiction/zone, same honesty-labeled source suffix from `union_i_realparcel_backfill_run3025`).
- `gold_standard_ultraloop_audit`: 5 new rows (ids 20575-20579), dispatch_id `3fbcb0c2-f460-499b-a213-ee79ee9a7564` — union E (survived=true), union I negative finding (survived=true), union J deliberate non-action (survived=true), union C ceiling reconfirm (survived=true), st_lucie C ceiling reconfirm (survived=true).
- `gold_standard_campaign` (id 5562): `criteria_passed`, `criteria_total=10`, `exit_reason='ceilings_reconfirmed_union_E_fixed'`, `session_end_at` set.
- `public.pencil_dod_evaluate_county('st_lucie')` / `('union')`: read at session start and end (see DoD table).

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| st_lucie A,B,D,F,G,H | unchanged PASS | VERIFIED | Live eval byte-matches issue-brief baseline at start and end |
| st_lucie C | FAIL, reconfirmed structural ceiling (78.9%) | VERIFIED | 50 `CLERK_SSOT_CANCELLED` rows (was 47 on 2026-09-01), 0 carry `sold_amount`; independently re-queried by a workflow verifier agent, not just re-asserted |
| st_lucie E | PASS, 96.8% (unchanged) | VERIFIED | No st_lucie writes made this session — E/I/J metrics drifted slightly (96.8/95.6/99.2→100.0) from other shards'/cron background work, not this session |
| st_lucie I,J | unchanged PASS (J drifted 99.2→100.0, background accrual, not this session's write) | VERIFIED | Final eval call, timestamp below |
| union A,B,D,F,G,H | unchanged PASS | VERIFIED | Live eval byte-matches issue-brief baseline at start; unchanged at end |
| union C | FAIL, reconfirmed structural ceiling (75.0%) | VERIFIED | 1 `CLERK_SSOT_CANCELLED` row (case 63-2025-CA-0053) with a real cited clerk order citation; unchanged from 2026-09-01 finding |
| union E | **FAIL(75.0) → PASS(100.0)** | VERIFIED | parcel/address/geo/zoning written for case 63-2026-CA-0022 from 2 independently-fetched sources (unionclerk.com FC calendar + floridaparcels.com parcel roll) plus a US Census Bureau geocode; independently adversarially re-verified by a separate Agent (verdict SURVIVED, agent id `accb66a257c83228d`) before being logged as a survived audit claim |
| union I | FAIL, 75.0% (unchanged numerically, but now correctly diagnosed) | VERIFIED | Evaluator SQL (`supabase/migrations/20260810_gold_standard_shard3_lake_clerk_ssot_cd_recognition.sql` lines ~89-97) requires address+geo+`COALESCE(assessed_value,market_value)`+zoned-parcel; address/geo/zoning now satisfied for the 4th row, value is not — re-ran the evaluator after the E write to confirm empirically rather than guess |
| union J | FAIL, 75.0% (unchanged, deliberate) | VERIFIED (deliberate non-action) | `bid_decisions` has exactly the pre-existing 3 rows (checked live); the shipped `scripts/gold_standard_shard11_union_j_generator.py` would fall back to a $120K statewide default ARV for the 4th row with zero property-specific input — declined to run it given the fleet-wide "J ghost-fill" concern flagged elsewhere in this campaign (see the `pasco-f-audit-and-j-scope` skill) |

Session-start baseline and session-end result (full JSON) both captured below in "Negative test result" / final query block.

## Negative test result
Full-county live re-evaluation at session end (2026-09-02, ~08:50Z):
```
st_lucie: {"A":{"pass":true,"metric":122},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":78.9},
  "D":{"pass":true,"metric":99.2},"E":{"pass":true,"metric":96.8},"F":{"pass":true,"metric":100.0},
  "G":{"pass":true,"metric":95.5},"H":{"pass":true,"metric":0.1},"I":{"pass":true,"metric":95.6},
  "J":{"pass":true,"metric":100.0},"auctions_total":251}  -- 9/10, unchanged from baseline (J drifted +0.8 from background accrual)

union: {"A":{"pass":true,"metric":1},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":75.0},
  "D":{"pass":true,"metric":100.0},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},
  "G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.0},"I":{"pass":false,"metric":75.0},
  "J":{"pass":false,"metric":75.0},"auctions_total":4}  -- 7/10, up from 6/10 (E flipped)
```
No other letter regressed in either county. No writes were made to st_lucie rows this session (correctly — C is the sole failing letter there and is a reconfirmed ceiling, not a data gap).

## Deviations from intent
- Spent most of the session's fresh-research budget on union rather than splitting evenly, because st_lucie's sole failing letter (C) was already 2x independently reconfirmed as a canon-level structural ceiling by the prior day's session (`GOLD_STANDARD_SHARD2_CHARLOTTE_STLUCIE_OKALOOSA_TAYLOR_DISPATCH_9160A21E_SESSION_REPORT.md`), which explicitly recommended *not* re-investigating without new evidence. Honored that guidance; did a cheap DB-only reconfirm instead of a full re-investigation.
- Declined to run the existing union J-generator for the newly-linked row despite it being mechanically available, because its only available fallback (a generic $120K statewide default ARV) would have been fabricated-feeling data for this specific property. This is a judgment call, not an evaluator requirement violation — logged transparently as a deliberate non-action in `gold_standard_ultraloop_audit`.
- Did not run `gold_standard_loop()` / `gold_standard_certify()` — per PARALLEL-FLEET RULES, these are skipped when other shards may be mid-flight (confirmed likely mid-flight: hit `55P03 canceling statement due to lock timeout` on the very first `pencil_dod_evaluate_county` call this session, consistent with concurrent load). Per-county `pencil_dod_evaluate_county` was used throughout instead, as instructed.

## Open items / handoff
1. **union I**: structurally blocked on `assessed_value`/`market_value` for parcel `06-06-20-00-000-0600-0` (case 63-2026-CA-0022). Tried: FL GIO Statewide Cadastral ArcGIS FeatureServer (CO_NO=63 returns 0 features for both owner-name and exact-PIN queries — a real, verified empty result, not a network failure, though the same endpoint became flaky/timeout-prone under repeated calls this session), Zillow (403), Realtor.com (429 Kasada bot-block), Union County Property Appraiser CAMA widget at union.floridapa.com/GIS/ (legacy GrizzlyLogic/Schneider viewer, search form lives in a session-state-dependent iframe that a static/API approach cannot reproduce — this is the exact thing the workflow's refuter caught when a subagent claimed to have read a results page from it). Next session: worth one attempt at a real interactive Playwright session (not just static fetch) against the CAMA widget with `page.on('request')`/`page.on('response')` capturing *every* XHR (not just URL-substring-filtered ones) during an actual successful click-driven search, since this session confirmed the search form fields exist (`#searchInput`, `SearchBy` radios with values NAME/STREET/PIN/ALL, `#butSearch`) but never got a live results DOM to inspect.
2. **union J**: gated on I's value data per above — the county-agnostic Shapira J-generator pattern already exists and is proven (3/4 union rows), just needs a real ARV input for the 4th row once #1 unblocks it.
3. **union C / st_lucie C**: both reconfirmed 3x now as the same fleet-wide `CLERK_SSOT_CANCELLED`-vs-`matched_clean` canon question (documented in `GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md`). Not single-shard-fixable — needs an owner/architect decision on that doc's Option A/B/C. Do not re-investigate a 4th time without new evidence.
4. If a future session gets FL GIO cadastral queries reliably working for CO_NO=63 (this session saw it flip between working — real 0-result responses — and timing out across ~8 calls), it's worth re-trying an exact-PIN query for `0606200000006000` — the empty results this session may reflect thin/stale Union County sync rather than the parcel genuinely being absent.
