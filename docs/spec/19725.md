# Spec — issue #19725 — Gold Standard shard-2: calhoun, okaloosa, charlotte, holmes

## Intent reference
None — no docs/intent/19725.md exists; the issue body was the only brief (per M6).

## What was built
No code files changed. This was a live-DB diagnose/fix/adversarial-verify session (ULTRALOOP
protocol, `Workflow` fallback mode) against `multi_county_auctions` via Supabase PostgREST, plus
the mandatory close-out rows. Direct `psql`/`SUPABASE_DB_PASSWORD` connection failed as documented
(known constraint) — all reads/writes used `SUPABASE_SERVICE_ROLE_KEY` + PostgREST/RPC, per the
session's standing instructions.

Workflow script persisted at:
`.claude/workflows/gold-standard-shard2-calhoun-okaloosa-charlotte-holmes-54730e95.js` (run id
`wf_87398e94-ae2`, 5 subagents, 178 tool calls, 460,672 tokens).

## SSOT bindings used
- `public.pencil_dod_evaluate_county(p_county)` RPC — before/after evaluation for all 4 counties.
- `public.multi_county_auctions` — read/write (writes limited to 3 rows, all reverted — see below).
- `public.gold_standard_ultraloop_audit` — 7 rows inserted (ids 20531–20537).
- `public.gold_standard_campaign` — 1 row updated (id 5559, dispatch_id `54730e95-...`).
- `public.agent_ops_log` — 1 row inserted (id `2756d085-...`).
- `public.summit_chat_dispatch` — pre-existing row for this dispatch, unchanged (state=`dispatched`).

## DoD status (canon A–J, per county)

### calhoun — 9/10 (unchanged)
| Letter | Status | Evidence |
|---|---|---|
| A,B,D,E,F,G,H,I,J | PASS | Unchanged this session |
| C | FAIL 88.9% (matched_clean=8 of 9) | VERIFIED — 4th+ reconfirmation. Single-row block: case `546 OF 2024` (tax_deed), `parity_status=CLERK_SSOT_CANCELLED`. Live `calhounclerk.com/wp-json/kma/v1/taxdeeds` (pending feed) = `[]`; `/wp-json/kma/v1/taxdeedoverbids` (34 completed records) does NOT contain `546 OF 2024`, while 2 sibling cases from the identical 2026-08-13 sale date (`227 OF 2024`, `268 OF 2023`) DO appear and are already `PARITY_OK` — corroborates a genuine non-sale, not a scrape gap. Row count reconfirmed at exactly 9 (unchanged) — 95% is arithmetically unreachable while a single exclusion stands on a 9-row denominator (would need ~20 rows). Canon-level, not a data fix. No write made.

### okaloosa — 9/10 (unchanged; a false-positive was caught and reverted)
| Letter | Status | Evidence |
|---|---|---|
| A,B,C,D,E,F,G,H,J | PASS | Unchanged this session |
| I | FAIL 92.9% (card_complete=79 of 85) | A fixer subagent claimed I moved to 83/85 (97.6% PASS) by writing lat/lng/assessed_value for 4 rows (`2025-CA-002286-F/F3/F4/F5`) from the FL GIO Statewide Cadastral FeatureServer. **Independent adversarial refuter REFUTED this claim (survived=false)**: (a) live re-run of `pencil_dod_evaluate_county('okaloosa')` returned I unchanged at 79/85 — criterion I additionally requires a `v_zoning_gold_standard_card` zone-code join the fixer never addressed, so the metric could not have moved regardless; (b) genuine cross-county contamination found — `F3` (`PHY_CITY=MIRAMAR BEACH`) and `F4` (`PHY_CITY=DEFUNIAK SPRINGS`) are Walton County parcels (`CO_NO=76` per `scripts/_gis_sweep_order.py`'s own mapping), not Okaloosa; `F5`'s own stored `legal_description` literally reads "WALTON COUNTY, FLORIDA" — the fixer noticed this and rationalized it away instead of treating it as disqualifying. **Orchestrator reverted** `latitude`/`longitude`/`assessed_value`/`assessed_value_source` to `NULL` for F3/F4/F5 this session (verified via follow-up GET — all 3 confirmed null). `F` itself (case `2025-CA-002286-F`, `CO_NO=56` bbox match, address/value corroborated accurate by the refuter against the pre-existing stored address) was left untouched at `assessed_value=147788`. Post-revert live re-check: I = 79/85 = 92.9% FAIL, matching true baseline exactly — zero contamination remains in production. The 2 permanently-null rows (`2024-CA-000470`, `2024-TDD-000089`) were re-checked via the one new lever (Okaloosa PA site, `okaloosapa.com`) — Cloudflare 403 (curl+WebFetch) and Firecrawl 402 (out of credits); both rows also have zero search keys (no address/parcel_id/owner/plaintiff), so PA-site access alone would not resolve them even if unblocked. Reconfirmed genuine data-source ceiling.

### charlotte — 8/10 (unchanged)
| Letter | Status | Evidence |
|---|---|---|
| A,B,E,F,G,H,I,J | PASS | Unchanged this session |
| C | FAIL 58.3% (matched_clean=180 of 309) | Not targeted this session — independently reconfirmed 3x in prior sessions (2026-08-29, 08-31, 09-01) as a canon-level ceiling (113/309 rows are genuine `CLERK_SSOT_CANCELLED`, permanently excluded from `matched_clean` by design; even 100% reclassification of cancellations only reaches 94.4%). No further action taken per explicit prior-session guidance not to re-investigate without new evidence. |
| D | FAIL 94.8% (matched_any=293 of 309) | VERIFIED, new diagnostic finding. 16 rows lack `parity_status`; 10 are tax-deed cases from the 2026-09-01 auction with zero tier1 data. Live-traced the authoritative source: `charlotteclerk.com/departments/taxdeed/` confirms `charlotte.realforeclose.com` (RealAuction) is the Clerk's official tax-deed platform. Live-queried its `FNC=LOAD` JSON endpoint for the 09/01/2026 sale — all 10 target cases genuinely present (with real opening-bid amounts), but disposition fields (`ASTAT_MSGA-D`, `MSG_SOLDTO_MSG`) are empty for every one of 20 sampled items, and a known-past sale date (08/11/2026) queried the same way returned a fully empty result — confirms disposition data is gated behind bidder login (`zmethod=login`), not a scrape gap. `taxdeeds.charlotteclerk.com` returns 403; `or.charlotteclerk.com/TaxDeeds` is network-unreachable (ECONNREFUSED). No third-party aggregator carries genuine post-sale disposition. Case `26-0178` (rescheduled) reconfirmed via `noticeregistry.com`'s original notice as a tax-deed case with no new sale date posted anywhere checked. No write made — new lever needed next session: authenticated RealAuction bidder session or a different disposition source. |

### holmes — 6/10 (unchanged)
| Letter | Status | Evidence |
|---|---|---|
| A,E,G,H,I,J | PASS | Unchanged this session |
| B,C,D,F | FAIL (verified=0/0, matched_clean/any=11/16=68.8%, tier1_sold=0/0) | VERIFIED, 19th+ reconfirmation of a well-documented structural dead end (18 prior sessions since 2026-07-10, most recently dispatch `4cb27213` on 2026-08-25). One genuinely new lever existed this session: 2 foreclosure rows dated 2026-08-27 (6 days past due as of today, 2026-09-02) — first time their sale date had actually passed. Live-fetched `holmesclerk.com/courts/foreclosures-tax-deeds/foreclosures/`: parcel `0531.03-003-028-007.000` (STATEN case) is STILL listed under "Upcoming Foreclosure Sales" with only the pre-sale judgment amount ($216,296.80), no disposition; parcel `0936.01-004-00C-008.000` does not appear on the page at all. No results/archive/sold page exists on the site. All other previously-exhausted levers (case-number/defendant-name site search, tax collector site, myfloridacounty ORI / civitek OCRS — both CAPTCHA-gated and out of bounds) were correctly NOT re-attempted this session. No write made. |

## Negative test result
- Charlotte: confirmed row count unchanged at 309 total / 293 matched_any both before and after (no regression on the other 293 rows).
- Okaloosa: confirmed 85 total rows unchanged; confirmed the 3 reverted rows are genuinely null again and the 79-row card_complete baseline is exactly reproduced.
- Calhoun: confirmed 9 total rows unchanged.
- Holmes: confirmed 16 total rows unchanged; the 2 checked past-due parcels are unchanged (`auction_status='scheduled'`, `sold_amount=null`).
- All other previously-passing letters across all 4 counties reconfirmed PASS via the same live evaluator calls (see the raw JSON blocks below) — zero regressions.

## Deviations from intent
- No `docs/intent/19725.md` existed; worked directly from the issue body per M6.
- Per PARALLEL-FLEET RULES, did not run `gold_standard_loop()`/`gold_standard_certify()` (other shards were actively writing concurrently — confirmed via a fresh `gold_standard_ultraloop_audit` row from a different dispatch, `05fae79e-...`, timestamped seconds before this session's own writes). Used `pencil_dod_evaluate_county()` per county instead, as instructed.
- One fixer subagent's write was incorrect (cross-county contamination) and was caught + reverted by this session's own ULTRALOOP verify step before being counted as done — reported honestly as a REFUTED claim (survived=false), not hidden. This is the ULTRALOOP protocol working as designed, not a deviation from it.

## Open items / handoff
1. **Charlotte D** (nearest target, 293→294/309 needed): needs either (a) an authenticated RealAuction bidder session for `charlotte.realforeclose.com`, or (b) discovery of a different Charlotte tax-deed disposition source not yet tried (Clerk's official-records portal is network-unreachable from this runner — may work from a different egress/IP). The 10 target case numbers are known: 26-0239, 26-0242, 26-0245, 26-0248, 26-0251, 26-0254, 26-0256, 26-0257, 26-0258, 26-0266.
2. **Okaloosa I**: the 2 genuinely-blocked rows (`2024-CA-000470`, `2024-TDD-000089`) need a Firecrawl credit top-up or a working `browser-use` install to get past Cloudflare on `okaloosapa.com` — but even then, these rows have zero search keys (no address/parcel/owner/plaintiff) in the DB, so a case-number-only PA lookup will not work; the real fix is enriching those 2 rows with ANY identifying field first (e.g. from the original court filing) before a PA lookup becomes possible at all.
3. **Data-quality flag for the fleet**: this session found the FL GIO Statewide Cadastral FeatureServer's `CO_NO` field does not reliably match the standard FL DOR county-number convention used elsewhere in this repo's scripts (`scripts/_gis_sweep_order.py`, `scripts/county_co_no_expand.py`) — spatial/PARCEL_ID matches for genuinely-Okaloosa vs genuinely-Walton parcels returned inconsistent `CO_NO` values (56 and 76 respectively, neither matching the documented Okaloosa=46). Any future session using this FeatureServer for cross-county parcel lookups should cross-validate the returned `PHY_CITY`/address against the target county before writing, not trust `CO_NO` alone.
4. **Calhoun C / Holmes B/C/D/F**: both are now 4th+ and 19th+ times reconfirmed structural ceilings. Per prior-session guidance, future sessions should not re-investigate either without a genuinely new lever (a new auction crossing its sale date, a new data source appearing, or an owner-level canon decision on the C-letter cross-county structural-block finding).

## Live evaluator output (VERIFIED, timestamp 2026-09-02T08:31Z, post-revert)

```json
calhoun:   {"A":true(3),"B":true(100.0),"C":false(88.9),"D":true(100.0),"E":true(100.0),"F":true(100.0),"G":true(100.0),"H":true(17.3),"I":true(100.0),"J":true(100.0)} -> 9/10
okaloosa:  {"A":true(28),"B":true(100.0),"C":true(97.6),"D":true(97.6),"E":true(97.6),"F":true(100.0),"G":true(100.0),"H":true(0.0),"I":false(92.9),"J":true(100.0)} -> 9/10
charlotte: {"A":true(41),"B":true(100.0),"C":false(58.3),"D":false(94.8),"E":true(100.0),"F":true(100.0),"G":true(97.8),"H":true(0.0),"I":true(95.8),"J":true(99.4)} -> 8/10
holmes:    {"A":true(6),"B":false(null),"C":false(68.8),"D":false(68.8),"E":true(100.0),"F":false(null),"G":true(100.0),"H":true(0.3),"I":true(100.0),"J":true(100.0)} -> 6/10
```

No county moved letters net this session. See "What was built" for the substantive findings
(1 caught-and-reverted false positive, 1 new root-cause diagnosis, 2 freshness reconfirmations).
