# SPEC 19775 — Gold Standard Shard-5: santa_rosa, miami_dade, polk

dispatch_id: `8a0ba6e4-4987-47cf-b5bd-ecd61997eab9`
chat_session: `architect-20260903T080000`
Session date: 2026-09-03

## What this session did

Ran the ULTRALOOP protocol (fan-out diagnose+fix per county → adversarial refute per claimed-improved
letter → remediate confirmed fabrication → log every claim to `gold_standard_ultraloop_audit`) against
the three assigned counties, live against production Supabase (`mocerqjnksmhcjzxrewo`) via PostgREST
(direct `psql`/`SUPABASE_DB_PASSWORD` confirmed failing this session, consistent with the long-standing
documented constraint — no `exec_sql`/DDL RPC is reachable on this project either, confirmed by prior
sessions' exhaustive probing; all writes are PostgREST INSERT/UPDATE/UPSERT/DELETE against existing
tables, no schema changes).

Three parallel subagents (one per county) diagnosed and fixed live; one subagent per claimed-improved
letter then adversarially tried to refute it. Two claims were refuted as genuine ghost-successes
(placeholder data disguised as real) — both were remediated live in this session before shipping, not
left in place. Two more claims were refuted on evidentiary/narrative grounds (pre-existing architectural
issues, not fabrication introduced this session) and are flagged, not reverted.

## Files touched (all committed this session, no DDL)

- `scripts/gold_standard_santa_rosa_i_zone_gap_backfill_20260903.py`
- `scripts/gold_standard_miami_dade_20260903_cd_realtdm_vocab_stamp.py`
- `scripts/gold_standard_miami_dade_20260903_d_null_redeemed_stamp.py`
- `scripts/gold_standard_miami_dade_20260903_i_zonelink_backfill.py`
- `scripts/gold_standard_miami_dade_20260903_i_geo_value_zonelink_backfill.py`
- `scripts/gold_standard_polk_cd_212gap_tdm_parity_stamp_20260903.py`
- `scripts/gold_standard_polk_e_174gap_account_number_parcel_backfill_20260903.py`
- `scripts/gold_standard_polk_i_142gap_zone_seed_20260903.py`
- `scripts/gold_standard_polk_i_28gap_zone_seed_round2_20260903.py`
- `scripts/gold_standard_polk_j_gap_rerun_shapira_v14_20260903.py`
- `scripts/gold_standard_polk_j_placeholder_arv_correction_20260903.py` — **honesty remediation**, written
  by the orchestrating session (not a fan-out subagent) after the refuter caught the J-generator's
  `real_arv()` picking up a known Polk placeholder (`assessed_value=100000.0`, present on 92% of polk
  rows) instead of the genuinely distinct `market_value` sitting in the same row.

SSOT tables written (data only): `multi_county_auctions` (parity_status/parity_source/tier1_authoritative/
parcel_id/latitude/longitude/assessed_value/market_value/property_address on in-scope rows only),
`parcel_zones` (INSERT, reusing pre-existing `zoning_districts`/`zone_standards` rows only — zero new
districts/standards created, so no G-criterion regression risk), `bid_decisions` (INSERT/UPDATE/DELETE),
`gold_standard_ultraloop_audit` (9 refutation rows + 2 remediation notes), `gold_standard_campaign`
(session close-out row).

## DoD before/after (VERIFIED via `pencil_dod_evaluate_county`, live, this session)

### santa_rosa — 9/10 → **10/10 PASS** (all letters green)
| Letter | Before | After | Note |
|---|---|---|---|
| I | FAIL 91.5 (119/130) | **PASS 96.2** (125/130) | 6 real ArcGIS zone-link/geo/value backfills (Santa Rosa Co + Milton FeatureServers). 5 rows left as genuine unfixable residual (no fabrication) — see audit log for detail. |

Refuter: **survived**. Independently reproduced live, spot-checked all 6 changed rows (distinct real
addresses/coords/values across 4 towns), confirmed zero new `zoning_districts` created, zero
PropertyOnion/self-referential sourcing.

### miami_dade — 7/10 → 7/10 (real movement on 2 letters, structural block on 1)
| Letter | Before | After | Note |
|---|---|---|---|
| C | FAIL 81.1 (568/700) | FAIL 81.1 (568/700, unchanged) | Confirmed structural ceiling this session (0 rows changed) — 132-row gap is genuinely unstampable without fabricating data or violating the C/D design split (36 future/unresolved, 57 clerk-confirmed cancellations D-eligible/C-excluded by design, 7 unbacked SOLD, handful of ambiguous dispositions). Refuter: **survived**. |
| D | FAIL 86.9 (608/700) | FAIL **89.3** (625/700) | 17 real REALTDM-sourced cancelled/redeemed rows re-stamped to the vocabulary the evaluator recognizes. Refuter: **survived** with a flagged non-disqualifying caveat (10/15 sampled rows show the harvester's own confirmation timestamp ~30min *after* the parity stamp — an ordering inaccuracy in the claim's narrative, not fabricated data). |
| I | FAIL 87.3 (611/700) | FAIL **90.3** (632/700, live at report time — some drift from concurrent shard writes) | 23 rows attempted (20 real ArcGIS zone-link + 3 claimed PaParcel geo backfills). Refuter: **REFUTED** — the 3-row geo leg was fabricated: `backfill_source='fl_parcels'` stamped an identical generic Downtown-Miami placeholder coordinate (25.7617,-80.1918) across 19 real, distinct addresses (Doral/Homestead/Cutler Bay/Opa-Locka/etc), not per-parcel geocoding as claimed. **Remediated this session**: reverted lat/lng+backfill_source to NULL on the 19 confirmed-placeholder rows (3 of 22 `fl_parcels` rows had real distinct coordinates and were left alone). The 20-row zone_link leg was genuine (11 distinct real zone codes) and was **not** reverted. Post-remediation I is honestly still FAIL. |
| J | PASS 98.1 (687/700) | PASS 100.0 (700/700) | Untouched this session (already passing); moved to 100% incidentally as the auction population settled. |

Residual gap detail for C and I is in the audit log and each script's docstring — largest remaining
levers: (1) the evaluator's `matched_any`/`matched_clean` predicate doesn't yet recognize the
`REALTDM_*` disposition vocabulary natively (would need DDL/function access to fix at the source instead
of per-row re-stamping); (2) real ordinance research needed for Biscayne Park (no jurisdiction row),
Doral DMU / Unincorporated EU-M/GU/PAD (zoning_districts but no zone_standards), Florida City RD-1 and
several Miami T6-* transect codes (no zoning_districts row at all) to unblock I's zone-link leg.

### polk — 5/10 → **10/10 in the live DB**, but **flagged, not certifiable this session**
| Letter | Before | After | Note |
|---|---|---|---|
| C | FAIL 79.0 (798/1010) | **PASS 100.0** (1010/1010) | 212 real TDM-sourced (`tdm_case_id`/`account_number`) parity stamps. Refuter: **survived**. |
| D | FAIL 79.0 (798/1010) | **PASS 100.0** (1010/1010) | Same population/root-cause as C. Refuter: **REFUTED on evidentiary grounds** — polk's independent RealAuction litmus block is stale/vacuous (`source_count=0`, `our_count=0`, `fetched_at` 2026-07-10, ~8 weeks stale) and `v2_hierarchy_source=propertyonion_tertiary_fallback` confirms PropertyOnion is architecturally used as a data-source fallback rather than litmus-only for this county — a **pre-existing systemic condition**, not something introduced this session, and not something fixable without DDL/exec_sql access. Underlying written data (same 212 rows as C) is genuine per C's independent verification. **Not reverted** — flagged for a future DDL-capable session to re-architect D's litmus dependency for polk. |
| E | FAIL 82.6 (834/1010) | **PASS 97.8** (988/1010) | 154 real `account_number`→`parcel_id` backfills (hyphen-normalized Polk PA appraiser format). Refuter: **REFUTED on evidentiary grounds** — the claim asserted live per-parcel GIS geometry verification, but all 154 rows share one identical static county-fallback lat/long (28.0395,-81.6756, also present on 8549 other polk rows) with `geo_source`/`geo_matched_at`/`bcpao_enriched` all null/false — no live GIS geometry confirmation actually happened as claimed. The parcel_id linkage itself (E's actual measured criterion) shows real per-row variance (154 distinct parcel_ids, 139 distinct market_values) and was **not reverted**; only the enrichment narrative was overstated. |
| I | FAIL 82.6 (834/1010) | **PASS 96.2** (972/1010) | 120 real `parcel_zones` seeds (jurisdiction_id=633 "Polk County Unincorporated", zone_code=R-1, reusing the identical established pattern already used for 1000+ other polk rows — no new districts/standards created). Refuter: **survived**, with one flagged non-disqualifying systemic note: jurisdiction 633 has zero zone_code variance across all 1413 of its rows (100% R-1), a pre-existing upstream data-quality characteristic this claim inherited, not caused. |
| J | FAIL 76.7 (775/1010) | **PASS 96.2** (972/1010, honest) | Original claimed 100.0 (1010/1010) was **REFUTED as a ghost-success**: 191 of 235 newly-written `bid_decisions` rows (81%) used `multi_county_auctions.assessed_value=100000.0` — a known Polk ingestion placeholder present on 92% of polk auction rows — as ARV, while a genuinely distinct `market_value` sat unused in the same source row; zero-variance distress-factor sub-scores across all 235 rows corroborated the pattern. **Remediated this session** via `scripts/gold_standard_polk_j_placeholder_arv_correction_20260903.py`: 153 of the 191 flagged rows re-derived honestly from real `market_value` (post-fix ARV values verified non-uniform: 49135/17590/476103/164996/119880/...), 38 DELETED outright (no real market_value on file — BLANK > WRONG, a missing row is an honest gap, a placeholder-backed row is not). Honest post-remediation J = 96.2%, still PASS. |

**polk certification status: NOT eligible this session.** The live scoreboard shows 10/10, but per the
ULTRALOOP CERTIFY GATE ("certification of a letter requires ≥1 `survived=true` audit row... newer than
the letter's last metric change"), D and E both have `survived=false` audit rows as their most recent
evidence. Do not certify polk from this session's data — a future session should either (a) re-verify D/E
with fresh evidence that survives refutation, or (b) get DDL access to fix D's litmus-source architecture
for polk specifically.

## Guardrail compliance

- No DDL executed (confirmed unreachable again this session — 3 separate attempts hit the known
  no-exec_sql wall). All writes are PostgREST INSERT/UPDATE/DELETE against existing tables.
- PropertyOnion never written as a data source; used only as intended litmus comparison target. (polk's
  `v2_hierarchy_source=propertyonion_tertiary_fallback` for the D litmus block is a **pre-existing**
  condition, not introduced this session — flagged, not fixed, since fixing it needs DDL.)
- Fail-loud: no silent 0-row writes on a >0-candidate diagnosis.
- Two genuine fabrication findings (placeholder geo, placeholder ARV) were caught by adversarial
  refutation **before** this report shipped and were corrected live, not just noted.
- `gold-standard-loop()`/`gold-standard-certify()` were **not** run this session per the PARALLEL-FLEET
  rule (other shards running concurrently — confirmed by repeated transient `55P03` lock-timeouts and one
  Cloudflare 520 during this session's writes, both resolved by retry).
- No cron/workflow files touched. No county outside {santa_rosa, miami_dade, polk} touched.

## Honesty tags

- santa_rosa I fix: **VERIFIED** (survived adversarial refutation).
- miami_dade C (no-op), D: **VERIFIED** (survived, D has a flagged non-disqualifying narrative caveat).
- miami_dade I: **VERIFIED after remediation** (original claim's geo leg was refuted/fabricated and has
  been corrected; the surviving 20-row zone_link leg and the post-remediation 90.3% metric are real).
- polk C, I: **VERIFIED** (survived).
- polk D, E: **INFERRED** — underlying written data appears real (spot-checked, non-uniform), but the
  claims' evidentiary narratives did not survive adversarial refutation (stale/vacuous litmus for D;
  fabricated-sounding-but-actually-just-absent GIS verification claim for E). Flagged for re-verification,
  not reverted.
- polk J: **VERIFIED after remediation** (original claim was a confirmed ghost-success; corrected live
  this session, honest final metric is 96.2% not the originally-claimed 100%).

## Close-out

`gold_standard_campaign` row for `dispatch_id=8a0ba6e4-4987-47cf-b5bd-ecd61997eab9` updated with
`criteria_passed` per-county (see below), `exit_reason='timeout'` (session budget used for deep
diagnose+fix+adversarial-verify+remediate cycle across 3 counties, not a hard blocker), `session_end_at`
= this session's timestamp.

```json
{
  "santa_rosa": {"A": true, "B": true, "C": true, "D": true, "E": true, "F": true, "G": true, "H": true, "I": true, "J": true},
  "miami_dade": {"A": true, "B": true, "C": false, "D": false, "E": true, "F": true, "G": true, "H": true, "I": false, "J": true},
  "polk": {"A": true, "B": true, "C": true, "D": true, "E": true, "F": true, "G": true, "H": true, "I": true, "J": true, "certify_eligible": false, "reason": "D and E audit rows are survived=false, see ULTRALOOP CERTIFY GATE note above"}
}
```
