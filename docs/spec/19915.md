# 19915 — ARCHITECT TRIAGE: blocked #19841 DoD (st_johns, gadsden, santa_rosa gold-standard certification)

dispatch_id: 4befb929-99a5-42ba-a8b4-7a902bb590ad
chat_session: auto-triage-issue-19841-202609041420
session date: 2026-09-04

## Intent reference
None at docs/intent/19915.md — issue body is the only brief (per dispatch header).

## Summary

Re-diagnosed the failing DoD for issue #19841 (`EXISTS ... gold_standard_certifications WHERE
county_slug = ANY('{st_johns,gadsden,santa_rosa}') AND certified` → must be TRUE). Confirmed
live it is still FALSE for all three. Root-caused each county's gap with direct evidence
(live evaluator re-run + raw table reads + audit-trail history), found **no software bug**
anywhere in the chain (evaluator, certify gate, cron wiring all behaving as designed), and
made **no data writes** — the only levers for two of the three gaps require writes to
`multi_county_auctions`, which is on the M2 protected-objects list and this issue does not
name it for write access. Findings posted to #19841, logged to `decision_log` (id 3247) and
`agent_ops_log` (id 756fed37-f516-41f4-bdf9-9f25460981cd).

## What was built

No code files changed. No migrations. No schema changes. Read-only diagnosis session:
Supabase PostgREST (`SUPABASE_SERVICE_ROLE_KEY`) used throughout for all queries and the
one `pencil_dod_evaluate_county` RPC call per county (explicitly sanctioned as safe/read
by #19841's own PARALLEL-FLEET RULES regardless of in-flight sibling sessions). Direct
psql/`SUPABASE_DB_PASSWORD` not attempted — known unavailable per standing constraint
(decision_log ids 169/205/287).

Outputs of this session:
- Comment on issue #19841: https://github.com/breverdbidder/cli-anything-biddeed/issues/19841#issuecomment-5541955039
- `public.decision_log` id 3247 (decision_type=triage)
- `public.agent_ops_log` id 756fed37-f516-41f4-bdf9-9f25460981cd (status=BLOCKED)
- This file

## SSOT bindings used (read-only)

- `public.gold_standard_certifications` — `county_slug, certified, revocation_reason,
  consecutive_non_gold, last_evaluated_run_id` for the 3 target counties (all `certified=false`).
- `public.pencil_dod_evaluate_county(p_county text)` RPC — live per-letter A-J metrics for
  st_johns, gadsden, santa_rosa.
- `public.gold_standard_ultraloop_audit` — `county_slug, letter, survived, claim, created_at`
  history for gadsden (E/I/J, Aug29–Sep4), santa_rosa (C/D/I, Aug8–Sep3), st_johns (C, Aug16–Sep2).
- `public.multi_county_auctions` — read-only: gadsden rows with `parcel_id IS NULL` (6 rows,
  case numbers `26000048TDC`..`26000051TDC`, `24000041CA`, `26000105CA`); santa_rosa
  `parity_status` distribution (269 rows: 145 NULL, 96 `matched_clean`, 28 `REALTDM_REDEEMED`).
- `public.decision_log` id 3213 (yesterday's martin/#19819 sibling triage) — read for precedent
  on what counts as an in-scope autonomous fix vs a protected-table write.
- Repo file `GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md` — read for
  the pre-existing fleet-wide canon finding that st_johns/C matches exactly.

## DoD status

| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| 1 | `EXISTS(gold_standard_certifications WHERE county_slug IN (st_johns,gadsden,santa_rosa) AND certified)` = TRUE | **BLOCKED (data ceiling, not a bug)** | Live query returns FALSE for all 3; `certified=false` confirmed for each with row-level `revocation_reason`. No fix applied — see Deviations. |

## Root cause per county (VERIFIED)

**st_johns (9/10, only C fails, 93.4% = 113/121).** Matches the fleet-wide
`GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md` pattern: C's canon
excludes `CLERK_SSOT_CANCELLED`/redeemed rows from `matched_clean`, and this county's real
cancellation rate eats the 95% threshold's 5% slack. `gold_standard_ultraloop_audit` shows
5 independent PASS↔FAIL flips since 2026-08-16, each re-verified live against clerk sources
and reconfirmed genuine (no fabrication). Latest flip (Sep-02 114/120 PASS → Sep-04 113/121
FAIL) is denominator growth from a new/reclassified row outpacing one-row-at-a-time manual
matching, same mechanism as every prior flip.

**gadsden (7/10, E/I/J fail, 91.5% = 65/71 each).** Root cause confirmed by direct row read:
6 of 71 rows have `parcel_id IS NULL AND property_address IS NULL` — 4 brand-new tax-deed
cases (`26000048TDC`–`26000051TDC`, created 2026-09-03) plus 2 long-scheduled cases never
enriched (`24000041CA` since Aug-19, `26000105CA` since Aug-27). E was stable PASS at 97.0%
(65/67) through Sep-01–03; it dropped to 91.5% (65/71) purely because the 4 new rows landed
with no address for a parcel lookup to key off. I/J are structurally capped at E's value
here (I≤E by construction; J's factor set references the same card fields), matching the
dependency chain already documented in #19841's "CRITERION-PARALLEL PIVOT" section.

**santa_rosa (7/10 currently — was 6/10 in the #19841 brief; E has since improved to PASS
98.1%). C/D/I still fail (35.7% / 46.1% / 46.5%).** Live `parity_status` distribution across
all 269 rows: 145 (54%) are `NULL` — never run against a litmus source at all — vs 96
`matched_clean` + 28 `REALTDM_REDEEMED`. Unlike st_johns, this is a large unactioned backlog,
not a razor's-edge structural ceiling; I's 46.5% plausibly rides the same unfinished
enrichment pass.

## Negative test result

Checked whether the certify gate itself, the evaluator function, or any cron/wiring was the
actual blocker (as opposed to real data gaps) — found no evidence of this. `last_error: none
logged` in the original DoD stub is correct: there is no crash/exception anywhere in the
chain. All three counties' live evaluator output matches their audit-trail history exactly
(no drift, no stale-cache mismatch). Confirmed `pinellas`/`martin`-style non-protected-table
levers (zoning_districts/zone_standards/parcel_zones) do **not** apply here — none of the
failing letters for these 3 counties are zoning-related (G passes in all 3).

## Deviations from intent

No fix was applied this session, unlike the successful martin/#19819 sibling triage
(decision_log id 3213) which fixed a zoning-linkage gap and pushed `gold_standard_certify()`
to flip `certified=true`. That fix worked because it lived entirely in
`zoning_districts`/`zone_standards`/`parcel_zones` — none of which are M2-protected. For
st_johns/C and santa_rosa/C,D,I, the only available lever is writing to
`multi_county_auctions` (parity_status / parcel_id / address / card fields), which **is**
on the M2 protected-objects list; this issue's body does not name `multi_county_auctions`
for write access (it only names `gold_standard_certifications`, inside a read-only DoD
check). Per M5 scope discipline, this triage session did not write to it. `gadsden`'s gap
has the same constraint (its 6 null rows also live in `multi_county_auctions`).
`gold_standard_loop()`/`gold_standard_certify()` were not run — none of the 3 counties are
at a genuine 10/10 raw evaluation, so running them would not change any outcome.

## Open items / handoff

1. **Human decision needed (flagged in #19841 comment, unanswered as of session end):**
   st_johns/C (and its calhoun/manatee/taylor siblings from the Aug-27 finding) will keep
   re-failing indefinitely under the current 95%-with-no-cancellation-carveout canon.
   Recommend either (a) apply D's existing cancellation-exclusion logic to C's denominator
   too, or (b) lower C's threshold to ~90% to reflect real FL foreclosure cancellation base
   rates. Fleet-wide scoring-semantics change — needs Ariel's sign-off, not a unilateral
   triage-session edit.
2. **No approval needed, just more campaign session time (already authorized under #19841's
   own issue body):** gadsden — link parcel_id/address for 6 rows (`26000048TDC`–
   `26000051TDC`, `24000041CA`, `26000105CA`) via the county property appraiser ArcGIS
   FeatureServer per the E playbook. santa_rosa — run the litmus/enrichment pass against the
   145 `parity_status IS NULL` rows (santarosa.realtaxdeed.com / realforeclose.com AJAX,
   per prior successful santa_rosa C/D fixes in the audit trail).
