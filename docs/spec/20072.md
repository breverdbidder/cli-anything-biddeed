# Spec — issue #20072 — port /support + /contact + camera=(self) from PR #20061, no palette

## Intent reference
None at docs/intent/20072.md — issue body was the only brief.

## What was built
- `src/worker.js`:
  - Added `SUPPORT_HTML` const (new, ~line 10297) and the `/support` + `/contact`
    route dispatch (new line, alongside the existing `/disclaimer` /`/security`
    routes, ~line 3151): both paths render the same static support page via
    `injectChatwootWidget(withPublicShell(SUPPORT_HTML, path), env)`, matching
    the `/security` and `/disclaimer` pattern exactly.
  - Added `/support` to the `/sitemap.xml` `staticUrls` array.
  - Changed `Permissions-Policy` `camera=()` → `camera=(self)` in
    `withSecurityHeaders()` (~line 2363), matching the existing
    `microphone=(self)`, with a comment explaining why (same-origin voice
    widget trap for the next video feature).
- Cherry-picked only these three hunks from PR #20061
  (`fix/20060-worker-palette-support-route`, commit `8932134f`). Did **not**
  take any of that commit's palette rewrite (it targeted the superseded memo
  seven, not the canon seven in `scripts/ui-audit/audit.py`).
- `SUPPORT_HTML` uses only canon colours: `#ffffff #222222 #002A54 #CCCCCC
  #0073CF` (a subset of the canon seven — `#E8F4FC` and `#005DAA` weren't
  needed by this template), copied from the current, already-canon
  `DISCLAIMER_HTML`/`SECURITY_HTML` `:root` block rather than PR #20061's
  memo `:root` block (`#005EB8 #1A1A1A #0A2540 #D7E3F1`).
- No migrations, no schema changes, no other files touched.

## SSOT bindings used
None written. Read-only check: `${SUPABASE_URL}/rest/v1/support_tickets?select=*`
via service-role key returned HTTP 200 with 0 rows — the table exists in
production (OpenAPI description: *"BidDeed.AI public support tickets
(biddeed.ai/support). RLS on, no policies: service-role API only. Ticket
numbers BD-YYYYMMDD-XXXX."*), but no committed code in this repo or in
`biddeed-web` (checked both repos' full git trees, tests, and code search)
builds or serves a form against it. The issue's mention of a ticket
smoke-tested as `BD-20260906-D631` is consistent with that table's existence,
but the frontend that would have produced it isn't in either repo's `main`
branch or any open branch I could find. Per the issue's own instructions
("if it lives in biddeed-web, proxy there; else check before writing"), since
neither exception applied, I shipped the static page — same call PR #20061
made. Flagging this as an open item below rather than guessing at scope.

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Cherry-pick /support + /contact + camera=(self), no palette | VERIFIED | `git diff` below; `grep -oE "#[0-9A-Fa-f]{6}" ` over the new `SUPPORT_HTML` block returns only `#002A54 #0073CF #222222 #CCCCCC #ffffff` |
| 2 | `python3 scripts/ui-audit/apply_worker_palette.py src/worker.js` prints "already applied" | VERIFIED | ran it post-edit: `already applied - nothing to do` |
| 3 | `node --check src/worker.js` | VERIFIED | exit 0, no output |
| 4 | RENDER/PALETTE/CONTRAST ✅ on /support /contact, both viewports | VERIFIED | see table below, from `python3 scripts/ui-audit/audit.py --base http://localhost:8787 --routes '["/support","/contact"]'` against a local `wrangler dev` |
| 5 | Support form check before writing | VERIFIED (negative result — see SSOT section) | grepped `src/worker.js` for `support_tickets`/`/admin/support` (0 hits); `gh api repos/breverdbidder/biddeed-web/git/trees/main?recursive=true` has no `support`/`contact` path; confirmed via `gh api repos/breverdbidder/biddeed-web/branches`/refs no support branch exists either |

### UI-audit harness result (localhost, wrangler dev, both viewports)

| Route | VP | HTTP | RENDER | PALETTE | CONTRAST | off-palette |
|---|---|---|---|---|---|---|
| /support | 1440 | 200 | ✅ | ✅ | ✅ | 0 |
| /contact | 1440 | 200 | ✅ | ✅ | ✅ | 0 |
| /support | 390 | 200 | ✅ | ✅ | ✅ | 0 |
| /contact | 390 | 200 | ✅ | ✅ | ✅ | 0 |

LAYOUT/TYPE gates show ❌ on both new routes (small tap targets / tiny text
from the shared `bd-shell` nav chrome) — but the identical harness run
against already-shipped `/security` and `/disclaimer` on the same local
server shows the same LAYOUT ❌ (both viewports) and TYPE ❌ (mobile only)
failures, confirming this is a pre-existing shared-shell issue, not a
regression introduced by this change. Not in scope for #20072 (route/template
+ camera policy only).

## Negative test result
- Checked `git show 8932134f -- src/worker.js` for every hunk touching colour
  literals and confirmed none of them were carried over — only the three
  route/header hunks were cherry-picked.
- Confirmed no other file changed: `git diff --stat` shows `src/worker.js`
  only.
- Confirmed `/support` and `/contact` were 404 before this change (route
  didn't exist) and are 200 after, locally.

## Deviations from intent
None — did exactly the three hunks the issue asked for, skipped the palette
hunk as instructed, and used the current canon colours (not memo) for the new
template as instructed.

## Open items / handoff
- The real support-ticket form (`support_tickets` table, ticket format
  `BD-YYYYMMDD-XXXX`, described in production Supabase's OpenAPI schema as
  "BidDeed.AI public support tickets (biddeed.ai/support)") has no committed
  frontend in this repo or `biddeed-web` as of this session. If the ticket
  `BD-20260906-D631` mentioned in the issue is real, that code either lives
  on an uncommitted/unpushed branch or was applied directly without a git
  record — worth Ariel confirming before the next iteration on `/support`
  replaces this static page with a real form.
- LAYOUT/TYPE gate failures on the shared `bd-shell` chrome (small tap
  targets, tiny mobile text) are pre-existing across every static page using
  `withPublicShell` — out of scope here, flagged for a separate issue.
