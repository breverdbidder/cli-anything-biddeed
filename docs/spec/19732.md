# 19732 — Architect triage: blocked issue #19723 (lafayette/suwannee/liberty certification)

## DoD
```sql
SELECT EXISTS (
  SELECT 1 FROM public.gold_standard_certifications
  WHERE county_slug = ANY('{lafayette,suwannee,liberty}'::text[]) AND certified
)
```
Status at session end: **FALSE** (verified live, see evidence below). Not fixable within
autonomous authority this session — see root causes.

## What was done
1. Read the full #19723 thread (24,971-char body, 1 comment — a GHA-only run notification,
   no prior diagnosis on that specific issue).
2. Live-verified `gold_standard_certifications` and `pencil_dod_evaluate_county` for all 3
   counties via Supabase PostgREST (direct psql confirmed unreachable — password auth fails,
   the documented, pre-existing constraint; PostgREST is the working fallback).
3. Searched `public.decision_log` for prior related triage — found 10+ prior architect
   sessions since 2026-08-30 independently reaching the same conclusions for these exact
   counties (ids 3041, 3039, 2970, 2936, 2902, 2901, 2867, 2799, 2766 among others).
4. Cross-checked open canon issue #19617 ("CANON DECISION NEEDED: Gold Standard letter C
   structurally caps 9+ counties") — still OPEN, 3+ days, zero decision from Ariel despite
   2 evidence-addendum comments already naming both lafayette and suwannee.
5. Live-reprobed libertyclerk.com tax-deeds and foreclosure-sales pages (fresh HTTP fetch
   this session, not reused from any prior log).
6. Per the triage protocol's "reproduce the failure with a direct probe" step, invoked
   `gold_standard_guard_diagnose_call` (a Gemini-Flash pre-dispatch diagnosis edge fn) for
   all 3 counties — all 3 failed with a live Gemini 404 (`gemini-2.0-flash` retired by
   Google). This was a genuinely new finding, not present in any prior decision_log entry.
7. Fixed and deployed the bug (see below), verified the fix via a direct Gemini API call
   (bypassing the edge fn's dispatch side effect deliberately).
8. Logged full diagnosis to `public.decision_log` (id=3074, decision_type=triage).
9. Posted the required BLOCKED comment on #19723: https://github.com/breverdbidder/cli-anything-biddeed/issues/19723#issuecomment-5511152411

## Files touched
- `supabase/functions/gold-standard-guard-diagnose/index.ts` — `GEMINI_MODEL` changed from
  the retired `gemini-2.0-flash` to `gemini-2.5-flash` (matching `claude-router/index.ts`'s
  already-working production usage of the same model/call shape), plus
  `generationConfig.thinkingConfig.thinkingBudget = 0` to prevent thinking tokens from
  consuming the 500-token output budget before the JSON answer (the exact failure mode the
  original 2.0 pin's code comment said it was avoiding). Deployed live via
  `supabase functions deploy gold-standard-guard-diagnose --project-ref mocerqjnksmhcjzxrewo`.

## SSOT tables/columns used
- `public.gold_standard_certifications` (county_slug, certified, consecutive_gold,
  consecutive_non_gold, revoked_at, revocation_reason) — read-only.
- `public.pencil_dod_evaluate_county(p_county)` RPC — read-only, per-letter A-J evaluation.
- `public.decision_log` — 1 new row inserted (id=3074).
- `public.multi_county_auctions` — not queried directly this session (row-level parity_status
  breakdown for lafayette/suwannee was already fresh as of yesterday's ids 3041/2936/2902;
  re-confirmed via the unchanged evaluator metric instead of re-running the same row query).

## DoD status per item
| County | Letters failing | Root cause | Fixable this session? |
|---|---|---|---|
| lafayette | C (75.0%, 3/4 matched_clean) | `CLERK_SSOT_CANCELLED` row (1 of 4) — canon question, issue #19617 open | No — requires Ariel's policy decision on #19617 |
| suwannee | C (80.0%, 28/35 matched_clean) | 7 `CLERK_SSOT_CANCELLED` rows of 35 — same canon question | No — same as above |
| liberty | A, B, F (fc=1 td=0; verified=0 closed_sold=0 both) | Zero current tax-deed/foreclosure listings, live-reconfirmed 2026-09-02 | No — genuine zero-activity data ceiling, no lever exists |

Additionally: even a hypothetical same-day fix to lafayette/suwannee C would **not** flip
`certified=true` today — `gold_standard_certify()` requires 2 consecutive daily 10/10
`gold_standard_loop()` runs, and both counties show `consecutive_gold=0` right now.

## Evidence (Honesty V3)
- **VERIFIED**: lafayette `pencil_dod_evaluate_county` — 9/10, C fail 75.0% (matched_clean=3/4). Live RPC call this session.
- **VERIFIED**: suwannee `pencil_dod_evaluate_county` — 9/10, C fail 80.0% (matched_clean=28/35). Live RPC call this session.
- **VERIFIED**: liberty `pencil_dod_evaluate_county` — 7/10, A/B/F fail (fc=1 td=0; verified=0 closed_sold=0 ×2). Live RPC call this session.
- **VERIFIED**: `gold_standard_certifications` — certified=false, consecutive_gold=0 for all 3 counties. Live PostgREST query this session.
- **VERIFIED**: issue #19617 still OPEN, 2 comments, no decision. `gh issue view 19617` this session.
- **VERIFIED**: libertyclerk.com/courts/tax-deeds/ and /courts/foreclosure-sales/ both report zero current listings. Live curl this session, 2026-09-02.
- **VERIFIED**: `gold-standard-guard-diagnose` edge fn was hardcoded to a retired Gemini model — live 404 reproduced via `gold_standard_guard_diagnose_call` RPC for all 3 counties this session.
- **VERIFIED**: fix deployed (`supabase functions deploy` succeeded, health check returns 200) and validated by a direct Gemini `generateContent` call using the new model + thinkingConfig, which returned HTTP 200 with real text output.
- **INFERRED**: the edge fn's guard-diagnose gate had been silently failing (fail-open, no diagnosis) for an unknown period fleet-wide, not just for these 3 counties — inferred from the model being fully retired server-side (not a transient error), but the exact onset date was not determined this session (would require reading `agent_ops_log` history for `task='guard-diagnose'`, not done — out of scope for this issue's DoD).
- **UNTESTED**: the fixed edge fn's full dispatch path (`launch_claude_code_session` side effect) was deliberately not re-exercised against lafayette/suwannee/liberty, to avoid triggering a redundant CC dispatch for an already-well-diagnosed policy-blocked case.

## Residual / next action
Human action required: decide issue #19617 (Option A/B/C on the `CLERK_SSOT_CANCELLED`
denominator question). That single decision would likely flip lafayette C, suwannee C, and
several other fleet counties' C letter to PASS on the next `gold_standard_loop()` run.
Liberty has no available lever until the county's clerk lists an actual foreclosure sale or
tax deed — no further automated session should re-attempt A/B/F without new activity
appearing on libertyclerk.com.
