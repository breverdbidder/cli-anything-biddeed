# Issue 19805 — GOLD STANDARD SHARD-3: indian_river, pinellas, highlands, citrus, wakulla

dispatch_id: `11e9f952-3a60-4a17-93a2-f4ee16495441` · chat_session: `architect-20260903T160000`
gold_standard_campaign row: `id=5659`

## What this session did

Orchestrated via `Workflow` (ULTRALOOP protocol, ultracode mode): one fix agent + one independent
adversarial verifier agent per county, pipelined so verification of an earlier county overlapped
with fixing of a later one. Every fix agent was given the exact live SQL body of
`public.pencil_dod_evaluate_county` (read directly from
`supabase/migrations/20260903h_pencil_dod_matched_any_redeemed_cancelled_gap.sql`, the newest
migration touching that function as of session start) plus row-level gap diagnostics I computed
myself beforehand by replicating the evaluator's per-letter SQL against live PostgREST queries.
Every verifier re-ran `pencil_dod_evaluate_county` fresh, spot-checked at least one written value
against its claimed public source, and checked for regressions on untouched letters before
returning SURVIVED/REFUTED. All 13 letter-level claims across the 5 counties SURVIVED verification
(no REFUTED claims) — see `gold_standard_ultraloop_audit` rows 20992–21004.

Direct psql (`SUPABASE_DB_PASSWORD`) was unreachable, as documented (decision_log 169/205/287) —
all reads/writes went through Supabase PostgREST + RPC, per the standing fallback pattern. The
Supabase project also returned transient Cloudflare 521s twice early in the session; both recovered
within ~15s and are noted only because they briefly looked like an auth failure.

## Per-county before/after (live `pencil_dod_evaluate_county`, VERIFIED)

### indian_river — 9/10 → **10/10** (I flipped PASS)
```
BEFORE: I FAIL card_complete=108 of 114 (94.7%)
AFTER:  I PASS card_complete=109 of 114 (95.6%)
```
Fix: `INSERT INTO public.parcel_zones` for parcel `31391900001598000026.0` (case `2023 CA 000637`,
171 Spring Valley Ave, Sebastian FL) — `jurisdiction_id=936` (City of Sebastian), `zone_code='RS-10'`,
source `sebastian_arcgis_zoning_20260903`. IRC's county-level zoning layer returned `ZONING=MUNI`
("contact City of Sebastian") for this point since it sits inside city limits; Sebastian's own
ArcGIS FeatureServer supplied the real code. Verifier re-fetched that FeatureServer at the exact
stored lat/lng and got an identical result. Migration:
`supabase/migrations/20260903i_pencil_dod_indian_river_i_sebastian_zoning.sql` (commit `28bbcedf`).

Letter G is now visible as FAIL (0.0%, `zone_standards` density/FAR/pk1000 completeness) — this is
**pre-existing**, not caused by this session (G is driven by `zone_standards`, an entirely separate
table from `parcel_zones`; a single-row `parcel_zones` insert cannot move it — confirmed by the
verifier). `indian_river` cannot auto-certify until G is fixed in a future session, even though it
is nominally 10/10 on this one evaluation (certification requires 2 consecutive daily 10/10 runs).

BLOCKED, not attempted further: 3 rows carry garbage `parcel_id` placeholders (`'MULTIPLE PARCELS'`,
`'Property Appraiser'`) from a bad prior scrape — resolving these requires re-deriving the real
parcel from the court docket, out of scope for this pass since 1 row already sufficed.

### pinellas — 9/10 → 9/10 (I improved, still short)
```
BEFORE: I FAIL card_complete=447 of 476 (93.9%)
AFTER:  I FAIL card_complete=451 of 476 (94.7%), need >=453
```
Fix: 4 real zone-code links written to `public.parcel_zones` via live GIS lookups (spot-checked
source: `egis.stpete.org` `ZONECLASS=NTM-1`, exact match to what was written). Migration:
`supabase/migrations/20260903i_gold_standard_pinellas_i_zone_linkage_4parcel.sql` (commit `531a585d`).
Remaining gap rows are blocked on gated/dead municipal endpoints (Largo token wall, Pinellas Park
DNS failure, a 500 from `PPC_Data`) plus a few rows needing full parcel-identity resolution from the
court docket (`'PERSONAL PROPERTY'` placeholder, 3 rows with null `parcel_id`).

### highlands — 8/10 → 8/10 (C, D improved, still short)
```
BEFORE: C FAIL matched_clean=368 of 404 (91.1%), D FAIL matched_any=368 of 404 (91.1%)
AFTER:  C FAIL matched_clean=381 of 404 (94.3%, need >=384, short by 3)
        D FAIL matched_any=382 of 404 (94.6%, need >=384, short by 2)
```
Root cause (verified, not guessed): 13 rows had genuine tier1-backed matches but carried
`parity_source='shard8_run6046_litmus_fallback:...'` instead of a `tier1%`-prefixed source, so the
evaluator's literal string match excluded them — a data-tagging bug, not an evaluator gap. Fixed by
re-stamping those 13 rows to their real `tier1_source_run_id`-backed provenance (verified: run ids
63337/80318/98477/187848 are real). Plus 1 `CLERK_SSOT_CANCELLED` normalization (case `25000905`).
This was a **pure data fix** — no `CREATE OR REPLACE FUNCTION`, confirmed by the verifier reading the
migration diff, so no fleet blast-radius check was required. Migration:
`supabase/migrations/20260903i_gold_standard_highlands_cd_litmus_tier1_vocab_stamp.sql` (commit
`617b9dab`).

18 rows remain blocked: independently confirmed absent from the live Highlands Clerk sale calendar
PDF (`webfiles.highlandsclerkfl.gov/ForeClosure/ClerkSaleCalendar.pdf`, re-fetched fresh by the
verifier, HTTP 200, contents checked) and `highlands.realforeclose.com` returns HTTP 403. 2 rows
(`HIGHLANDS-FC-2026-001/002`) carry a `synthetic_placeholder` parity_source from a prior session —
flagged as a data-quality issue, deliberately not touched.

### citrus — 6/10 → 9/10 (C, D, E, I all flipped PASS; **G regressed, disclosed**)
```
BEFORE: C FAIL 83.2% (203/244)  D FAIL 84.4% (206/244)  E FAIL 83.6% (204/244)  I FAIL 81.1% (198/244)
AFTER:  C PASS 98.4% (240/244)  D PASS 99.6% (243/244)  E PASS 98.8% (241/244)  I PASS 95.9% (234/244)
```
Root cause: ~40 tax-deed cases (`2026-0176TD`…`2026-0223TD`) had only `case_number` + status — a
scrape that captured the auction calendar but never ran enrichment. Fixed via a batch job resolving
real `parcel_id` + address + lat/lng + assessed/market value + `zone_code` through Citrus PA's
datalet, county GIS, and FL GIO fallback, plus a parity match against Citrus's tier1 litmus source.
Verifier independently re-derived one address's coordinates via the US Census geocoder and got a
bit-for-bit exact match to the stored lat/lng — strong evidence against fabrication. 3 rows remain
blocked (`2023 CA 000716 A`, `2024 CA 000179 A`, `2025 CA 000393 A`) — citruspa.org's datalet
requires a stateful ASPX session that a bare fetch can't complete. Migration + script:
`supabase/migrations/20260903j_gold_standard_citrus_cdei_td_batch_20261007.sql`,
`scripts/gold_standard_citrus_20260903_cdei_td_batch.py` (commit `4ca4dba5`).

**Disclosed regression**: G flipped PASS→FAIL (95.5%→91.1%). The new `parcel_zones` rows grew the
zoning-applicable denominator and exposed a pre-existing `zone_standards` density-catalog gap for
`jurisdiction_id=1327` that the smaller linked-parcel count had previously masked. This was
proactively documented in the migration's own "SIDE EFFECT NOTED, NOT FIXED" section, not
discovered by the verifier after the fact. **Open follow-up for a future citrus session**: backfill
`max_density_du_acre` (and check FAR/parking) for `jurisdiction_id=1327`'s districts.

### wakulla — 6/10 → 6/10 (honest non-progress on E/I/J; C ceiling reconfirmed)
```
E FAIL parcel_linked=48 of 52 (92.3%), need >=50 — UNCHANGED
I FAIL card_complete=48 of 52 (92.3%), need >=50 — UNCHANGED
J FAIL deal_complete=48 of 52 (92.3%), need >=50 — UNCHANGED
```
All 3 letters share the same 4 root-cause rows: `2026-TXD-124/125/126/127`, CANCELLED tax-deed
certs with zero scraped data (no parcel_id, address, geo, or value). Two real attempts were made
this session (a Wakulla property-appraiser probe and a LandmarkWeb case-number search) — both
confirmed blocked: `search.mywakullapa.com` resets the TLS connection on every attempt (independently
reconfirmed by the verifier with `curl -v`), and the LandmarkWeb probe did not turn up these 4
case/cert numbers. No data was written rather than guessed. Migration (comment-only, zero
INSERT/UPDATE, verified by the verifier grepping the diff):
`supabase/migrations/20260903i_gold_standard_wakulla_eij_landmarkweb_casenumber_ceiling_reconfirm.sql`
(commit `1874bbae`). J's existing generator (`pipeline_version='wakulla_j_generator_5cd42fe0_shapira_v14_real'`)
was confirmed already covering all 48 resolvable cases; it correctly produced zero `bid_decisions`
rows for the 4 blocked cases rather than fabricating them.

Letter C (`41/52`, 78.8%) was **not** touched — confirmed structurally capped by 11
`CLERK_SSOT_CANCELLED` rows under the same-day, explicitly-ratified evaluator design in
`supabase/migrations/20260903h_pencil_dod_matched_any_redeemed_cancelled_gap.sql` ("C is
intentionally left untouched — a redeemed/cancelled sale still can never be a genuine litmus
match"). Changing this would be a fleet-wide evaluator edit contradicting a same-day architectural
ruling — explicitly out of scope for this session.

## Verification protocol evidence (per issue mandate)

13 claims logged to `public.gold_standard_ultraloop_audit` (ids 20992–21004), one row per letter per
county attempted, each carrying live-requeried `refuter_evidence`. 12 SURVIVED, 1 (`citrus`/`G`)
logged `survived=false` — not because the *claim* (a disclosed regression) was false, but to make
the open regression visible in the false-positive ledger rather than silently dropped.

`public.gold_standard_campaign` row `id=5659` updated with `criteria_passed` (per-county A–J booleans
+ narrative notes), `criteria_total=10`, `exit_reason`, `session_end_at`. Full per-county before/after
JSON pasted above is the literal output of `pencil_dod_evaluate_county`, run independently by each
county's verifier agent, not by the fix agent that made the claim.

Per PARALLEL-FLEET RULES, `gold_standard_loop()` and `gold_standard_certify()` were **not** run this
session (other concurrent shard sessions were plausibly mid-flight — visible from the dozen
same-day `gold-standard-shard*` skills and commits from other dispatch_ids in `git log` above/below
this session's own commits). Per-county `pencil_dod_evaluate_county` was used instead, as instructed.

## Files changed (all committed + pushed directly to main, no PRs)

- `supabase/migrations/20260903i_pencil_dod_indian_river_i_sebastian_zoning.sql` (28bbcedf)
- `supabase/migrations/20260903i_gold_standard_pinellas_i_zone_linkage_4parcel.sql` (531a585d)
- `supabase/migrations/20260903i_gold_standard_highlands_cd_litmus_tier1_vocab_stamp.sql` (617b9dab)
- `supabase/migrations/20260903j_gold_standard_citrus_cdei_td_batch_20261007.sql` +
  `scripts/gold_standard_citrus_20260903_cdei_td_batch.py` (4ca4dba5)
- `supabase/migrations/20260903i_gold_standard_wakulla_eij_landmarkweb_casenumber_ceiling_reconfirm.sql`
  (1874bbae, comment-only / no-op — documents the blocked probe)
- This file.

## Open follow-ups for the next session touching these counties

1. **citrus G regression** — backfill `zone_standards` density (and check FAR/parking) for
   `jurisdiction_id=1327` districts; this is now the only letter blocking citrus.
2. **indian_river G** — needs the same `zone_standards` backfill class of work as other counties
   (density/FAR/pk1000 completeness); indian_river is otherwise 10/10.
3. **pinellas I** — 2 more complete rows needed (451→453); next-cheapest path is likely resolving
   the Largo token-gated endpoint or the 3 null-`parcel_id` rows via docket lookup.
4. **highlands C/D** — 3 (C) / 2 (D) more rows needed; the 18 remaining blocked rows look
   structurally stuck (absent from the clerk calendar, 403 on RealForeclose) — may need a different
   litmus source entirely, not just more of the same probe.
5. **wakulla E/I/J** — all capped on the same 4 CANCELLED TXD certs; property appraiser site is
   persistently TLS-broken. Worth trying the Wakulla Clerk's original tax-deed sale file (paper/PDF
   source) directly rather than the property appraiser search UI, or a public-records request path,
   in a future session.
