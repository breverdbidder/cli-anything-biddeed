# Spec — issue #20054 — Title Tiers STATEWIDE (C): 41 not_started counties, Landmark rollout, free custom portals, Kofile, Apify path

## Intent reference
No `docs/intent/20054.md` existed at dispatch time — the issue body is the only brief, per M6. Read first per the issue's own instruction: `title_tier_coverage`, `scripts/clerk_ssot/or_platform_map.json`, `scripts/or_adapters/landmark.py`, `docs/spec/20049.md`, `docs/spec/20050.md`.

## Precondition check (`db_restart_log`, trailing hour)
4 restarts in the trailing hour at report time (2026-09-06T14:57Z), matching the same baseline #20049/#20050 already observed (≥2 threshold, ongoing not new). Per that precondition, every harvest this session ran in small checkpointed batches (`--limit` ≤ 25, most far smaller) against `title_tier1_results`'s unique constraint — a mid-run reboot would lose at most the in-flight county's batch, never corrupt state.

## Credential/tooling reality check (STOP-and-log, not a HITL question)
`APIFY_API_KEY` is declared in this runner's env but was **empty at runtime** (`len=0`). The sanctioned vault accessors named in this repo's CLAUDE.md CREDENTIAL HANDLING section do not cover `apify_api_token`: `cli_anything_get_secret`'s allow-list is `cli_anything_shared_secret`/`everest_*_pat`/`cli_anything_*`; `get_vault_secret_gated` only ever returns `everest_gh_pat`. The only accessor that would return it, `get_vault_secret_mcp`, is explicitly flagged in that same section as an **open, ungated security finding** ("do not treat this function as safe until this grant is tightened") — declined to use it. Logged `BLOCKED` to `public.agent_ops_log` (dispatch_id `20054`, severity `blocker`) at session start. **Net effect: the entire Part 3 "route through Apify" instruction could not run this session.** Everything reported below for Cloudflare/Akamai-blocked counties was gathered via direct httpx/Playwright from this runner's own egress, honestly labeled as such — no proxy, no captcha-solving, no bypass attempted.

## What was built

### `scripts/or_adapters/landmark.py` — extended (Part 1)
Added 9 new `COUNTY_LANDMARK` entries (`st_johns`, `clay`, `escambia`, `wakulla`, `indian_river`, `levy`, `monroe`, `walton`, `charlotte`), each live-verified via a real Playwright session against the county's own `Home/Index` nav (probe script checked for the `LaunchDisclaimer('searchCriteriaCaseNumber')` onclick handler `case_lookup()` itself keys off — see `git log` for the exact diff). `lee`'s entry is unchanged in shape but its `has_case_number_search` stays `None` — live-verified this session that its block is **not** the issue's assumed HTTP/2 protocol bug: both HTTP/2 (Playwright default) and HTTP/1.1-forced (`curl --http1.1`) connections complete the TLS handshake against Akamai edge IPs (23.48.203.x) and then stall with no response at all — a network-layer bot-management block, same class as `martin`, not fixable client-side.

### `scripts/or_adapters/manatee.py` — new (Part 2)
Manatee County (MCCCC Public Records Hub), built first per the issue's own "smallest first" instruction. Plain ASP.NET MVC app with **no bot-management at all** — a plain `httpx` client (GET the search page for its anti-forgery token + session cookie, POST to `/OfficialRecords/Search/DoSearch`) works with zero Playwright. The one real engineering finding: this portal's Case Number field expects its **own short case-number form** (`2018CA006069`), not the UCN this repo stores in `multi_county_auctions.case_number` (`412018CA006069CAAXMA` / `2018CA006069AX`) — `_to_manatee_case_number()` strips the circuit-court prefix and division suffix via regex, live-verified against 12/12 real upcoming Manatee auctions this session (0 fetch failures). Results parse from a plain HTML `<table id="results">` (BeautifulSoup), not a JSON API — From/To are each an `<ol><li>` party-name list, joined the same way `landmark.py`'s multi-name fields already are.

### `scripts/or_adapters/benchmark.py` — untouched
No new finding this session; lane B's non-finding from #20050 stands.

### `title_tier_coverage` — every touched county updated with live evidence, no guesses
64 of 67 rows now carry a 2026-09-06 lane-C note (only the 3 already-live-from-lane-A AcclaimWeb rows — brevard/duval/highlands/santa_rosa — plus broward/st_lucie were left untouched, out of lane C's scope per the issue text). Every status change below is backed by a live fetch this session, not an inference.

## Part 1 results — Landmark rollout

| County | Case# search? | Result | Evidence |
|---|---|---|---|
| **indian_river** | Yes | **live** — 4/4 cases, 10 instruments, 0 failures | Corrects lane B's `county_custom` mislabel — real platform is root-mounted Landmark Web (`ori.indian-river.org`), found via the state's own myfloridacounty.com statewide OR directory |
| st_johns | Yes (works, different flow) | blocked | Search genuinely returns results (confirmed "LIS PENDENS" text for case CA26-0463) but this deployment renders via full-page HTML postback to `/Landmark/search/index`, not the AJAX DataTables JSON every other county in this adapter parses. Needs a distinct HTML-table parser — not built this session, disclosed not stubbed. 10 auctions waiting. |
| clay | Yes (nav exists) | blocked | Disclaimer accept did not reveal `#caseNumber` for any of 8 attempted cases (`Page.wait_for_selector` timeout every time) — a DOM/JS flow quirk, not a bot block (HTTP 200 throughout). Not resolved this session. |
| escambia | Yes (nav exists) | blocked | Same quirk as clay, all 14 attempted cases failed identically. Highest-volume unresolved Landmark county. Corrected `virtual_dir` to the real versioned path `LandmarkWeb1.4.6.134`. |
| palm_beach | Yes (unreliable) | blocked | Re-attempted per issue instruction — 0/12 this session (was 1/3 in lane B). `GetSearchResults` never fires; root cause still not identified. |
| levy / monroe / walton | No (verified False) | blocked | Reachable, real Landmark Web, but this clerk does not enable the Case Number Search nav tab. No owner-name source wired (same structural gap as martin/okeechobee). All 3 also have 0 genuinely-future upcoming auctions right now. |
| charlotte | Unknown | blocked | Both candidate URLs either 403'd (Cloudflare) or timed out under a real Chromium session — unlike bay/citrus/flagler/indian_river, this challenge did not clear. 0 auctions right now regardless. |
| wakulla | Yes (verified True) | not_started | Reachable, tab enabled, adapter config ready — 0 genuinely-future upcoming auctions right now, nothing to harvest. |
| lee | — | blocked | Confirmed **not** a protocol bug (see credential/tooling section above) — Akamai network-layer block, needs the (currently unavailable) Apify path. Highest-value blocked county, 198+ auctions. |
| martin | No + Akamai | blocked | Unchanged from lane B — two independent blockers, a proxy alone would not fix the missing Case Number tab either. |
| okeechobee | No (re-verified) | blocked | Re-confirmed live per issue instruction — still no Case Number tab, needs an owner-name source not built this session. |

## Part 2 results — free custom portals

| County | Result | Evidence |
|---|---|---|
| **manatee** | **live** — 12/12 cases, 37 instruments, 0 failures | `scripts/or_adapters/manatee.py`, see above |
| hernando, washington, dixie (kofile family) | blocked | Portals are reachable, real ASP.NET WebForms apps confirmed matching the issue's premise (no Case Number field — party-name only). Live-queried `multi_county_auctions` for a usable defendant/owner name to search with: hernando 0/19, washington 0/50, dixie 0/13 have `owner_name` (dixie has a `plaintiff` — the foreclosing lender, not useful for a defendant lien sweep — on 4/13). No WebForms scraper built: there is nothing to search with. Disclosed gap, not a stub, same class as martin/okeechobee's missing owner-name source. |
| osceola, collier, seminole, marion, hillsborough, pasco, sarasota, okaloosa, union | not_started (unchanged or lightly corrected) | Time-boxed — fingerprinted via the statewide directory (see Part 3) but no adapter built this session. Reachable/platform notes updated where new evidence surfaced (see coverage notes per county). |

## Part 3 results — the 21 Cloudflare/WAF-blocked counties named in the issue

**Apify path did not run this session (credential-blocked, see above).** Everything below is a direct re-fingerprint from this runner's own egress, no proxy.

The most valuable finding of this whole session: `https://www.myfloridacounty.com/official_records/index.html`'s own **county-selector dropdown is a live, authoritative, statewide directory of every county's real current Official Records search URL**, maintained by the state itself. Fetching it once resolved or corrected the platform for well over a dozen counties that three prior sessions (lane A, lane B, and this session's own earlier guesses) had marked `unknown`:

- **Corrected mislabels found this session:** `leon` (was myfloridacounty — actually a separate platform at `cvweb.leonclerk.com`, not fingerprinted further), `baker` (was DuProcess — actually Landmark Web, still Cloudflare-blocked), `alachua` (was "login page with captcha, blocked" against the wrong domain — the real URL, `alachuacounty.us/.../Disclaimer.aspx`, is reachable with no login/captcha), `orange` (was "unknown, parked domain" — real URL is a reachable Tyler Technologies disclaimer flow), `lake` (was "unknown, Cloudflare-gated" — actually a reachable AcclaimWeb deployment, no block encountered), `jefferson`/`hamilton` (were mislabeled GrantStreet/myfloridacounty — neither actually participates in myfloridacounty's platform; both are the counties' own WordPress sites with an unlocated search form).
- **Confirmed still Cloudflare/WAF-blocked via direct fetch (no change):** `bradford`, `columbia`, `pinellas`, `taylor`, `charlotte` (Part 1), `martin`/`lee` (Akamai, Part 1).
- **`polk`**: reachable on retry (one transient connection-reset), a BrowserView AngularJS SPA — same vendor family this session identified for Osceola/Marion.
- **`miami_dade`**: a second entry point (`StandardSearch.aspx`) returns a plain "Redirecting..." disclaimer page, distinct from the Turnstile-gated React SPA lane B found at the domain root — not confirmed whether it avoids the Turnstile gate or redirects back into it; left `blocked`, flagged as the top re-verification target, not downgraded without proof.
- **`volusia`**: real URL confirmed reachable (`app02.clerk.org/or_m/`), no recognized platform signature identified yet.

## Part 4 results — myfloridacounty family (index-only party-name search)

**Live-tested the actual search submission, not just the landing page — this is the load-bearing finding of Part 4.** `myfloridacounty.com/orisearch/<co_no>`'s search form GET loads cleanly with no bot-management (plain httpx). But **every POST to its search action returns a Cloudflare Turnstile "please verify you are human" page instead of results** — confirmed live against 20 counties' own co_no this session (`calhoun`, `gilchrist`, `nassau`, `dixie`, `washington`, `hardee`, `hendry`, `jackson`, `suwannee`, `bradford`, `putnam`, `sumter`, `desoto`, `franklin`, `gulf`, `lafayette`, `liberty`, `madison` — 18 confirmed Turnstile-gated; `glades`/`holmes` returned HTTP 500 "Error" on 2 separate attempts each, likely a co_no mapping mismatch, not the same block, left as a distinct residual). Per the issue's own instruction ("if a Turnstile/captcha still gates the search form, record blocked: captcha and stop"), all 18 are recorded `blocked`, no captcha solved or bypassed, no Apify attempted (credential-blocked). This closes out every county named in the issue's Part 4 list (`gilchrist`, `hardee`, `hendry`, `nassau`, `suwannee`, `calhoun`, `jackson` — confirmed blocked; `leon` corrected out of this family entirely, see Part 3).

## Statewide picture (67 counties, live query at session end, 2026-09-06T14:57Z)

| tier1_status | count at #20050's end | count now | delta |
|---|---|---|---|
| live | 7 | **9** | +2 (indian_river, manatee) |
| blocked | 19 | **40** | +21 (real evidence replacing `not_started` guesses — not new failures, resolved unknowns) |
| not_started | 41 | **18** | −23 |

**Total real recorded instruments in `title_tier1_results` this session's additions:** indian_river +10, manatee +37 = **+47 new real instruments** (LIS PENDENS/JUDGMENT/ORDER/RELEASE, with real book/page/instrument#/case# citations, per M3/UPL-seam). Table-wide total is now 127 rows across 9 counties (note: `title_tier_coverage.instruments_total` for the 4 pre-existing lane-A AcclaimWeb counties — brevard/duval/highlands/santa_rosa — is stale versus the live `title_tier1_results` count for those rows; e.g. coverage says highlands=0 but the table has 5. Not fixed this session — those rows belong to lane A/#20049's scope, not lane C's, per M5).

Apify actor runs this session: **0** (credential-blocked). Cost: **$0**. Restart count during the run: **4** (stable baseline, unchanged from session start to end).

### SQL VERIFICATION
```sql
select tier1_status, count(*) from public.title_tier_coverage group by tier1_status;
-- live=9, blocked=40, not_started=18

select county, or_platform, cases_with_tier1, instruments_total
from public.title_tier_coverage where tier1_status='live' order by county;
-- bay|LandmarkWeb|8|20, brevard|AcclaimWeb|9|10, citrus|LandmarkWeb|6|7,
-- duval|AcclaimWeb|12|5, flagler|LandmarkWeb|6|12, highlands|AcclaimWeb|5|0,
-- indian_river|LandmarkWeb|4|10, manatee|county_custom:MCCCC|12|37, santa_rosa|AcclaimWeb|5|0

select county, count(*) from public.title_tier1_results
where county in ('indian_river','manatee') group by county;
-- indian_river=10, manatee=37

select count(*) from public.title_tier1_results;
-- 127
```
Timestamp: 2026-09-06T14:57Z (UTC), read via PostgREST against the live Supabase project (direct psql blocked per the standing, pre-documented `SUPABASE_DB_PASSWORD` constraint — used PostgREST for all reads/writes, the established working pattern).

## Deviations log (Loop Discipline)
1. Did not attempt the Apify browser path at all — logged `BLOCKED` to `agent_ops_log` at session start rather than discovering it mid-task, since the credential gap was verifiable in under a minute (empty env var + allow-list check) and re-checking later would not have changed the answer.
2. Spent meaningfully more time than planned re-fingerprinting "blocked/unknown" counties directly (no proxy) after discovering myfloridacounty.com's own statewide directory page — judged this in-scope because it directly serves the issue's own Part 3/4 goal ("re-fingerprint each one, update or_platform_map.json with evidence") even though the originally-planned mechanism (Apify) was unavailable; the directory made a large amount of that re-fingerprinting free (no proxy needed) and produced several outright platform corrections (leon, baker, alachua, orange, lake, jefferson, hamilton) that a proxy alone would not have surfaced.
3. Did not build the kofile-family WebForms scraper (hernando/washington/dixie) — verified first that there is no usable defendant/owner name to search with for 19/19 and 50/50 of the two largest counties in that family; building a nontrivial ASP.NET ViewState scraper for near-zero achievable searches was judged not worth the time this session. Disclosed, not stubbed.
4. Did not build adapters for the other reachable custom portals found this session (osceola, collier, seminole, marion, hillsborough, pasco, sarasota, okaloosa, union, volusia, polk, orange, lake, alachua) despite several looking tractable (lake is AcclaimWeb and likely reuses existing harvest code; orange/okaloosa share a Tyler DOCSEARCH pattern) — time-boxed the same way lane B time-boxed its own custom-portal investigation, in favor of shipping the two adapters that actually went live (indian_river via the existing Landmark adapter, manatee as new).
5. `or_platform_map.json` (the committed JSON file) was **not** updated this session — all platform corrections and evidence went into `title_tier_coverage.notes` instead, which is the table this issue and #20049/#20050 both treat as the live source of truth. Flagged as a residual: the committed JSON file is now stale relative to the DB for at least 7 counties (leon, baker, alachua, orange, lake, jefferson, hamilton).

## Residual / open items for the next dispatch, in priority order
1. **Lee (198+ auctions)** and **Martin** — both need the Apify residential-proxy path once `apify_api_token` is actually wired into the runner env (or a sanctioned vault accessor is extended to cover it). This is the single highest-leverage unblock available — proxy access, not more engineering, is the bottleneck.
2. **Lake** — confirmed live-reachable AcclaimWeb, likely works with a small addition to `pre_auction_lien_harvest.py`'s `COUNTY_ACCLAIM`, not `landmark.py`. Fast follow.
3. **Clay / Escambia** — Case Number Search confirmed to exist and load, but the disclaimer→`#caseNumber` DOM flow doesn't complete for these two specific deployments. Needs step-by-step DOM tracing (this session got as far as confirming the modal doesn't dismiss, not why).
4. **St. Johns** — needs a second HTML-table parser variant for Landmark Web's full-page-postback rendering style (distinct from the AJAX DataTables style `landmark.py` already handles).
5. **Miami-Dade** — re-verify whether `StandardSearch.aspx` is a genuine Turnstile-free entry point or just redirects into the same gated SPA.
6. **Escambia/Putnam/Polk** (from #20050) remain the 3 highest-volume fully-unresolved counties; this session resolved Escambia's platform+partial-block but not Putnam or Polk beyond a fingerprint.
7. **`or_platform_map.json`** needs a sync pass against this session's `title_tier_coverage` corrections (see deviation #5).
8. `ship_status` for `lien_search`/`title_search` remains `'blocked'` — unchanged, out of scope per the issue's own "Out of scope" section.

## Session 2 (re-dispatch, same day) — RLS gate re-check + Lake residual item #2

Re-dispatched after the GHA run for the commit above (`fa2733af`) posted `conclusion=failure, intent=missing, rls_gate=failed` (comment at 2026-09-06T15:00:06Z, run `34038488838`). `intent=missing` is expected/informational (no `docs/intent/20054.md`, confirmed at top of this file). Re-ran `rls_gate_check()` live at this session's start: `passed=true, new_violations=0, total_violations=303` (baseline 307, i.e. 4 fewer than baseline, not more) — the prior failure did not reproduce. Not chasing further: the gate is deterministic and pointed at live DB state, so a clean re-check is real evidence the violation (or a transient gate-unreachable condition, per the workflow's own comment) is resolved, not something papered over.

Apify path re-checked, still blocked: `APIFY_API_KEY` empty at runtime again this session, same allow-list gap as before. Lee/Martin/Part-3/Part-4 unchanged.

Picked up **residual item #2 (Lake)** since it was flagged the fastest tractable follow-up. Two real, live-verified fixes were needed beyond "just add the county":

1. **`case_number_filter`**: every existing `COUNTY_ACCLAIM` county works with `CaseNumberFilter=0`; Lake's own form default is `1` and `0` silently returns `{"data":[],"total":0}` for a case with real recorded documents (confirmed side-by-side: same case number, same session, only the filter value changed). Added `case_number_filter` as a new per-county `AcclaimSession` constructor param (default `"0"`, preserving every existing county's behavior unchanged) rather than hardcoding a county check inline.
2. **Date/field shape**: Lake's raw `case_lookup()` JSON is byte-for-byte the same non-standard shape Brevard already had a fix for (`RecordDate` as `/Date(epoch_ms)/`, lowercase `Bookpage` key) — every `title_tier1_results` insert 400'd until `normalize_brevard_docs()` was applied to `lake` too (extended the existing `if county == "brevard"` check to `if county in ("brevard", "lake")`).

Live run after both fixes (`pre_auction_lien_harvest.py --county lake --lookahead-days 120 --limit 5`, only 2 genuinely-future Lake auctions exist right now so `--limit` was never binding): 2/2 cases harvested, 4/4 real instruments written (CP/JUD/LP, real book/page/instrument#/dates, e.g. case `2024CA000186` → clerk's own docket `352024CA000186AXXX01`, LP recorded 2024-01-31 book/page 6278/626 instrument 2024011866), 0 insert failures. Deleted 2 `NO_DOCUMENTS_FOUND` marker rows this session's own earlier (pre-fix) attempt had written for these same 2 cases — they were now factually wrong (claimed 0 documents; 3 and 1 real documents respectively actually exist) and this session created them minutes earlier, so removing them is correcting this session's own error, not touching pre-existing production data. `title_tier_coverage.lake` updated: `tier1_status` `not_started` → `live`, `cases_with_tier1`=2, `instruments_total`=4. No `owner_name` on either Lake MCA row, so the broader `name_lookup()` sweep did not run for either case — disclosed, not stubbed.

### SQL VERIFICATION
```sql
select tier1_status, count(*) from public.title_tier_coverage group by tier1_status;
-- live=10, blocked=40, not_started=17  (was live=9/blocked=40/not_started=18 at end of session 1)

select county, or_platform, cases_with_tier1, instruments_total, tier1_status
from public.title_tier_coverage where county='lake';
-- lake|AcclaimWeb|2|4|live

select case_number, instrument_type, recording_date, book_page, instrument_number, direct_name, indirect_name
from public.title_tier1_results where county='lake' order by case_number, recording_date;
-- 2024CA000186|CP |2024-03-19|6301/1331|2024031443|PENNYMAC LOAN SERVICES LLC|NATAL SPOUSE
-- 2024CA000186|LP |2024-01-31|6278/626 |2024011866|PENNYMAC LOAN SERVICES LLC|NATAL NATALIA
-- 2024CA000186|JUD|2026-04-14|6719/140 |2026044915|PENNYMAC LOAN SERVICES LLC|NATAL NATALIA
-- 2025CA002898|LP |2025-12-23|6654/1515|2025156962|FREEDOM MORTGAGE CORPORATION|CALDERON LUIS E

select count(*) from public.title_tier1_results;
-- 131  (was 127 at end of session 1)
```
Timestamp: 2026-09-06T16:06Z (UTC), read via PostgREST against the live Supabase project (`SUPABASE_DB_PASSWORD` direct-psql path still blocked per the standing constraint — PostgREST used throughout, matching session 1 and the established pattern).

Files changed this session: `scripts/pre_auction_lien_harvest.py` (`lake` entry in `COUNTY_ACCLAIM`, `case_number_filter` param threaded through `AcclaimSession`, `normalize_brevard_docs()` applied to `lake`). `agent_ops_log` row written (`dispatch_id=20054`, `task=TITLE_TIER_LANE_C_REDISPATCH`, `status=PARTIAL`).

### Updated residual list (supersedes item 2 above)
Item 2 (Lake) is now **done**, not a residual. Items 1, 3, 4, 5, 6, 7, 8 above are unchanged — none were in this session's scope (time-boxed to the one fast-follow item plus the RLS-gate re-check). Apify (item 1, Lee/Martin) remains the single highest-leverage unblock and is still purely a credential/access problem, not an engineering one.
