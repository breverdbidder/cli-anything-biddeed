# Spec — Issue #20052 (GTM-6): reel short links → deal page + promote kokoro EN drafts to launch finals

Status: **SHIPPED** (both work items complete and DB/HTTP-verified live)

## Binding decisions (Ariel, chat 2026-09-06)
1. Kokoro English audio is the launch-week voice — no ElevenLabs spend, no OpenRouter calls.
2. `biddeed.ai/r/<code>` end-card link must land on the deal page (`/deal/<county>/<case>`), never the `/reels/<code>` interstitial.

## What this session found already done (from run 34037370369 / commit `7f3ec911`)
- `public.resolve_reel_link()` was already rewritten (migration `20260906g_resolve_reel_link_deal_page_20052.sql`) to return the parent reel's `landing_url` for variant-minted codes when `page_http_status=200`, falling back to `/reels/:code` only when the deal page isn't verified live.
- `agents/reel_studio/analyst.py::mint_variant_short_link()` was already changed to default new `reel_links.target` rows to the parent reel's `landing_url`.
- 4 of the 20 in-scope EN kokoro drafts (reel `659a0c65…`, variants A–D) had already been promoted to final via `agents/reel_studio/bolt32_bridge.py promote-kokoro-final`.
- Migration `20260906h_kokoro_final_tts_model_20052.sql` had already relaxed `reel_variants_draft_tts_model_check` to permit a final row with `tts_model='kokoro'`.

This session re-verified all of the above live and finished the remaining work.

## Work item A — short links resolve to the deal page

**Files touched:** none (the Worker's `/r/:code` handler already reads `resolve_reel_link()`'s `target` field unconditionally — no hardcoded `/reels/` path exists in `src/worker.js`; the fix lives entirely in the RPC per the migration above).

Evidence (curl, 2026-09-06, this session):

| short_code | 302 Location | deal page HTTP |
|---|---|---|
| F5oeMD | `https://biddeed.ai/deal/polk/2024ca000341000000?utm_source=reel_variant&utm_medium=short_link&utm_campaign=A&a=shock_number` | 200 |
| J0Re6e | `https://biddeed.ai/deal/polk/2024ca000341000000?...&utm_campaign=C&a=hidden_value_reveal` | 200 |
| 2g6PUS | `https://biddeed.ai/deal/polk/2024ca000341000000?...&utm_campaign=B&a=underdog_bidder` | 200 |
| PQOS1C | `https://biddeed.ai/deal/polk/2024ca000341000000?...&utm_campaign=D&a=red_flag_warning` | 200 |
| O7EPH7 | `https://biddeed.ai/deal/martin/25001204caaxmx?...&utm_campaign=D&a=underdog_bidder` | 200 |

All 5 UTM/`a=` params preserved. VERIFIED.

`winnerdata.reel_links.target` distribution (live query, this session):
- deal page (`/deal/…`): **92**
- interstitial (`/reels/…`, no live deal page for that reel): **15**
- other (`/auctions`, out of reel-link scope): **4**
- total: 111

Click-counter test: `winnerdata.reel_variant_metrics` row for `F5oeMD` (day=2026-09-06, platform=short_link) `clicks` 14 → 15 after one `GET /r/F5oeMD`. VERIFIED.

Pipeline default: confirmed `mint_variant_short_link()` sets `target_url = reel.get("landing_url") or f"{LANDING_BASE}/reels/{code}"` — every new variant-minted short link defaults to the deal page. VERIFIED (code read, `agents/reel_studio/analyst.py:68`).

## Work item B — promote kokoro EN drafts to finals

Scope: `winnerdata.reel_variants` where `lang='en' AND qa_pass=true AND tts_model='kokoro' AND is_draft=true`.

- 4 already final at session start (reel `659a0c65-d1fc-4cd0-98b5-9e0f9d4c4f4b`, variants A–D).
- **16 promoted this session** via `agents/reel_studio/bolt32_bridge.py promote-kokoro-final --variant-id <id>` (reuses the existing draft kokoro audio, re-assembles video with `is_draft=False` — strips the DRAFT AUDIO watermark, writes to `.../variant_{key}_en/reel_bolt32.mp4`, draft file untouched).
- Installed `ffmpeg` (was missing from the runner image; `apt-get install -y ffmpeg`, no infra spend, tool only).
- 2 transient failures hit and cleared on retry (Supabase Storage `DatabaseTimeout` HTTP 544 on one upload, Storage `429 Too Many Requests` fetching one draft audio file) — both are the documented "instance reboots ~4/hr" class of failure; retried with backoff per the continuation brief, both succeeded on retry.
- DB confirms **20/20** `is_draft=false`, `render_mode='final'`, `pending_final_voice=false`, `tts_model='kokoro'` (query: `select count(*) filter (where is_draft=false) from winnerdata.reel_variants where lang='en' and qa_pass=true and tts_model='kokoro'` → 20).

### Per-variant table (all 20 finals)

| variant_id (short) | key | short_code | county | case_number | deal page target | HTTP | qa_pass | render_mode | tts_model |
|---|---|---|---|---|---|---|---|---|---|
| `58c305c6…` | A | `F5oeMD` | polk | 2024CA000341000000 | /deal/polk/2024ca000341000000 | 200 | VERIFIED true | final | kokoro |
| `bd896815…` | B | `2g6PUS` | polk | 2024CA000341000000 | /deal/polk/2024ca000341000000 | 200 | VERIFIED true | final | kokoro |
| `be613ef4…` | C | `J0Re6e` | polk | 2024CA000341000000 | /deal/polk/2024ca000341000000 | 200 | VERIFIED true | final | kokoro |
| `150f5096…` | D | `PQOS1C` | polk | 2024CA000341000000 | /deal/polk/2024ca000341000000 | 200 | VERIFIED true | final | kokoro |
| `449c8865…` | A | `sJU7ku` | escambia | 2025 CA 001460 | /deal/escambia/2025-ca-001460 | 200 | VERIFIED true | final | kokoro |
| `798429f1…` | B | `VKJOmF` | escambia | 2025 CA 001460 | /deal/escambia/2025-ca-001460 | 200 | VERIFIED true | final | kokoro |
| `0c4f1abb…` | C | `uE531t` | escambia | 2025 CA 001460 | /deal/escambia/2025-ca-001460 | 200 | VERIFIED true | final | kokoro |
| `aa70e120…` | D | `DGIj4T` | escambia | 2025 CA 001460 | /deal/escambia/2025-ca-001460 | 200 | VERIFIED true | final | kokoro |
| `256ebae8…` | A | `wvs4hW` | lee | 2026000141 | /deal/lee/2026000141 | 200 | VERIFIED true | final | kokoro |
| `7b9e8e7a…` | B | `a3Q9N2` | lee | 2026000141 | /deal/lee/2026000141 | 200 | VERIFIED true | final | kokoro |
| `5ca1aa1f…` | C | `ndaBDb` | lee | 2026000141 | /deal/lee/2026000141 | 200 | VERIFIED true | final | kokoro |
| `28941d3e…` | D | `1BrNFC` | lee | 2026000141 | /deal/lee/2026000141 | 200 | VERIFIED true | final | kokoro |
| `d6b2f711…` | A | `z22Uu7` | martin | 25001204CAAXMX | /deal/martin/25001204caaxmx | 200 | VERIFIED true | final | kokoro |
| `4377d421…` | B | `zs0Qo2` | martin | 25001204CAAXMX | /deal/martin/25001204caaxmx | 200 | VERIFIED true | final | kokoro |
| `7be4a6cf…` | C | `5oHHlR` | martin | 25001204CAAXMX | /deal/martin/25001204caaxmx | 200 | VERIFIED true | final | kokoro |
| `7cd891e4…` | D | `O7EPH7` | martin | 25001204CAAXMX | /deal/martin/25001204caaxmx | 200 | VERIFIED true | final | kokoro |
| `df986457…` | A | `Hvj4tq` | broward | CACE-24-008115 | /deal/broward/cace-24-008115 | 200 | VERIFIED true | final | kokoro |
| `b9797d13…` | B | `yzuC3t` | broward | CACE-24-008115 | /deal/broward/cace-24-008115 | 200 | VERIFIED true | final | kokoro |
| `ec36fdb9…` | C | `XKyMyn` | broward | CACE-24-008115 | /deal/broward/cace-24-008115 | 200 | VERIFIED true | final | kokoro |
| `c1d8b586…` | D | `dzW2Uj` | broward | CACE-24-008115 | /deal/broward/cace-24-008115 | 200 | VERIFIED true | final | kokoro |

Each row's `HTTP` and `deal page target` column above is a live `GET /r/<short_code>` → follow the 302 Location (querystring stripped) → live `GET` on the target, all done signed-out, all this session (M9 check).

### ffprobe (3 samples, VERIFIED)
- `.../2025_CA_001460/variant_C_en/reel_bolt32.mp4` — h264 1080×1920, aac audio, duration 32.000000s
- `.../2024CA000341000000/variant_D_en/reel_bolt32.mp4` — h264 1080×1920, aac audio, duration 32.000000s
- `.../2026000141/variant_A_en/reel_bolt32.mp4` — h264 1080×1920, aac audio, duration 32.000000s

### Director/QA re-run
Ran `director_qa.review_variant()` (the same check function `review-reel` uses) against each of the 20 EN finals, computing sibling-diversity against the reel's distinct `variant_key` set exactly as `review_reel()` does, but persisting `qa_scores`/`qa_pass` **only** to the 20 target rows — the non-EN sibling rows (es/pt-BR translations sharing the same `reel_id`) were read for the diversity comparison but never written, per the continuation brief's "do not touch non-EN variants." Result: **20/20 qa_pass=true**. DB confirms (`select count(*) where lang='en' and tts_model='kokoro' and is_draft=false and qa_pass=true` → 20).

### `youtube_publish_queue` mirror (ariel_decision dropped)
Reproduced the view's join/filter logic (`rv.qa_pass=true AND br.page_http_status=200 AND no active youtube_uploads row`) without the `ariel_decision='approved'` predicate and without the `limit 6`, restricted to the 20 target variant_ids: **20/20** clear every gate. `ariel_decision` is `null` for all 20 (LMS approval pending — M8 gate, untouched by this session).

## Not touched (in scope guard)
- No ES/HE variants rendered or modified.
- No writes to `reel_variant_review` or `spi_gates`.
- No ElevenLabs, OpenRouter, or Anthropic API calls (kokoro reuses the existing draft audio verbatim — no re-synthesis).
- No publish step (M8) — all 20 sit in `youtube_publish_queue` pending only `ariel_decision`.
- No schema changes beyond the two migrations already committed in the prior session (`20260906g`, `20260906h`) — this session made no DDL changes.

## Evidence log files (this session, ephemeral — not committed)
- `/tmp/promote_results{2..7}.log` — bolt32_bridge promote-kokoro-final stdout/stderr per variant
- `/tmp/link_verify.log` — curl verification of all 20 short codes → deal page 302 → 200
- `/tmp/qa_run_result.json` — director_qa re-run results per variant

## Tags
VERIFIED — every claim above backed by a live curl, ffprobe, or Management-API SQL query run in this session. No claim in this document is INFERRED or ASSUMED.
