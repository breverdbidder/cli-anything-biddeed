# Spec — issue #19742 — Gold Standard shard-5: st_johns, miami_dade, seminole (dispatch 544138fb-d880-4e17-88e5-a2a0abfa6a11)

## Intent reference
None at docs/intent/19742.md — issue body was the only brief (per M6).

## What was built
No repo files were changed (no scrapers/schema shipped this session). All work was live DML against
existing Supabase tables via PostgREST/RPC (direct psql confirmed unreachable — known constraint,
re-verified, not re-diagnosed). Work was orchestrated via one Workflow run (`wf_b640d631-bab`, ultracode
opt-in): 3 county diagnose+fix agents → 3 independent adversarial-verify agents, `pipeline()`'d so each
county's verify ran as soon as its fix finished. 202 real tool calls across 6 agents, ~539K tokens.

Live writes made (all real, adversarially confirmed against independent live sources — see DoD table):
- `multi_county_auctions`: 1 row updated for st_johns (case CA25-0645: real `parcel_id`, `parity_status`,
  `parity_source` from St Johns County ArcGIS Parcel MapServer).
- `multi_county_auctions`: 21 rows updated for miami_dade (10 `geo_source='miamidade_gis_parcel_centroid'`
  lat/lng backfills from live Miami-Dade ArcGIS parcel polygons; 11 `assessed_value_source='miamidade_pa_api_2026'`
  backfills from the live Miami-Dade Property Appraiser API).
- `bid_decisions`: 4 rows inserted for seminole (`pipeline_run_id='SHARD-SEMINOLE-J-GAPFILL-v1'`),
  replicating the exact formula/shape of the pre-existing `SHARD7-S65-SEMINOLE-J-v1` precedent
  (max_bid = arv×0.70 − repairs − 10000 − MIN(25000, 0.15×arv); real `judgment_amount` used where present,
  proxy values marked `honesty_marker: "INFERRED"` where not).
- `gold_standard_campaign` id=5595: `criteria_passed`/`criteria_total`/`exit_reason`/`session_end_at`
  checkpointed (see below) — used the dispatch's own literal id from the brief metadata rather than the
  brief's generic `WHERE state='processing' ORDER BY updated_at DESC LIMIT 1` subquery, because multiple
  shards run concurrently and that subquery could grab a different shard's row.
- `gold_standard_ultraloop_audit`: 5 rows inserted (ids 20720–20724), one per adversarially-survived
  claim, per ULTRALOOP PROTOCOL item 7.

Not shipped this session (explicitly out of scope / genuinely blocked — see below): no GHA workflow or
cron wiring (WIRING MANDATE), no DDL/schema changes (no `exec_sql`-style RPC exists; direct psql
unreachable), `gold_standard_loop()`/`gold_standard_certify()` were NOT run (other shards may be
mid-flight per PARALLEL-FLEET RULES — per-county `pencil_dod_evaluate_county` was used instead, as
instructed).

## SSOT bindings used
- `public.pencil_dod_evaluate_county(p_county)` RPC — before/after scoring, all 3 counties, called live
  multiple times per county.
- `public.pencil_dod_criteria` — read to confirm exact A–J thresholds/semantics before making claims.
- `public.multi_county_auctions` — read+write (parity_status, parcel_id, geo, assessed_value fields).
- `public.bid_decisions` — read (precedent discovery) + write (4 new seminole rows).
- `public.gold_standard_campaign` id=5595 — updated (see DoD table for full before/after).
- `public.gold_standard_ultraloop_audit` — 5 rows inserted, ids 20720/20721/20722/20723/20724.
- `public.gold_standard_cert_scope`, `public.gold_standard_exclusions` — read (confirmed empty for
  miami_dade, explaining the C/D denominator-reconstruction blocker).

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| st_johns C | 94.2%→95.0% PASS | VERIFIED | Before `matched_clean=113`; after `matched_clean=114`. Refuter independently queried live sjcfl.us ArcGIS and confirmed PIN 1453600000/605 Boating Club Rd is real and matches. `gold_standard_ultraloop_audit` id 20720. |
| st_johns overall | 9/10 → **10/10** | VERIFIED | Fresh `pencil_dod_evaluate_county('st_johns')`: all of A–J `pass:true`. Not yet certified — certification requires 2 consecutive 10/10 daily 07:30Z runs plus audit rows within 7d for ALL 10 letters (only C has a fresh audit row from this session; A/B/D/E/F/G/H/I/J audit rows are older/absent and would need to be within-7-day-fresh at certify time per EVALUATOR V6 SQL CERTIFY GATE). |
| miami_dade C | 82.7% unchanged | BLOCKED (honestly reported) | `gold_standard_cert_scope`/`gold_standard_exclusions` confirmed empty for miami_dade (unlike brevard/duval/etc) — the true 687-row scoped denominator is not reconstructable via unscoped REST paging (naive queries return 9,906+ rows). Zero writes, zero pass claimed. Audit id 20722. |
| miami_dade D | 88.5% unchanged | BLOCKED | Same root cause as C. |
| miami_dade I | 89.1% unchanged | VERIFIED (real work, insufficient) | 21 rows genuinely backfilled with live-sourced geo/value data (independently re-derived by refuter to ~5m accuracy and exact-value match against live sources) but RPC before/after is byte-identical — these particular rows' cards were still incomplete on other fields. Audit id 20721. |
| miami_dade overall | 7/10 unchanged | VERIFIED | Fresh RPC: A,B,E,F,G,H,J pass; C,D,I fail. |
| seminole E | 81.5%→83.1% (drift, not this session) | BLOCKED (honestly reported) | Attempted write hit `uq_mca_county_sale_date_parcel` unique-constraint conflict (HTTP 409/23505) against an existing twin row; 4 rows have no linkage lever (`account_number` null). Refuter reproduced the same 409 independently and confirmed via `updated_at` timestamps that the 159→162 drift predates this session's run window (other shard's concurrent write). Zero successful writes this session. Audit id 20723. |
| seminole I | 79.0% unchanged | BLOCKED (dependency) | Structurally gated on E per evaluator's own join contract; confirmed static both snapshots. |
| seminole J | 80.5%→82.6% (still FAIL) | VERIFIED | Before `deal_complete=157`; after `deal_complete=161` (+4, exact match to rows inserted). Refuter independently recomputed max_bid for all 4 rows to the cent and confirmed real vs. INFERRED-marked proxy usage. Audit id 20724. |
| seminole overall | 5/10 unchanged (J improved within FAIL) | VERIFIED | Fresh RPC: A,B,F,G,H pass; C,D,E,I,J fail. |
| Close-out checkpoint | `gold_standard_campaign` id 5595 updated | VERIFIED | PATCH response confirmed `criteria_passed` nested per-county, `criteria_total=10`, `exit_reason='ultraloop_complete_stjohns_10of10_C_flipped_miamidade_ceiling_reconfirmed_seminole_J_partial'`, `session_end_at` set — returned representation pasted into this session's tool output. |

## Negative test result
Regression-checked: st_johns' 6 `CLERK_SSOT_CANCELLED` rows were read but deliberately left untouched
(they represent a genuinely different, already-correct clerk-confirmed-cancelled state, not a bug to
game) — confirmed still `CLERK_SSOT_CANCELLED`/`auction_status=CANCELLED` post-session. All other
st_johns letters (A,B,D,E,F,G,H,I,J) reconfirmed still passing after the C fix, not just assumed.
miami_dade E/F/G/H/J and seminole A/B/F/G/H reconfirmed unchanged (no accidental regressions from the
21 miami_dade writes or the 4 seminole bid_decisions inserts).

## Deviations from intent
- Used `WHERE dispatch_id = '544138fb-d880-4e17-88e5-a2a0abfa6a11'` (the literal id from this dispatch's
  own metadata) instead of the brief's generic `WHERE state='processing' ORDER BY updated_at DESC LIMIT 1`
  subquery for the `gold_standard_campaign` close-out UPDATE, to avoid a race with concurrently-running
  sibling shards' dispatch rows.
- Did not run `gold_standard_loop()` / `gold_standard_certify()` (per PARALLEL-FLEET RULES, could not
  confirm no other shard was mid-flight) — used `pencil_dod_evaluate_county` per county instead, as the
  brief explicitly permits.
- `ultraloop_mode` logged as `"fallback"` rather than `"native"` — this session used the Workflow tool's
  multi-agent fan-out (the actual available orchestration mechanism), not the literal `/effort ultracode`
  CLI menu described in the ULTRALOOP PROTOCOL doc. Functionally equivalent (fan-out → adversarial
  survival vote), but labeled honestly per the protocol's own instruction to record which mode was used.
- Did not attempt Duval-style PO-id→court-case-number repair for seminole's PO-prefixed rows within this
  session (found during diagnosis but the E/C/D fix agent prioritized the higher-confidence E lever first,
  which hit a hard constraint conflict; ran out of session budget before returning to the PO-id repair).
  Flagged as the top next-session lever below.

## Open items / handoff
- **seminole PO-id repair (C/D/E, highest leverage)**: seminole has PropertyOnion-style case_numbers
  (e.g. `PO-1198367`, `PO-1117954`) that structurally cannot match official/court records — same root
  cause already solved for Duval in this campaign's history. Next session should port that exact repair
  (clerk tax-deed file lookup by parcel_id+sale date → real court case_number) to seminole.
- **seminole data-quality flag**: one sampled unlinked row (`701 Bayou Dr, Destin, FL 32541`) is a FL
  panhandle address, inconsistent with Seminole County (Orlando-area) — this looks like a bad
  county-tag/PO-import row, not a genuine seminole auction. Next session should audit for more
  mis-tagged rows before force-linking parcels.
- **miami_dade C/D denominator**: `gold_standard_cert_scope` has no row for miami_dade. Someone with
  visibility into how the 687-row scope was originally derived (likely a frozen snapshot date, per the
  EVALUATOR V6 SNAPSHOT SCOPE rule used for brevard/duval) needs to either backfill a `gold_standard_cert_scope`
  row for miami_dade or document the scoping logic so REST-only sessions can reconstruct it.
- **miami_dade I**: 21 rows now have real geo/value but card is still incomplete — likely still missing
  `zone_code` or another required field on those same rows; next session should check
  `v_auction_property_card` directly for those 21 ids to find the remaining gap.
- **st_johns certification**: 10/10 live, but certification needs fresh (≤7d) audit rows for ALL 10
  letters per EVALUATOR V6 SQL CERTIFY GATE — only C has one from this session. If st_johns is still
  10/10 tomorrow, a session should refresh audit rows for the other 9 letters to unblock certify.
