# Spec — issue #19809 — GOLD STANDARD SHARD-4: levy, baker, miami_dade, washington

## Intent reference
None at docs/intent/19809.md — issue body was the only brief. dispatch_id=191d6345-5de2-4f38-8b61-fd1fae2fd01f.

## Environment constraints hit (verified live, apply to all future shard-4-style sessions)
- Direct psql to the pooler (`aws-0-us-west-2.pooler.supabase.com`, both plain `postgres` and
  `postgres.<project_ref>` users, ports 5432 and 6543) fails: `ENOIDENTIFIER` / `password authentication
  failed`. `SUPABASE_DB_PASSWORD` is stale for direct connections this session.
- Supabase Management API SQL endpoint (`api.supabase.com/v1/projects/<ref>/database/query`) times out
  ("Connection terminated due to connection timeout").
- No `exec_sql`/`execute_sql` RPC exists in the schema cache (confirmed via live probe — PGRST202).
- `supabase` CLI is not installed on the runner.
- **Net effect: this session had zero working path to run arbitrary SQL or DDL.** Every fix below is a
  PostgREST GET/PATCH/POST against existing tables, or a call to an existing RPC
  (`pencil_dod_evaluate_county`). No new tables/columns/functions were created or could have been.

## What was built
- `scripts/gold_standard_shard4_19809_washington_cdij_backfill.py` — washington C/D/I/J backlog-catchup
  fix (harvests washington.realtaxdeed.com AJAX for the 6 gap auction dates, links parcel_zones into the
  existing R-1/jurisdiction_id=916 district, runs the real Shapira v14 J generator). **Run live, real
  writes confirmed** (see DoD table).
- `scripts/gold_standard_shard4_19809_washington_i_value_backfill.py` — follow-up real
  address+assessed_value backfill for washington from the same live calendar harvest. **Run live.**
- `scripts/gold_standard_shard4_19809_baker_bf.py` — baker B/F: authenticated
  `baker.realforeclose.com` Auction Results Report harvest (report_id=18), wrote 4 real independent
  `foreclosure_outcomes` rows + 4 `multi_county_auctions` tier1_sold_amount patches. **Run live** by the
  parallel workflow's fix agent; did not flip B/F (see Open items).
- `scripts/gold_standard_shard4_19809_miami_dade_cdi_reverify_no_safe_writes.py` — miami_dade C/D/I:
  live re-verification of the forensics-identified candidate rows; concluded zero writes were safe this
  session (see Deviations). **Run live, zero writes by design.**
- No fix script for levy (structurally blocked, see below) — none needed, none written.

## SSOT bindings used
- `public.pencil_dod_evaluate_county(p_county)` RPC — ground truth for every claim below, called fresh
  (never cached) before and after every write, by both the fixer and an independent verifier.
- `multi_county_auctions` — read/patched (parity_status, parity_source, parity_checked_at,
  property_address, assessed_value, tier1_sold_amount, tier1_sale_status, tier1_authoritative,
  tier1_verified_at).
- `parcel_zones` — inserted (washington: 39 new parcel→R-1 links).
- `bid_decisions` — inserted/updated (washington: 51 rows total via real Shapira v14.0 inference,
  `shapira_models` storage bucket, `parcel_valuations` comps where available).
- `foreclosure_outcomes` — inserted (baker: 4 rows, `data_source='tier1:realforeclose_results_report:baker'`,
  independent of the promote pipeline).
- `gold_standard_ultraloop_audit` — 40 rows inserted this session (per-letter survival votes, see below).
- `gold_standard_campaign` (id=5660, dispatch_id=191d6345-…) — mandated close-out row, updated.

## DoD status (pencil_dod_evaluate_county, live, 2026-09-03 ~16:35Z)

| County | Before (from issue) | After (live, verified) | Delta |
|---|---|---|---|
| levy | 9/10 (I fails, 88.9%) | 9/10 (I fails, 88.9%) — **unchanged** | none — structurally blocked this session |
| baker | 8/10 (B,F fail, null/null) | 8/10 (B,F fail, null/null) — **unchanged numerically**, real independent data added | partial — see Open items |
| miami_dade | 7/10 (C 81.1%, D 89.3%, I 90.6% fail) | 7/10 — **unchanged**, all 3 gaps re-confirmed structurally blocked | none — correctly refused unsafe writes |
| washington | 6/10 (C 51.0%, D 51.7%, I 51.0%, J 51.0% fail) | **8/10** — C=97.9% PASS, D=98.6% PASS, I=78.3% fail (was 51.0%), J=86.7% fail (was 51.0%) | **+2 letters passing, +2 letters substantially improved** |

Full before/after JSON per county (VERIFIED, live RPC calls, pasted verbatim):

**levy** (independent re-verify by workflow refuter agent):
```json
{"A":{"pass":true,"metric":1},"B":{"pass":true,"metric":100.0},"C":{"pass":true,"metric":97.8},"D":{"pass":true,"metric":97.8},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":10.3},"I":{"pass":false,"metric":88.9,"detail":"card_complete=40 of 45"},"J":{"pass":true,"metric":100.0}}
```

**baker** (independent re-verify by workflow refuter agent):
```json
{"A":{"pass":true,"metric":1},"B":{"pass":false,"metric":null,"detail":"verified=0 closed_sold=0"},"C":{"pass":true,"metric":100.0},"D":{"pass":true,"metric":100.0},"E":{"pass":true,"metric":100.0},"F":{"pass":false,"metric":null,"detail":"tier1_sold=0 closed_sold=0"},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.0},"I":{"pass":true,"metric":100.0},"J":{"pass":true,"metric":100.0}}
```

**miami_dade** (independent re-verify by workflow refuter agent):
```json
{"A":{"pass":true,"metric":239},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":81.1,"detail":"matched_clean=568"},"D":{"pass":false,"metric":89.3,"detail":"matched_any=625"},"E":{"pass":true,"metric":97.1},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":98.2},"H":{"pass":true,"metric":0.1},"I":{"pass":false,"metric":90.6,"detail":"card_complete=634 of 700"},"J":{"pass":true,"metric":100.0}}
```

**washington** (final live re-check by orchestrating session, post both fix scripts):
```json
{"A":{"pass":true,"metric":15},"B":{"pass":true,"metric":100.0},"C":{"pass":true,"metric":97.9,"detail":"matched_clean=140"},"D":{"pass":true,"metric":98.6,"detail":"matched_any=141"},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.0},"I":{"pass":false,"metric":78.3,"detail":"card_complete=112 of 143"},"J":{"pass":false,"metric":86.7,"detail":"deal_complete=124"}}
```

## Negative test result (guardrail checks — all passed, no regressions)
- No `data_source='propertyonion'` rows written or relied upon for any pass claim (checked live for
  baker and washington; miami_dade wrote nothing).
- No fabricated zoning values: washington reused the pre-existing (already-live, already-tagged
  "honesty: HYPOTHESIS") R-1/jurisdiction_id=916 district — no new district invented. miami_dade
  correctly withheld 14 ArcGIS-resolved zone-code writes because none of the 10 backing
  (jurisdiction, zone) pairs have a real `zone_standards` row — writing would have inflated I without a
  real G-compliant backing, so it was not done.
- No B>105% anomaly introduced anywhere (levy/baker/washington B all read exactly 100.0% or null;
  miami_dade B unchanged at 100.0%).
- `gold_standard_loop()`/`gold_standard_certify()` were NOT run (per PARALLEL-FLEET RULES — other shards
  were mid-flight; per-county `pencil_dod_evaluate_county` was used instead, as instructed).
- Cron jobs 109/111/115 and the gold-standard-loop-* scoring jobs were not touched.
- Only the 4 assigned counties' rows were read or written; no cross-shard county was modified.

## Deviations from intent
- Fleet-wide "loop → certify" close-out sequence in the brief's VERIFICATION PROTOCOL was intentionally
  skipped per the brief's own PARALLEL-FLEET RULES clause ("Do not run public.gold_standard_loop()
  mid-session ... for verification use SELECT public.pencil_dod_evaluate_county('<county>') per county").
  Per-county evaluation was used instead, as instructed.
- miami_dade's fix stage produced a script but zero writes — this is a deliberate, evidence-backed
  decision (see DoD table), not an incomplete task. Re-verified independently and the refusal itself
  was upheld as correct by the adversarial verify stage.
- levy's fix stage attempted zero writes because forensics + independent re-verification both confirmed
  the only blocker (5 parcels' zoning source, `qpublic.schneidercorp.com/Application.aspx?AppID=930`)
  returns HTTP 403 (Cloudflare "Just a moment..." challenge) with no working ArcGIS alternative found —
  confirmed live twice (forensics stage and adversarial verify stage independently hit the same 403).

## Open items / handoff
1. **baker B/F pipeline gap (real, evidence-backed, needs DDL access to close):** this session sourced
   4 real independent `foreclosure_outcomes` rows (baker.realforeclose.com authenticated Results Report,
   report_id=18 — cases 022026CA000007CAAXMX=$55,100, 022025CA000038CAAXMX=$180,100,
   022026CA000018CAAXMX=$241,300, 022025CA000124CAAXMX=$92,100) and patched
   `tier1_sold_amount`/`tier1_sale_status`/`tier1_authoritative` onto the matching `multi_county_auctions`
   rows. Called `promote_tier1_from_outcomes()` — returned `{"promoted": 0}`. Canon's B/F denominator is
   `COUNT(sold_amount IS NOT NULL)`, and nothing in this session's available toolset (PostgREST-only, no
   DDL) writes `sold_amount` from `tier1_sold_amount` + a qualifying independent-source match. Writing
   `sold_amount` directly from our own scrape would be the exact self-referential "ghost success" pattern
   already banned elsewhere in this repo (`scripts/county_outcome_harvester.py`'s comments). **Needs a
   session with psql/DDL access to inspect `promote_tier1_from_outcomes`'s body and either (a) fix it to
   also write `sold_amount` when a qualifying independent outcome exists, or (b) get an explicit repo
   decision on the sanctioned path.** The remaining 11 of baker's 15 gap cases were not in the Results
   Report as "Sold" as of this run — genuinely not yet closed, left untouched.
2. **washington I/J residual (17 case_numbers, one auction_date=2026-07-21):** these have no match on
   the live realtaxdeed.com calendar for that date (harvest returned 15 items, only 14 of 31
   no-value rows matched) — either delisted, continued to an unlisted date, or a case_number formatting
   mismatch. Also, none of the address-recovered rows have real lat/lon: the harvested addresses are
   street-only (no house number — typical for vacant tax-deed lots) and the US Census geocoder (tested
   live) returns zero matches for street-only input. A future session should find a real parcel-level
   geocode source for Washington County (property-appraiser GIS did not resolve to a working endpoint
   in this session's time budget) rather than fabricate a city-centroid.
3. **miami_dade C (81.1%→need 95%, 132-row gap) / D (89.3%→need 95%, 75-row gap) / I (90.6%→need 95%,
   66-row gap):** all three re-confirmed structurally blocked this session with specific, named
   blockers (7 unbacked `tier1_sold_amount` claims with zero backing outcomes row — case_numbers
   2025-014301-CA-01, 2025-010211-CA-01, 2025-010500-CA-01, 2025-013301-CA-01, 2025-009306-CA-01,
   2025-013969-CA-01, 2025-013299-CA-01; 16 unmapped cancellation-vocabulary rows; 6 zone pairs with no
   `zoning_districts` row; 4 zone pairs with a district but no `zone_standards` row; 12 D-candidates that
   fail a two-field corroboration check). None of these are volume/scraping problems — they are data
   provenance gaps needing either DDL access (to fix the vocabulary mapping) or real ordinance-text
   research (for the zoning pairs). Full candidate lists are in the workflow's forensics-stage output
   (referenced in this file's commit) for whoever picks this up next.
4. **levy I (88.9%→need 95%, 5 parcels):** blocked purely by `qpublic.schneidercorp.com` (AppID=930)
   returning a Cloudflare challenge to automated requests. Needs either a Cloudflare-capable
   fetch path (e.g. browser automation) or an alternative Levy County GIS/ArcGIS source for parcels
   00881-000-00, 03427-002-00, 03467-002-0A, 05775-000-00, 17660-000-00.
