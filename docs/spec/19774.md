# SUMMIT #19774 — GOLD STANDARD SHARD-2: charlotte, okaloosa, baker, leon, holmes

dispatch_id: `ee9657a3-d298-4781-af52-cbbe1f5d23ed` | chat_session: `architect-20260903T080000`

## TL;DR

**leon flipped 7/10 → 10/10, fully live-verified.** The other four counties
(charlotte, okaloosa, baker, holmes) were re-confirmed live, unchanged, and
independently cross-checked against extensive prior-session documentation
that already established each of their remaining failing letters as
genuine structural ceilings (Cloudflare Turnstile CAPTCHA gates, or zero
completed sales — not scraper/logic bugs). No regressions on any of the 5
counties. All facts below are VERIFIED by live `pencil_dod_evaluate_county`
calls pasted verbatim, or by direct re-fetch of the named external source.

## Session note on environment

Direct `psql`/pooler auth failed (documented long-standing constraint, decision
log 169/205/287) — confirmed again this session (`password authentication
failed for user "postgres"`). All reads/writes this session went through
Supabase PostgREST (`$SUPABASE_URL/rest/v1`, `SUPABASE_SERVICE_ROLE_KEY`) and
the Supabase Management API SQL endpoint (`api.supabase.com/v1/projects/{ref}/database/query`,
`SUPABASE_ACCESS_TOKEN`) — the same fallback pattern used by ~150 prior
scripts in this repo (e.g. `scripts/apply_brevard_g_fix.py`).

## Per-county before/after (VERIFIED, `pencil_dod_evaluate_county`)

### leon — 7/10 → **10/10** (FIXED THIS SESSION)

BEFORE (session start, matches issue-body baseline exactly):
```json
{"A":{"pass":true,"metric":113},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":94.2,"detail":"matched_clean=244"},"D":{"pass":false,"metric":94.2,"detail":"matched_any=244"},"E":{"pass":true,"metric":97.3},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":95.9},"H":{"pass":true,"metric":0.1},"I":{"pass":false,"metric":93.8,"detail":"card_complete=243 of 259"},"J":{"pass":true,"metric":96.1},"auctions_total":259}
```

AFTER (live, this session, post-fix):
```json
{"A":{"pass":true,"metric":113},"B":{"pass":true,"metric":100.0},"C":{"pass":true,"metric":99.2,"detail":"matched_clean=257"},"D":{"pass":true,"metric":99.2,"detail":"matched_any=257"},"E":{"pass":true,"metric":98.1},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":96.1},"H":{"pass":true,"metric":0.0},"I":{"pass":true,"metric":97.3,"detail":"card_complete=252 of 259"},"J":{"pass":true,"metric":96.1},"auctions_total":259}
```

**C/D fix** (`scripts/gold_standard_shard2_19774_leon_cd_pipeline_lag_20260903.py`):
15 rows had `parity_status IS NULL` — fresh `calendar_sweep_mca_v3` stub rows
with a known `auction_date` (2026-09-09 through 2026-10-21) that had never
been run through the leon parity harvester. This is the exact, previously-
proven "pipeline lag" pattern from `scripts/leon_c_d_j_pipeline_lag_20260808.py`
(2026-08-08 session, a different/disjoint 12-row batch) — forked verbatim,
re-scoped to this session's 15 case numbers. Live-harvested
`leon.realforeclose.com` (foreclosure) and `leon.realtaxdeed.com` (tax_deed)
AJAX calendars (full `AREA=W`+`AREA=C` pagination) for the 5 distinct
`(sale_type, auction_date)` pairs among these rows. 13/15 matched by
normalized case_number → `parity_status='matched_clean'`,
`parity_source='tier1:leon_shard2_19774_pipeline_lag_20260903_ajax_harvest:...'`.
2/15 (`26-0102`, `26-0103`) did not match the live tax_deed calendar and were
**left untouched** (still NULL) — no fabrication.

**Adversarial dispute and resolution:** an independent refuter agent
initially claimed 6 of the 13 promotions were fabricated (case numbers not
found on the live calendar it re-fetched). I independently re-ran the exact
harvest a second time myself and dumped the raw item lists:
`leon.realtaxdeed.com` 2026-09-16 returned **34** items including all 5
disputed tax-deed cases (`26-0096/97/98/99/106`); `leon.realforeclose.com`
2026-09-17 returned **2** items including the disputed `2025 CA 001417`. The
refuter's tool did not paginate `AREA=W`+`AREA=C` the way `harvest_date()`
does and so only saw 19 of 34 / 1 of 2 items. Refuter's finding overturned by
direct re-fetch — this is documented in `gold_standard_ultraloop_audit`
(`survived=true`), including the dispute and its resolution, not hidden.

**I fix** (`scripts/gold_standard_shard2_19774_leon_i_zoning_backfill_20260903.py`,
forked from the proven, dynamically-scoped
`scripts/gold_standard_leon_i_zoning_backfill_20260809_c5a8b2c7.py` under a
fresh dispatch tag): 10 parcels had a `parcel_id` but no `parcel_zones`
linkage. Resolved via live spatial point-in-polygon query against Leon
County's own ArcGIS layer (`intervector.leoncountyfl.gov`
`TLC_OverlayZoning_D_WM` MapServer layer 0) — real zone codes (`RP-1`, `R-3`,
`RP`, `MR-1`, `UF`) written to `parcel_zones`, no fabrication. 7 of those 10
rows also needed `latitude`/`longitude` backfilled via the free US Census
Bureau geocoder (`Public_AR_Current` benchmark); first attempt hit a
transient PostgREST lock-timeout (`55P03`, concurrent-session lock
contention, not a bug) on 8 of the writes, all 7 recoverable ones succeeded
on retry (1 already had coordinates from an earlier manual test patch).
Independently re-verified by the refuter agent against 2 of the 10 parcels'
exact stored coordinates — zoning attributes matched exactly, no red flags.

Both fixes logged to `gold_standard_ultraloop_audit`
(`dispatch_id=ee9657a3-d298-4781-af52-cbbe1f5d23ed`, `survived=true` for
letters C and I).

### charlotte — 9/10, unchanged (C fails: documented canon-level structural block, not re-chased)

```json
{"A":{"pass":true,"metric":41},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"metric":58.3,"detail":"matched_clean=180"},"D":{"pass":true,"metric":95.5},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":97.8},"H":{"pass":true,"metric":0.0},"I":{"pass":true,"metric":95.8},"J":{"pass":true,"metric":100.0},"auctions_total":309}
```

Live `parity_status` breakdown confirms the C gap (129 of 309 not
`matched_clean`) is 114 rows correctly reconciled to
`parity_status='CLERK_SSOT_CANCELLED'` (genuinely cancelled/redeemed sales,
per clerk SSOT) plus 14 `NULL`-parity rows, not a data defect. This is the
**same canon-level tension already documented fleet-wide** in
`GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md` (7+
independent counties, including charlotte itself on 2026-08-27 at a starker
37.8% cancellation rate) and by charlotte's own prior sessions
(`charlotte_cd_realforeclose_tier1_backfill_ch_cd.py`,
`charlotte_cd_tier1_run93161_parity_stamp.py`): C's canon deliberately
excludes `CLERK_SSOT_CANCELLED` from `matched_clean` while D accepts it —
by design, not a bug. Per that document's explicit recommendation, this is a
**canon-level decision for the AI Architect/Ariel** (exclude cancelled rows
from C's denominator, or admit them to C's passing set), not a per-county
re-diagnosis target. Per the same document's guidance ("stop burning
further sessions on structurally-unfixable re-diagnosis"), this session did
**not** re-verify the 14 NULL-parity rows in depth (charlotte's C gap is
dominated by the 114 confirmed-cancelled rows either way and cannot cross
95% without a canon change) — flagged as a residual, not claimed fixed.
No write made to any charlotte row this session.

### okaloosa — 9/10, unchanged (I fails: documented exhausted infrastructure block)

```json
{"A":{"pass":true,"metric":28},"B":{"pass":true,"metric":100.0},"C":{"pass":true,"metric":97.6},"D":{"pass":true,"metric":97.6},"E":{"pass":true,"metric":97.6},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.7},"I":{"pass":false,"metric":92.9,"detail":"card_complete=79 of 85"},"J":{"pass":true,"metric":100.0},"auctions_total":85}
```

The same 6-row I gap has been independently re-confirmed **6+ consecutive
sessions** (per `20260831_gold_standard_shard3_okaloosa_cdei_ceiling_reconfirm.sql`
and `20260901_gold_standard_shard4_dd1518d9_okaloosa_liberty_playwright_lever_exhausted.sql`):
2 rows (`2024-CA-000470`, `2024-TDD-000089`) are blocked by a Cloudflare
Turnstile CAPTCHA gate on Okaloosa Clerk's ClerkQuest case search, confirmed
via raw curl POST, Firecrawl (credits exhausted), **and** real headless
Playwright/Chromium (2026-09-01) — all three levers reach the search form
and are rejected at Turnstile-token validation. 4 rows
(`2025-CA-002286-F/-F3/-F4/-F5`) reference legal descriptions/PINs
structurally absent from Okaloosa's own cadastral GIS (2 are provably
Walton County properties, not Okaloosa). This session tried a **genuinely
new, previously-undocumented lever** (Bright Data's web-unlocker MCP tool,
`scrape_as_markdown`, against `clerkapps.okaloosaclerk.com/ClerkQuest/`) —
it returned no usable content (empty result), consistent with the tool
being GET/render-only and unable to submit the Turnstile-gated search POST
that is the actual block point. No new lever succeeded. No write made.

### baker — 8/10, unchanged (B/F fail: documented zero-completed-sales + CAPTCHA block)

```json
{"A":{"pass":true,"metric":1},"B":{"pass":false,"metric":null,"detail":"verified=0 closed_sold=0"},"C":{"pass":true,"metric":100.0},"D":{"pass":true,"metric":100.0},"E":{"pass":true,"metric":100.0},"F":{"pass":false,"metric":null,"detail":"tier1_sold=0 closed_sold=0"},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.0},"I":{"pass":true,"metric":100.0},"J":{"pass":true,"metric":100.0},"auctions_total":16}
```

All 16 baker rows have `sale_date IS NULL` and `sold_amount IS NULL` —
`closed_sold=0` makes B/F's ratio `NULLIF(0,0)` → `null`, never passable
until a real closed sale with a published outcome exists. Per
`GOLD_STANDARD_SHARD5_BAKER_DISPATCH_14CBAE1A_SESSION_REPORT.md` and
`GOLD_STANDARD_SHARD4_17123_CALHOUN_SARASOTA_BAKER_SUWANNEE_SESSION_REPORT.md`,
this is a confirmed, multiply-reconfirmed structural block: Baker's only
outcome-bearing source, `civitekflorida.com/ocrs/county/02` (and
`bakerclerk.com`), is gated by Cloudflare Turnstile on search submission
(Playwright-confirmed screenshots, dispatches `271433e2` and `4fd52dfc`);
`bakerpa.com`/Baker GIS provide property data only, never sale outcomes;
`baker.realforeclose.com` has empty parcel/address fields for the specific
gap cases at the source level. Not re-attempted this session (same
exhausted-lever class as okaloosa I above). No write made.

### holmes — 6/10, unchanged (B/C/D/F fail: documented 4th+ consecutive structural reconfirmation)

```json
{"A":{"pass":true,"metric":6},"B":{"pass":false,"metric":null,"detail":"verified=0 closed_sold=0"},"C":{"pass":false,"metric":68.8,"detail":"matched_clean=11"},"D":{"pass":false,"metric":68.8,"detail":"matched_any=11"},"E":{"pass":true,"metric":100.0},"F":{"pass":false,"metric":null,"detail":"tier1_sold=0 closed_sold=0"},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.1},"I":{"pass":true,"metric":100.0},"J":{"pass":true,"metric":100.0},"auctions_total":16}
```

C/D's exact 5-row gap (`TD#2020-589`, `TD#2023-185`, `TD#2023-225`,
`TD#2023-496`, `TD#2023-584`, all `parity_status IS NULL`) is the same set
independently reconfirmed absent from `holmesclerk.com`'s tax-deeds page
(boilerplate "no sales scheduled" content, zero site-search hits) across the
17th and 18th recheck dispatches (`3b7ed6ea`, `4cb27213`) and again in
`20260901_gold_standard_shard1_f7cf6ec7_holmes_c_d_no_op_exhausted_blockers.sql`
— a 4th consecutive confirmation with zero drift. B/F's `closed_sold=0` is
the same root cause as baker: no holmes auction has ever concluded with a
published sale outcome (`holmesclerk.com` tax-deeds page is genuinely
empty; `holmes.realtaxdeed.com`/`holmes.realforeclose.com` are dead
(HTTP 403); no `tax_deed_outcomes`/`foreclosure_outcomes` rows exist for
holmes). Not re-attempted this session (exhausted-lever class). No write
made.

## Regression check

All 5 counties' non-targeted letters (A, E, G, H, and whichever of B/C/D/F/I/J
were already passing) were re-confirmed unchanged via the live evaluator
calls pasted above — no regressions introduced this session.

## Files this session

- `scripts/gold_standard_shard2_19774_leon_cd_pipeline_lag_20260903.py` (new, executed live)
- `scripts/gold_standard_shard2_19774_leon_i_zoning_backfill_20260903.py` (new, executed live, forked from `gold_standard_leon_i_zoning_backfill_20260809_c5a8b2c7.py`)
- `docs/spec/19774.md` (this file)
- DB writes: `multi_county_auctions` (13 parity promotions + 4 card backfills + 7 lat/lon backfills, all leon), `parcel_zones` (10 new leon rows), `gold_standard_ultraloop_audit` (2 new rows, leon C and I, `survived=true`), `gold_standard_campaign` (close-out row for this dispatch)
- No writes to any protected object (M2). No client-facing surface touched (M3). No FF/producer-facing send (M1 n/a). Scope stayed within the 5 named shard counties (M5).

## Not done / explicitly deferred (BLANK > WRONG, not a failure to report)

- **charlotte C**: canon-level decision needed from AI Architect/Ariel (exclude `CLERK_SSOT_CANCELLED` from C's denominator, or admit it to C's passing set, or accept C as structurally capped in high-cancellation counties). Flagged, not re-litigated, per explicit prior-document guidance.
- **okaloosa I / baker B,F / holmes B,C,D,F**: confirmed-exhausted infrastructure blocks (Cloudflare Turnstile on search submission; zero completed sales at source). A genuinely new lever (Bright Data web-unlocker) was tried once this session for okaloosa I and did not succeed (GET-only tool, cannot submit the gating search form). Would need either a Turnstile-solving capability or a fundamentally different data source to move further — not available this session.
- `gold_standard_loop()` / `gold_standard_certify()` were **not** run this session: 4 other shard dispatches (`8a0ba6e4` santa_rosa/miami_dade/polk, `4e1cce4c` brevard/levy/pinellas/bradford/taylor, `f10e941c` lafayette/st_lucie/martin/lee/seminole, `8568fcac` nassau/lake/dixie) were created at the identical `2026-09-03T08:00:00Z` dispatch timestamp, consistent with a concurrent fleet wave — per PARALLEL-FLEET RULES this session reported per-county `pencil_dod_evaluate_county` evaluations only and skipped the fleet-wide loop/certify to avoid stepping on concurrent work.

## Tags

VERIFIED: all before/after JSON blocks above are live `pencil_dod_evaluate_county` output pasted verbatim this session. VERIFIED: leon harvest results (raw item lists) independently re-fetched twice. VERIFIED: leon zoning results cross-checked by an independent adversarial agent against the live ArcGIS layer. INFERRED: none of the "not done" items above were re-verified beyond citing prior sessions' live-fetch evidence (their own documents mark themselves VERIFIED as of 2026-08-27 through 2026-09-01) — treated as still current since baseline metrics for those counties/letters are byte-identical to those prior sessions' post-investigation numbers.
