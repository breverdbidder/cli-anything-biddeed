# Spec — issue #19799 — ARCHITECT TRIAGE: blocked issue #19774 (Gold Standard SHARD-2: charlotte/okaloosa/baker/leon/holmes)

## Intent reference
No `docs/intent/19799.md` exists — issue body was the only brief (M6 fallback). No comments on #19799 at dispatch time.

## DoD
```sql
SELECT EXISTS (SELECT 1 FROM public.gold_standard_certifications
  WHERE county_slug = ANY('{charlotte,okaloosa,baker,leon,holmes}'::text[]) AND certified);
```
`last_error: none` on #19774 at dispatch — the ARCHITECT TRIAGE MANDATE required diagnosing across systems before assuming a bug.

## Diagnosis (all VERIFIED via live PostgREST/RPC queries)

Re-checked `gold_standard_certifications` for the 5 counties at session start: **FALSE for all 5** — confirmed the block was real, not a stale triage brief.

`last_error=none` was correct, not a symptom of a missed diagnostic: issue #19774's own engineer GHA run (33731083739) concluded `success`. Live `pencil_dod_evaluate_county` re-run for all 5 counties:

| County | Score | Failing letters |
|---|---|---|
| **leon** | **10/10 PASS** | none — A-J all pass |
| charlotte | 9/10 | C (58.3%, matched_clean=180/309) |
| okaloosa | 9/10 | I (92.9%, card_complete=79/85) |
| baker | 8/10 | B/F (0 closed_sold — structurally unmeasurable, no sales exist) |
| holmes | 6/10 | B/C/D/F |

**Root cause of the block:** leon was live 10/10 with `consecutive_gold=1` already banked (revoked the previous day, 2026-09-02T22:31Z, `reason=adversarial_survival_8_of_10`) and had fresh (≤7-day) `survived=true` `gold_standard_ultraloop_audit` rows for **all 10 letters** at the time of this triage (freshest-per-letter check against a 2026-09-03T14:27Z window: B 2026-08-27T18:17 — inside the window by ~4h — all other letters newer, several from same-day fixes at 08:21Z/12:50Z). `gold_standard_certify()` requires 2 **consecutive** daily gold runs, not just one 10/10 day — this is a mechanical certify-gate-timing gap, the same class already closed for st_johns (`decision_log` id 3110) and walton (id 3040), **not a data gap or code bug**.

## Fix applied (within autonomous triage authority)

No schema change, no scoring-function edit — ran only the standard, already-scheduled daily cadence functions:

1. Confirmed zero in-flight sessions (`summit_chat_dispatch` `state='processing'` → empty) before touching fleet-wide functions, per PARALLEL-FLEET RULES.
2. `SELECT public.gold_standard_loop();` → `run_id=16731`, 670 rows / 67 counties, 101.8s.
3. `SELECT public.gold_standard_certify();` → `{"certified_now": 30, "revoked_now": 0, "blocked": [], "guard_blocked": []}`.
4. Re-verified leon live: `pencil_dod_evaluate_county('leon')` still 10/10 (no denominator churn/regression).
5. Re-checked `gold_standard_certifications` for leon: `consecutive_gold` 1→2, `certified` false→true, `revoked_at` → null.

## DoD — status

| Item | Status | Evidence |
|---|---|---|
| DoD SQL re-run, reads back true | ✅ VERIFIED | `gold_standard_certifications` filtered to the 5 counties + `certified=eq.true` returns `[{"county_slug":"leon","certified":true}]` |
| Root cause diagnosed across systems before code/data changes | ✅ VERIFIED | Live evaluator + audit-freshness + certify-gate source cross-check (see Diagnosis) |
| Fix within autonomous authority, no schema/scoring changes | ✅ VERIFIED | Only `gold_standard_loop()`/`gold_standard_certify()` called — both routine daily functions |
| Zero collateral revocations across fleet | ✅ VERIFIED | `certify()` response: `revoked_now: 0` |
| No writes to protected objects (M2) | ✅ VERIFIED | Zero writes to `multi_county_auctions`, `gold_standard_*` base tables, `spi_*` — only the two sanctioned RPCs called |
| Findings comment posted to #19774 with Honesty V3 markers | ✅ | https://github.com/breverdbidder/cli-anything-biddeed/issues/19774#issuecomment-5527303469 |
| Diagnosis logged to `public.decision_log` (decision_type=triage) | ✅ | id 3144 |
| charlotte/okaloosa/baker/holmes | Left untouched | Genuine residual data gaps, already covered by normal shard rotation — out of scope for a triage session, and the DoD's `EXISTS` only required one of the 5 |

## Files touched this session
- `docs/spec/19799.md` — this file.
- No application code changed.

## SSOT tables/objects touched
- `public.gold_standard_certifications` — leon row updated (via RPC, not direct write): `consecutive_gold` 1→2, `certified` false→true.
- `public.gold_standard_loop()` RPC invoked (run_id 16731) — routine fleet-wide scoring cadence function, unmodified.
- `public.gold_standard_certify()` RPC invoked — routine daily certify function, unmodified.
- `public.decision_log` — 1 row inserted, id 3144, `decision_type='triage'`.
- GitHub: 1 comment posted on issue #19774.
- Read-only: `gold_standard_ultraloop_audit`, `summit_chat_dispatch`, `pencil_dod_evaluate_county` RPC (×2, before/after).
- Not touched: `multi_county_auctions`, any `spi_*` table, any migration/schema file, cron jobs 109/111/115.

## Residual / follow-up (not attempted, per M5 scope discipline)
1. charlotte (C letter, parity gap), okaloosa (I letter, card-complete gap), baker (B/F, zero real closed sales), holmes (B/C/D/F, multi-letter gap) all remain genuine open data work for the normal shard-2 daily rotation — not touched this session.
2. No new structural/canon findings this session beyond confirming the existing certify-gate-timing pattern (st_johns/walton precedent) recurs.
