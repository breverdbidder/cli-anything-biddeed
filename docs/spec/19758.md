# Spec — issue #19758 — Architect triage for blocked issue #19741 (shard-4: nassau/charlotte/holmes)

## Intent reference
None at `docs/intent/19758.md` — issue body was the only brief (per M6, body wins over comments).

## What was built
No code, schema, or data changes. This was a diagnostic-only architect triage session (protocol steps
1–7 of the issue body): read #19741's thread, re-verified the DoD live, reproduced the failure via direct
Supabase PostgREST probes, cross-referenced 15+ prior identical-shaped `decision_log` triage entries for
this campaign, and confirmed no autonomous fix or new lever exists for any of the 3 assigned counties this
session.

- `public.decision_log` id=3108 (`decision_type='triage'`) — full diagnosis, reasoning, and alternatives
  considered.
- GitHub comment on issue #19741 (`https://github.com/breverdbidder/cli-anything-biddeed/issues/19741#issuecomment-5517308349`)
  — findings with Honesty V3 markers, per mandate step 6.
- This file.

## SSOT bindings used (read-only)
- `public.gold_standard_certifications` (county_slug in nassau/charlotte/holmes) — confirmed `certified=false`
  for all 3, live, at session start.
- `public.pencil_dod_evaluate_county(county)` RPC — called for all 3 counties, matched the same-day prior
  session's numbers exactly (zero drift in ~6h).
- `public.multi_county_auctions` (county=charlotte/nassau) — read-only `parity_status` breakdown to check for
  a fixable-but-not-yet-run "unprocessed rows" class (found for charlotte: 14 rows, insufficient to cross
  threshold even if processed; none found for nassau).
- `public.foreclosure_outcomes` / `public.tax_deed_outcomes` (county=holmes) — read-only, confirmed unchanged
  (1 row / 0 rows respectively).
- `public.decision_log` — read (15+ prior entries: ids 2496, 2528, 2529, 2731, 2733, 2767, 2937, 3005, 3007,
  3073) + 1 new row written (id=3108).

## DoD status
`SELECT EXISTS (SELECT 1 FROM public.gold_standard_certifications WHERE county_slug = ANY('{nassau,charlotte,holmes}'::text[]) AND certified)`
— **FALSE**, unchanged from session start. Re-verified live at session end via the same live
`pencil_dod_evaluate_county` calls (nassau 9/10, charlotte 9/10, holmes 6/10 — identical to the prior
same-day session, `docs/spec/19741.md`).

| County | Letters passing | Sole/remaining blockers | Root cause class |
|---|---|---|---|
| nassau | 9/10 | C = 94.6% (53/56) | 2-row ceiling: 1 `PHANTOM_NOT_ON_CLERK`, 1 `CLERK_SSOT_CANCELLED` — both correctly-excluded classes, not fixable without fabrication |
| charlotte | 9/10 | C = 58.3% (180/309) | Settled fleet-wide canon (`CLERK_SSOT_CANCELLED` excluded from numerator, not denominator) — arithmetically capped ~62.8% even in the best case; `decision_log` id=2935 already ruled this closed |
| holmes | 6/10 | B/F = null (0/0), C/D = 68.8% (11/16) | No public online foreclosure/tax-deed platform exists (in-person courthouse only) — reconfirmed 19th+ consecutive session, zero drift |

## Diagnosis (this triage's contribution beyond re-confirming the prior session)
1. **Charlotte C row-level composition, computed fresh this session**: 180 `matched_clean` + 115
   `CLERK_SSOT_CANCELLED` + 14 unprocessed (`parity_status IS NULL`) = 309. This surfaced a *potentially*
   fixable class (14 unprocessed rows, the same pattern that fixed `indian_river` C/D in `decision_log`
   id=2767) — but quantified that even a full successful harvest only reaches 194/309 = 62.8%, far short of
   95%, so it was **not** shipped (would not move the letter's pass/fail, and a same-day sibling session
   already worked charlotte this session-window — redundant non-metric-moving write avoided).
2. **Nassau row-level re-check**: confirmed no unprocessed (`parity_status IS NULL`) rows exist for nassau —
   ruling out the one class of gap that has been mechanically fixable elsewhere in this campaign
   (indian_river, pasco). The 2 residual rows are both canon-excluded classes already investigated
   exhaustively by 3 prior nassau-specific sessions.
3. **Holmes**: confirmed zero drift in `foreclosure_outcomes`/`tax_deed_outcomes` since the 19th recheck
   session; did not re-attempt live web fetches against already-exhausted sources (per HARD GUARDRAILS,
   avoiding redundant re-proof of an already-proven negative).
4. **Structural certify() gate**: independently reconfirmed (not re-derived — cited from `decision_log`
   ids 2733/2767/2937/3005/3007/3073) that `gold_standard_certify()` requires `consecutive_gold >= 2`
   (2nd consecutive daily 10/10 evaluation). Moot for all 3 counties this session since none are at 10/10,
   but noted for completeness.

## Negative test result
No writes were made to any table this session. `pencil_dod_evaluate_county` output for all 3 counties at
session start and session end is byte-identical — confirmed no regression, no accidental drift.

## Deviations from intent
None. Worked exactly the 3 named counties (nassau, charlotte, holmes) per the issue body's DoD scope. Did
not touch protected tables, did not run `gold_standard_loop()`/`gold_standard_certify()` (no metric changed
that would warrant it), did not modify cron jobs, workflows, or shared evaluator functions.

## Open items / handoff
- **charlotte C**: permanently capped under current canon and real-world redemption rates (~37% of the
  denominator is genuinely cancelled/redeemed). Not a code fix. Would require either a new litmus/data
  source (none found in 5+ investigations) or an owner-level canon change to the `CLERK_SSOT_CANCELLED`
  denominator treatment — already ruled closed fleet-wide at `decision_log` id=2935; do not re-litigate
  without new evidence.
- **nassau C**: 53/56 ceiling. The one open (non-metric-moving) lever from the prior session — direct clerk
  docket cross-check of `26TD000013AXYX` vs sibling `26TD000014AXYX` — remains unresolved and low priority
  (would not change C's pass/fail either way).
- **holmes B/C/D/F**: 19+ consecutive sessions confirm no public online data source. Recommend (non-urgent,
  no click-path attached, not escalated as BLOCKED per issue-comment reasoning) that a future session or
  Ariel decide between (a) a paid courthouse-records source, currently unauthorized/unbudgeted for this use
  case, or (b) treating Holmes as a standing structural exception rather than a daily recheck target.
- No further action taken this session; issue #19741 remains open for the next scheduled engineer/architect
  wave per the campaign's 24/7 cadence.
