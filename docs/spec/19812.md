# Spec — issue #19812 — Exit Vercel: migrate off brevardbidderai Pro, then cancel

## Intent reference
No `docs/intent/19812.md` exists — per M6, the issue body is the only brief.

## Session 2 outcome (2026-09-03, this session)
**Still BLOCKED, but for a different, now-precisely-diagnosed reason.** The Step-1 inventory
ceiling from session 1 is CLEARED — Ariel posted the Pro-account project list and account state
directly in an issue comment (screenshots, not an API token). That comment asked this session to
take four concrete actions; all four require Vercel dashboard or API access, and this session
VERIFIED that access is structurally absent from this job, not merely unconfigured. New
`agent_ops_log` row `id=046be642-6686-4bfb-bb4b-4bc9764486cc`, `dispatch_id='19812'`,
`status='BLOCKED'`, `severity='blocker'` — VERIFIED (HTTP 201).

### The account is at real, time-sensitive risk (per Ariel's comment — not independently re-verified)
Ariel reported (2026-09-03 17:24 UTC screenshot): account banner "Overdue", "Action Required —
Payment failed, pay any open invoices before your account is shut down", no payment method on
file, invoice D2LOTNWY-0007 ($102.37) unpaid, and $16.70 of the $20 included Build CPU Minutes
already consumed this cycle with 14 days remaining. This session could not independently confirm
any of this (no dashboard/API access — see below), so it is carried forward as **Ariel-reported,
not VERIFIED-by-CC**. He has separately been advised to pay the invoice as continuity insurance;
that is a human/billing action outside this session's reach either way.

### Why none of the four requested actions could be executed this session
The comment asked CC to: (1) delete/disable Vercel projects that are paused or have no connected
git repo, (2) set an Ignored Build Step + disable preview deploys on the 3 live projects
(zonewise-web, biddeed-web, biddeed-mcp), (3) report the 23 account-level env var *names*, (4)
enumerate the rest of the project list behind "Show More". All four need either the Vercel
dashboard (human-only) or the Vercel API (a scoped token). This session has neither:

1. **This session genuinely runs inside the sanctioned GHA context** — confirmed via
   `GITHUB_ACTIONS=true`, `GITHUB_WORKFLOW=CC Runner — GHA-only (no Hetzner)`,
   `GITHUB_WORKFLOW_REF=.../cc-runner-ghonly.yml`. So the CREDENTIAL HANDLING rule that credential
   work must happen "INSIDE the GHA runner's own env: block" is satisfied procedurally — but:
2. **`cc-runner-ghonly.yml`'s own `env:` block (lines 117–141) does not inject Vercel or
   Cloudflare secrets.** Read directly from the workflow file this session: it wires 20 named
   secrets (Supabase ×4, GH, NPM, Stripe, Resend, Hetzner, RealForeclose ×3, BrightData ×2,
   Tracerfy ×2, Firecrawl, Exa, Gemini, Apify, Hunter, Telegram ×2) but **not**
   `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`, even though `gh secret list -R
   breverdbidder/cli-anything-biddeed` shows all three exist at the repo level (added
   2026-08-01 / 2026-03-19). This is a **workflow-wiring gap**, not a missing-credential gap —
   the fix is a one-line addition to the workflow's `env:` block, which M5 explicitly forbids this
   session from making ("no workflow-file edits"). `env | grep -iE 'vercel|cloudflare'` inside this
   job returned zero matches, and `npx vercel whoami` returned `Logged out` with no cached token
   anywhere in the runner — both VERIFIED.
3. **Cloudflare is a separate, harder gap.** No `CLOUDFLARE_*` var exists anywhere in this
   session either (same `env` grep, zero matches) — reconfirming session 1's finding and #19678's
   independent finding one day earlier (`docs/spec/19678.md`). #19813 (the companion biddeed-web
   migration issue) reported 2026-09-03 ~17:00 UTC that Ariel minted a new `CF_API_TOKEN` with
   `DNS:Edit` scope — but it was added to `breverdbidder/biddeed-web`'s `cloudflare-staging`
   **GitHub environment**, a different repo and different environment than this session runs in.
   GitHub Actions secrets cannot be read back once written (by design, not a permissions gap this
   session can close), so there is no way to reuse that token from here even in principle without
   Ariel/a human adding it again on this repo, or a future biddeed-web-scoped session using it
   in place.
4. **No Vercel MCP tool is registered in this session** (`ToolSearch("vercel")` → no matches),
   so there is no alternate tool-based path either.

Net: every one of the four requested actions is genuinely un-executable from this session, and
the correct instruction-following move — per Honesty Protocol and M4 — is to say so precisely
rather than attempt a workaround (e.g. trying to read the GH Actions secret value some other way,
which CREDENTIAL HANDLING explicitly prohibits) or silently skip the ask.

### Additional finding: mcp.biddeed.ai's likely source repo doesn't obviously match a Vercel deploy
Went looking for the repo behind the Vercel project `biddeed-mcp` (per Ariel's inventory,
domain `mcp.biddeed.ai`). The only plausibly-named repo is `breverdbidder/biddeed-mcp-server`
(there is no `biddeed-mcp` repo). Its `package.json` shows a `bin`-entry stdio MCP server
(`@modelcontextprotocol/sdk`, `puppeteer`, `pg`) with `main: build/index.js` and no HTTP
framework; no `vercel.json` was found in its root listing. A stdio MCP server is not the shape of
thing Vercel normally serves over HTTP, which is consistent with the earlier observation that
`https://mcp.biddeed.ai/` returns Vercel's generic `404 NOT_FOUND` at `/` (root has no route) —
but I could not confirm from this session whether that's (a) the right repo and it exposes routes
elsewhere, (b) a different repo entirely, or (c) a build wrapper not visible in the top-level repo
tree. **HYPOTHESIS, not CONFIRMED** — flagging rather than guess-scaffolding a Cloudflare
migration for the wrong artifact. No Cloudflare-prep work was attempted for `biddeed-mcp` this
session as a result; doing so without knowing the real deployed shape would be exactly the kind
of unverified action the Honesty Protocol prohibits.

### Production check (re-run this session, unchanged from session 1)
```
$ curl -sI https://zonewise.ai/       -> HTTP/2 200, server: Vercel
$ dig +short mcp.biddeed.ai CNAME     -> cname.vercel-dns.com.
$ curl -sI https://mcp.biddeed.ai/    -> HTTP/2 404, server: Vercel (root route only; app may be alive elsewhere)
```
Both still on Vercel, both still serving (no outage). VERIFIED, 2026-09-03T17:29Z.

### What was NOT done this session (all correctly gated by the credential ceiling above)
- No Vercel project deleted, disabled, or had its build settings changed.
- No DNS record touched.
- No env var names or "Show More" project list reported — I have no dashboard/API access to
  obtain them; fabricating either would violate the Honesty Protocol. This is an open ask back to
  Ariel, not a completed deliverable.
- No Cloudflare scaffolding attempted for `zonewise-web` or `biddeed-mcp-server` (unlike
  #19678's `biddeed-web`, where the target repo/framework was unambiguous). `zonewise-web` IS a
  confirmed Next.js 14 app (`gh repo view` → primaryLanguage TypeScript, description confirms
  Next.js) and *could* get the same OpenNext scaffolding-only PR treatment as #19678 in a future
  session — flagged as ready-to-do work, not done here because this session's remaining scope was
  spent precisely diagnosing the wiring gap and producing the actionable checklist below, which
  the "stop the bleeding" urgency in Ariel's comment made the higher-priority output.
- Did not touch #19811 or #19810 (still open, zero `agent_ops_log` rows as of this session) — out
  of lane per M5.

## Ready-to-execute manual checklist for Ariel (since CC cannot do this from here)
Given the account is reported overdue/at shutdown risk, the fastest real unblock is Ariel
executing these directly in the dashboard he already has open — each is a small number of clicks:

**A. Delete/disable (paused or no-git-repo projects only — do NOT touch zonewise-web, biddeed-web,
biddeed-mcp):**
- `everest-nexus` (paused)
- `dashboard` (paused)
- `shapira-life-os` (paused)
- `brevard-bidder-landing-v2` (paused)
- `brevard-bidder-site` (paused)
- `life-os-v36` (paused)
- `cli-anything-biddeed` (no git repo connected)
- `everest-geo-tracker` (no git repo connected)
- `brevard-bidder-skills-dashboard` (state unconfirmed in the comment — check before deleting)
- Plus whatever is under "Show More" — please paste that list (or delete directly, your call)

**B. On the 3 live projects (zonewise-web, biddeed-web, biddeed-mcp) — Settings → Git:**
- Add an Ignored Build Step, e.g.: `git diff HEAD^ HEAD --quiet .` (skips a build if nothing in
  the repo changed) — exact expression should match each repo's actual path structure; a generic
  starting point is fine, tune per project.
- Turn off preview deployments for non-primary branches / bot commits if that setting is available
  under Project Settings → Git → Preview Deployments.

**C. To let a future CC session do (A) and (B) via API instead of dashboard clicks:**
Two options, either closes the gap permanently:
1. Add `VERCEL_TOKEN` / `VERCEL_ORG_ID` / `VERCEL_PROJECT_ID` (already exist as repo secrets on
   `cli-anything-biddeed`, just unwired) to `cc-runner-ghonly.yml`'s `env:` block — a human/you
   needs to do this since M5 blocks CC from workflow-file edits. **Note**: `VERCEL_ORG_ID`/
   `VERCEL_PROJECT_ID` as currently named look single-project-scoped (per issue body: they map to
   team `everestcapital8`, Hobby tier) — worth confirming they're not just for that other team
   before relying on them for `brevardbidderai` Pro-account-wide operations.
2. Or paste the account's env var names + remaining project list directly (as originally asked),
   and CC can at least keep documentation/checklists current without ever needing write access —
   though (A)/(B) will still need dashboard clicks or a real API token regardless.

**D. Billing (you, not CC):** pay invoice D2LOTNWY-0007 now as continuity insurance — already
advised per the comment thread. Doing so doesn't waive the credit request in #19810.

---

## Session 1 outcome (2026-09-03, prior attempt — kept for history)
**BLOCKED at Step 1 of the issue's own ordering** ("Inventory first... nothing else in this
issue can be planned honestly without it"). No migration, DNS change, or cancellation action
was attempted or is possible from this session. `agent_ops_log` row
`id=2febda46-be02-48ca-822f-ef44219ae74b`, `dispatch_id='19812'`, `status='BLOCKED'`,
`severity='blocker'` — VERIFIED (HTTP 201, row returned via Supabase PostgREST).

### Why this was a genuine data/credential ceiling, not a confirmation question
1. **No Cloudflare credentials in this session.** `env` dump (full name enumeration) shows no
   `CLOUDFLARE_API_TOKEN` or `CLOUDFLARE_ACCOUNT_ID`. VERIFIED.
2. **No Vercel token scoped to the `brevardbidderai@gmail.com` Pro account.** `env` dump shows no
   `VERCEL_*` var at all in this session. VERIFIED.
3. **Repo-level GH secrets exist but don't close the gap.** `gh secret list -R
   breverdbidder/cli-anything-biddeed` shows `VERCEL_TOKEN` (updated 2026-03-19),
   `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` (added 2026-08-01) — VERIFIED to exist, but (a) not
   injected into this session's env (confirmed by the same `env` dump), and (b) per the issue
   body's own text, the only Vercel MCP/API access this org currently has confirmed working is
   scoped to team `everestcapital8` (`team_hnNsngSBFPRmWAzVPkxTCMgu`, Hobby tier — a different,
   free-tier account, not the invoiced Pro account this issue targets).
4. **The companion migration hit the identical ceiling one day earlier.** Issue #19678
   (`biddeed-web` → Cloudflare) ran 2026-09-02, produced `breverdbidder/biddeed-web#15`
   (scaffolding only), and documented the same Cloudflare credential gap in `docs/spec/19678.md`.
5. **Production constraint confirmed real** — `zonewise.ai` and `mcp.biddeed.ai` both still
   Vercel-served at that time.

### Single consolidated ask made to Ariel at the end of session 1
1. Vercel: a `brevardbidderai@gmail.com`-scoped API token, or paste the project list.
2. Cloudflare: a token with `Zone:DNS:Edit` scope.
3. **Result:** Ariel answered (1) by pasting the project list directly (session 2, above) rather
   than providing a token — so the inventory ask is satisfied, but the *execution* ask (a token
   for CC to act on the account) was not, which is why session 2 is blocked on a narrower, more
   specific gap than session 1 was.

## DoD status (cumulative across both sessions)
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| Inventory | Full Pro-account project table | PARTIAL | Project list + names known (Ariel's comment); framework/env-var-count/build-minutes/traffic per project still not itemized; "Show More" tail not yet seen. |
| Stop-the-bleeding (delete paused/orphan projects) | Not started | BLOCKED | No Vercel API/dashboard access this session — see manual checklist above. |
| Build-volume cuts on live projects | Not started | BLOCKED | Same gate. |
| Migration | Per-project cutover | NOT ATTEMPTED | Correctly gated on the above; `zonewise-web` confirmed migration-ready (Next.js 14) for a future session. |
| DNS | Zero domains resolving to Vercel | NOT ATTEMPTED | Same gate. Current state confirmed unchanged (still Vercel) — see live checks above. |
| Cancellation | Account closed, payment removed | NOT ATTEMPTED | Same gate; also requires migration-complete first regardless; billing action (pay overdue invoice) is human-only. |
| agent_ops_log | BLOCKED status logged with evidence, both sessions | DONE | `id=2febda46-...` (session 1), `id=046be642-...` (session 2), both VERIFIED HTTP 201. |

## Deviations from issue body
None. The issue body's own non-goals ("no cancellation before migration is complete") and the
comment's own escalation ("treat production continuity as the priority over migration elegance")
are both honored — no action was taken that risks production, and no action was faked to appear
complete when it structurally could not be performed from this session.
