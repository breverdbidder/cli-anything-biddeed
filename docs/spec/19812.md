# Spec — issue #19812 — Exit Vercel: migrate off brevardbidderai Pro, then cancel

## Intent reference
No `docs/intent/19812.md` exists — per M6, the issue body is the only brief.

## Outcome
**BLOCKED at Step 1 of the issue's own ordering** ("Inventory first... nothing else in this
issue can be planned honestly without it"). No migration, DNS change, or cancellation action
was attempted or is possible from this session. `agent_ops_log` row
`id=2febda46-be02-48ca-822f-ef44219ae74b`, `dispatch_id='19812'`, `status='BLOCKED'`,
`severity='blocker'` — VERIFIED (HTTP 201, row returned via Supabase PostgREST).

## Why this is a genuine data/credential ceiling, not a confirmation question
1. **No Cloudflare credentials in this session.** `env` dump (full name enumeration) shows no
   `CLOUDFLARE_API_TOKEN` or `CLOUDFLARE_ACCOUNT_ID`. VERIFIED.
2. **No Vercel token scoped to the `brevardbidderai@gmail.com` Pro account.** `env` dump shows no
   `VERCEL_*` var at all in this session. VERIFIED.
3. **Repo-level GH secrets exist but don't close the gap.** `gh secret list -R
   breverdbidder/cli-anything-biddeed` shows `VERCEL_TOKEN` (updated 2026-03-19),
   `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` (added 2026-08-01) — VERIFIED to exist, but (a) not
   injected into this session's env (confirmed by the same `env` dump), and (b) per the issue
   body's own text, the only Vercel MCP/API access this org currently has confirmed working is
   scoped to team `everestcapital8` (`team_hnNsngSBFPRmWAzVPkxTCMgu`, Hobby tier — a different,
   free-tier account, not the invoiced Pro account this issue targets). Whether the repo-secret
   `VERCEL_TOKEN` happens to be scoped to `brevardbidderai@gmail.com` is UNKNOWN — I did not
   attempt to read or exercise it: doing so from this chat/session context (rather than from
   inside a GHA workflow's own `env:` block) is exactly the pattern CREDENTIAL HANDLING (CLAUDE.md,
   GTM-22D) prohibits, since it would put a raw token in this session's process space with no
   masking guarantee. `npx vercel` / `npx wrangler` are also not installed and this sandbox has no
   outbound npm install for them cached, so there is no CLI path to test scope safely either way.
4. **The companion migration hit the identical ceiling one day earlier.** Issue #19678
   (`biddeed-web` → Cloudflare, referenced by #19812 as "Extends the Sept 1 decision") ran
   2026-09-02, produced `breverdbidder/biddeed-web#15` (scaffolding only), and documented in
   `docs/spec/19678.md`: *"No `CLOUDFLARE_API_TOKEN` or `CLOUDFLARE_ACCOUNT_ID` anywhere in this
   session's environment... This blocks: creating the KV namespace, setting any `wrangler secret
   put` value, any `wrangler deploy`."* Same class of session, same repo, same credential gap,
   24 hours earlier. VERIFIED via `git log` (`faa28d0d`) and file read.
5. **Production constraint is real and current** (not something to relax while blocked). Direct
   check, this session:
   ```
   $ dig +short zonewise.ai A       -> 216.150.1.1
   $ curl -sI https://zonewise.ai/  -> server: Vercel, x-vercel-cache: MISS, HTTP 200

   $ dig +short mcp.biddeed.ai CNAME -> cname.vercel-dns.com.
   $ curl -sI https://mcp.biddeed.ai/ -> server: Vercel, x-vercel-error: NOT_FOUND, HTTP 404
     (root has no route — app itself may still be live under a path; not investigated further,
     out of scope while blocked at inventory)
   ```
   Both are VERIFIED still Vercel-served as of 2026-09-03T16:22Z. `biddeed.ai` itself resolves to
   Cloudflare anycast IPs (104.21.54.12 / 172.67.222.51) — consistent with other already-migrated
   properties, not evidence either way about the two Vercel-hosted subdomains above.

## What I did NOT do (deliberately, per the issue's own non-goals + risk of the actions)
- Did not call the Vercel API with any token (scope unverified, would risk exercising the wrong
  account or leaking a token into this session's output).
- Did not touch any DNS record.
- Did not downgrade, cancel, or modify the Vercel account or its payment method.
- Did not dispatch or touch #19811 (spend caps) or #19810 (invoice audit) — confirmed both are
  still `OPEN` with zero `agent_ops_log` rows (checked via `gh issue view` and a PostgREST query
  filtered to `dispatch_id in (19811,19810)`), i.e. the interim spend-cap protection the issue
  references as "still set the cap NOW" has **not** been applied by anyone yet. Flagging only —
  actioning it is out of this issue's lane per M5 (one issue = one lane, no sibling dispatch).

## Could not verify (not fabricated)
- The issue body's `~$102/mo` Vercel run-rate figure from `finance.v_recurring_costs`: this
  session's Supabase role got `permission denied for schema finance` via PostgREST
  (`Accept-Profile: finance` header, service-role key) — the `finance` schema is not exposed to
  this session's grants. UNTESTED, not restated as fact. Same applies to
  `finance.v_vendor_spend_controls` — PostgREST reports no such table (closest match offered:
  `finance.v_recon_exceptions`), so the exact view name in the issue's step 5 ("Record the closure
  date in `finance.v_vendor_spend_controls`") could not be confirmed to exist under that name
  either. Flagging as a naming/access question for whoever picks this up next, not fixed here
  (out of scope while blocked at inventory).

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| Inventory | Full Pro-account project table | BLOCKED | No usable credential this session (see above). |
| Migration | Per-project cutover | NOT ATTEMPTED | Correctly gated on Inventory per the issue's own ordering. |
| DNS | Zero domains resolving to Vercel | NOT ATTEMPTED | Same gate. Current state confirmed unchanged (still Vercel) — see live checks above. |
| Cancellation | Account closed, payment removed | NOT ATTEMPTED | Same gate; issue explicitly requires migration-complete-and-verified first regardless. |
| agent_ops_log | BLOCKED status logged with evidence | DONE | `id=2febda46-be02-48ca-822f-ef44219ae74b`, VERIFIED (HTTP 201). |

## Single consolidated ask for Ariel (per the issue's own instruction to avoid piecemeal asks)
1. **Vercel**: either (a) a `brevardbidderai@gmail.com`-scoped API token, saved to the vault as
   `vercel_api_token_brevardbidderai` per the issue's own naming, or (b) paste the Pro account's
   project list (name, framework, domains) directly.
2. **Cloudflare**: a token with `Zone:DNS:Edit` scope — already independently known to be missing
   (blocked `lms.winnerdataai.com`/`ff.winnerdataai.com` per the issue body) and will block every
   DNS cutover step here too. One token covers both needs.
3. Once both are in hand, the next session should start at Step 1 (inventory) exactly as scoped,
   and can reuse `docs/spec/19678.md`'s already-solved pieces (OpenNext scaffolding pattern,
   Node-22 workaround for `wrangler`, the orphan-process kill pattern for `wrangler dev`) for any
   Next.js project in the Pro account.

## Deviations from issue body
None — the issue body itself anticipates this exact blocked state ("Nothing else in this issue
can be planned honestly without it") and names the two ways to unblock it, both reproduced above.
