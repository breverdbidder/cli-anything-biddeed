# 19838 — GOLD STANDARD SHARD-3: gilchrist, st_lucie, madison, union, sumter

dispatch_id: c77dbc8d-f2f1-4ea8-a61f-340bf78381ba
chat_session: architect-20260904T080000
session date: 2026-09-04

## Summary

Shard-3 counties (gilchrist, st_lucie, madison, union, sumter) were audited letter-by-letter
against `public.pencil_dod_evaluate_county`, fixed where a genuine, non-fabricated lever
existed, and every claim (fix or structural-block) was independently re-verified by an
adversarial refuter subagent per this campaign's ULTRALOOP protocol before being logged.

**gilchrist: 10/10 PASS (certified-eligible).** **union: 9/10** (blocked only on a
fleet-wide structural canon exclusion, not a data gap). **sumter, madison, st_lucie:**
unchanged from baseline — all residual gaps reconfirmed as genuine structural/data-access
ceilings already exhaustively documented by 4-10 prior sessions each; no new lever was
available this session, so nothing was force-fixed or fabricated (BLANK > WRONG).

## Files touched

- `docs/spec/19838.md` (this file)
- No code files modified. No schema changes. Data-only fixes via Supabase PostgREST
  (direct psql confirmed unavailable per standing constraint; REST/RPC used throughout).

## Live DB writes this session

1. **`multi_county_auctions` id=9bbeb28e-d2ec-4b2a-a7f5-bc6ce46b0484** (gilchrist,
   case 212025CA000033CAAXMX) — restored `parcel_id`, `property_address`,
   `assessed_value`, `market_value` to the values from the already-verified
   2026-08-27 fix (documented in repo-root `gilchrist_i_card_completeness_blocked_followup.sql`),
   which had regressed back to a garbage placeholder (`parcel_id='Property Appraiser'`,
   `property_address=NULL`) by session start today. See **P0 finding** below.
2. **`multi_county_auctions` id=af29ad63-3b4e-4ec8-8ec5-b6bbfb0a63b2** (union,
   case 63-2026-CA-0022) — backfilled `assessed_value`/`market_value`=89876 and
   `owner_name`, sourced from FL DOR Statewide Cadastral (CO_NO=73,
   PARCEL_ID=0606200000006000 — exact address match to the pre-existing
   `property_address`). Source: `https://services9.arcgis.com/Gh9awoU677aKree0/arcgis/rest/services/Florida_Statewide_Cadastral/FeatureServer/0`.
3. **`bid_decisions` insert id=1089316** (union, case 63-2026-CA-0022) — new row using
   the same county-agnostic Shapira-formula methodology already live for the other 3
   union rows (`max_bid = max(arv*0.7 - repairs - 10000, min(25000, arv*0.15))`,
   `cma_resale = arv*1.12`, `cma_distressed = arv*0.87`, `ml_score=0.52`,
   `confidence=0.55`), all 5 required factor keys present.
4. **`gold_standard_ultraloop_audit`** — 11 rows inserted (dispatch_id=c77dbc8d-...),
   one per (county, letter) claim made this session, all `survived=true` after
   independent adversarial re-verification.
5. **`gold_standard_campaign` id=5692** — `criteria_passed` (per-county A-J booleans),
   `criteria_total=10`, `exit_reason='timeout'`, `session_end_at=now()`.

`public.gold_standard_loop()` / `gold_standard_certify()` were **not** run this session
per the PARALLEL-FLEET RULES (cannot confirm no sibling shard is mid-flight); per-county
`pencil_dod_evaluate_county` was used for all verification instead.

## P0 finding: gilchrist gold-standard regression (needs next-session follow-up)

gilchrist was already certified 10/10 on 2026-08-27 (see
`gilchrist_i_card_completeness_blocked_followup.sql`). At this session's start
(2026-09-04), the exact same row had reverted: `parcel_id` back to the literal string
`"Property Appraiser"` (a known-bad placeholder — `calendar_sweep_mca.py` already has a
`_clean_parcel()` guard against this exact text) and `property_address` back to `NULL`.
`scraped_at`/`last_changed_at` on the row show a fresh re-scrape on 2026-09-03, i.e. some
process re-wrote good data to garbage roughly a week after the original fix landed.

Root cause was **not conclusively isolated** this session (out of narrow single-county
scope per the K3 surgical-changes discipline — fixing the regression's blast radius would
require auditing every gold-certified row shard-wide, not just this one). The row's
`parity_source` (`tier1:shard14_gilchrist_run6148_live_realauction_ajax`) is the more
likely suspect than `calendar_sweep_mca.py` itself (which already guards against this
placeholder), but this is unproven. **Next session should run a diff-audit across all
previously-certified counties for `parcel_id IN ('Property Appraiser','MULTIPLE',
'various','see attachment','n/a','na','none','unknown','see documents')` or `NULL`
regressions** to determine if this is isolated to gilchrist or a systemic clawback
affecting the wider gold-standard scoreboard.

## DoD status per county (A-J, canon per `pencil_dod_criteria`)

### gilchrist — 10/10 PASS
All letters PASS live. I flipped 92.9%→100.0% this session (see fix #1 above).

### union — 9/10
I and J flipped 75.0%→100.0% this session (fixes #2/#3). **C fails at 75.0%**
(matched_clean=3 of 4) — case 63-2025-CA-0053 carries
`parity_status='CLERK_SSOT_CANCELLED'`, which the canon evaluator
(`20260810_gold_standard_shard3_lake_clerk_ssot_cd_recognition.sql`) deliberately
excludes from `matched_clean` fleet-wide (it counts toward `matched_any`/D, not C — by
design, not a bug). This is the same structural class already documented for
sumter/st_lucie/gadsden/brevard/etc. Not fixable at single-county scope; would require a
fleet-wide, reviewed change to the shared evaluator semantics.

### sumter — 6/10 (unchanged)
C, E, I, J still FAIL. **C** (87.9%, 29/33): 4 CLERK_SSOT_CANCELLED tax-deed-redeemed
certs, same structural class as union — independently re-verified live against
sumterclerk.com as recently as 2026-08-29 with zero drift. **E/I/J** (93.9%, 31/33):
gated on the same 2 rows (2026-CA-000074 Ratliff, 2026-CA-000129 Strong/Young), which are
Cloudflare-Turnstile-gated on civitekflorside.com OCRS — confirmed blocked across 4 prior
sessions through 2026-09-01 (JSF navigation flow is provably completable; the
search-submission step itself requires Turnstile-solving capability this session does not
have). Re-verified live this session (both rows still null on all target fields); not
re-attempted since no new lever exists.

### madison — 8/10 (unchanged)
B, F still FAIL (null metric, 0 of 8 closed_sold). Reconfirmed structural for the 10th+
consecutive session: all 8 madison auctions have `sold_amount IS NULL`; the only
outcome-adjacent record (case 24-62-CA) is a plaintiff-reverted NO_SALE from
auction.com, not a bona fide third-party sale — writing it in as `closed_sold` would be
fabrication. madisonclerk.com never publishes post-sale disposition; secondary sources
(myfloridacounty.com OCRS, civitekflorida.com) are CAPTCHA/JS-gated with no available
solver this session.

### st_lucie — 9/10 (unchanged)
C still FAILs at 78.5% (197/251). Reconfirmed structural: the 54-row gap is 53
CLERK_SSOT_CANCELLED rows (same canon-exclusion class as union/sumter) + 1
correctly-classified multi-parcel `matched_divergent` case (RealForeclose's own auction
report lists "MULTIPLE PARCELS" for this case; our schema stores one parcel_id per row —
a genuine data-model mismatch, not a normalization bug). Independently re-verified live
against acclaimweb.stlucieclerk.gov as of 2026-08-28 with the 47→53 growth matching
organic new cancellations, not drift or a stale flag.

## Verification evidence (VERIFIED — commands run, output observed)

Live `pencil_dod_evaluate_county` before/after (full JSON pasted from this session):

**gilchrist before:** `I: card_complete=13 of 14, metric=92.9, pass=false` (all others already PASS)
**gilchrist after:** `I: card_complete=14 of 14, metric=100.0, pass=true` — **10/10 PASS**

**union before:** `C: matched_clean=3, metric=75.0, pass=false` | `I: card_complete=3 of 4, metric=75.0, pass=false` | `J: deal_complete=3, metric=75.0, pass=false`
**union after:** `C: unchanged (structural)` | `I: card_complete=4 of 4, metric=100.0, pass=true` | `J: deal_complete=4, metric=100.0, pass=true` — **9/10**

**sumter / madison / st_lucie:** live-reconfirmed unchanged (see JSON snapshots in the
adversarial refuter subagent's report, captured this session — sumter 6/10, madison 8/10,
st_lucie 9/10).

All 5 counties' final live JSON snapshots and the full adversarial-refutation transcript
(claim-by-claim REFUTED/SURVIVED verdicts) are captured in
`gold_standard_ultraloop_audit` rows for dispatch_id `c77dbc8d-f2f1-4ea8-a61f-340bf78381ba`
(11 rows, all `survived=true`).

## Honesty Protocol tags

- Gilchrist 10/10 and union I/J fixes: **VERIFIED** (live query before, live write, live
  query after, independently reproduced by a separate subagent with no access to this
  session's reasoning).
- Sumter/madison/st_lucie structural-block reconfirmations: **VERIFIED** (fresh live
  queries this session, not a citation of prior-session claims alone).
- Gilchrist regression root cause (which script clobbered the row): **UNKNOWN** — flagged
  explicitly as unproven, not guessed at with false confidence.

## Deferred / out of scope this session

- Fleet-wide `refresh_parity_tier1_outcomes()` parity_source allow-list widening (would
  be required to ever move C past its structural ceiling in union/sumter/st_lucie) —
  explicitly out of scope for a single-shard session, affects the whole clerk_ssot fleet.
- Turnstile-solving capability for sumter's 2 residual E/I/J rows — no such tool available
  this session.
- Root-cause isolation of the gilchrist regression — flagged as the top next-session
  priority above.

## Telegram notification

No certification event fired this session (no county reached a fresh 10/10 that wasn't
already previously certified at the DB level via `gold_standard_certify()`; gilchrist
reached 10/10 live but the loop/certify functions were not run per the PARALLEL-FLEET
concurrency rule). Not applicable this session.
