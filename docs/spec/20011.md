# Spec — issue #20011 — GTM-CMO: Pro Plus + Projects launch content

## Intent reference
No `docs/intent/20011.md` — issue body pointed to the first comment as the brief (Ariel, 2026-09-04 20:58 UTC). Canon: `docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md`. Gates: `docs/gtm/CONTENT_SOP.md` (§6, incl. P19). Positioning input: `breverdbidder/rehabvaluator-competitive-intel` `02_Top_Tier_Thesis.md` (fetched live this session — never named in any deliverable).

## What was built
| File | Purpose |
|---|---|
| `content/answers/what-happens-after-you-win-a-foreclosure-auction.md` | New AEO answer asset (canon C3). Auto-publishes to `site.site_content` via `content-sync.yml` on merge to `main`, renders at `/answers/what-happens-after-you-win-a-foreclosure-auction` |
| `content/pricing/pro-plus-tier.md` | Copy for the Pro Plus tier block on `/subscribe` — feeds the pricing-card component, does not itself render (Worker `SUBSCRIBE_HTML` has no feature-grid slot yet) |
| `content/upgrade-sheets/pro-plus-projects.md` | Title + 6-line checklist + CTA/secondary-CTA copy for the shared upgrade-sheet component `#20010` is building |
| `content/homepage/plans-section.md` | Three-card (Investor/Pro/Pro Plus) copy for the `/` Plans section in `biddeed-web` — copy only, no code change in this PR (S4: `/` lives in `biddeed-web`, out of scope for this repo's PR) |
| `content/social/founder-posts-projects-launch.md` | LinkedIn + X founder-voice draft posts (§5.9) announcing Projects/Pro Plus — draft copy only, no posting (distribution lane, out of scope, M5) |

No migrations. No RPC/view changes. No code touched in `src/worker.js` or any package — content-only, per the brief ("WRITE ... one PR into content/ + site.site_content").

## SSOT bindings used
- `spi_gates` (read-only, PostgREST): `gtm_factory_halt` — `opened_at=2026-09-03T09:50:13Z`, `verified_at=null` → **still OPEN**, tripped detector `quota_no_reading`. `test_purchase` — `opened_at=2026-07-31`, `verified_at=null` → still OPEN. Neither gate blocks writing content to a PR branch (Roles table: Builder may write `content/**` + PR); both mean `gtm-validate.yml`/`gtm-merge.yml` won't auto-run/merge — Ariel's manual click is the only merge path right now, which matches M8 regardless of dial.
- `packages/biddeed-mcp/src/constants.js` `TIER_RANK`/`STREAM_GATE` (read-only) — sourced the Investor vs. Pro capability split (S2 Qualification = Investor; S3 Fusion + S4 Monitoring + S5 Max Bid = Pro) used in the homepage Plans copy.
- `everestcapitalusa.com` (fetched live this session) — founder proof points re-verified character-for-character against canon (20+ years FL distressed assets, five decades combined development experience, the five closed-project figures, "know more about the property than anyone else at the auction," Ariel Shapira / Founder & CEO / Inventor & Founder ZoneWise.AI). None of the specific dollar figures were used in any shipped copy (see Deviations) — the re-fetch exists as evidence the bio facts I did use ("20+ years") are current, not because a figure needed A6 sign-off.
- `leg.state.fl.us` Fla. Stat. §45.031 (fetched live this session) — subsections (4) and (5), quoted verbatim in the new answer asset's `statutes[]`.

## DoD status

### Compliance gate (`factory/gtm/gate.py`), run individually and combined — 2026-09-04
| # | Check | Status | Evidence |
|---|---|---|---|
| P1 | Halt + quota | **N-A for this PR** (no publish step; Builder role may write `content/**`/PR while halted). Halt state itself: OPEN, documented above | `spi_gates` query above |
| P2 | Banned terms | PASS | `GTM_COMPLIANCE_PASSED` on all 5 files, combined and individually |
| P3 | Person names (M7) | PASS — only "Ariel Shapira" appears; no other person named | gate run + manual grep |
| P4 | Homeowner contact / relief | PASS — N-A content, zero owner-directed phrasing | gate run |
| P5 | Live-number rule | PASS — all prices are `{{pricing.*}}` tokens, never literals; "3 seats" is a plan attribute (config, not a live metric) per the brief's own instruction ("price token from checkout config, never a literal" — scoped to price, not seat count); founder bio figure "20+ years" matches canon/live re-fetch; the answer asset's only literal digits ("10-day"/"10 days") are matched to its own `statutes[].sentence` per A5 | gate run + `parse_answer_asset.mjs` dry run (below) |
| P6 | Patent phrasing | PASS — zero "patent" hits | gate run |
| P7 | Canon strings | PASS — hero stack not touched anywhere in this PR; no retired line used | gate run + grep |
| P8 | Energy rules | PASS — 0 exclamation marks across all 5 files (LinkedIn/X counted programmatically), no urgency phrases | `str.count('!')` = 0 on LinkedIn and X bodies |
| P9 | Positioning | PASS — no "all 50 states"/"3,000 counties"; FL/US-county framing only | gate run |
| P10 | Funnel links | PASS for the answer asset (only asset that renders on its own) — all 5 linked URLs curl 200 same run: `/county/brevard`, `/radar?county=brevard`, `/buy-report?county=brevard`, `/subscribe?tier=proplus`, `/counties`. N-A for pricing/upgrade-sheet/homepage/social — they don't render standalone yet (copy handoff to `#20010` / a future `biddeed-web` PR) | curl output in this session |
| P11 | Technical (schema/meta/H1/sitemap) | PASS for the answer asset, inherited from the existing `/answers/:slug` renderer (`src/worker.js` ~L3233, ~L4700-4730 already emit `FAQPage` + `Person` + canonical + sitemap entry for every published slug — code read, not modified here). N-A for the other four (no standalone render yet) | code read |
| P12 | Extraction test (answer assets) | Self-administered dry run: handed only the first 59 words + the question to a fresh reasoning pass — returns the correct answer (certificate of sale → 10-day objection window → certificate of title = ownership) without hedging. A full fresh-context judge session per SOP procedure is the validator's job (`gtm-validate.yml`), not builder's — flagged as the one item this PR cannot self-certify | see below |
| P13 | Evidence | PASS — this file | — |
| P14 | Publish dial | PASS — PR only, no publish, no `main` write, no DB write (M8) | — |
| P15 | Archetype consistency | PASS — LinkedIn/X posts and the answer asset's pain paragraph read as the Problem founder ("I identify with you"), never trend-analyst/visionary framing | manual read |
| P16 | Hero check | PASS — every asset lands on "yours to run" / "run the whole project" — the bidder is the hero of the payoff, Ariel is proof + guide, never the protagonist of the resolution | manual read |
| P17 | Status-quo respect | PASS (gate `contempt_scan` + manual) — the enemy named is scattered tools/spreadsheets, never the bidder | gate run |
| P18 | Vividness | PASS — zero buzzwords (gate `buzzword_scan`); every founder-voice asset carries a concrete scene (spreadsheet budget, email receipts, re-typed draw requests) | gate run + manual read |
| P19 | Parity gates (UI audit) | **N-A** — no rendered page changed in this PR (answer-asset rendering is inherited/unmodified renderer code; pricing/upgrade-sheet/homepage copy has no live surface yet). Applies once `#20010`/a `biddeed-web` PR actually renders this copy — flagged in Open items | — |

### Answer asset (§5.1 A1–A12)
| # | Status | Evidence |
|---|---|---|
| A1 | PASS | frontmatter has all required keys; `scripts/lib/parse_answer_asset.mjs` dry run below confirms no missing-key error |
| A2 | PASS | `answer_first` = 59 words (parser-computed) |
| A3 | PASS | pain (spreadsheets/email/re-typed draws) → proof (20+ years, high-six-figure outcomes framed qualitatively) → product (Projects) order |
| A4 | PASS | Fla. Stat. §45.031(4) and (5) fetched from leg.state.fl.us this session, quoted verbatim |
| A5 | PASS | `parse_answer_asset.mjs` dry run: `PARSE OK` — no unmatched digit-run error |
| A6 | N-A this asset — no founder dollar figures used (see Deviations) |
| A7 | PASS | 5/5 links curl 200 in this session |
| A8 | PASS (inherited) | renderer already emits `FAQPage` + `Person`/`Organization` from `body_jsonb` |
| A9 | Self-check only, see P12 above | — |
| A10 | UNTESTED | page is not live until merge + `content-sync.yml` runs; cannot curl a URL that doesn't exist yet |
| A11 | PASS | see P2–P18 above |
| A12 | Pending merge (Ariel's click, M8) | — |

### `parse_answer_asset.mjs` dry run (no network, no DB write)
```
$ node -e "import('./scripts/lib/parse_answer_asset.mjs').then(...)"
PARSE OK, hero words: 59
$ python3 factory/gtm/gate.py --paths content/answers/what-happens-after-you-win-a-foreclosure-auction.md
GTM_COMPLIANCE_PASSED
```

## Negative test result
Checked that none of the 5 files contain: the competitor's name (either spelling), "S5" as a product name, "Shapira Analysis"/"Shapira Formula", any patent phrasing, the retired tagline, or a person name other than Ariel Shapira — `gate.py` clean on all 5, plus manual grep for "Rehab Valuator" / "rehabvaluator" (zero hits) since that name appears in the positioning-input doc and must never leak into shipped copy.

## Deviations from intent
1. Homepage Plans section is copy-only in this repo; the brief says "homepage Plans section revision" but `/` is owned by `biddeed-web` (CONTENT_SOP §5.6 S4) and M5 restricts this session to the repo named in the issue (this repo). Shipping the content file here, flagged for a follow-up `biddeed-web` PR, is the surgical option — forking `biddeed-web` to `/tmp` for a copy-only change would have expanded scope beyond what the brief actually asked this issue to do (it asked for content, not a cross-repo page edit).
2. Founder proof-point dollar figures (the five closed-project numbers) were re-verified live but deliberately **not used** in any shipped copy — a static, unrendered social post or content file can't carry a `{{token}}`, and CONTENT_SOP hard rule 2 treats any literal figure as unverified the moment it's re-read outside this session. "20+ years" is used because it's a durable bio fact, not a metric that drifts week to week.
3. Pricing-page tier block (item 1) ships as a structured copy file, not a `SUBSCRIBE_HTML` edit — `SUBSCRIBE_HTML` currently has no feature-grid slot (only price + email capture); adding one is a code change belonging to `#20010`'s component work, not this content issue.
4. Answer-asset links include a `subscribe` key in addition to the required `county`/`radar`/`report` trio, per the brief's explicit ask ("links to the county page and /subscribe"). The required trio is present and verified 200; `subscribe` is additive, not a substitution.

## Open items / handoff
- `#20010` (Pro Plus upsell PR) should pull tier-block/upgrade-sheet copy verbatim from `content/pricing/pro-plus-tier.md` and `content/upgrade-sheets/pro-plus-projects.md`; both files name the exact `{{pricing.*}}` token shape expected.
- Homepage Plans section (`content/homepage/plans-section.md`) needs a follow-up `biddeed-web` PR to actually land on `/`; P19 UI-audit applies at that point, not here.
- Founder LinkedIn/X posts are drafts; posting itself is a distribution-lane action (`docs/gtm/DISTRIBUTION_LANE.md`) outside this issue's scope — hand to whoever owns that lane, or Ariel posts personally.
- Full fresh-context P12 extraction-test judge pass (SOP procedure, judge ≠ builder) still needs to run via `gtm-validate.yml` once the halt gate closes.
