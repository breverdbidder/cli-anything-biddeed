# 19797 — ARCHITECT TRIAGE for blocked issue #19772 (shard-1: brevard/levy/pinellas/bradford/taylor)

## DoD
```sql
SELECT EXISTS (SELECT 1 FROM public.gold_standard_certifications
                WHERE county_slug = ANY('{brevard,levy,pinellas,bradford,taylor}'::text[]) AND certified)
```
Re-verified live at session start via `gold_standard_certifications` REST (service-role key, PostgREST — direct psql unavailable per known constraint): **FALSE**, all 5 rows `certified=false`. [VERIFIED]

| county | certified | first_certified_at | revoked_at | consecutive_non_gold |
|---|---|---|---|---|
| brevard | false | 2026-06-22 | 2026-07-26 | 506 |
| levy | false | 2026-08-13 | 2026-08-27 | 94 |
| pinellas | false | 2026-07-10 | 2026-09-01 | 26 |
| bradford | false | (never) | — | 593 |
| taylor | false | 2026-06-28 | 2026-07-02 | 593 |

`last_error: none logged` on #19772 matches reality — the prior engineer GHA run (33731084009) concluded `success`; this is a genuine remaining-data dispatch, not a crash/stuck session. [VERIFIED]

## Files touched this session
- `docs/spec/19797.md` (this file)
- No code/schema/data changes made — diagnostic session only (see "why no fix shipped" below).

## Live per-county diagnosis (`pencil_dod_evaluate_county`, re-run this session)

**bradford** (8/10) — B/F FAIL, `verified=0 closed_sold=0`. Confirmed via direct count: `multi_county_auctions` has zero rows with `sold_amount IS NOT NULL` for bradford, and both `tax_deed_outcomes` and `foreclosure_outcomes` have **zero** rows for bradford (`content-range: */0` on all three probes). Hard accrual gate — there is no closed/verified sale in bradford to measure yet (only 5 auctions total, all still pending/upcoming). Not fixable by code. [VERIFIED]

**taylor** (8/10) — same shape: B/F FAIL, `verified=0 closed_sold=0`, zero rows in both outcomes tables (14 auctions total, none closed). Hard accrual gate. [VERIFIED]

**levy** (9/10) — only I fails, 88.9% (`card_complete=40 of 45`). Root-caused down to the exact 5 parcels missing criterion-I's zoning join. Read the live `pencil_dod_evaluate_county` function body (`supabase/migrations/20260810_gold_standard_shard3_lake_clerk_ssot_cd_recognition.sql`, lines 85-96): I requires `parcel_id` (or `tax_account`) present in `v_zoning_gold_standard_card` (sourced from `public.parcel_zones`) with `zone_code IS NOT NULL`, plus address+geo+value non-null. The 5 failing levy parcels — `05775-000-00`, `00881-000-00`, `03467-002-0A`, `17660-000-00`, `03427-002-00` — are confirmed absent from `v_zoning_gold_standard_card` AND absent from the upstream `zoning_assignments` source table under `county='levy'` (checked by stripped-dash parcel_id and substring search; the only hits were false-positive collisions with St. Johns/other counties' unrelated parcels sharing the same numeric string). This rules out the "unbridged data sitting unused" fix pattern that worked for alachua in decision_log id 3143 (14h earlier, same shard, same county) — that session reached the identical conclusion independently: "levy 9/10 (I FAIL 88.9%)... deeper residual" and chose alachua instead as higher-leverage. Two independent sessions agree: no already-ingested data exists to bridge for levy's 5 parcels. Closing this gap requires a fresh GIS/appraiser lookup for 5 specific rural parcels (Levy County Property Appraiser is qPublic/Schneider-hosted, a JS-driven ASP.NET search form — not scriptable via a single WebFetch without session/viewstate handling; attempted, not completed this session). Need 3 of the 5 to flip levy to 43/45=95.6% PASS. [VERIFIED gap, HYPOTHESIS on remediation path]

**pinellas** (9/10) — only I fails, 93.9% (`card_complete=447 of 476`), needs 6 more of the ~29 missing to cross 95%. Not individually root-caused this session (time-boxed after levy's investigation); same class of gap as levy. [VERIFIED metric, UNTESTED root cause]

**brevard** (9/10 on the official scoped scoreboard, run 16698; **worse live**) — official scoreboard (`gold_standard_county_status`, respects `gold_standard_cert_scope`'s Jun-12 denominator freeze — confirmed by grep, that freeze is wired into `gold_standard_loop()` only, NOT into the live `pencil_dod_evaluate_county` RPC, by design: one is the certification-of-record path, the other is an always-live diagnostic probe) shows only I failing (86.0%, 6106/7099). A live unscoped re-run this session shows `auctions_total` has grown to 7491 (from ~7099) and now C also fails (94.4%, matched_clean=7075) while D and I churn between calls (D flipped FAIL→PASS between two calls 10 minutes apart as backfill matching caught up live). This is NOT a new bug — it reproduces, live, the exact "growing denominator vs frozen numerator" dynamic that motivated the Jun-12 `gold_standard_cert_scope` freeze for brevard specifically; the freeze protects the *official* scoreboard used by `gold_standard_certify()`, so brevard's actual certification-relevant state is the scoped 9/10 (I-only fail), not the noisier live numbers. I's gap here (993 records short of 95% on the scoped snapshot) is far larger than levy/pinellas' — not a same-session fix regardless. [VERIFIED]

## Why no fix shipped this session
1. bradford/taylor are structurally unfixable today — zero underlying data exists to satisfy B/F (accrual gate, not a bug).
2. levy/pinellas/brevard are each blocked purely on I, but the shortfalls (5 / ~29 / ~993 parcels respectively) all require **new** zoning data acquisition, not a bridging fix — confirmed for levy by direct query of the source table, corroborated by an independent architect session 14h earlier reaching the same conclusion on the same county. Fabricating zone codes to close the gap is explicitly banned by CANON ("guessed standards = ghost-success, BANNED") and by the Honesty Protocol's 3× penalty on wrong VERIFIED claims — not attempted.
3. Structural gate independent of the above: even a county that reaches 10/10 mid-session cannot certify same-session — `gold_standard_certify()` requires `consecutive_gold >= 2` (two consecutive daily-cycle 10/10 evaluations) plus fresh (<7d) `gold_standard_ultraloop_audit` survived=true rows for **all 10** letters (confirmed via `gold_standard_certifications.revocation_reason` strings on all 5 target counties, e.g. pinellas: `adversarial_survival_5_of_10`). None of the 5 target counties currently has that full-coverage fresh audit trail. Moot here since none reached 10/10 anyway.

**Not a credential/dashboard/permission/spend blocker.** No click-path action changes any of this today. Per the ARCHITECT TRIAGE MANDATE step 5, the BLOCKED/Approve comment format is reserved for genuine human-action blockers; this is ongoing multi-day data-completion work already scheduled via the existing daily 08:00Z/16:00Z/00:00Z SUMMIT wave cadence described in #19772's own body. No BLOCKED comment posted; a findings comment was posted instead (Honesty V3 tagged), matching precedent (decision_log ids 3108, 3109, 3143 and 15+ prior architect-triage sessions on this campaign).

## Recommendation for next session
Highest-leverage target across this shard: **levy** and **pinellas**, both single-letter-I gaps, both far smaller than brevard's 993-parcel gap. A session with GIS-scraping tooling (not just diagnostic REST access) should target Levy County Property Appraiser (qPublic/Schneider, `qpublic.schneidercorp.com/Application.aspx?App=LevyCountyFL`) for the 5 named parcels above, and separately root-cause pinellas' ~29 missing parcels the same way, before attempting a `gold_standard_loop()` + `gold_standard_certify()` double-cycle (the alachua id-3143 playbook).

## Verification evidence (session end)
DoD SQL re-executed at session end: `[]` (empty result set) → **still FALSE**, unchanged. Zero DB writes made this session (one incidental, harmless read triggered a Telegram post as a side effect of calling `gold_standard_status_report()` while probing available RPCs — not a data mutation, noted for transparency).

Full diagnosis logged to `public.decision_log` (`decision_type=triage`).
