# Spec — issue #19723 — Gold Standard shard-3: lafayette, suwannee, liberty

## Intent reference
None at docs/intent/19723.md — issue body was the only brief (dispatch 674dae7f-1e08-4e3d-b52d-fd0276214d89, loop run 16227).

## What was built
No code or schema changes. This session ran an ULTRALOOP fan-out-and-adversarial-verify
audit (Workflow tool, 3 investigator + 3 refuter subagents, per CLAUDE.md ULTRALOOP
PROTOCOL) against the shard's 3 failing letters and reconfirmed all 3 as pre-existing,
extensively-documented structural ceilings, not fixable data-quality bugs. No writes
were made to `multi_county_auctions`, `foreclosure_outcomes`, `tax_deed_outcomes`, or
`pencil_dod_evaluate_county`.

Files touched: this file (`docs/spec/19723.md`) only.

RPCs called (read-only): `public.pencil_dod_evaluate_county('lafayette'|'suwannee'|'liberty')`,
run at session start and session end — byte-identical results (H's freshness metric drifted
by the expected ~1h of elapsed wall-clock time; no other field changed).

Rows written: 5 new rows in `public.gold_standard_ultraloop_audit` (ids 20488-20490,
20515-20516) — audit trail only, per ULTRALOOP PROTOCOL step 7 (certify gate requires a
survived=true row per letter within 7 days). `public.gold_standard_campaign` row (id 5560,
dispatch_id 674dae7f-...) updated per the mandatory close-out protocol.

## SSOT bindings used
- `public.multi_county_auctions` (read-only) — 4 lafayette rows, 35 suwannee rows, 1 liberty row, queried directly by case_number/parity_status/parity_source.
- `public.pencil_dod_evaluate_county(text, timestamptz)` (RPC, read-only) — canonical A-J evaluator, migration `20260810_gold_standard_shard3_lake_clerk_ssot_cd_recognition.sql` is the live definition.
- `public.gold_standard_ultraloop_audit` (insert) — 5 rows, see above.
- `public.gold_standard_campaign` (update) — 1 row, id 5560.

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| lafayette C (matched_clean >=95%) | Reconfirmed structural ceiling, still FAIL | VERIFIED — 3/4 rows clean; the 4th (`25000056CAAXMX`) is live-confirmed `Status: cancelled` on lafayetteclerk.com foreclosure-sales page (fetched this session). Canon (`pencil_dod_evaluate_county`) correctly excludes `CLERK_SSOT_CANCELLED` from matched_clean — documented fleet-wide in `GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md`, independently reconfirmed for lafayette on 2026-08-26/28/29/31 and again this session. |
| suwannee C (matched_clean >=95%) | Reconfirmed structural ceiling, still FAIL | VERIFIED — 28/35 rows clean; the 7 excluded rows (4694,4741,4676,4681,4744,4693,4672, all `CLERK_SSOT_CANCELLED`/`redeemed`) independently confirmed absent from the live Suwannee Clerk tax-deed schedule PDF (`Schedule-08.28.2026.pdf`, fetched and re-fetched independently twice this session, byte-exact match). Case 4741 is newly grown into the blocking set since the last full per-case check (6→7) and was individually verified for the first time this session. |
| liberty A (dual-lane coverage) | Reconfirmed structural ceiling, still FAIL | VERIFIED — libertyclerk.com/courts/tax-deeds/ fetched live this session, still reads "There are no properties on the list of tax deeds at this time," unchanged wording across 8+ independent sessions since 2026-07-05. fc=1 td=0; A requires both >0. |
| liberty B (verified independent outcomes) | Reconfirmed structural ceiling, still FAIL | VERIFIED — sole case 24-CA-22 (sale date 2026-07-21, now 43 days past) has aged off the public calendar. Both candidate independent-outcome sources (Civitek OCRS, myfloridacounty.com ORI) remain live Cloudflare-Turnstile-gated on search-submit (a 2026-09-01 Playwright attempt against the same two sites also failed to clear Turnstile). Firecrawl account credits fleet-wide exhausted (-6 remaining, checked live this session). No CAPTCHA bypass attempted per HARD GUARDRAILS. |
| liberty F (tier1 sold-amount) | Reconfirmed structural ceiling, still FAIL | VERIFIED — direct consequence of B; no independent sold-amount exists to promote. `promote_tier1_from_outcomes()` hourly cron (not modified) will pick this up automatically once/if B is ever unblocked. |
| lafayette/suwannee/liberty D,E,F(lafayette/suwannee),G,H,I,J | Unchanged, still PASS | VERIFIED — `pencil_dod_evaluate_county` re-run at session end for all 3 counties, byte-identical to session-start baseline (H's hours-since-last-seen metric moved by the expected ~1h of wall clock; no other field changed). No regression. |

## Negative test result
Checked for a genuinely new lever on all 3 blockers (not just re-asserting old findings):
- lafayette: fetched both the foreclosure-sales AND tax-deed pages live, confirmed the
  cancelled case has not moved lanes and no 5th row has appeared.
- suwannee: fetched the *current* schedule PDF (rolled from `08.24` to `08.28` since the
  last documented check), individually verified the newly-added 7th cancelled case (4741)
  for the first time — it is genuinely absent, not a stale carry-forward assumption.
- liberty: fetched both clerk pages live, ran a fresh WebSearch for the case/plaintiff name
  (no new public disclosure found), confirmed Firecrawl credits are still negative (-6, a
  different value than every prior check, confirming this was a live API call, not reused
  text), and did not re-attempt the known Turnstile gates (guardrail compliance).
All three remain unchanged from the session-start baseline.

## Deviations from intent
None — worked only the 3 assigned counties, made no writes to protected tables, did not
touch shared migrations/crons/evaluators, did not run `gold_standard_loop()`/`certify()`
(other shards were concurrently in-flight per PARALLEL-FLEET RULES — reported per-county
evaluations only, as instructed).

One adversarial-review finding is worth flagging explicitly: the ULTRALOOP refuter for
lafayette-C initially returned `survived=false`, correctly catching that the investigator's
evidence cited a case number (`25000119CAAXMX`) not previously documented anywhere in this
repo's prior lafayette session reports. This was resolved, not overridden — this orchestrating
session had independently queried `multi_county_auctions` directly via REST *before* launching
the workflow and already held the exact 4-row composition (including `25000119CAAXMX` as a
real `PARITY_OK` row) as primary-source evidence. That resolution is logged inline in the
`gold_standard_ultraloop_audit` row (id 20515) so it is auditable independent of this doc.

## Open items / handoff
All 3 letters (lafayette-C, suwannee-C, liberty-A/B/F) are now backed by fresh
`gold_standard_ultraloop_audit` rows (2026-09-02) and are within the 7-day certify-gate
freshness window. No actionable next step exists for any of them without either:
- a fleet-level canon decision on the C/D CLERK_SSOT_CANCELLED tension (Option A/B/C in
  `GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md` — affects lafayette
  and suwannee identically, and every other county running clerk_ssot reconciliation); or
- Firecrawl credits being replenished fleet-wide, or a sanctioned CAPTCHA-solving
  integration being authorized (affects liberty B/F, and every other shard blocked on the
  same two Turnstile-gated sources).

Both are fleet-level/owner-level decisions flagged by multiple prior sessions already —
not something a single-shard session can resolve. Future sessions dispatched to this exact
shard should not re-run identical daily rechecks of a structurally unchanged ceiling; the
only useful trigger is one of the two items above changing.
