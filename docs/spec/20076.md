# Spec — issue #20076 — P0 STOP: restore Ariel's seven-colour canon, freeze the grader

## Intent reference
None at docs/intent/20076.md — issue body was the only brief.

## What was built
No new features. Pure revert/restore work across two repos.

**cli-anything-biddeed** (commits a058cf78, 692f9f9e):
- `scripts/ui-audit/audit.py` — CANON restored byte-for-byte to commit 7950391 (the seven
  colours: `#ffffff #e8f4fc #222222 #002a54 #cccccc #0073cf #005daa`, comment included).
  `git diff 7950391 -- scripts/ui-audit/audit.py` is empty.
- `src/worker.js` — reverted 9cb2490e ("fix(brand): convert canonical worker to house
  palette") and eb819f31 ("fix(worker): improve shell and report contrast"), restoring the
  state as of 79503919 (the prior revert of 449c00c1). The unrelated /support +
  /admin/support routing and /contact 301 from 8bb615a7 were preserved — confirmed via
  `git diff 79503919 HEAD -- src/worker.js` showing only that routing diff remained.
- `scripts/ui-audit/apply_worker_house_brand.py` — deleted (the script that applied the
  tampered palette to worker.js; introduced by 9cb2490e).
- Second tamper caught mid-fix: commit 0fa614aa ("fix(worker): improve counties contrast
  and drawer containment") landed on main at 23:58:32 UTC, one minute after a058cf78,
  and reintroduced `#FBFAF7 #F8D4C5 #8F4028 #7A3424 #1F1B16 #DDD5C9` into the /counties
  page and mobile nav drawer. Reverted in 692f9f9e.

**biddeed-web** (commit a461475, cloned to /tmp per M5 since it's a separate repo):
- `lib/design-tokens.ts` — LIGHT block + comment restored byte-for-byte to c6c102b.
- `scripts/palette-gate.mjs` — restored byte-for-byte to d6a5687 (the reference gate named
  in the issue), undoing CANON_LIGHT rewrites from c4b59c3/f04abe5/111fbf0/78f9a4d.
- `app/globals.css` — rebuilt from c6c102b (last known-good) with only the legitimate
  4caa52f mobile touch-target/font-family Mapbox fix replayed on top via `git apply`.
  None of the contrast-workaround CSS from f04abe5/111fbf0/78f9a4d was kept — it existed
  only to compensate for the retired palette.
- `scripts/ui-audit/critical-gates.mjs`, `scripts/ui-audit/diagnose-critical-offenders.mjs`,
  `tests/e2e/critical-ui-gates.spec.ts` — canon color sets swapped from the tampered
  16-colour cream/terracotta list to the real seven colours (these files were created
  after f04abe5 had already broken the palette, so they baked in the wrong canon from
  the start via c97213a/78f9a4d).

No migrations. No RPCs/views touched. No schema changes.

## SSOT bindings used
None — this was a pure code/CSS revert. No Supabase tables read or written.

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| 1 | Restore audit.py CANON to exactly the seven from 7950391, comment included | VERIFIED | `git diff 7950391 -- scripts/ui-audit/audit.py` → empty, in a058cf78 |
| 2 | Revert biddeed-web token change toward cream/terracotta; palette-gate.mjs as reference | VERIFIED | biddeed-web@a461475; `node scripts/palette-gate.mjs` → "0 colour literals outside the token files; token files carry only the canon values" |
| 3 | Re-run ui-audit.yml and post the table | VERIFIED | 3 runs posted to issue #19829: 34068056569 (77 red, /counties tamper still live), 34068484157 (50 red, biddeed-web fixed), 34068688377 (54 red, final, both tampers reverted) — comment https://github.com/breverdbidder/cli-anything-biddeed/issues/19829#issuecomment-5563238017-adjacent thread |
| 4 | /county/brevard and /county/miami_dade no longer report Ariel's canon as off-palette | VERIFIED | Final table (00:07 UTC): both routes ✅ PALETTE ✅ CONTRAST at 1440 and 390 viewports |
| 5 | No further edits to audit.py CANON, gate thresholds, or allowed font set | VERIFIED | Only change to audit.py was the CANON revert itself; no other edits made |

## Negative test result
Checked repo-wide (`grep -rniE` over `.ts .tsx .mjs .css .sh` in biddeed-web, excluding
node_modules/.git) for any of the 16 retired cream/terracotta hex values after the fix —
zero matches remain in either repo's tracked source.

Checked that the legitimate, unrelated work landed during the tamper window survived the
revert: 8bb615a7 (support/contact routing) in cli-anything-biddeed, and in biddeed-web the
support-ticket feature (f1af985), Clerk appearance hardening (1f88a30 lineage → kept via
ConditionalClerkProvider token-based diffs, not reverted), mobile-control hardening
(4caa52f, 29c1549, ec86ae7), and SEO metadata additions on sign-in/sign-up — none of these
touched hex/HSL colour literals outside the token files, confirmed by diffing each commit
individually against app/globals.css.

## Deviations from intent
The issue's required steps named only `scripts/palette-gate.mjs` in biddeed-web as "the
reference" without asking for edits there — but that file had itself been tampered with
(CANON_LIGHT rewritten in the same three commits), so restoring it was necessary to make
"the reference" actually true again, not a scope expansion.

The issue didn't ask about `scripts/ui-audit/critical-gates.mjs` /
`diagnose-critical-offenders.mjs` / `tests/e2e/critical-ui-gates.spec.ts` in biddeed-web —
these were discovered while verifying no stray tampered-canon values remained anywhere in
that repo. Fixed them because they are graders that had baked in the wrong palette from
birth (same failure mode the issue is about), not because the issue named them.

The 0fa614aa revert (692f9f9e) was not requested by the issue text — it didn't exist when
the issue was filed. It's the same fix pattern applied to a second live instance of the
exact problem, caught by the required re-run in step 3 still showing one off-palette hit
after the named fixes were in.

## Open items / handoff
**Needs Ariel's attention, not another revert:** something pushed 0fa614aa directly to
main one minute after this session's first fix commit, using the same "fix(worker):
improve contrast" framing as the original tamper commits and reintroducing the identical
retired palette. This session has no visibility into what produced it (a still-running
AUTOLOOP iteration? a stale parallel agent session? a scheduled job?). If it recurs, the
next session should check `git log --since="1 hour ago" --all` across both repos before
touching anything else, and should treat repeated recurrence as grounds to look for a
running process/workflow rather than reverting in a loop.

Residual (not part of this issue's scope, left untouched): CONTRAST still red on
/radar, /radar?view=map, /auctions (1 bad text node each, both viewports) — pre-existing,
not part of the cream/terracotta tamper pattern. LAYOUT/TYPE reds (smallTap, tiny text)
and SEO reds (meta description length, /discover and /support missing single-H1) are
pre-existing and unrelated to this incident. Two mobile rows (/sign-in, /sign-up) returned
HTTP 429 on the final re-run — Clerk rate-limiting from running the full audit three times
in ~10 minutes, not a code defect; expect green on the next natural run.
