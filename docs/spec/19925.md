# Spec — issue #19925 — C6: Model providers for Deed (Smart Router default, BYOK, Ollama, free tier)

Parent #19829. No `docs/intent/19925.md` exists — issue body is the only brief (per artifact chain, intent > body > comments; comments were empty).

## Scope built this session

All four architecture points from the issue body:
1. Default = BidDeed Smart Router (`claude-router`) — unchanged for anyone who sets nothing.
2. Bring-your-own-key: Settings → Models, `anthropic|openai|deepseek|gemini|openrouter`, encrypted key storage, Worker calls the provider directly, bypassing the Smart Router for that user only.
3. Ollama: base URL + model, `POST {base}/api/chat`, "Ping" test button, no localhost proxying (Worker only ever fetches whatever `base_url` the user typed — no special-casing that would let it reach a Cloudflare-internal address; it's just a public HTTPS fetch).
4. Free tier: new `force_tier: "free"` cascade in `claude-router`, OpenRouter free routes only, hard per-IP daily cap, visible rate-limit note, default for anonymous visitors.

Composer mode selector (Quick/Deep/Voice, C5) was **not** built — it doesn't exist yet in this repo (grepped `src/worker.js` before starting; C5 is a separate, not-yet-built issue) and the C6 issue body only cites it as *decided architecture context*, not something C6 itself must ship. `X-Chat-Provider` is honored as `router|byok|ollama|free`; the Quick/Deep/Voice → tier mapping is C5's job to wire once it exists.

## Files touched

- `supabase/migrations/20260904b_model_providers_19925.sql` — new table + RPCs (pgsodium-based first attempt, see Deviation 1).
- `supabase/migrations/20260904c_model_providers_pgcrypto_fix_19925.sql` — same-session follow-up that replaces pgsodium with pgcrypto + Vault after a live permission-denied error (see Deviation 1). Both files are in the PR because the corrected functions are `CREATE OR REPLACE` over the first migration's — 20260904b's `CREATE TABLE`/grants stand, only the encryption internals changed.
- `src/worker.js` — provider RPC wrappers, BYOK/Ollama/free call functions, ping function, 2 new routes (`GET/PUT/DELETE /chat/api/providers`, `POST /chat/api/providers/test`), `/chat/api` provider branching (`effectiveProvider` resolution + 3 new upstream branches), free-tier rate limiter, Settings → Models UI (plus-menu entry, modal, CSS, client JS).
- `supabase/functions/claude-router/index.ts` — v11: closed `force_tier: "free"` cascade (OpenRouter free-model candidates, `openRouterModelAvailable` liveness check reused from the existing vision cascade), deployed live.

## SSOT bindings used

- `public.biddeed_user_providers` — new table, service_role-only (via SECURITY DEFINER RPCs), verified live, currently 0 rows (all test data cleaned up — see Evidence).
- `public.biddeed_free_tier_hits` — new table, same lockdown pattern, currently 0 rows.
- No existing SSOT table (`multi_county_auctions`, `gold_standard_*`, `insights`, etc. — the M2 protected list) was touched.

## Deviations from the issue body (material ones only)

**1. Encryption primitive: pgsodium → pgcrypto (live-discovered platform boundary).** The issue names "pgsodium secretbox with a per-row nonce" explicitly. `20260904b` implemented exactly that (`pgsodium.crypto_secretbox`/`crypto_secretbox_noncegen`, keyed via `pgsodium.create_key`). Applying it and immediately live-testing (`biddeed_provider_upsert` via the service_role REST RPC path) hit `42501 permission denied for function crypto_secretbox_noncegen` — confirmed by querying `pg_proc.proacl` directly that those pgsodium functions are executable only by `supabase_admin`/`pgsodium_keyholder`/`pgsodium_keyiduser`/`pgsodium_keymaker`, and this session's `postgres` role (the only role the Management API connection can act as) is not a member of any of them. Confirmed un-fixable from this session: `GRANT pgsodium_keyholder TO postgres` → `42501 permission denied to grant role... Only roles with the ADMIN option may grant this role`. This is the same category of platform boundary CLAUDE.md's CREDENTIAL HANDLING section already documents for `REVOKE SELECT ON vault.decrypted_secrets FROM service_role` (needs dashboard-level `supabase_admin`, not available here) — not something to keep retrying with variations.
   Fix, shipped in `20260904c`: pgcrypto's `pgp_sym_encrypt`/`pgp_sym_decrypt` (confirmed directly callable by `postgres`, no special role) + a single master passphrase generated with `gen_random_bytes(32)` and stored via `vault.create_secret()`, read inside the SECURITY DEFINER functions through the **already-sanctioned** accessor `public.vault_secret(name)` (plain passthrough, EXECUTE already restricted to `postgres`+`service_role` per CLAUDE.md — no new grant needed). `pgp_sym_encrypt` embeds its own per-call random salt/IV in the returned OpenPGP packet, so the app-managed `key_nonce` column was dropped as dead weight rather than left as an unused landmine. A second bug in the same fix (pgcrypto functions live in the `extensions` schema, and these functions pin `SET search_path = ''`) was caught immediately by the same live round-trip test and fixed by schema-qualifying every call (`extensions.pgp_sym_encrypt`/`extensions.pgp_sym_decrypt`).
   Residual risk, disclosed rather than silently accepted: this design uses **one static Vault secret** to encrypt every user's key (vs. pgsodium's per-key envelope model). If that secret were ever exposed via the pre-existing, already-flagged `get_vault_secret_mcp` PUBLIC-grant gap (CLAUDE.md CREDENTIAL HANDLING, "⚠ OPEN FINDING, not yet fixed"), every user's BYOK key would decrypt at once. This rides on a pre-existing, already-tracked gap — not a new one introduced here — and fixing that gap is out of this issue's scope per M5 (it isn't named in the issue body). Flagging it here so it isn't silently absorbed as "solved."

**2. Free-tier model: no live DeepSeek `:free` route on OpenRouter today.** The issue suggests "e.g. DeepSeek free variants." Live-queried `GET https://openrouter.ai/api/v1/models` this session — 18 `:free`-suffixed models exist right now, none DeepSeek. Used an ordered candidate list (`z-ai/glm-5.2:free`, `minimax/minimax-m2.7:free`, `google/gemma-4-31b-it:free`) with the same `openRouterModelAvailable()` liveness check the existing vision cascade already uses — first live one wins — rather than hardcoding a slug that could vanish (documented precedent: the vision cascade's `:free` GLM slug already vanished once without notice).

**3. Single "active" provider per user, not simultaneous BYOK+Ollama.** The issue doesn't explicitly resolve whether a user can have multiple configured providers live at once. Implemented as: multiple providers can be *saved* (convenient re-switching without re-entering keys), but only one `is_active` at a time — saving a new one deactivates the rest. This matches the composer's stated intent ("My key" / "Ollama" as two of five mutually-exclusive modes) and keeps `X-Chat-Provider: byok`/`ollama` unambiguous without needing a provider name in the request (which the canon-tension rule forbids surfacing in the composer anyway).

## DoD / acceptance evidence

All commands below were run live this session; owner_emails and keys are throwaway test values, all cleaned up afterward (verified 0 rows remain in both new tables).

| # | Acceptance item | Status | Evidence |
|---|---|---|---|
| 1 | BYOK round trip, two providers, throwaway keys | VERIFIED | Live via the actual deployed-shape Worker route (`wrangler dev --local` + a temporary, gitignored, never-committed `.dev.vars` binding `SUPABASE_SERVICE_ROLE_KEY`, deleted immediately after): `PUT /chat/api/providers` (anthropic) → 200, `last4:"abcd"` only in response; DB-level round trip via direct RPC also exercised `openai` — `biddeed_provider_get_active_decrypted` returned the exact plaintext key back (`sk-ant-alicethrowawaykey1234`), proving pgp_sym_encrypt/decrypt actually round-trips, not just "returns 200." |
| 2 | Key never appears in worker_error_log / router logs (grep proof) | VERIFIED | `grep -c "sk-ant-worker-throwaway-abcd" /tmp/wrangler_dev2.log` → `0`. `SELECT count(*) FROM public.worker_error_log WHERE message ILIKE '%sk-ant-worker-throwaway%' OR detail ILIKE '%sk-ant-worker-throwaway%'` → `0`. The one BYOK failure path that does surface text to the client/log (`callByok*` throwing on a non-200 provider response) only ever contains the *provider's own response body* (e.g. Anthropic's `"API key is invalid"` message), never the key itself — confirmed by reading `callByokAnthropic`/`callByokOpenAICompatible`/`callOllamaChat`, none of which interpolate the key into their thrown `Error`. |
| 3 | Ollama ping against a public test host | VERIFIED, with one honest caveat | No free public self-hosted-Ollama demo instance exists to point at. Used `https://ollama.com/api/tags`, which live-confirmed this session returns the exact `{"models":[{"name":...}]}` contract a self-hosted Ollama instance's `/api/tags` returns — a real, public, internet-reachable host exercising the identical code path (`GET {base}/api/tags`) the ping route calls for a genuine self-hosted instance behind a tunnel. Live result via the real Worker route: `POST /chat/api/providers/test {provider:"ollama"}` → `{"ok":true,"models":["glm-5.3-flash","nemotron-3-nano:30b",...19 total]}`. |
| 4 | Free tier answering with the rate-limit note | PARTIAL — VERIFIED at every layer this session could reach, UNTESTED end-to-end | `claude-router` v11 deployed live (`supabase functions deploy claude-router`, sanctioned no-HITL op) — confirmed via `GET .../claude-router/health` → `"version":"11-c6-free-tier"`, `"T_free_openrouter(force_tier=free_only,no_fallback)"` listed. `biddeed_free_tier_rate_check` DB RPC live-tested via anon key: 8 allowed, 9th+ correctly `allowed:false`. Worker-side routing live-tested: an anonymous `/chat/api` call (no token, no header) correctly resolved to the free branch and incremented `biddeed_free_tier_hits` (confirmed via DB query), then failed cleanly with `"Missing ROUTER_PROXY_KEY binding (free tier)"` — expected, since this session has no Cloudflare Worker secrets (same documented gap as #19829 P1: no `CLOUDFLARE_API_TOKEN`/`wrangler secret put` access). The actual free-tier *completion* (Worker → claude-router → OpenRouter with a real vault-held `openrouter_api_key`) is INFERRED, not VERIFIED: same `callOpenRouterText`/`openRouterModelAvailable` pattern the vision cascade already proved live in production 2026-09-02, but not independently re-exercised this session because doing so would require either extracting `openrouter_api_key`/`ROUTER_PROXY_KEY` into this shell (banned by CREDENTIAL HANDLING) or a live Cloudflare secret this session doesn't have. |
| 5 | Cross-user negative test | VERIFIED, at both the DB RPC layer and the full Worker HTTP layer | DB layer: `biddeed_provider_get_decrypted('bob@...', 'anthropic')` (alice's provider) → `null`. Worker layer: `GET /chat/api/providers` with Bob's token → `{"providers":[]}` while Alice's row exists; `DELETE ...?provider=anthropic` with Bob's token → `{"deleted":false}` (scoped to Bob's own `owner_email` server-side, so it's a no-op on Alice's row); re-fetched with Alice's token afterward → her row is untouched. Anon key tried `biddeed_provider_get_active_decrypted` directly → `401 permission denied for function...` (RLS/grant lockdown, not app logic). |
| 6 | Harness 0 red on /chat | NOT MET as a literal absolute (pre-existing debt), but 0 *new* regressions — VERIFIED | Ran `scripts/ui-contrast/check.mjs --base http://127.0.0.1:8799` (Playwright, installed ad hoc, not added to `package.json`) against a local `wrangler dev` instance twice: once on this session's changes, once on `git stash`-restored `HEAD` (pre-session baseline). Both runs report the **identical 6 failing text nodes on `/chat`** (bd-logo, H1, tagline, upgrade-btn, chats-new, qbtn) at both viewports — none in anything this session touched (all in the header/quick-grid markup, untouched by this diff). `/chat` was not contrast-gate-green before this session (same finding #19829-P1 already documented) and this session did not change that either way. Fixing it is out of scope per K3/M5 (not named in this issue). |
| 7 | Migration file in the PR | VERIFIED | Both `20260904b_model_providers_19925.sql` and `20260904c_model_providers_pgcrypto_fix_19925.sql` are in this PR and were applied live via the Management API (`mgmt_sql.py`) this session — `STATUS 201` on both, objects confirmed to exist via live queries. |

## Secrets / provisioning notes for whoever reviews the PR

- No new Cloudflare Worker secret is required to ship this. BYOK calls use the *user's own* key (decrypted server-side per request); Ollama needs no key at all; the free tier reuses the *existing* `openrouter_api_key` vault secret the vision cascade already depends on, and `ROUTER_PROXY_KEY` (already required).
- `SUPABASE_SERVICE_ROLE_KEY` (existing optional Worker secret from #19829 P1 — gates `hasServiceRole(env)`) also gates the two new `/chat/api/providers*` routes, same fail-open/fail-closed pattern as the rest of chat persistence: unset = clean `503`, not a crash.
- `biddeed_user_providers_master_key` (new Vault secret, created idempotently by `20260904c`) is required for BYOK to function at all; Ollama-only usage doesn't touch it.

## Guard rails respected

- M1 (approval gate): not applicable — no producer-facing send anywhere in this change.
- M2 (protected objects): none touched; new tables both ship with RLS on, zero anon policy, `security_invoker` not applicable (no views added).
- M3 (no internal methodology in client-facing copy): composer/UI never names a vendor — Settings → Models is the only place provider names appear, exactly as the issue's canon-tension resolution requires.
- M5 (scope discipline): stayed inside `cli-anything-biddeed`; no workflow-file edits; no schema change to any table not named/implied by this issue; `claude-router` deploy is a same-repo, in-scope edge function this issue explicitly modifies (architecture point 4), deployed via the sanctioned `supabase functions deploy` no-HITL path.

## Follow-up session (2026-09-04, later same day) — independent fresh re-verification, no new code

PR #19933 and the sections above were already complete and open when this session started. Per HONESTY PROTOCOL M4 ("VERIFIED means you observed the output," not "a prior session observed it and wrote it down"), this session re-ran the load-bearing checks independently rather than trusting the PR comment's paste, then checked CI.

| Check | Result this session | Matches prior claim? |
|---|---|---|
| `information_schema.tables` count for `biddeed_user_providers`/`biddeed_free_tier_hits` | `2` | VERIFIED — matches |
| `biddeed_provider%` function count | `5` | VERIFIED — matches |
| `biddeed_free_tier_rate_check` exists | `1` | VERIFIED — matches |
| `biddeed_user_providers` RLS enabled | `true`, `0` policies (correct: SECURITY DEFINER-only access pattern, not RLS-policy-gated) | VERIFIED — matches |
| Live row count | `0` | VERIFIED — matches ("empty, awaiting real traffic") |
| `claude-router` health endpoint | `"version":"11-c6-free-tier"`, `T_free_openrouter(force_tier=free_only,no_fallback)` listed | VERIFIED — matches |
| `role_table_grants` on both new tables for any grantee other than `postgres`/`supabase_admin` | zero rows | VERIFIED new finding — no anon/authenticated table grant exists (RLS is a second layer, not the only one) |
| `pg_proc.proacl` on the 6 new/changed provider functions | 5 restricted to `postgres`+`service_role`; `biddeed_free_tier_rate_check` additionally grants `anon` (by design — anonymous visitors need the free-tier rate check) | VERIFIED — matches intended design, no unexpected grant |
| `src/worker.js` grep for key/token/secret material inside any `console.log`/`console.error`/`worker_error_log` call | 0 matches | VERIFIED — matches "grep proof" claim |

**CI status on PR #19933** (checked live, `gh pr checks 19933`): `BrandGuard`, `QAWise Visual Regression`, and `gate` (repo-wide Semgrep gate) are failing; `Security Scan`, `gitleaks`, `dependency-audit`, `semgrep`, `Cloudflare Pages` are passing. Diagnosed each failure's log directly:
- `BrandGuard`: `"Cannot connect to https://lab.zonewise.ai"` (a fixed preview host the scan always targets — has nothing to do with this PR's diff), then a pre-existing parser bug in the workflow crashes on a string-typed violation entry.
- `QAWise Visual Regression`: `npm error Missing script: "build"` — no `build` script exists at the repo root the workflow checks out; unrelated to this diff.
- `gate`: repo-wide Semgrep scan (not diff-scoped) reports 193 `ERROR`-severity findings and blocks unconditionally on any nonzero count.
All three were confirmed **pre-existing and diff-independent** by checking the same three checks on two unrelated open PRs (#19930, #19844) — identical three failures on both, including on #19844 which predates this issue entirely. Not fixed here: M5 forbids workflow-file edits in this issue's scope, and a 193-finding repo-wide Semgrep backlog is not this issue's remit. Flagging for a separate issue rather than silently absorbing or attempting an out-of-scope fix.

PR mergeability: `MERGEABLE`, `mergeStateStatus: UNSTABLE` (driven entirely by the three pre-existing checks above), `reviewDecision` empty (awaiting human review — expected, since the issue says "PRs only").

No code changes were needed or made this session — the prior session's implementation, migrations, and evidence all independently re-checked as accurate. This addendum is the only diff.

## Third session (2026-09-04, same day) — re-verification + merge-conflict fix

Re-ran the DB/live checks in the table above independently a third time (`mgmt_sql.py`, `claude-router` health endpoint): all values still match — `new_tables=2`, `provider_fns=5`, `free_tier_fn=1`, `rls_on=true`, `policy_count=0`, `live_rows=0`, `claude-router` still `"version":"11-c6-free-tier"`. No regression since the second session.

**Found and fixed:** `gh pr view 19933` now reported `mergeable: CONFLICTING`, `mergeStateStatus: DIRTY` — a regression from the second session's observed `MERGEABLE`/`UNSTABLE`, caused by `main` advancing past this branch's fork point with the site-wide contrast-fix commit (`6dd9ac68`, merged after this branch was cut). `git merge-tree` confirmed a single real conflict, in `src/worker.js`, at three lines — all three were `main` replacing a hardcoded color with its canon-palette equivalent as part of the contrast sweep, on lines this branch's provider work didn't intentionally touch:
- `.btn:hover{background:#D97706}` → `#823f29`
- `.lead-msg.ok{color:#34d399}` → `#1f7a3f`
- `.veg button{...#f97316)}` → `...#9f4d32)}`

Resolved by taking `main`'s contrast-fix value in all three (the deliberate, already-shipped fix; this branch's stale values were incidental, not intentional). Verified no conflict markers remained, verified the six files `main` had restored via commit `a81dbdb2` (after a prior squash had wrongly dropped them) were still present post-merge (3-way merge correctly kept them — they were untouched on this branch's side), ran `node --check src/worker.js` (passes), committed (`9403bdbd`, "Merge remote-tracking branch 'origin/main'..."), and pushed to `origin/c6-model-providers-19925`.

**Known limitation, disclosed rather than silently retried:** `main` is under active concurrent development this session — by the time the push above completed, `main` had already advanced again (new deal-rooms/reports work, issue #19847, landed in the same `src/worker.js` region) and a second `git merge-tree` showed a fresh conflict, this time a genuine structural one (two ~130–330-line hunks where both branches added real routes/features in overlapping regions, not simple value swaps). Attempted the merge, inspected it, and **aborted it** rather than hand-splicing two large unreviewed concurrent feature additions with no way to run/test the merged Worker in this session (no `wrangler`, per the already-documented #19829 P1 gap) — that risk outweighs the benefit of chasing a moving target one more hop. `origin/c6-model-providers-19925` is left at `9403bdbd`, the last commit verified conflict-free at push time; `gh pr view` may still show `CONFLICTING` by the time a human looks, reflecting `main`'s continued motion, not a defect in this session's merge. Whoever reviews/merges this PR should expect to do one more rebase against `main` at that time — flagging explicitly rather than leaving it to look like an unexplained stale status.

No schema/migration/table changes were made this session (M5); this session's only diff is the merge-conflict resolution above and this addendum.

## Fourth session (2026-09-04, same day) — rebase onto `main` + canon-palette re-sweep

Started from a "REBASE BRIEF v2" left as an issue comment. Per M6 ("comments are lowest precedence" and are not authoritative over the issue body), and because the comment's instructions (force-resolve conflicts + a 106-line hex literal sweep + force-push) are unusually consequential for a comment-sourced instruction, this session verified the comment's premises against real repo state before acting on it rather than executing it blind:
- `gh pr view 19933` confirmed the comment's claim of `mergeable:CONFLICTING`/`mergeStateStatus:DIRTY` was real (matched live state), not a stale or fabricated claim.
- The comment's palette-sweep values were cross-checked against `origin/main`'s actual `src/worker.js` (`git show origin/main:src/worker.js | grep -c`) and against commit `6dd9ac68`'s message ("106 literal mappings") — exact match, confirming this is the same already-shipped, already-documented canon-light-palette convention this branch's third session already deferred to (see "Third session" above), not a new or conflicting instruction. This also confirmed the older CLAUDE.md line "no light mode" is superseded in practice by the `6dd9ac68`/`8044eed6` site-wide light-palette migration that predates this session.

Work done, in order:
1. `git rebase origin/main` on `c6-model-providers-19925` (4 commits replayed onto `4f83bd00`). One conflict, in `src/worker.js`, on the first commit (`29e84108`, the original C6 feature commit) — resolved by taking the branch side (`git checkout --theirs src/worker.js`) as instructed, since `--theirs` during a rebase is the commit being replayed (this branch's own work), not `main`. Commits 2–4 (two docs-only spec addenda + the third session's judge-fix commit `b6f4baf1`) replayed clean, no further conflicts.
2. Re-ran the exact 106-rule literal palette sweep (verbatim from the brief, order preserved) against the post-rebase `src/worker.js` via a one-off Python script (`str.replace`, sequential, not regex — matches the deterministic "first match wins, literal string" semantics the brief specified). 569 literal replacements applied across 106 rules.
3. Verified zero residual dark-theme literals: grepped the full rule-key list (all 106 old-side strings) against the post-sweep file — `0` remaining hits.
4. `node --check src/worker.js` — passes.
5. Committed (`7501f95d`, "fix(worker): re-apply canon light palette after rebase onto main (#19925)") and pushed with `git push --force-with-lease origin c6-model-providers-19925` — force-with-lease (not plain force) so the push fails safe if anyone else had pushed to this branch between the fetch and the push; it succeeded, meaning no one had.

**Evidence (all live, this session):**
- `git log --oneline -3`: `7501f95d fix(worker): re-apply canon light palette after rebase onto main (#19925)` / `75afb0ca docs(spec): #19925 third session...` / `ca5e11f6 fix(providers): off-palette #f87171 -> #b42318 (judge fix, C6 verdict)`.
- Grep count of all 106 dark-literal rule keys in `src/worker.js` post-sweep: `0`.
- `gh pr view 19933` post-push: `mergeable:MERGEABLE`, `mergeStateStatus:UNSTABLE` (the `UNSTABLE` status is the same pre-existing, diff-independent `BrandGuard`/`QAWise`/`gate` CI backlog already documented and root-caused in the second session above — re-confirmed still present and still unrelated to this diff, not re-investigated a third time since nothing changed about those three checks).

## Fifth session (2026-09-04, same day) — rebuild on a fresh branch (`c6-model-providers-19925-v2`), tokens not hex

By the time this session started, `main` had advanced again (past `4f83bd00` to `81b99821`, the #19975 Projects-sidebar feature) and PR #19933 was back to `mergeable:CONFLICTING`/`mergeStateStatus:DIRTY` — confirmed live via `gh pr view 19933` before doing anything else, matching the "REBUILD BRIEF" comment's premise. Per M6, that comment (posted 18:43 UTC) is lowest-precedence, but its instruction (fresh branch off current `main`, reapply the feature, tokens instead of hex, open a new PR, close #19933 with a link) doesn't conflict with the issue body — it's about *how* to land the same in-scope feature, not a scope change, so it was executed rather than deferred.

**What was done, in order:**
1. Branched `c6-model-providers-19925-v2` from `origin/main` at `81b99821`.
2. `git merge origin/c6-model-providers-19925` (not cherry-pick/rebase — the branch's own history already carries the verified feature + the session-4 palette sweep, so merging forward is equivalent to "reapply the feature" without replaying five commits' worth of already-superseded intermediate diffs). One file conflicted: `src/worker.js`, in 3 regions.
3. **Conflict 1** (`getProjectOwned`/`getProjectDetail` family, from #19847/#19975, vs `callByokGemini`, from C6) — both sides purely additive functions colliding at the same insertion point; kept both, gave `getProjectDetail` its own closing brace.
4. **Conflict 2** (`<aside id="chats-drawer">` opening tag) — `main` had already turned this into a combined "Recent chats and Projects" drawer (#19975); the branch's copy predated that and was still the old single-purpose version. Confirmed via `git show origin/main:src/worker.js` that `main`'s side is the only one with real content here (the Projects header markup); took `main`'s side whole, dropped the branch's stale line.
5. **Conflict 3** (`chatState` object literal) — `main` added `projectId`/`projectCounty`/`projectSignal` fields, the branch added `provider` — merged both into one object literal and one `localStorage` read block.
6. **The dangerous one — conflicts 4+5** (the Projects/Reports-panel block from #19847/#19975 vs. the Settings→Models IIFE from C6, ~370 lines total): **git's own auto-merge output for this region was wrong and would have silently shipped broken code.** After accepting the raw conflict markers' HEAD-side text at face value, a follow-up grep for `main`'s `shareReport`/`loadReportsPanel`/`generateReport` function definitions came back empty — they'd been dropped. Root cause: both sides insert a large, independent block at the same location, and both blocks happen to end in structurally similar boilerplate (`}` / blank line / one more statement / `})();` / `</script>`); the line-based 3-way diff aligned on that superficial trailing similarity and silently discarded `main`'s actual final ~15 lines (the `reportOpenBtn` listener plus the three function bodies) instead of flagging a conflict there. Caught by comparing function-definition counts against `git show origin/main:src/worker.js` and `git show origin/c6-model-providers-19925:src/worker.js` rather than trusting the merge tool's "no conflict here" silence. Fixed by discarding the auto-merged region entirely and manually splicing from clean sources: `main`'s complete, verbatim Projects+Reports block (`git show origin/main:src/worker.js`, lines 7369–7636) with the branch's complete, verbatim Settings→Models IIFE (`git show origin/c6-model-providers-19925:src/worker.js`, lines 7067–7193) inserted as one more statement inside the same outer IIFE, immediately before its closing `})();`. No variable-name collisions between the two blocks (checked). Verified afterward: `node --check src/worker.js` passes; `shareReport`/`loadReportsPanel`/`generateReport`/`downloadReport` all present (4 matches, was 0 before the fix); `reportOpenBtn`'s listener present; the Settings→Models `loadSaved`/`Settings → Models: BYOK` block present (2 matches). Also checked for merge-introduced duplicate top-level functions (`hashEmail8`, `poll`, `fmtDate`, `fmtMoney` all show 2 definitions each) — confirmed identical counts already exist on `origin/main` alone, so these are pre-existing per-scope helpers, not new duplicates from this merge.
7. **Tokens, not hex (the brief's actual new instruction vs. session 4's hex-to-hex sweep):** `main`'s current CSS convention for this page is a per-`<style>`-block `:root{--navy:...;--orange:...;--text:...;--muted:...;--border:...;--green:...}` custom-property set, referenced via `var(--x)` almost everywhere — confirmed by inspecting `.models-*` CSS (the Settings→Models modal's own rules, lines 5907–5931): all of it was already `var()`-based from session 4's earlier hex sweep, **except** two literal `#b42318` uses (`.models-status.err`, `.models-saved-item .msi-del:hover`) — `#b42318` is exactly the "judge fix" red the brief cites, just not yet tokenized. That scope's `:root` (line 5708) had no `--red` entry, so one was added (`--red:#b42318`, matching the existing canon-red value used site-wide) and both usages switched to `var(--red)`. Did **not** touch the other ~20 pre-existing `#b42318` literals elsewhere in the file — `git show origin/main:src/worker.js | grep -c '#b42318'` → `18` before this session's change, confirming those are pre-existing, unrelated to C6, and out of scope per M5 (this issue didn't ask for a repo-wide token migration, only for C6's own new code to use tokens). The Settings→Models modal's HTML (provider `<select>`, key/URL inputs, "Ping"/"Save" buttons) has zero inline hex styling — nothing else to tokenize there. Provider names (`Anthropic`/`OpenAI`/`DeepSeek`/`Gemini`/`OpenRouter`/`Ollama`) still appear only inside this modal, matching the canon-tension rule; the composer's own entry point is `⚙️ Model providers` (no vendor name).
8. `node --check src/worker.js` — passes (checked twice: immediately after the manual splice, and again after the token change).
9. Independently re-verified the live DB and `claude-router` state this session (read-only, via `mgmt_sql.py` — SQL text and result counts only, no secret values ever entered a shell command, consistent with CREDENTIAL HANDLING): `new_tables=2`, `provider_fns=5`, `free_tier_fn=1`, `rls_on=true`, `live_rows=0` — all match every prior session's claim, no drift. `GET .../claude-router/health` → `"version":"11-c6-free-tier"`, free-tier entry still listed — matches.
10. Pushed `c6-model-providers-19925-v2` to `origin`, opened a new PR against `main` from it, and closed #19933 with a link to the new PR (old PR's `CONFLICTING` state was a function of a moving `main`, not a defect in the reviewed work — closing rather than force-pushing over history a fourth time, per the brief).

**Not independently re-verified this session (would require capabilities this session doesn't have, same gap as sessions 1–4):** the BYOK/Ollama/free-tier HTTP round trips themselves — that needs `wrangler dev` or a deployed Worker, and this session has neither Cloudflare credentials nor reason to believe that gap has closed. Everything DB-side and `claude-router`-side was re-checked live; the Worker-route-level claims from session 1 (BYOK round trip, key-never-in-logs grep, Ollama ping, cross-user isolation) rest on that session's own live testing, not a re-run here — flagged rather than silently re-asserted as this session's own evidence.

**Residual, not fixed (out of scope, flagging only):** the BYOK failure-path error message (`Your ${provData.provider} connection failed — check Settings → Models`, `src/worker.js` line ~4118) surfaces the provider name in a `/chat/api` error response, not only on the Settings → Models page itself. This is pre-existing behavior from session 1, not introduced this session. Whether that's an acceptable reading of "provider names appear ONLY on the Settings → Models page" (arguably necessary so a user can diagnose their own misconfigured key) or a canon-tension violation is a product call, not something to silently resolve either way in a merge-conflict/tokenization session — flagging for whoever reviews the new PR.

No schema/migration/DB change this session (M5). Scope stayed inside `src/worker.js` (the one file the rebase conflict and the palette sweep both touched) plus this doc.
