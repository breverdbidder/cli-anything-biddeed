# Spec — issue #19826 — SPRINT NOW: SPR-01/03/06/07/08, 72h launch sprint parallel dispatch

<!-- Written by CC at the END of the session (mandate M6). -->

## Intent reference
None at `docs/intent/19826.md` — issue body was the only brief (per the dispatch header).

## What was built

### Lane A — SPR-01 gate extension (this repo, `main`, no publish) — DONE
- `factory/gtm/gate.py` — grew from 6 to 14 compliance checks. New: `check_competitor_terms`
  (`COMPETITOR_TERMS`, canon hard rule 1, both spellings), `check_retired_lines_and_product_names`
  (`RETIRED_LINES` + `S5`/`Shapira Analysis`/`Shapira Formula` denylist), `check_patent_phrasing`
  (`PATENT_ALLOWED_PHRASES` mask-then-fail-on-remainder), `check_canon_strings` (`load_canon_strings()`
  parses `docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md` live — hero stack, banner, short-forms,
  YouTube description — with a hardcoded fallback list if the file is unreadable),
  `check_energy_rules` (`<=1 "!"`, urgency phrases, strips `<script>`/`<style>` first),
  `check_positioning`, `check_contempt_scan` (`CONTEMPT_RE`, P17), `check_buzzword_scan`
  (`BUZZWORD_RE`, P18). Widened `check_person_name_detector` with `FOUNDER_NAME = "Ariel Shapira"`
  allowlist and a broader (but not over-broad — no bare `by` trigger) attribution-verb trigger set.
  Widened `HOMEOWNER_CONTACT_PATTERNS` for P4 second-person-to-owner phrasing.
- `factory/gtm/fixtures/caption_with_*.txt` — 12 new fixtures, one planted failure per new check
  plus a founder-allowlist control (all-PASS) and the homeowner-contact widening control.
- `factory/gtm/test_gate_negative.sh` — extended with a negative-test block per new fixture.
- `docs/gtm/CONTENT_SOP.md` §6 — P2, P3, P4, P6, P7, P8, P9, P17, P18 flipped GAP→EXISTS (P6 noted
  as check-EXISTS-but-live-page-still-fails, since the copy fix is SPR-03/#16's job not SPR-01's).
- Commit `70154cdd`.

### Lane C — SPR-06 C0 technical fixes (Worker side, this repo) — DONE, live-verified
- `src/worker.js`: `<meta name="description">` added to `/chat`, `/terms`, `/privacy`, `/disclaimer`,
  `/security` (all <=155 chars — the last one because the issue misattributed it to biddeed-web, see
  Deviations). `Article` JSON-LD added to `buildBlogPost()` (shared template — covers all 6 `BLOG_POSTS`
  automatically).
- Commits `15abe87e`, `96ab12c3`. Both deployed via `deploy-worker.yml` (runs `33827747219`,
  `33831xxxx` — success) and curl-verified live (see DoD table).

### Lane C — SPR-06 C0, biddeed-web side — PR opened, not merged (per shard: requires W1 click)
- `breverdbidder/biddeed-web` PR #19 (`spr06-c0-jsonld-h1-19826`): `Organization`+`WebSite`+`Person`
  (Ariel Shapira, M7) JSON-LD added to `app/page.tsx`; `<h2>`→`<h1>` promotion on
  `components/auctions/AuctionsLayout.tsx` (`/radar`'s page title, only `<h1>` in that page).
  **Not build-verified** — sandbox has Node 20, project requires Node >=22 (`nanoid@6` engine
  constraint blocks `npm install`). Diff hand-reviewed for syntax; matches Next.js's documented
  static-JSON-LD pattern.

### Lane B — SPR-03 canon copy, non-homepage (biddeed-web) — PR opened, not merged (W1 click)
- `breverdbidder/biddeed-web` PR #18 (`spr03-canon-copy-non-homepage-19826`): swept `app/`,
  `components/`, `lib/` outside `components/deed-home` (PR #16's homepage territory — confirmed
  already merged to `main` mid-session) for S5/patent/Mariam/licensed-GC/retired-tagline/competitor
  hits. One real hit: `lib/deed/context.ts`'s `contextPreamble()` instructed the Deed chat assistant
  "The patent is 12 provisional claims pending, never issued" — canon says 14 or number-free, never
  12. Fixed to match canon's default posture (don't volunteer it; if asked, no claim count, nothing
  issued). Checkout/order, `/discover`, `/alerts` already clean.

### Lane E — SPR-07 county page verification, all 67 (this repo, no publish) — DONE, K2 defect found and fixed
- **Found live (K2):** 53/67 `/county/{slug}` pages showed an "upcoming" count and date derived from
  `fetchCountyLots()`'s 35-day-cutoff/300-row-limit query (built for the interactive table), not from
  `auctions_summary_ssot()` (canon SSOT). Any county with auctions scheduled beyond 35 days out was
  silently undercounted — e.g. Escambia showed 54 vs the RPC's 268.
- **Fixed (K2, authorized by the shard spec — "fixes only for K2/K4 defects"):**
  `fetchRuntimeConfig()` (`src/worker.js`) now also fetches `auctions_summary_ssot()` (8s-bounded via
  `AbortController`, fail-open to the old lots-window count on timeout/error — this RPC has a
  documented cold-aggregation cost per `docs/spec/19813.md`, 17.3s observed once) and exposes
  `countiesDetail` keyed by slug. `buildCountyPage()` prefers that number. Also shortened the
  meta-description template while touching the same line — the old template ran 164-172 chars on
  52/67 counties (over the 155-char P11 limit, found in the same pass); new template is <=149 chars
  on all 67 (computed against live data). Commit `23894a07`, deployed, re-verified live: **67/67 K2
  pass, 0/67 over 155 chars** (was 14/67 and 15/67 respectively before the fix; see DoD table for the
  full before/after).
- **K4 (competitor-field leakage):** PASS, 0/67 — confirmed both by code read (`po_url` deliberately
  not rendered, comment at `src/worker.js` ~L4992) and live grep across all 67 pages.
- **Other findings (K3/K5/K6/K7), not fixed (out of this lane's authorized scope):** filed as
  `breverdbidder/cli-anything-biddeed#19827` — duplicate `<h1>` in `COUNTY_PAGE_TEMPLATE` (systemic,
  all 67), county pages don't deep-link `/radar?county=` (link contract §2.2), unknown county slug
  returns 200 not 404 (K7 negative test fails), buy-CTA gating unverified (K3), `BreadcrumbList`/
  `FAQPage` schema absent (K5).
- 67 rows logged to `public.agent_ops_log` (`dispatch_id='content-county-verify'`, all `VERIFIED`
  post-fix, each with paired RPC-vs-curl evidence).

### Lane D — SPR-08 county-page merge prep — NOT DONE
Not attempted this session — out of budget after A/B/C/E. `biddeed-web` PR #17 (CP-C2, 67
programmatic county pages) exists and is open; the SOP §5.2 K1-K9 staging-preview verification,
Rich Results Test, and 301-map prep were not started. This is the largest remaining lane.

## SSOT bindings used
- `auctions_summary_ssot()` RPC (Supabase, via PostgREST) — read live 4x this session for
  verification and once per county-page config-cache refresh going forward (new, this session).
- `public.agent_ops_log` — 67 rows inserted, `dispatch_id='content-county-verify'`.
- `docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md` — read at runtime by `gate.py`'s
  `load_canon_strings()` (new dependency — if this file moves or its structure changes materially,
  `check_canon_strings` falls back to a hardcoded list rather than erroring).

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| A — gate checks | CONTENT_SOP §6 P2-P9,P17-P18 GAP→EXISTS | VERIFIED | `bash factory/gtm/test_gate_negative.sh` — 16/16 cases pass (2 controls + 14 negative); `agents/reel_studio` eval suite 39/39 unaffected |
| A — no regression | `factory/gtm/doctor.py`, downstream `gate.*` consumers | VERIFIED | doctor.py dial unchanged (still 1); `eval_director_qa.py` full run, 0 failures |
| C — Worker meta descriptions | `/chat` `/disclaimer` `/privacy` `/terms` `/security` | VERIFIED | live curl, all 200, all `<meta name="description">` present, all <=155 chars (150/112/102/138/97) |
| C — blog Article JSON-LD | 6/6 `BLOG_POSTS` | VERIFIED | live curl on all 6 slugs, `"@type":"Article"` present, 200 |
| C — biddeed-web JSON-LD + H1 | Organization/WebSite/Person on `/`, H1 on `/radar` | UNTESTED (build) / PARTIAL (shipped as PR #19, not merged — W1 click required, not build-verified in sandbox) | PR link; no live curl possible pre-merge |
| B — canon copy sweep | non-homepage S5/patent/person/tagline/competitor | VERIFIED (sweep) / UNTESTED (build) | grep sweep output in this session; PR #18, not merged |
| E — K2 | live upcoming/next_auction_date == RPC, all 67 | VERIFIED | before: 14/67 pass; after fix+deploy: **67/67 pass** — `/tmp/county_check/results_final.json` equivalent re-derivable via the script in this file's session log |
| E — K4 | no competitor-sourced field rendered, all 67 | VERIFIED | 0/67 leaks, code read + live grep |
| E — K7 negative test | unknown county → 404 | FAIL (found, not fixed — filed #19827) | `curl -o /dev/null -w '%{http_code}' https://biddeed.ai/county/not-a-real-county-xyz` → 200 |
| E — agent_ops_log | one row per county, `content-county-verify` | VERIFIED | `select count(*), status from agent_ops_log where dispatch_id='content-county-verify' group by status` → 67 rows, all VERIFIED |
| D | staging verify, Rich Results Test, 301 prep | NOT STARTED | — |

## Negative test result
- Lane A: 14 planted-failure fixtures, each fails **exactly** the intended check (verified via
  `--json` output diffing failed-check names against the expected one, not just exit code) — see
  `factory/gtm/test_gate_negative.sh`.
- Lane E K7: unknown-county negative test **itself failed** (200 instead of 404) — this is a real
  defect, filed as #19827, not silently passed over.
- Reel-studio suite (`agents/reel_studio/eval_director_qa.py`, 39 assertions) re-run in full after
  the gate.py changes: unchanged, 0 regressions.

## Deviations from intent
- Issue body assigned "meta description on `/security`" to biddeed-web; `/security` is actually
  served by this repo's `src/worker.js` (`SECURITY_HTML`) — no such route exists on biddeed-web.
  Fixed in the correct repo instead (commit `96ab12c3`), noted in the biddeed-web PR #19 description.
- SPR-07's DoD only authorizes fixes for K2/K4; the K2 root cause (a scope/window mismatch between
  the interactive table's 35-day query and the SSOT RPC) required a real code change (new
  `fetchRuntimeConfig()` field + `buildCountyPage()` logic), not a one-line patch — flagged here since
  "fix" turned out to be non-trivial, and it also required shortening the meta-description template
  (a P11 length violation found in the same pass, fixed on the same touched line rather than opening
  a second issue for something already being edited).
- Lane E filed **one** consolidated GitHub issue (#19827) for all non-K2/K4 findings rather than one
  issue per finding — the shard spec says "a filed issue" without specifying granularity; chose the
  lower-noise interpretation.
- Lanes B/C-biddeed-web PRs are **not build-verified**: this sandbox runs Node 20; biddeed-web's
  `package.json` requires Node >=22 (blocked by `nanoid@6`'s engine constraint on `npm install`).
  Diffs were hand-reviewed for syntax correctness and pattern-matched against existing conventions in
  the same files, but neither `next build` nor `next lint` ran. Flagged explicitly in both PR
  descriptions as a required CI/local check before merge.
- Lane D not attempted — out of session budget after A/B/C/E; flagged as the largest open item, not
  silently dropped.
- A live Supabase infrastructure incident (521s across all REST/RPC endpoints) occurred mid-session,
  independent of any change made here — confirmed by direct curl to unrelated lightweight endpoints
  during the window, and by the outage self-resolving without any action taken. It transiently caused
  one county-page-config cache write to store `countiesDetail: null` (the SSOT sub-fetch failed
  during the outage), which is exactly the fail-open behavior the 8s-timeout/try-catch was designed
  to produce — not a bug, but it delayed live K2 re-verification by ~5 minutes (one cache TTL) until
  the cache naturally refreshed post-recovery.

## Open items / handoff
- **Lane D (SPR-08)** is fully open: stage biddeed-web PR #17 (CP-C2, 67 county pages) to a preview,
  run SOP §5.2 K1-K9 on 5 counties with paired RPC-vs-curl evidence at the same minute, run
  `node scripts/audit-site.mjs` on that branch, Rich Results Test on 2 URLs, then prepare (not apply)
  the 301 map + sitemap diff as a third PR. Post the verdict on PR #17.
- **biddeed-web PR #18 and #19** need a real `npm run build && npm run lint` pass (Node >=22
  environment) before merge — not possible in this session's sandbox.
- **Issue #19827** (5 findings: dup H1, missing `/radar?county=` deep-link, K7 404 fail, K3
  unverified, K5 schema gaps) is unassigned — next session's natural pickup.
- SPR-02 (answer-asset renderer), SPR-04/05 (answer assets), SPR-09 (long-form script), SPR-10 (GEO
  baseline), SPR-11 (un-halt, Ariel-only), SPR-12 (launch report) — **not touched this session**, per
  the issue's own sequencing note ("#19825 is the priority if runner capacity is contended" — this
  session prioritized A/B/C/E as scoped, did not attempt the answer-asset or GEO lanes at all).
