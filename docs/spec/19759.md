# Spec — issue #19759 — ARCHITECT TRIAGE for blocked issue #19742 (shard-5: st_johns/miami_dade/seminole)

## Intent reference
None at docs/intent/19759.md — issue body was the only brief (per dispatch header).

## What was built
- `supabase/migrations/20260902r_architect_triage_19742_19759_stjohns_certify_guard_refresh.sql` — audit-trail record of the live fix (file body is a documentation comment + `SELECT 1;`; the actual writes below were applied live via PostgREST/RPC before this file was authored, matching the repo's established architect-triage precedent pattern).
- 2 rows inserted into `gold_standard_precert_guards` (ids 6271, 6272): fresh `denominator_integrity` and `calendar_parity` guard rows for `county_slug='st_johns'`, both `passed=true`, sourced from a live recount of `multi_county_auctions.parity_status` (not fabricated).
- 1 row inserted into `public.decision_log` (id 3110, `decision_type='triage'`).
- No code files changed, no schema DDL, no other county's data touched.

## SSOT bindings used
- `public.gold_standard_certifications` — read (baseline) + written via `gold_standard_certify()` RPC. st_johns row: `certified` false→true, `consecutive_gold` 0→2, `revoked_at` 2026-08-25→NULL, `last_verified_run` 16429→16463.
- `public.gold_standard_county_status` — read only, `loop_run_id=16429/16462/16463`, `county_slug='st_johns'` → 10/10 PASS on all three (no regression across cycles).
- `public.gold_standard_ultraloop_audit` — read only. Confirmed all 10 A-J letters for st_johns already had `survived=true` rows within the 7-day window (no insert needed here, unlike the miami_dade/seminole precedents).
- `public.gold_standard_precert_guards` — written. 2 new rows for st_johns (ids 6271/6272), `passed=true`.
- `public.multi_county_auctions` — read only, `county='st_johns'`: 120 rows, `parity_status` breakdown `matched_clean=65, PARITY_OK=49, CLERK_SSOT_CANCELLED=6` → matched_clean=114 (95.0%), matched_any=120 (100.0%). Exact match to `pencil_dod_evaluate_county('st_johns', null)` RPC output at query time.
- `public.gold_standard_loop()` RPC — called twice (`loop_run_id` 16462, 16463), fleet-wide (67 counties, `gold_standard_cert_scope`), ~95-110s each.
- `public.gold_standard_certify()` RPC — called twice, once per loop run, to accrue the 2-consecutive-gold hysteresis.
- `public.summit_chat_dispatch` — read only (pre-flight concurrency check, `state='processing'` → 0 rows).

## DoD status
| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| 1 | `SELECT EXISTS (SELECT 1 FROM public.gold_standard_certifications WHERE county_slug = ANY('{st_johns,miami_dade,seminole}'::text[]) AND certified)` | **VERIFIED TRUE** | Re-executed live post-fix via PostgREST: `[{'county_slug': 'miami_dade', 'certified': False}, {'county_slug': 'seminole', 'certified': False}, {'county_slug': 'st_johns', 'certified': True}]` → `EXISTS` = `True`. |

## Negative test result
- miami_dade re-confirmed live (`pencil_dod_evaluate_county`/`gold_standard_county_status` run 16429): 7/10, C=82.7% FAIL, D=88.5% FAIL, I=89.1% FAIL — all genuine, >5pt below the 95% threshold, matching this session's own fresh `gold_standard_ultraloop_audit` BLOCKED rows from 16:25Z (same day, prior session). Left untouched — no writes.
- seminole re-confirmed live: 6/10, C=75.4% FAIL, D=75.4% FAIL, E=83.1% FAIL, J=82.6% FAIL — genuine data gaps (seminole was briefly certified 2026-09-01 then regressed and was re-revoked 2026-09-02T08:45Z on a fresh evaluation, unrelated to this session). Left untouched — no writes.
- Neither county was touched; DoD is an `EXISTS` over the 3-county array so st_johns alone satisfies it.

## Deviations from intent
None — issue body was the only brief and was followed as specified (ARCHITECT protocol: read thread → read decision_log → diagnose → fix within autonomous authority → verify by re-executing DoD → log to decision_log).

## Open items / handoff
- **miami_dade C/D/I** and **seminole C/D/E/J** remain genuine, unresolved data/parity gaps (not certify-gate staleness) — require the normal shard playbooks (C/D: parity backfill + clerk/official-records supplementary litmus; I: property-card geo/value/zone enrichment). Out of architect-triage's autonomous-fix scope; next shard session should pick these up per the issue #19742 shard brief.
- **Disclosed collateral**: `leon` and `gilchrist` crossed their own pre-existing `consecutive_non_gold` 2→3 threshold during the required 2nd fleet-wide `gold_standard_loop()`/`certify()` cycle and were revoked on their own stale adversarial evidence (`adversarial_survival_7_of_10` / `adversarial_survival_9_of_10`) — unrelated to this session's st_johns-only writes, genuine per the certify function's own hysteresis logic, not caused or claimed by this session.
- **Fleet-wide observation (not actioned, out of scope)**: the `gold_standard_precert_guards` calendar_parity/denominator_integrity rows are only refreshed by a daily cron and by ad-hoc architect-triage sessions like this one. This is now the *N*th documented occurrence of "10/10 letters, blocked purely on a stale guard row" (see precedents `20260807d_..._putnam_certify_freshness_refresh.sql`, `20260901_..._seminole_stale_audit_certify.sql`, and this one). A fleet-wide fix (e.g. re-running the guard refresh automatically whenever the underlying C/D letter status changes, not just on a fixed daily cadence) would reduce future architect-triage load, but touches shared production scoring logic used by the whole 67-county fleet — flagged for a dedicated ticket, not actioned here per M5 scope discipline.
