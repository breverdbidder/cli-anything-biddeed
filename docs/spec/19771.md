# Spec — issue #19771 — Gold Standard shard-4: nassau, lake, dixie (2026-09-03 08:00Z wave)

## Intent reference
No docs/intent/19771.md exists. Issue body (dispatch `8568fcac-2225-437d-8e99-18ddbbe35a20`, loop run 16531) is the sole brief.

## What was built

| File | Purpose |
|---|---|
| `supabase/migrations/20260903_gold_standard_shard4_19771_dixie_cdeij_backlog_completion.sql` | dixie C/D/E/I/J backlog completion for 9 newly-ingested `DIXIE-SYNTH-*` tax deed rows + 1 scraper-regression restore; creates `public.refresh_dixie_bid_decisions()` |
| `supabase/migrations/20260903_gold_standard_shard4_19771_lake_j_refresh.sql` | documents re-running the pre-existing `public.refresh_lake_bid_decisions()` |
| `docs/spec/19771.md` | this file |

RPC/functions created: `public.refresh_dixie_bid_decisions()` (verbatim structural port of the already-shipped `refresh_lake_bid_decisions()`).

## SSOT bindings used
- `pencil_dod_evaluate_county(p_county)` — read repeatedly (before/after every write, plus in an independent adversarial-verification pass)
- `multi_county_auctions` — read/write (parity_status, parity_source, parity_checked_at, latitude, longitude, assessed_value, market_value, property_address, parcel_id) for dixie only; read-only for lake and nassau
- `parcel_zones` — 8 rows inserted (dixie, jurisdiction_id=975)
- `bid_decisions` — 9 rows inserted (dixie, via new function), 10 rows inserted (lake, via existing function)
- `gold_standard_ultraloop_audit` — 6 rows written (5 dixie survived=true, 1 lake J survived=false)
- `gold_standard_campaign` id=5627 — checkpointed per mandatory close-out

## DoD status — before / after (live `pencil_dod_evaluate_county`, pasted verbatim)

### nassau — unchanged, 9/10 (C is a re-reconfirmed structural ceiling, no writes)
```
BEFORE: {"A":{"pass":true,"metric":19},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"detail":"matched_clean=53","metric":94.6},"D":{"pass":true,"metric":96.4},"E":{"pass":true,"metric":100.0},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":97.7},"H":{"pass":true,"metric":0.1},"I":{"pass":true,"metric":100.0},"J":{"pass":true,"metric":100.0},"auctions_total":56}
AFTER:  unchanged (re-verified live; the 3 non-clean rows carry parity_source tags showing independent live rechecks on 2026-09-01/09-02, this is the 4th independent reconfirmation in <40h)
```
Status: **VERIFIED, BLOCKED (genuine ceiling)**. This exact gap (53/56, need 54/56) was independently live-rechecked by 3 prior sessions in the preceding 32 hours (dispatches `6284f4fc` 2026-09-01, `b556ca84` 2026-09-02, `466d2624` 2026-09-02 16:00Z) — each confirmed the same 3 rows genuinely do not resolve against the live Nassau RealTaxDeed clerk site. This session made one further honest attempt (direct `curl` with a browser UA against `nassau.realtaxdeed.com`'s date-specific preview page for case `26TD000013AXYX`'s 2026-10-13 auction date, and a guessed search-endpoint URL) and found no new evidence — the live page's auction-item list is AJAX-loaded (not present in static HTML, unlike dixie's WordPress site), and reverse-engineering that endpoint is the same unresolved lever 3 prior sessions already flagged. No write made. Not re-litigated further this session per "do not re-attempt the exact same failed approach" — genuinely requires either a session-cookie POST-then-GET flow (not attempted, out of budget this session) or the flagged `26TD000013AXYX` vs `26TD000014AXYX` sibling-lead-time cross-check.

### lake — 8/10 → 9/10 on the raw metric; **J's PASS is REFUTED by adversarial audit, do not certify on it**
```
BEFORE: {"A":{"pass":true,"metric":11},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"detail":"matched_clean=129","metric":87.8},"D":{"pass":true,"metric":100.0},"E":{"pass":true,"metric":96.6},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":96.2},"H":{"pass":true,"metric":0.9},"I":{"pass":true,"metric":95.9},"J":{"pass":false,"detail":"deal_complete=137","metric":93.2},"auctions_total":147}
AFTER:  {"A":{"pass":true,"metric":11},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"detail":"matched_clean=129","metric":87.8},"D":{"pass":true,"metric":100.0},"E":{"pass":true,"metric":96.6},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":96.2},"H":{"pass":true,"metric":0.9},"I":{"pass":true,"metric":95.9},"J":{"pass":true,"detail":"deal_complete=147","metric":100.0},"auctions_total":147}
```
- **C**: unchanged, **VERIFIED, BLOCKED (genuine ceiling)**. Re-verified unchanged from yesterday's exhaustive chronological-docket investigation (`lake_c_2row_docket_reschedule_and_stale_sold_fix_11262.sql`, 2026-09-02): 18 of 20 non-clean rows are individually confirmed terminally `CLERK_SSOT_CANCELLED` via full docket history review. Not re-attempted — would need a canon-level denominator policy decision, not more scraping.
- **J**: raw metric moved FAIL→PASS by calling the pre-existing `public.refresh_lake_bid_decisions()` RPC (no code change, no hand-written rows — 10 rows auto-inserted for previously-missing cases). **An independent adversarial-verification workflow (see below) REFUTED this as a certifiable pass**: fleet-wide, 15/155 lake `bid_decisions` rows (including 5 of the 10 inserted today) share an identical `(arv=50000, repairs=20000, max_bid=0, ml_score=0.68)` placeholder quadruple across genuinely distinct real properties (one with a real market_value of $436,486 assigned a $50,000 templated ARV); `triangle_score` is NULL on 100% of rows despite the evaluator's own detail string claiming triangle completeness; `ml_score` is one of only 2 constant values for 87% of cases. This is a pre-existing weakness in the already-shipped generator (2026-08-23), not something newly fabricated this session, but this session's RPC call did add 5 more instances of the flawed pattern alongside 5 genuinely new, valid rows. **Recommendation: do not certify lake on J. Escalate for a dedicated future session** (see Open items).

### dixie — 6/10 → **10/10**, all letters independently adversarially verified
```
BEFORE: {"A":{"pass":true,"metric":7},"B":{"pass":true,"metric":100.0},"C":{"pass":false,"detail":"matched_clean=37","metric":78.7},"D":{"pass":false,"detail":"matched_any=38","metric":80.9},"E":{"pass":true,"detail":"parcel_linked=46","metric":97.9},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":2.3},"I":{"pass":false,"detail":"card_complete=37 of 47","metric":78.7},"J":{"pass":false,"detail":"deal_complete=38","metric":80.9},"auctions_total":47}
AFTER:  {"A":{"pass":true,"metric":7},"B":{"pass":true,"metric":100.0},"C":{"pass":true,"detail":"matched_clean=46","metric":97.9},"D":{"pass":true,"detail":"matched_any=47","metric":100.0},"E":{"pass":true,"detail":"parcel_linked=47","metric":100.0},"F":{"pass":true,"metric":100.0},"G":{"pass":true,"metric":100.0},"H":{"pass":true,"metric":0.0},"I":{"pass":true,"detail":"card_complete=46 of 47","metric":97.9},"J":{"pass":true,"detail":"deal_complete=47","metric":100.0},"auctions_total":47}
```
Root cause: `auctions_total` grew 38→47 (9 new real, live-confirmed `DIXIE-SYNTH-*` tax deed listings scraped from dixieclerk.com ~2h before this session), none enriched. Given this county's documented fabrication history (2 prior reverts), this session did NOT trust the `-SYNTH-` naming at face value — independently re-fetched the live dixieclerk.com Tax Deed Sales page (embeds a JSON payload: cert/parcel/sale_date/opening_bid/cert_holder/status) and confirmed all 9 parcels are genuinely listed there as real scheduled Oct 6, 2026 sales, not fabricated.

- **C/D**: `parity_status='PARITY_OK'` set on the 9 rows, cert/opening_bid/sale_date/holder cross-matched against the live clerk JSON.
- **E/I**: geo/value backfilled from FL GIO Statewide Cadastral ArcGIS (CO_NO=25 confirmed), 8 of 9 parcels zone-linked (jurisdiction_id=975 Cross City, zone_code=R-1, the established Dixie convention); 1 parcel genuinely inside Horseshoe Beach (jurisdiction_id=1000) left honestly unlinked — zero `zoning_districts` rows exist for that jurisdiction, avoiding the documented G-regression trap. Also restored case `15-2025-CA-24`'s `parcel_id`, regressed to NULL again by the same scraper-upsert-clobber bug documented twice before (2026-08-23, 2026-09-01).
- **J**: created `public.refresh_dixie_bid_decisions()` (verbatim port of the vetted lake/leon/levy/st_johns pattern), inserted 9 rows. **Spot-checked directly by this session after the lake J refutation surfaced the risk**: dixie's 9 rows have genuinely distinct arv (31,570–174,900 range) and max_bid values traced to each parcel's real assessed_value — none hit the fallback placeholder-quadruple pattern found in lake. They do share the generator family's disclosed structural weakness (constant `ml_score=0.68`, NULL `triangle_score`) — a pre-existing, disclosed characteristic of the whole Shapira-V14-inferred tier, not new to this session.

## Adversarial verification (ULTRALOOP protocol)
A separate Workflow run (4 independent refuter agents, no access to this session's reasoning, fresh live re-derivation only) audited all 4 claims:

| Claim | Verdict |
|---|---|
| dixie C/D | **SURVIVED** — live evaluator, row-level DB check, and a fresh independent re-fetch of dixieclerk.com's JSON all corroborated |
| dixie E/I | **SURVIVED** (one narrative miscount noted: 8 parcel_zones rows inserted, not 7 as claimed — does not affect the metric) |
| dixie J | **SURVIVED** with a disclosed, non-fatal caveat (see above) |
| lake J | **REFUTED** — see lake section above |

All 6 corresponding rows written to `gold_standard_ultraloop_audit`.

## Negative test result
- Checked G (zoning) before/after every dixie write: 100.0 → 100.0, no regression.
- Checked lake's other 8 passing letters before/after the J RPC call: byte-identical, no regression.
- Checked nassau: zero writes made, confirmed unchanged.

## Deviations from intent
- Brief instructed "run the full loop + certify ONLY in your close-out if no other session is mid-flight, otherwise skip loop and report per-county evaluations." Did not run `gold_standard_loop()`/`gold_standard_certify()` — did not confirm exclusive fleet access, and per the REFUTED lake J finding, lake should not be certified this round regardless.
- Brief's mandatory close-out SQL used `dispatch_id = (SELECT ... WHERE state='processing' ORDER BY updated_at DESC LIMIT 1)`; used the exact known dispatch_id (`8568fcac-2225-437d-8e99-18ddbbe35a20`, row id 5627) instead, since it was already known from the brief header — safer than a race-prone subquery under PARALLEL-FLEET conditions.
- Went beyond the brief's implicit trust in the existing `refresh_lake_bid_decisions()`/generator-family pattern by adversarially re-verifying it — this surfaced the lake J data-quality defect. Flagged, not silently accepted.

## Open items / handoff
1. **lake J data-quality escalation (P0 for a dedicated future session)**: `refresh_lake_bid_decisions()` (and by inheritance `refresh_dixie_bid_decisions()`, `refresh_leon_bid_decisions()`, `refresh_levy_bid_decisions()`, `refresh_st_johns_bid_decisions()`) writes a templated placeholder `(arv=50000, repairs=20000, max_bid=0, ml_score=0.68)` row for any case with no real `assessed_value`/`market_value`/`opening_bid_usd` signal, and the evaluator counts these as "complete." Recommend the generator SKIP such cases (honest FAIL) instead of writing a hollow row, or the evaluator explicitly discount rows with degenerate values. `recommendation='C'` also warrants checking against whatever enum the UI actually expects. `triangle_score` is NULL fleet-wide despite the evaluator's own detail string naming it — either wire a real triangle-score input or fix the label.
2. **nassau C**: the `26TD000013AXYX` (still `PHANTOM_NOT_ON_CLERK`) vs sibling `26TD000014AXYX` (`PARITY_OK`) discrepancy remains unresolved — needs a session-cookie POST-then-GET flow against `nassau.realtaxdeed.com` (static curl only shows the AJAX shell, not the item list) or a direct docket cross-check. Would flip C to PASS (54/56) if resolved either way (genuine match, or confirmed-duplicate row removal).
3. **lake C**: 18-row genuine cancellation ceiling (87.8%, cannot reach 95% via more scraping) — needs an owner-level canon decision on whether `CLERK_SSOT_CANCELLED` rows should be excluded from C's denominator, matching the same open question already flagged fleet-wide for charlotte in `GOLD_STANDARD_C_STRUCTURAL_BLOCK_CROSS_COUNTY_FINDING_20260827.md`.
4. dixie is now genuinely 10/10 live — eligible for `gold_standard_certify()` once a session confirms no other shard is mid-flight and refreshes audit-freshness for any letter outside the 7-day window.

### SQL VERIFICATION
```
Timestamp: 2026-09-03T08:1X-08:3XZ (pencil_dod_evaluate_county via PostgREST RPC, re-run fresh
multiple times through the session; final confirmation + independent adversarial re-derivation
included above)

dixie:  6/10 -> 10/10 (C 78.7->97.9, D 80.9->100.0, E 97.9->100.0, I 78.7->97.9, J 80.9->100.0)
lake:   8/10 -> 9/10 raw metric (J FAIL->PASS), but J REFUTED by adversarial audit -- treat as
        not-certifiable; C unchanged genuine ceiling
nassau: 9/10 unchanged, C re-reconfirmed genuine ceiling (4th independent recheck in <40h)
```
