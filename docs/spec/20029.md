# docs/spec/20029.md — GTM-0 launch-night readiness

Issue: #20029. Branch: `main`. Session: headless `claude -p`, no human attached.
Precedence followed: `docs/intent/20029.md` > this file's findings > issue body > comments (M6).
Operating doc: `docs/gtm/GTM_SOP_v1.md` (read in full before starting, per the issue's own instruction).
Connection method: PostgREST via `SUPABASE_SERVICE_ROLE_KEY` for `public` schema; Supabase
Management API (`mgmt_sql.py`, `SUPABASE_ACCESS_TOKEN`) for `winnerdata` schema (confirmed live,
again, this session: PostgREST returns `42501 permission denied for schema winnerdata` even with
the service-role key — `winnerdata` is not exposed to the Data API, matching every prior session's
finding in this repo). `SUPABASE_DB_PASSWORD`/psql was not attempted (known non-working path).

Every claim below is tagged VERIFIED (I ran the query/request and observed the output this
session) or INFERRED (stated plainly where used). No secret value was read or printed at any
point — only secret *names* (`vault.secrets.name`, `gh secret list`) and boolean health-probe
results.

## 1. Baseline re-verification (VERIFIED, matches the issue's own numbers exactly)

| Metric | Issue claim | This session's live query | Match |
|---|---|---|---|
| `winnerdata.biddeed_reels` pending_approval / error | 42 / 188 | 42 / 188 | ✅ |
| `winnerdata.reel_variants` | 25 | 25 | ✅ |
| `winnerdata.reel_variant_review` | 0 | 0 | ✅ |
| `winnerdata.reel_links` | 59 | 59 | ✅ |
| `youtube_uploads` | 0 | 0 | ✅ |
| `social_content_queue` total / status mix | 705 (0 published) | 705: pending 665, draft 37, pending_approval 3 (this session added 3 more pending_approval, see §7) | ✅ |
| `spi_gates.test_purchase` | OPEN | `verified_at IS NULL` | ✅ |
| `spi_gates.gtm_factory_halt` | OPEN | `verified_at IS NULL` | ✅ |
| `youtube_token_health` | ok Sep 4 | `ok=true`, `checked_at=2026-09-04T15:37:45Z` (latest row) | ✅ |

New finding not in the issue's baseline: `winnerdata.youtube_publish_queue` is a **view**, not a
table (`pg_class.relkind='v'`). Its definition hard-filters `rv.qa_pass = true AND rv.is_draft =
false AND br.page_http_status = 200 AND vs.ariel_decision = 'approved'`. Live counts against those
same predicates: **all 25 variants have `is_draft = true`, all 25 have `qa_pass = false`, all 42
reels have `page_http_status IS NULL`.** The view is correctly empty (0 rows) — not because
nothing is ready, but because none of its three independent gates has been satisfied for any row
yet. This governs §3 and §5 below.

## 2. Deliverable 1 — Platform credential inventory (VERIFIED)

`docs/gtm/runbooks/platform-credentials.md` (new file, committed). Method: `select name from
vault.secrets` (metadata only, no decryption — the sanctioned path per this repo's
`CREDENTIAL HANDLING` CLAUDE.md section) + `gh secret list` + live `select * from
public.social_token_health` (populated by the existing `agents/distribution/*.py` adapters'
`required_secrets` probe).

- YouTube: **CONFIRMED** — `youtube_client_id`/`youtube_client_secret`/`youtube_oauth_refresh_token` all present in vault.
- Instagram, Facebook, TikTok, X, LinkedIn Company Page: **all NOT_CONFIGURED**, verified live via `social_token_health.healthy=false` with the exact missing-secret-name detail for each.
- Typefully: `typefully_api_key` absent from vault (searched `%typefully%`, zero rows) — matches SOP's "pending in vault" note.
- LinkedIn browser secrets (`LINKEDIN_EMAIL`/`PASSWORD`/`LI_AT`/`SESSION_JSON`) exist as GH secrets but belong to a different, pre-existing tool (`linkedin/vendor/linkedin_scraper`, read-only extraction) — flagged in the runbook as NOT reusable for the Company Page Posts-API adapter.

Runbook gives Ariel a recommended order (Typefully first, ~3 min, unblocks real Typefully drafts; then Meta + TikTok to start the App Review clock; X held as a spend decision, not a credential gap; LinkedIn Company Page flagged as the longest pole).

## 3. Deliverable 2 — LMS approve/reject screen for reels + variants (VERIFIED, built + deployed)

**Confirmed it did not exist before this session**: `workers/winnerdata-lms/src/index.js` only had
FF-batch routes (`/ff-batches*`) — grepped for `reel`/`variant` in the router, zero hits before
this session's change. Per the issue's own fallback instruction ("If it does not exist or is
broken, BUILD the minimal version"), built and shipped:

- `supabase/migrations/20260905a_lms_reel_variant_review_20029.sql` — applied live via
  `mgmt_sql.py`. Adds `public.lms_reel_variants_list()` (service_role-only read), `public.
  reel_variant_review_authenticated()` and `public.reel_variant_batch_approve_authenticated()`
  (authenticated-only writes), plus a `BEFORE INSERT` trigger on `winnerdata.reel_variant_review`
  that hard-rejects any insert without a real `auth.uid()`/`auth.email()` matching an active row in
  `winnerdata.lms_admins` (same allow-list and same unforgeable-approval pattern as the FF-batch
  gate from issue #19745 — reused, not duplicated). This is what makes guardrail #2
  ("`reel_variant_review` is Ariel's ground truth — never write rows to it") structurally true for
  every caller, including this session: I have no path to insert into that table other than a real
  Supabase Auth session for `ariel+lms-admin@everestcapitalusa.com`, which I never minted.
- `workers/winnerdata-lms/src/index.js` — new `/reels` GET (card grid, one card per variant:
  `<video controls>` from `video_url`, hook title, archetype, short link, QA verdict summary with
  failed-check names, current decision badge) and two POST routes (`/reels/:id/review`,
  `/reels/batch-approve`), both routed through `rpcAsAuthenticatedAdmin()` exactly like the
  existing FF-batch approve action. New CSS: single-column card grid below 480px, `nav{flex-wrap}`.

**Deployed**: pushed `afcc73c5` to `main`; `.github/workflows/deploy-winnerdata-lms.yml` ran
(33969408777), `wrangler deploy` succeeded, live-verified login/session steps in that same run
passed (session cookie issued, `/healthz` and `/orgs` both 200 with the cookie). A separate,
**pre-existing** Playwright debug step in that same job (added in a prior #19745 debug session,
unrelated to this change) failed with `Direct fetch() POST /login -> 401` even using the exact
correct secret values — its own script's diagnostic message says this points at
edge-propagation/secret-value timing, not at Playwright or at this session's code; the two prior
runs of this exact workflow (2026-09-02) failed the identical way before this session touched
anything, so this is not a regression I introduced.

**390px Playwright proof (VERIFIED, this session, local)**: could not use the real
`LMS_AUTH_USER`/`LMS_AUTH_PASS`/`LMS_SUPABASE_AUTH_EMAIL`/`PASSWORD` secrets from this session per
CLAUDE.md's credential-handling rule (never hold a raw secret value) and per M5 (no workflow-file
edits, so I could not add a job that reads them safely inside the runner either). Instead ran the
exact same `src/index.js` locally via `wrangler dev` with disposable, session-generated test
credentials (`LMS_AUTH_USER=test_local_admin`, random password), pointed at the real Supabase
project with the real `SUPABASE_SERVICE_ROLE_KEY` (read-only for this GET path — the RPC never
mints the FF-style authenticated admin token unless a POST is submitted, and I submitted none).
Real production data rendered:
- 20 `.reel-card` elements (the 20 `lang='en'` variants), 20 Approve/Reject/Request-improvement
  button sets, correct titles/archetypes/short links, correct QA-fail reasons pulled straight from
  `qa_scores` (e.g. Broward's set genuinely fails the diversity check: `duplicate archetype in set`
  — a real, pre-existing data-quality finding, not something I introduced).
- Viewport 390×844: `grid-template-columns` computed to a single 342px column, `document.
  documentElement.scrollWidth > clientWidth` = `false` (no horizontal overflow), **zero console
  errors**. Screenshot at `/tmp/reels_390.png` (not committed — ephemeral local test artifact).
- Did not click Approve/Reject/Batch-approve at any point (would have needed a real admin JWT I
  never minted, and would have written to `reel_variant_review` — guardrail #2).
- `.dev.vars` (test-only, never-committed local file) deleted before finishing; no
  `package-lock.json` left behind either.

## 4. Deliverable 3 — 7-day publish queue / 14 picks (VERIFIED selection, NOT inserted anywhere)

`winnerdata.biddeed_reels.rank_score` is a pre-existing, pipeline-computed column — used directly
rather than inventing a new ranking heuristic. Of the 42 pending reels, **28 have a working
`short_url`/`landing_url`; 14 do not** (`landing_url IS NULL`) — live-verified by fetching all 42
`landing_url`/`short_url` pairs:

| Missing link (needs generation before publish-ready) |
|---|
| seminole 2025CA001862, alachua 01 2025 CA 002136, palm_beach 502025CA009901XXXAMB, broward COWE-12-011377, santa_rosa 572025CA000489CAAXMX, walton 25CA000534, seminole 2025CA000273, seminole 2025CA002400 (foreclosure) |
| suwannee 4756, 4704, 4679, 4752, 4677, 4680 (tax_deed) |

This matters because 4 of my top-ranked tax_deed candidates (suwannee, `rank_score` 47.43/29.67/
8.7/7.68 — the highest tax_deed scores in the entire pending pool) fall in the "no link yet"
bucket. Only 3 tax_deed reels are both link-ready AND have real numbers (lee ×2, bay ×1); a further
3 are presale santa_rosa reels with no numbers yet. **The 14-pick table below is honest about
which picks are publish-ready today vs. which need a link-generation pass first** — I did not
silently reorder around the gap.

**Proposed 14 picks (Sep 8–14), ranked by `rank_score`, cadence rule applied (1 foreclosure + 1
tax_deed/day, never two variants of one property the same day — trivially satisfied since slot A
and B are always different properties):**

| Day | Slot A — Foreclosure (rank_score) | Slot B — Tax deed (rank_score) | Link status |
|---|---|---|---|
| Sep 8 | Escambia, `2025 CA 001460`, $39,600 vs $112,410 assessed (44.48) | Lee, `2026000141`, $101,100 vs $155,040 (36.44) | both ready — variants+QA already exist for both |
| Sep 9 | Broward, `CACE-24-008115`, $279,200 vs $464,650 (43.72) | Lee, `2026000086`, $16,600 vs $24,225 (28.41) | A ready (variant exists); B link-ready, no variant yet |
| Sep 10 | Martin, `25001204CAAXMX`, $826,200 vs $902,653 (33.25) | Bay, `2026-3969TD`, $7,800 vs $10,421 (25.51) | A ready (variant exists); B link-ready, no variant yet |
| Sep 11 | Polk, `2024CA000341000000`, $162,100 vs $238,169 (28.43) | Suwannee, `4756`, $29,600 vs $97,747 (47.43) | A ready (variant exists); **B needs link generation** |
| Sep 12 | Polk, `2025CA003611A000BA`, $211,200 vs $256,183 (23.85) | Suwannee, `4704`, $88,100 vs $128,288 (29.67) | A link-ready, no variant; **B needs link generation** |
| Sep 13 | Marion, `422024CA000511CAAXMX`, $152,881 (no assessed on file) (22.62) | Suwannee, `4679`, $7,400 (no assessed on file) (8.7) | A link-ready, no variant; **B needs link generation** |
| Sep 14 | Escambia, `2025 CA 001415`, $170,600 vs $140,249 (19.42) | Suwannee, `4752`, $27,100 (no assessed on file) (7.68) | A link-ready, no variant; **B needs link generation** |

Not written anywhere as "queue rows" because (a) `youtube_publish_queue` is a read-only view, not a
table — there is nothing to INSERT into, and (b) making these picks real requires Hook
Writer→Director/QA to run for the 9 candidates above with no variant yet, then Ariel's LMS
approval (guardrail #2), then the missing-link candidates need their short/landing links generated
— none of which this session did, per scope. This table is the report the issue asked for
("Report the 14 picks... in a comment"), not a fabricated done-state.

## 5. Deliverable 4 — Funnel surfaces verified live (VERIFIED, real fetches + one real end-to-end capture)

Fetched all 42 `landing_url` + `short_url` pairs live (`httpx`, no browser): **28/42 return
`deal 200` / `r 302`; 14/42 have no link yet** (table above; not "broken," never generated).

Deep Playwright check on one representative deal page (`biddeed.ai/deal/broward/cace-24-008115`):
- Street View image present (1 match), aerial/parcel element present (1 match), email-capture
  `<input type="email">` present, sold amount ($279,200) renders correctly in body text.
- `/r/{code}` redirects to `/reels/{code}` (the player), **not** directly to `/deal/...` — the deal
  page is one click away via the reel's clickable end card (confirmed: `/reels/agthqs` HTML
  contains `href="https://biddeed.ai/deal/broward/cace-24-008115?preview=..."`). This satisfies
  the DoD's "/reels/{code} player with clickable end card" but is a more literal reading than
  "`/r/{code}` 302 with UTMs to the deal page" — worth Ariel knowing the actual hop count.
- **Real end-to-end capture test** (synthetic row, deleted after — same pattern this repo's own
  LMS deploy workflow already uses for its FF-batch Playwright proof): submitted
  `gtm0-funnel-test@example.com` through the live email form. A real POST fired to
  `/deal/broward/cace-24-008115/lead`, and the row landed in `public.lead_profiles` with
  `source='reel'` (issue's DoD said `source deal_page` — actual value differs, worth a doc fix).
  Row deleted immediately after verification (`delete from lead_profiles where email=...`, confirmed 0 rows remain).
- **No PostHog network request observed** on page load or on the capture submit (checked via
  `page.on('request')` regex for `posthog`). Could not confirm the "every CTA fires a PostHog
  event" contract from `GTM_SOP_v1.md` §6 — flagging as UNVERIFIED, not silently marking it pass.
- `/reels` gallery: 200, real content.

`page_http_status` was **not** written back to the 28 working rows — the issue's ask was to
verify, and I did not want to silently promote rows toward the `youtube_publish_queue` view's
`page_http_status = 200` gate without Ariel seeing this report first, since that gate exists
specifically to require an actual verification pass before publish eligibility. Flagging as the
one place I deliberately stopped short of what a "fix what's broken" reading might have done.

## 6. Deliverable 5 — Upload lane armed, not fired (VERIFIED)

`python3 agents/youtube/uploader.py --self-test`: **13/13 PASS**, including the exact gates this
issue asks about: `(c)` the view hard-filters `ariel_decision = 'approved'`; `(19793-a)` both the
view and a second, defensive Python-level filter reject `is_draft = true` rows even if one reached
the loop; `(19804-*)` same-property/day, starvation ladder, and exploration-overlay logic all pass.

**Live negative test**: `python3 agents/youtube/uploader.py` (real run, no flags) against
production →  `cadence: 2026-09-05 is a weekend and this cadence is Mon-Fri only. Nothing to do.`
Independently confirmed via direct query that `select count(*) from winnerdata.
youtube_publish_queue` = **0** right now — so even ignoring the weekend gate, the lane has zero
eligible rows to upload today (see §1's three-gate finding). Combined with the unit-level proof,
this satisfies "attempt to run the upload lane against an unapproved row → it must refuse and log
the reason": it refuses, for two independent, logged reasons.

Private-upload-then-delete smoke test: **not re-run this session** — already executed and
documented 2026-09-03 (`docs/gtm/COWORK_YOUTUBE_SETUP_RUNBOOK.md` Part F: video `ORTZjYLruaQ`
uploaded private and deleted, HTTP 204). Nothing in the upload code path changed since then;
re-spending YouTube quota to repeat an already-documented proof was judged unnecessary.

## 7. Deliverable 6 — Daily digest (VERIFIED — found a live, pre-existing M1 violation)

**Schedule mismatch**: `.github/workflows/biddeed-daily-digest.yml` cron is `0 10 * * 1-5` = 06:00
ET, not the 18:00 ET specified in `GTM_SOP_v1.md` §3.

**Critical, pre-existing, ongoing finding (not caused by this session)**: `scripts/
biddeed-daily-digest.cjs`'s `getLeads()` selects **every** `public.lead_profiles` row where
`stage != 'unsubscribed'` and `email IS NOT NULL` — there is no consent-flag filter at all. This
workflow has run successfully on its cron **daily since at least 2026-08-24** (11 consecutive
scheduled runs, verified via `gh run list`). The 2026-09-04 run's own log: `Leads: 60`, `Sending to
60 recipients`, `SENT bobbylyons@aol.com`, `SENT kuenkong@yahoo.com`, `SENT tina.lawless@hotmail.com`,
`Done: 3 sent, 57 failed`. Three real external addresses received a real Resend send with zero
Ariel approval gate; the other 57 failures look like Resend's own sandbox-domain restriction, not
a consent check in the code. This directly matches M1's own prohibition and this issue's baseline
statement that only 1 of ~361 `lead_profiles` rows is a consented inbound lead — the rest are
scraped LLC-buyer/skip-trace/TAM contacts.

Logged to `public.agent_ops_log` this session (`dispatch_id='20029'`, `status='BLOCKED'`,
`severity='blocker'`) with the full run evidence above. **Did not** edit the workflow, the cron, or
the script (M5: no workflow-file edits, no cron edits — out of this issue's scope; also the safer
choice given a code fix to the exact function with the compliance bug deserves its own reviewed
issue, not a same-session patch bundled into an unrelated LMS/credentials issue). **Did** run the
one safe, in-scope action the issue explicitly asked for: `gh workflow run biddeed-daily-digest.yml
-f test_email=everestcapital8@gmail.com` (run 33969934990) → `TEST MODE → everestcapital8@gmail.com`,
`Sending to 1 recipients across 50 counties` (correctly scoped to 1, not all leads) — but the send
itself then failed on an unrelated bug (`FAIL everestcapital8@gmail.com: lead is not defined`,
`Done: 0 sent, 1 failed`). Both findings (consent-filter gap, `lead is not defined` bug) need a
human decision before this script runs again; the next scheduled fire is 2026-09-08T10:00 UTC
(Tuesday morning, hours before the 21:00 ET launch-night runbook).

## 8. Deliverable 7 — Founder LinkedIn drafts ×3 (VERIFIED — numbers reconciled against canon, not blindly copied)

`typefully_api_key` absent (§2) → used the issue's own documented fallback: 3 rows in `public.
social_content_queue`, `target_platform='linkedin_personal'`, `source_type='founder_ghostwriter'`,
`source_ref='20029'`, `status='pending_approval'`, scheduled Tue/Thu/Sat (`2026-09-08`,
`2026-09-10`, `2026-09-12`, matching `GTM_SOP_v1.md` §3's weekly cadence) — ids `ef9709a0`,
`ae98c9f0`, `34077022`.

**Number reconciliation (important)**: the issue body's figures were "Lakewood $28,100 → $320,000
/ $1,756 → $20,000 per door" and "Rainsville $5,330 → $398,600." The only canon source found
(`docs/gtm/BIDDEED_VOICE_CONTENT_META_PROMPT_v2.md`, "numbers must match that site exactly —
re-verify before publishing, never round or embellish") records: "$5,330 tax deed → ground-up
build sold $398,600" (**exact match** — used as-is) and "$20,100 two lots → 16-unit multifamily
entitled pad sold $320,000" (**exit matches, entry does not** — canon says $20,100, not $28,100;
$1,756 appears nowhere in canon, while $320,000 ÷ 16 units = $20,000/door **is** independently
derivable from the same canon line). Per this repo's Honesty Protocol ("wrong VERIFIED = 3×
penalty," "no unverified numbers in copy"), I used the canon-verified $20,100 entry / $320,000 exit
/ $20,000-per-door figures rather than the issue's $28,100/$1,756, and I did not use a "Lakewood"
or "Rainsville" property name in either draft since no canon source ties those names to these
specific numbers — the drafts refer to the deals by their numbers only, which is also what the
canon doc itself does. All three drafts use only the pre-approved hero-copy-stack lines ("The best
prices in US real estate are set at foreclosure and tax deed auctions," "Our data is your unfair
advantage at every US county auction," "For Everyone. Everywhere.") and the founder-story facts
from the same file (20+ years, vertically integrated, courthouse steps) — no person named other
than the founder (implicitly, first person; M7), no vendor/model names, no relief language.

## Files touched this session
- `docs/gtm/runbooks/platform-credentials.md` — new.
- `supabase/migrations/20260905a_lms_reel_variant_review_20029.sql` — new, applied live.
- `workers/winnerdata-lms/src/index.js` — `/reels` review screen + CSS, deployed live.
- `docs/spec/20029.md` — this file.

## SSOT tables/columns touched
- `winnerdata.reel_variant_review` — trigger + RLS policy added (no rows inserted by this session).
- `public.lms_reel_variants_list`, `public.reel_variant_review_authenticated`, `public.reel_variant_batch_approve_authenticated` — new functions.
- `public.social_content_queue` — 3 rows inserted (`source_ref='20029'`).
- `public.agent_ops_log` — 1 row (`dispatch_id='20029'`, blocker severity, digest finding).
- `public.lead_profiles` — 1 synthetic test row inserted and deleted same session (funnel capture proof).
- Not touched: `spi_gates` (guardrail #2 — chat closes these, not this session), `reel_variant_review` rows, `winnerdata.youtube_publish_queue` (a view — nothing to write), any workflow/cron file, any production secret value.

## Residual / needs-Ariel items (ranked by urgency)
1. **Daily digest consent-filter gap + `lead is not defined` bug** (§7) — needs a human decision + code fix before the next scheduled run (2026-09-08T10:00 UTC), and the cron time itself needs correcting to 18:00 ET.
2. **9 of the 14 proposed picks have no variant yet; 4 of the highest-ranked tax_deed candidates have no short/landing link yet** (§4) — Hook Writer/Director-QA + link generation need to run before those picks are actually reviewable in the new `/reels` LMS screen.
3. **PostHog event not observed** on deal-page load or capture submit (§5) — needs a real browser-based instrumentation check, not assumed working.
4. **`qa_pass=false`/`is_draft=true` on all 25 existing variants** (§1) — the `youtube_publish_queue` view will stay empty even after Ariel approves in the LMS until Director/QA re-passes and final (non-draft) voice renders land.
5. Platform credentials (§2) — Typefully first (fastest unlock), then Meta/TikTok to start App Review clocks.

## Honesty Protocol tags
Every claim above is VERIFIED (query/request run this session, output shown) or explicitly marked
INFERRED/UNVERIFIED inline. Nothing is claimed SHIPPED — this issue is inventory + one real feature
build (the LMS screen) + one real critical-finding escalation, not a production content publish.
