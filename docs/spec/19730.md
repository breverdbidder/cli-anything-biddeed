# 19730: re-run contact enrichment for pending batches 2026-08-31 (28) + 2026-09-01 (16)

## Scope
Populate `winnerdata.ff_batch_leads` (the table `public.lms_ff_batch_detail` actually
reads `contact_confidence` from for `batch_kind='seller_digest'` batches) for the two
`pending_approval` batches left empty when #19727 discovered Tracerfy was at 0 credits.
Ariel topped up Tracerfy this morning (2026-09-02); this issue re-runs enrichment now
that the account has real balance. **No send, no approval — verified by SQL only.**

## What was actually broken vs. what this session found (CONFIRMED, live evidence)

- `select count(*) from winnerdata.ff_batch_leads where batch_date in ('2026-08-31','2026-09-01')`
  returned **0** at session start — confirmed the P0's premise.
- `mcp__tracerfy__check_balance` → `{"credits": 3000}` — confirmed the account-balance
  root cause from #19727 (HTTP 402 on every call) is resolved.
- `winnerdata.ff_batch_leads` did **not** already contain rows for these two
  `batch_date`s to enrich — it only had 37 rows from an unrelated, older
  "nine-case portfolio" batch shape (`2026-08-26`: 9, `2026-08-27`: 28), because
  `winnerdata.build_ff_portfolio_batch()` is hardcoded to a 9-parcel crosswalk and
  does not apply to general `seller_digest` batches. `winnerdata.seller_digest_leads`
  (28 + 16 rows, confirmed present from #19727's routing fix) is a **different**
  table with its own unused `phone`/`email`/`phone_tier`/`email_tier` columns, but
  `public.lms_ff_batch_detail()`'s `seller_digest` branch joins to `ff_batch_leads`
  by `case_number` for `confidence_tier` — so that table had to be populated for the
  LMS screen to show anything.

## Work performed

1. **Backfilled `winnerdata.ff_batch_leads`** with 44 new rows (28 for 2026-08-31,
   16 for 2026-09-01), one per `winnerdata.seller_digest_leads` row, joined to
   `public.multi_county_auctions` on `(case_number, auction_date=batch_date,
   tier1_buyer_type='third_party')` — a clean 1:1 join, verified no duplicates before
   inserting. Populated the NOT NULL columns the table's older schema requires
   (`auction_id`, `winning_bidder`, `auction_parcel_id`, `pin_clean` — normalized
   from `parcel_id`) plus `resolved_entity_name`, `source_snapshot`, and a heuristic
   `identity_type` (business / land_trust_unpierceable / person_joint / person,
   regex on `winning_bidder` for LLC/INC/TRUST+TRUSTEE/"and"/"&" patterns — **INFERRED**,
   not LLM-reviewed per-lead; matches the style of the 37 pre-existing rows but is not
   guaranteed as accurate as a manual review for every entity name).
2. **Ran the sanctioned enrichment state machine** (`scripts/ff_billable_loop.py`,
   issue #19712 — stage1 fl_parcels self-match → stage2 Sunbiz/identity cascade →
   stage3 Tracerfy skip-trace → stage4 Exa web → stage5 DNC, reused unmodified),
   scoped to only `batch_date in ('2026-08-31','2026-09-01')` via a driver script
   that filters the claim query to these two dates only (M5 — the tool's own
   `main()` has no batch scoping and would have also picked up 12 untouched
   `retry_due`/orphaned rows from the unrelated 2026-08-27 batch; avoided).
3. **Self-healed one orphaned row** my own first (killed) run left stuck at
   `row_enrichment_status='stage1_identity'` (case `292025CA010976A001HC`,
   2026-08-31) back to `retry_due` so a future tick can resume it — this state
   machine has a pre-existing bug where only `not_started`/`retry_due` are
   re-claimable, so any row killed mid-stage orphans permanently otherwise (same
   pattern as the 1 orphaned row already present in the older 2026-08-27 batch;
   not this issue's bug to fix, flagged here as a residual for `ff_billable_loop.py`).

## Result: hit a real, documented ceiling — the daily Tracerfy+BrightData call cap

`public.ff_daily_credit_ledger` enforces **100 combined units/UTC day, no
carryover** (`ff_ledger_spend()`, migration `20260824_ff_daily_credit_ledger.sql`)
— separate from and unrelated to the Tracerfy *account* balance. This cap already
had 56/100 spent from earlier same-day work before this session started, and this
session's enrichment run consumed the remaining 44, hitting `cap_hit_at
2026-09-02T13:43:08Z`. This is **not** the 402-at-0-credits failure mode #19727
diagnosed (confirmed: `stage3_skiptrace` logged 29 real `miss` outcomes — genuine
Tracerfy no-hits, not errors — proving the account is live and answering calls) —
it is a separate, working-as-designed governance throttle that resets at UTC
midnight (2026-09-03T00:00Z, ~10h after this session). Per the session brief
("stop for a genuine data ceiling, not a credential question, and document it
inline"), this session stopped here rather than looping on a gate that cannot
grant more calls until the day rolls over.

## DoD status (live SQL, 2026-09-02T13:46Z)

| DoD item | Status | Evidence |
|---|---|---|
| `ff_batch_leads` row count = 28 / 16 | **MET** | `select batch_date,count(*) ... group by batch_date` → `2026-08-31: 28`, `2026-09-01: 16` |
| Real tier distribution + 2-of-3 billable count | **MET (partial coverage, honest)** | See table below — 0 leads currently meet 2-of-3 in either batch |
| `lms_ff_batch_detail` returns non-null `contact_confidence` | **PARTIALLY MET** | 2026-08-31: 15/28 leads now show a real tier (13 `NOT AVAILABLE`, 2 `LIKELY·SINGLE SOURCE`); 2026-09-01: 0/16 — every lead in that batch was still `retry_due` (never reached Tracerfy) when the daily cap hit, so all 16 still show `confidence_tier: null` in the RPC today |
| Both `ff_batches` rows still `pending_approval`, `approved_at`/`sent_at` null | **MET** | `select batch_date,status,approved_at,sent_at ...` → both `pending_approval`/`null`/`null` |
| No email sent, no batch approved | **MET** | `winnerdata.ff_digest_log` has 0 rows for either `batch_date`; `winnerdata.lms_audit_log` last-2h shows only the 3 pre-existing `flag_lead` rows from #19727 — no `ff_batch_approve`/`ff_batch_lead_review` rows from this session; `ff_approve_batch()`/`lms_ff_approve_batch()` never called |

### Tier distribution (live SQL)

| batch_date | total | BILLABLE (2-of-3 met) | EXHAUSTED | still retry_due (gated by daily cap) | VERIFIED·PRIMARY | VERIFIED·CROSS-CHECKED | LIKELY·SINGLE SOURCE | UNCONFIRMED CLAIM | NOT AVAILABLE | tier not yet assigned |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-08-31 | 28 | **0** | 15 | 13 | 0 | 0 | 2 | 0 | 13 | 13 |
| 2026-09-01 | 16 | **0** | 0 | 16 | 0 | 0 | 0 | 0 | 0 | 16 |

Note on tier vocabulary: the issue body used hyphenated tier names
(`VERIFIED-PRIMARY`, etc.) — `winnerdata.ff_batch_leads.contact_confidence` now
enforces the canonical middle-dot spelling from #19712's migration
(`VERIFIED·PRIMARY`, `VERIFIED·CROSS-CHECKED`, `LIKELY·SINGLE SOURCE`,
`UNCONFIRMED CLAIM`, `NOT AVAILABLE`). Used the live schema's canonical strings,
not the issue body's spelling.

**Zero leads meet the 2-of-3 billable rule in either batch as of this session.**
The `LIKELY·SINGLE SOURCE` tier (2 leads, both 2026-08-31: case `2025CA004149A000BA`
"Coy Properties LLC" and `2025CA004355A000BA` "Mr America Export LLC") is 1-of-3
evidence (a `fl_parcels` self-match address, no phone/email hit yet) — real
progress, not yet billable. This is the honest number; it is lower than Ariel may
expect because 44/44 leads needed the full stage1→stage5 cascade run from scratch
(none had ever been attempted before this session) and the shared daily call cap
cut the run off partway through, before most leads reached stage3 (Tracerfy) at all.

## Residual work (next session, after 2026-09-03T00:00Z UTC cap reset)

- Re-run `ff_billable_loop.py` scoped to `batch_date in ('2026-08-31','2026-09-01')`
  to advance the 29 leads still `retry_due` (13 in 2026-08-31, all 16 in 2026-09-01).
- 2026-09-01 has not had a single lead reach Tracerfy yet — expect its tier
  distribution to look like 2026-08-31's once it runs.
- The 3 numeric-`winning_bidder` Lee County leads flagged under #19727
  (`2026000036`/`086`/`141`, all in the 2026-09-01 batch) will very likely resolve
  to `NOT AVAILABLE` even after a full run — Tracerfy has no name to trace against
  a bare digit string. That is a pre-existing, already-flagged data-quality issue,
  not a new finding.
- `ff_billable_loop.py`'s claim protocol permanently orphans any row killed
  mid-stage (only `not_started`/`retry_due` are re-claimable) — flagged, not fixed
  here (out of this issue's scope per M5); the one orphan this session created was
  self-healed back to `retry_due`.

## Tags
- Tracerfy account balance restored (3000 credits) and answering real calls (29
  genuine `miss` outcomes logged, not errors): **VERIFIED**
- `ff_batch_leads` row counts (28/16) and tier distribution table above: **VERIFIED**
  (live SQL, queries reproduced in this file)
- `identity_type` classification (business/person/person_joint/land_trust): **INFERRED**
  (regex heuristic on `winning_bidder` text, consistent with but not identical to
  how the 37 pre-existing rows appear to have been classified)
- Daily 100-call combined Tracerfy+BrightData cap as the stopping reason (not a
  credential/402 issue): **VERIFIED** (`public.ff_daily_credit_ledger` row,
  `cap_hit_at` timestamp, migration text confirming UTC-day/no-carryover semantics)
- No send, no approval this session: **VERIFIED** (`ff_digest_log` 0 rows,
  `lms_audit_log` no new rows, `ff_batches.status` unchanged for both dates)

## Files touched
- `docs/spec/19730.md` (this file)
- No application code changed — reused `scripts/ff_billable_loop.py` unmodified via
  a session-scoped driver script (not committed; ephemeral, ran from `/tmp`)
- Database: 44 new rows in `winnerdata.ff_batch_leads`; `winnerdata.ff_enrichment_attempts`
  ledger rows for every stage attempted this session (evidence trail, per Honesty
  Protocol M4); no schema changes
