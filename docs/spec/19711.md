# Spec — issue #19711 — register the artifact-chain convention in SSOT docs

<!-- Written by CC at the END of the session (mandate M6), or by chat-Claude
     before dispatch for large tasks. This is the machine-actionable record of
     what was actually built, so the next agent starts cold with zero chat history. -->

## Intent reference
docs/intent/19711.md @ 30b46602 (HEAD at session start)

## What was built

Files touched:
- `BIDDEED_SSOT.md` — no pre-existing `## CANONICAL CC DISPATCH` heading was
  found (`grep -n "CANONICAL CC DISPATCH" BIDDEED_SSOT.md` → no match, exit 1),
  so per the intent's task-specific mandate the whole section was added at the
  top of the file, immediately after the file's intro/status block and before
  the pre-existing `## CANON — read first` section. Contains
  `### Artifact chain (2026-09-02)` stating the 5-tier brief precedence,
  the pre-`fire_workflow_dispatch` commit requirement, the `docs/spec/<issue>.md`
  pre-exit write requirement, and the `intent=present|missing` breadcrumb
  contract, with links to `docs/intent/README.md` and `docs/spec/README.md`.
  No other section of the file was touched.
- `CC_META_PROMPT.md` — added item 5 to the numbered rules list under
  `## 1. BEFORE YOU START` (the file's "operating-contract rules" section):
  `"Artifact chain: the brief you receive is layered — standing mandates
  (docs/intent/MANDATES.md) outrank the intent file, which outranks the issue
  body, which outranks comments. Write docs/spec/<issue>.md before exit."`
  No other line was touched.
- `docs/spec/19711.md` — this file.

No migrations, no RPCs/views, no workflow files touched.

**Evidence the wrapper injected the layered brief** (mandate #3 of the issue —
literal first 3 header lines of the brief this session actually received):

```
NON-INTERACTIVE HEADLESS SESSION: this runs via `claude -p` with no human attached — nobody will read or answer any question you print. You already hold full standing authorization per this repo's CLAUDE.md (zero-HITL, execute-first, ship-to-main mandate, STANDING AUTHORIZATIONS section) — do not stop to ask for confirmation; there is no one to confirm with, and doing so wastes this attempt. If SUPABASE_DB_PASSWORD / direct psql fails (a known, long-standing, already-documented constraint — see decision_log ids 169/205/287 — not something to re-diagnose or wait on), that is expected: fall back to Supabase PostgREST (SUPABASE_SERVICE_ROLE_KEY) and RPC calls for all reads/writes, which is the established working pattern this pipeline runs on daily. Make the maximum safe, verified progress against the DoD below within this session and report exact evidence (before/after metrics). Only stop early for a genuine data ceiling (not a credential/authorization question) and document it inline in your final output — do not end the session by asking a question.

=================== STANDING MANDATES (override everything below) ===================
# STANDING MANDATES — injected into EVERY cc-runner dispatch, ahead of the issue body
```

The 5th section of the brief (`=================== INTENT: docs/intent/19711.md (outranks the issue body) ===================`)
was also present, immediately followed by the full text of `docs/intent/19711.md`.

## SSOT bindings used

None. No Supabase table or RPC was read or written for the file edits above
(pure git-tracked markdown). `public.agent_ops_log` was written for the
required ops-log row — see below.

## DoD status

| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| 1 | `BIDDEED_SSOT.md` contains `### Artifact chain (2026-09-02)` | VERIFIED | Section added, see diff; `grep -n "Artifact chain (2026-09-02)" BIDDEED_SSOT.md` matches post-edit. |
| 2 | `CC_META_PROMPT.md` contains `Artifact chain:` | VERIFIED | Item 5 added to `## 1. BEFORE YOU START`; `grep -n "Artifact chain:" CC_META_PROMPT.md` matches post-edit. |
| 3 | `docs/spec/19711.md` exists, DoD table filled, quotes literal first 3 brief header lines as VERIFIED evidence | VERIFIED | This file; header-line quote block above. |
| 4 | `public.agent_ops_log` row, `task='artifact_chain_first_dispatch'` | VERIFIED | Inserted via Supabase PostgREST (service-role key) per CC_META_PROMPT.md §4 fallback ladder — direct psql not attempted (known-stale credential per decision_log 169/205/287, CLAUDE.md CREDENTIAL HANDLING). `status='VERIFIED'`, `evidence` lists all 5 brief sections observed present (STANDING MANDATES, INTENT, ISSUE BODY, ISSUE COMMENTS [empty], END-OF-BRIEF marker). |

## Negative test result

`git log -1 --format=%H -- .github/workflows/cc-runner-ghonly.yml` at session
start → `0fd0aa1987e99e3896d63c9d3433bf1aa03e7c70`.
`git rev-parse HEAD:.github/workflows/cc-runner-ghonly.yml` (blob sha) →
`7ad7536629f5a6d92742bd252d2966d51d06a110` — matches the negative test's
expected sha (`7ad75366...`) both before and after this session's commit; the
file was never opened or edited in this session.

## Deviations from intent

None. The intent's one conditional instruction ("if BIDDEED_SSOT.md has no
`## CANONICAL CC DISPATCH` heading, add the subsection at the top of the
file and say so in the spec") applied — confirmed via grep before editing —
and is recorded here as required.

## Open items / handoff

- `public.unified_context` key `artifact_chain_convention_v1` (named in the
  intent's SSOT-sources list as a decision row with commit shas) was not
  created or updated by this session — the issue's `/goal` and `/DoD` did not
  ask for it, and the intent's non-goals restrict Supabase writes to
  `agent_ops_log` only. Flagged, not built.
- No other open items. Task is small and self-contained per the intent's
  "Single session... under 30 min" constraint.
