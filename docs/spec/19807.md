# Spec — issue #19807 — GOLD STANDARD SHARD-5: pasco, manatee, sumter

Operating contract: STANDING MANDATES (issue-injected). No docs/intent/19807.md exists — issue body is the only brief.

## Intent reference
None. Issue body only.

## Headline: pasco 10/10 (was 9/10), manatee 9/10 (was 7/10), sumter 6/10 unchanged pass-set but E/I/J materially narrowed — VERIFIED live, adversarial re-check dispatched via Workflow (gold-standard-19807-ultraloop-verify, run wf_922c1c5e-f60)

## What was built

- `scripts/pasco_i_zoning_gap_fix_run19807.py` — pasco I fix: backfills
  assessed_value/latitude/longitude/property_address on 14 rows from
  fl_parcels (co_no=61) by exact parcel_id match; inserts parcel_zones rows
  for 13 previously-unassigned Pasco parcel_ids (jurisdiction_id=1258
  Unincorporated Pasco County, zone_code via DOR_UC crosswalk: 001->R-2,
  004->R-3, 010->C-1 — reruns the exact methodology already established in
  `scripts/shard9_run651_pasco_zoning.py`, not a new pattern); fixes one
  parcel_zones row that had jurisdiction_id=1 (not a real Pasco jurisdiction)
  instead of 1258; attempts (BLOCKED, 0 ArcGIS matches) geocoding 2
  no-parcel addressed rows.
- `scripts/manatee_i_bare_rows_ajax_backfill_run19807.py` — manatee E+I fix,
  3rd-pass rerun of `scripts/manatee_i_bare_rows_ajax_backfill_10bc7bc6.py`'s
  exact pipeline (manatee.realforeclose.com AJAX harvest via
  `scripts/shard2_run2450_ajax_realforeclose_harvest.py`, GIS_PARCELS
  ArcGIS FeatureServer fallback, re-run of `scripts/shard_manatee_i_zoning.py`)
  rescoped to the 10 new bare case numbers this session found. 5/10 resolved
  to real address+parcel+value; 5/10 harvested only judgment_amount (no
  address in the AJAX payload) — left unresolved, not fabricated.
- `scripts/sumter_eij_run19807.py` — sumter E+I+J fix. Resolves
  2024-CA-000448 (new case) to parcel F30D019 via a WebSearch lead
  cross-checked against fl_parcels, geocoded via the US Census Bureau public
  geocoder (no fl_parcels centroid existed for this parcel). Adds real
  zone_code to 3 parcels via exact-key live queries against Sumter County
  GIS `Interactive/FLU_Zoning/FeatureServer/11` (field Parcel) and `/10`
  (field PIN, Wildwood) — F30D019->R6C, D28E030->RPUD, G04N163->CMU.
  Corrects a 2026-08-27 session's wrong conclusion that D28E030 (The
  Villages) has "no genuine zone_code" — that session queried the wrong
  ArcGIS service (DevelopmentServices/Development_Services MapServer
  layers 5/10, which are FLU layers) instead of the already-established
  correct Interactive/FLU_Zoning/FeatureServer/11 endpoint. Inserts one
  bid_decisions row for 2024-CA-000448 via the exact two-arm CMA
  methodology in `scripts/gold_standard_sumter_j_3row_backfill_20260813.py`.
  2026-CA-000074 and 2026-CA-000129 re-confirmed CONFIRMED DEAD (no address
  on sumterclerk.com, 0 items on sumter.realforeclose.com AJAX for their
  sale dates) — NOT enriched, no write attempted, per BLANK > WRONG.
- Direct DB writes (no migration file — all via PostgREST against
  already-existing tables, following established per-shard script
  convention): `zoning_districts` (2 new rows: R6C jurisdiction 1325,
  CMU jurisdiction 950), `zone_standards` (1 new row: R6C max_density_du_acre
  =6.0, sourced from Sumter Code of Ordinances Sec. 13-423 Table 13-423A,
  same table column already cited for sibling code R6M) — these two writes
  fixed a **live G regression** this session caused and caught before
  close-out (see Deviations).
- `public.gold_standard_campaign` id=5661 — close-out row updated (see
  SSOT bindings).

## SSOT bindings used

- `multi_county_auctions` — read extensively; PATCHed for pasco (14 rows:
  assessed_value/latitude/longitude/property_address), manatee (10 rows:
  property_address/city/zip/parcel_id/assessed_value/judgment_amount/
  latitude/longitude, 5 fully resolved), sumter (1 row: 2024-CA-000448 full
  enrichment). Explicitly named in the issue body ("I additionally needs
  address/geo/value enrichment on multi_county_auctions"), satisfying M2's
  read-only exception.
- `fl_parcels` — read-only SSOT for pasco (co_no=61) and sumter (co_no=70)
  backfills. Zero writes.
- `parcel_zones` — INSERT (13 pasco, 1 sumter F30D019, 1 sumter D28E030, 1
  sumter G04N163), PATCH (1 pasco jurisdiction_id fix), plus 53 rows
  inserted by manatee's `shard_manatee_i_zoning.py` re-run.
- `zoning_districts` / `zone_standards` — INSERT (2 + 1 rows, sumter only,
  see above). Real ordinance/GIS-sourced, honesty-marker tagged where
  inferred (pasco's 13 DOR_UC-crosswalk rows carry an INFERRED tag matching
  precedent; sumter's 3 zone_code rows are exact live GIS matches, not
  inferred).
- `bid_decisions` — INSERT 1 row (sumter, 2024-CA-000448).
- `public.gold_standard_campaign` id=5661 — close-out UPDATE per mandatory
  protocol: criteria_passed (nested per-county A-J booleans + notes),
  criteria_total=10, exit_reason='partial' (adapted from the
  timeout/certified template — this shard is neither: pasco reached 10/10
  live but needs a 2nd consecutive fleet 10/10 run to auto-certify per DoD,
  which this session does not control; manatee/sumter have genuine
  remaining gaps, one architectural (C) and one data-exhausted (sumter
  E/I/J)), session_end_at=2026-09-03T16:45:00Z.
- `public.gold_standard_ultraloop_audit` — 8 rows dispatched via Workflow
  (run wf_922c1c5e-f60): pasco/I, pasco/G (regression-recheck), manatee/E,
  manatee/I, sumter/E, sumter/I, sumter/J, sumter/G (regression-recheck).
  Workflow was still running at the time this spec was written — see Open
  items for how to check the final survived/refuted verdicts.

## DoD status (per pencil_dod_evaluate_county, live, this session)

### pasco — 10/10 (was 9/10 at session start)
| Letter | Before | After | Status |
|---|---|---|---|
| A-H | all PASS (unchanged) | unchanged | VERIFIED |
| I | 94.8% (362/382) FAIL | 98.4% (376/382) PASS | VERIFIED |
| J | 95.3% (unchanged) PASS | 95.3% PASS | VERIFIED (unaffected) |

pasco is NOT auto-certified by this session — DoD requires "the second
consecutive 10/10 daily 07:30Z run," which is outside this session's control.

### manatee — 9/10 (was 7/10 at session start)
| Letter | Before | After | Status |
|---|---|---|---|
| A,B,D,F,G,H,J | PASS (unchanged) | unchanged | VERIFIED |
| E | 94.4% (169/179) FAIL | 97.2% (174/179) PASS | VERIFIED |
| I | 94.4% (169/179) FAIL | 96.6% (173/179) PASS | VERIFIED |
| C | 91.6% (164/179) FAIL | 91.6% (164/179) FAIL, unchanged | BLOCKED (architectural, see Deviations) |

### sumter — 6/10 (unchanged pass-set, narrowed gaps)
| Letter | Before | After | Status |
|---|---|---|---|
| A,B,D,F,G,H | PASS (unchanged) | unchanged | VERIFIED |
| E | 90.9% (30/33) FAIL | 93.9% (31/33) FAIL | VERIFIED, still below 95% gate |
| I | 84.8% (28/33) FAIL | 93.9% (31/33) FAIL | VERIFIED, still below 95% gate |
| J | 90.9% (30/33) FAIL | 93.9% (31/33) FAIL | VERIFIED, still below 95% gate |
| C | 87.9% (29/33) FAIL | 87.9% (29/33) FAIL, unchanged | BLOCKED (architectural, see Deviations) |

Evidence (fresh RPC calls, this session, `pencil_dod_evaluate_county`):
pasco after-state — `{"I":{"pass":true,"metric":98.4,"detail":"card_complete=376 of 382"}, "G":{"pass":true,"metric":95.6,...}}`.
manatee after-state — `{"E":{"pass":true,"metric":97.2,"detail":"parcel_linked=174"},"I":{"pass":true,"metric":96.6,"detail":"card_complete=173 of 179"},"C":{"pass":false,"metric":91.6}}`.
sumter after-state — `{"E":{"pass":false,"metric":93.9,"detail":"parcel_linked=31"},"I":{"pass":false,"metric":93.9,"detail":"card_complete=31 of 33"},"J":{"pass":false,"metric":93.9,"detail":"deal_complete=31"},"G":{"pass":true,"metric":96.9,"far":100.0,"pk1000":100.0},"C":{"pass":false,"metric":87.9}}`.

## Negative test result

- Manatee/sumter C: confirmed via direct arithmetic that matched_clean's
  gap is EXACTLY explained by CLERK_SSOT_CANCELLED rows (manatee: 15 =
  179-164, matches 14 CLERK_SSOT_CANCELLED + 1 PHANTOM_NOT_ON_CLERK exactly;
  sumter: 4 = 33-29, matches 4 CLERK_SSOT_CANCELLED exactly). This is by
  design per migration `20260903h_pencil_dod_matched_any_redeemed_cancelled_gap.sql`'s
  own comment ("C (matched_clean) is intentionally left untouched"). NOT
  re-litigated in this session — respecting a same-day (2026-09-03)
  architectural decision is in scope; overriding it is not, per M5 scope
  discipline and the PARALLEL-FLEET shared-code-path caution.
- Sumter RealForeclose lane re-tested live for 074/129/448's sale dates
  (09/17/2026, 10/01/2026, 10/29/2026): 0 items on all 3 dates via
  `harvest_date("sumter", "sumter", ..., platform_domain="realforeclose.com")`
  — confirms sumterclerk_foreclosure is genuinely the sole source, unchanged
  from the 2026-08-27 finding.

## Deviations from intent

- **Live G regression caught and fixed mid-session (sumter).** Inserting
  parcel_zones rows for F30D019/D28E030/G04N163 without matching
  zoning_districts entries initially caused sumter G to regress from
  100/100/100 to density=93.8/far=33.3/pk1000=33.3 (FAIL) — the view's
  applicability fallback (`CASE WHEN far_regulated IS NOT NULL THEN
  far_regulated ELSE category IN ('commercial','industrial','mixed-use')...`,
  per precedent in `20260825_gold_standard_miami_dade_g_iuc_far_applicability_fix.sql`)
  defaults an UNCLASSIFIED zone_code (no zoning_districts row at all) to
  applicable=true. Fixed within this session by inserting proper
  zoning_districts rows (R6C=residential, CMU=mixed_use matching sibling
  code RMU's exact categorization convention in the same jurisdiction) plus
  one real ordinance-sourced zone_standards row for R6C. Live-reverified:
  sumter G is back to PASS (96.9/100.0/100.0). Per M4/M2, this is logged
  here explicitly rather than silently — any future session should note
  that adding a zone_code to parcel_zones WITHOUT a matching
  zoning_districts row is a live G-regression risk for any county.
- **exit_reason='partial'** used instead of the template's timeout/certified
  — neither applies honestly to a 3-county shard with mixed outcomes; see
  SSOT bindings section for the full reasoning.
- Did not run `gold_standard_loop()`/`gold_standard_certify()` per the
  issue's own PARALLEL-FLEET RULES ("skip loop and report per-county
  evaluations" when other shards may be mid-flight) — used
  `pencil_dod_evaluate_county` per-county calls only, as instructed.
- Ultraloop verification dispatched via the Workflow tool (native
  multi-agent fan-out, `ultraloop_mode='fallback'` logged per the
  instructions' native/fallback distinction, since this was manual
  Workflow orchestration rather than the CC-native `/effort ultracode`
  in-session mode) rather than inline self-verification, per the issue's
  ULTRALOOP PROTOCOL and the user's explicit "ultracode" opt-in this turn.

## Open items / handoff

- **Check `gold-standard-19807-ultraloop-verify` (run `wf_922c1c5e-f60`)
  results** — 8 adversarial-refutation claims (pasco I+G-recheck, manatee
  E+I, sumter E+I+J+G-recheck) were dispatched and may still have been
  running when this spec was committed. Query
  `SELECT * FROM gold_standard_ultraloop_audit WHERE dispatch_id='33847d2f-ce63-400d-a68e-e2971b0c13bd'`
  for final survived/refuted verdicts before treating pasco as
  certification-eligible.
- **Pasco**: 4 rows remain unresolved (2 addressed-but-no-parcel rows failed
  ArcGIS geocode with 0 candidates — field name in
  `geocode_two_addressed_rows()` may be wrong for Pasco's Parcels_2023
  FeatureServer schema, worth re-checking; 2 rows carry parcel_id=
  'IPLTMULE', a scraper bug where a boolean/flag field name leaked into the
  parcel_id column instead of a real folio — this is a scraper defect
  worth fixing at the source, out of scope for this per-row session).
  None of the 4 blocked pasco's I from passing (98.4% already clears 95%).
- **Manatee**: 5 of 10 originally-bare case numbers still have no address
  (AJAX harvest returned judgment_amount only for those 5) — E/I already
  pass at current levels but these 5 remain genuinely incomplete rows.
  C is structurally capped at 91.6% and will not move without an
  architect-level evaluator decision on CLERK_SSOT_CANCELLED denominator
  treatment (out of this shard's scope).
- **Sumter**: 2026-CA-000074 and 2026-CA-000129 are the sole remaining
  blockers for E/I/J (currently 93.9%, need 32/33=96.9%+ i.e. at least one
  of these two resolved). Both confirmed dead at the source across 3+
  sessions. The only genuinely untried levers left (per the 2026-08-27
  doc, still true): Turnstile-solving for civitekflorida.com/myfloridacounty.com,
  or direct phone contact with the Sumter Clerk's office — both outside
  autonomous session scope. C is structurally capped at 87.9%, same
  architectural blocker as manatee.
