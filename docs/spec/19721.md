# Gold Standard shard-1: brevard, lake, st_johns, madison, miami_dade (dispatch `05fae79e-d054-4642-9a75-46fe992d73e3`)

Session: 2026-09-02 08:00Z wave, headless (`claude -p`, no human in loop), issue #19721.
Diagnose -> fix -> adversarial-verify pipeline, one work item per letter/county. Multiple
parallel agents shared this working directory across counties/letters within the run; all
pushes went through `git pull --rebase origin main` before push per the parallel-fleet rules.

## Intent reference
None (recurring Gold Standard campaign session — brief delivered inline via
`summit_chat_dispatch.summit_body`, not a `docs/intent/` file).

## What was built

- `scripts/lake_c_ssot_cancelled_recheck_gs_session.py` — lake C live-docket recheck of
  19 `CLERK_SSOT_CANCELLED` rows against `courtrecords.lakecountyclerk.org/sci`; flipped 1
  genuine reopen (`2025CA000251`) to `CLERK_VERIFIED`.
- `supabase/migrations/20260902_gold_standard_stjohns_c_ca25_0645_data_ceiling.sql` —
  st_johns C: documents case `CA25-0645` as a genuine data ceiling (county has not yet
  attached parcel/judgment data); 0 rows written, `agent_ops_log` entry only.
- `scripts/gold_standard_miami_dade_20260902_c_null_parity_triage.py` +
  `supabase/migrations/20260902d_gold_standard_miami_dade_c_null_parity_triage.sql` —
  triaged 73 `parity_status IS NULL` miami_dade rows; 2 honestly stampable as
  `CLERK_SSOT_CANCELLED` (helps D only, C is unaffected by design).
- `scripts/gsd_miami_dade_20260902_d_orphan_stamp_fix.py` +
  `supabase/migrations/20260902_shard3_miami_dade_d_orphan_stamp_fix.sql` — stamped 3
  orphaned `tier1_authoritative=true` rows to `CLERK_SSOT_CANCELLED` (genuine
  bankruptcy/order cancellations), moving D numerator 605->608.
- `supabase/migrations/20260902e_gold_standard_miami_dade_i_zonelink_backfill_6parcel.sql` —
  inserted 6 `parcel_zones` rows (real zone codes researched via Miami-Dade ArcGIS
  MunicipalZone_gdb / MD_MDCZoning layers), each gated on a pre-existing
  `zoning_districts`+`zone_standards` pair to avoid a G regression; moved I 602->612.
- `supabase/migrations/20260902_madison_bf_9th_reconfirm_blocked.sql` — 9th independent
  reconfirmation that madison B/F are a genuine zero-accrual ceiling (no dollar figure
  exists anywhere to backfill).
- brevard I: diagnosis-only this session (no fix attempted) — logged BLOCKED, structural
  ceiling (BCPAO's own system of record shows no situs address for the dominant failure
  bucket; Firecrawl lever confirmed fleet-wide dead, HTTP 402, same-day predecessor
  session).

Migrations applied live via PostgREST/Supabase during the session (see commit list below);
all committed to `supabase/migrations/` for repo history per HARD GUARDRAIL 3.

## SSOT bindings used

- RPC `public.pencil_dod_evaluate_county(p_county text)` — read before/after for all 5
  counties (full JSON pasted in DoD table below and in the issue comment).
- `public.multi_county_auctions` — `parity_status`/`parity_source`/`parity_checked_at`
  patched for 1 lake row + 5 miami_dade rows (2 for C-triage/D-side-effect, 3 for D-orphan).
- `public.parcel_zones` — 6 rows inserted (miami_dade I fix), ids 877007-877012.
- `public.agent_ops_log` — 7 rows inserted this session documenting blocked/partial items
  (brevard I, madison B, madison F, st_johns C, miami_dade C, miami_dade D, lake C).
- `public.gold_standard_ultraloop_audit` — adversarial-verify rows for lake/C (id 20521),
  miami_dade/D (id 20527), miami_dade/I (id 20538), all `survived=true`,
  `dispatch_id=05fae79e-d054-4642-9a75-46fe992d73e3`.
- `public.gold_standard_campaign` (id 5558) — close-out UPDATE (criteria_passed, exit_reason,
  session_end_at) applied at end of session.

## DoD status

| # | Work item | Status | Evidence |
|---|-----------|--------|----------|
| 1 | brevard I | BLOCKED | Diagnosis-only; structural ceiling (BCPAO no-situs-address + brevard_clerk source lacks parcel_id); `agent_ops_log` id `fdaca81e-583b-4d57-9dc0-2c66e1486e61` |
| 2 | lake C | VERIFIED (survived=true) | Metric moved 85.8%->86.5% (matched_clean 121->122); still FAIL at threshold. `gold_standard_ultraloop_audit` id 20521. Commit `393760d` |
| 3 | st_johns C | BLOCKED (no metric movement) | 94.2% before == 94.2% after; genuine 1-row data ceiling (case CA25-0645, county hasn't published parcel/judgment yet). `agent_ops_log` id `a164d0b3-4a09-41ab-ba0b-e2b79feee850`. Commit `e97abbb` |
| 4 | madison B | BLOCKED | 9th independent reconfirmation, zero closed/sold rows exist (structurally unmeasurable). `agent_ops_log` id `72ba484f-9114-4f28-b947-28582005c93a` |
| 5 | madison F | BLOCKED | Same root cause as B (closed_sold=0). `agent_ops_log` id `e0e10b3a-92a1-4a52-9659-ef1266db8944`. Commit `ca1552e7` (prior in fleet history, reconfirmed this session) |
| 6 | miami_dade C | FALSE POSITIVE (survived=false — no metric movement, reported honestly) | 82.7% before == 82.7% after. 2 rows fixed correctly went to D not C by canon design. `agent_ops_log` BLOCKED row. Commit `095683b` |
| 7 | miami_dade D | VERIFIED (survived=true) | Metric moved 88.1%->88.5% (matched_any 605->608); still FAIL at threshold. `gold_standard_ultraloop_audit` id 20527. Commit `a6ddb6e` |
| 8 | miami_dade I | VERIFIED (survived=true) | Metric moved 87.6%->89.1% (card_complete 602->612, denominator reconciled at 687 vs stale 657 in brief). `gold_standard_ultraloop_audit` id 20538. Commit `11d224b` |

## Fresh before/after (queried live at session close-out, all 5 counties)

**brevard** — 7/10 (C, D, I fail):
```json
{"A":{"pass":true,"metric":1090,"detail":"fc=6398 td=1090"},"B":{"pass":true,"metric":98.7,"detail":"verified=297 closed_sold=301"},
 "C":{"pass":false,"metric":94.4,"detail":"matched_clean=7072"},"D":{"pass":false,"metric":94.7,"detail":"matched_any=7093"},
 "E":{"pass":true,"metric":97.7,"detail":"parcel_linked=7313"},"F":{"pass":true,"metric":99.0,"detail":"tier1_sold=298 closed_sold=301"},
 "G":{"pass":true,"metric":99.1,"detail":"density=99.7 far=99.1 pk1000=100.0"},"H":{"pass":true,"metric":0.0},
 "I":{"pass":false,"metric":84.4,"detail":"card_complete=6322 of 7488"},"J":{"pass":true,"metric":95.8,"detail":"deal_complete=7172"},
 "auctions_total":7488}
```
(Task-brief baseline `I=86.0` was stale — denominator grew 7099->7488 between brief-write and this session's live read; I fix was blocked this session so I did not move: still ~84.4%.)

**lake** — 9/10 (C fails):
```json
{"A":{"pass":true,"metric":11,"detail":"fc=130 td=11"},"B":{"pass":true,"metric":100.0,"detail":"verified=8 closed_sold=8"},
 "C":{"pass":false,"metric":86.5,"detail":"matched_clean=122"},"D":{"pass":true,"metric":99.3,"detail":"matched_any=140"},
 "E":{"pass":true,"metric":97.9,"detail":"parcel_linked=138"},"F":{"pass":true,"metric":100.0,"detail":"tier1_sold=8 closed_sold=8"},
 "G":{"pass":true,"metric":96.0,"detail":"density=96.0 far=100.0 pk1000=100.0"},"H":{"pass":true,"metric":1.2},
 "I":{"pass":true,"metric":95.0,"detail":"card_complete=134 of 141"},"J":{"pass":true,"metric":97.2,"detail":"deal_complete=137"},
 "auctions_total":141}
```
(Before: C=85.8% matched_clean=121. After this session's 1-row fix: C=86.5% matched_clean=122 — moved, still FAIL.)

**st_johns** — 9/10 (C fails):
```json
{"A":{"pass":true,"metric":55,"detail":"fc=65 td=55"},"B":{"pass":true,"metric":100.0,"detail":"verified=1 closed_sold=1"},
 "C":{"pass":false,"metric":94.2,"detail":"matched_clean=113"},"D":{"pass":true,"metric":99.2,"detail":"matched_any=119"},
 "E":{"pass":true,"metric":99.2,"detail":"parcel_linked=119"},"F":{"pass":true,"metric":100.0,"detail":"tier1_sold=1 closed_sold=1"},
 "G":{"pass":true,"metric":100.0,"detail":"density=100.0 far=100.0"},"H":{"pass":true,"metric":0.1},
 "I":{"pass":true,"metric":99.2,"detail":"card_complete=119 of 120"},"J":{"pass":true,"metric":98.3,"detail":"deal_complete=118"},
 "auctions_total":120}
```
(Before == After: 94.2%, matched_clean=113 — no movement, confirmed genuine 1-row ceiling.)

**madison** — 8/10 (B, F fail — structural zero-accrual):
```json
{"A":{"pass":true,"metric":2,"detail":"fc=6 td=2"},"B":{"pass":false,"metric":null,"detail":"verified=0 closed_sold=0"},
 "C":{"pass":true,"metric":100.0,"detail":"matched_clean=8"},"D":{"pass":true,"metric":100.0,"detail":"matched_any=8"},
 "E":{"pass":true,"metric":100.0,"detail":"parcel_linked=8"},"F":{"pass":false,"metric":null,"detail":"tier1_sold=0 closed_sold=0"},
 "G":{"pass":true,"metric":100.0,"detail":"density=100.0 far=100.0"},"H":{"pass":true,"metric":1.2},
 "I":{"pass":true,"metric":100.0,"detail":"card_complete=8 of 8"},"J":{"pass":true,"metric":100.0,"detail":"deal_complete=8"},
 "auctions_total":8}
```
(Before == After: null/null — unchanged, 9th reconfirmation of a genuine data ceiling, zero closed auctions exist.)

**miami_dade** — 6/10 (C, D, E, I fail):
```json
{"A":{"pass":true,"metric":239,"detail":"fc=448 td=239"},"B":{"pass":true,"metric":100.0,"detail":"verified=36 closed_sold=36"},
 "C":{"pass":false,"metric":82.7,"detail":"matched_clean=568"},"D":{"pass":false,"metric":88.5,"detail":"matched_any=608"},
 "E":{"pass":false,"metric":93.2,"detail":"parcel_linked=640"},"F":{"pass":true,"metric":100.0,"detail":"tier1_sold=36 closed_sold=36"},
 "G":{"pass":true,"metric":98.1,"detail":"density=98.1 far=100.0 pk1000=100.0"},"H":{"pass":true,"metric":0.1},
 "I":{"pass":false,"metric":89.1,"detail":"card_complete=612 of 687"},"J":{"pass":true,"metric":100.0,"detail":"deal_complete=687"},
 "auctions_total":687}
```
D moved 88.1%->88.5% (survived=true) and I moved 87.6%->89.1% (survived=true) this session; C was
attempted (2 rows fixed) but stayed flat at 82.7% by canon design (cancellations count toward D,
never C); E was not targeted this session (denominator grew, dragging E from prior-session levels
down to 93.2% — flagged as next-session priority, not touched this session).

## Negative test result

- No cron jobs 109/111/115 or `gold_standard_loop`/`gold_standard_certify` scoring jobs touched.
- `public.gold_standard_loop()` / `public.gold_standard_certify()` NOT run mid-session per guardrail 4.
- No PropertyOnion-derived `data_source` written as verified/independent for any B or F outcome
  (guardrail 1) — explicitly checked and confirmed clean on every write this session (lake, miami_dade
  C/D/I fixes) and explicitly NOT used even though a PropertyOnion hit surfaced address/date data for
  st_johns CA25-0645 (left unwritten per guardrail).
- No protected object (`gold_standard_*` core tables, `public.insights`, `taxi_meter_*`,
  `multi_county_auctions` bulk ops, `spi_*`, `winnerdata.*`) dropped/truncated/bulk-deleted.
- Fail-loud invariant preserved — no new silent except/catch introduced in any script this session.

## Deviations from intent

- Task brief's stated brevard-I baseline (6106/7099, 86.0%) and miami_dade C/D/I baselines
  (87.5/93.2/91.6) were stale versus live denominators at session start — auction ingestion grew
  the denominators between brief-authoring and this session's live reads. Diagnoses used live
  numbers; reported explicitly rather than silently reconciling to the brief's stale figures.
- miami_dade C fix (2-row triage) produced zero C movement by design (cancellations route to D,
  not C) — reported as a false positive (`survived=false`), not shipped as a win.
- st_johns C fix attempt produced zero movement — reported as BLOCKED/false-positive rather than
  claiming any progress.

## Open items / handoff

- **brevard I**: structural ceiling at ~84.4%. Remaining levers: (1) Firecrawl vs bcpao.us once
  budget restored (max ~55 rows), (2) case_number->parcel_id resolution for 170 `brevard_clerk`
  rows. Neither alone nor combined reaches 95%; likely needs a new data source.
- **lake C**: 86.5%, ceiling largely irreducible (most remaining `CLERK_SSOT_CANCELLED` rows are
  genuinely cancelled). Separate pipeline bug flagged (not fixed): `scripts/clerk_ssot/run_parity.py`
  permanently excludes past-`auction_date` rows from re-check, making any wrongly-tagged
  cancellation unfixable by design once its date passes.
- **st_johns C**: 94.2%, 1 row short of threshold (`CA25-0645`), genuine timing gap — needs a
  tier1/RealForeclose calendar re-scrape once the county publishes parcel/judgment data for that
  case (sale not until 2026-10-08).
- **madison B/F**: 9th consecutive session reconfirming a genuine zero-accrual ceiling. Do not
  re-attempt without either a real sale closing or a CAPTCHA-solving budget for secondary sources.
- **miami_dade C**: 82.7%, needs the 73 `parity_status IS NULL` rows run through the tier1
  litmus-matching harvester (31-row 2026-09-02 batch is the largest single lever); do NOT touch
  the 46 already-correctly-excluded cancellation rows.
- **miami_dade D**: 88.5%, ~70 orphaned rows remain untouched (unresolved LISTED/JUDGMENT_VACATED/
  unbacked-duplicate-track dispositions) — left NULL per classify-don't-guess canon.
- **miami_dade E**: newly identified as failing this session (93.2%, not targeted) — flag for next
  session; likely shares root cause with I's zone-linkage gap (23 parcels missing from zoning
  substrate).
- **miami_dade I**: 89.1%, 8 more parcels documented as NOT safely fixable without either a new
  `zoning_districts`/`zone_standards` row (5 parcels) or creating a `jurisdictions` row from
  scratch (1 parcel, Biscayne Park) — both out of scope for a minimal I-fix.
- No county in this shard reached genuine 10/10 this session — step 4's Telegram certification
  notification was correctly skipped (see close-out UPDATE below).
