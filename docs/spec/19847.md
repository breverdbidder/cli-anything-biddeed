# Spec — issue #19847 — C3: Projects + Report side panel (Claude.ai Projects/Artifacts parity)

Sub-task of #19829. This document supersedes the original C3 session's spec after a **JUDGE VERDICT fix pass**
(2026-09-04 16:26 UTC comment on PR #19930, pushed to the same branch `claude/issue-19847-deal-rooms-reports-c3` —
no second PR was opened, per that comment's explicit instruction).

## What changed in this fix pass (do not re-litigate — these were the three JUDGE VERDICT blockers)

1. **Palette regression fixed.** The original session's two standalone-document generators
   (`buildConversationReportHtml`, `shareLinkPageShell`) hardcoded 19+ literal hex values from the retired dark
   theme (`#020617`, `#0f172a`, `#1E3A5F`, `#F59E0B`, `#94a3b8`, `#cbd5e1`, `#e2e8f0`, `#f1f5f9`, `#64748b`) instead
   of the canon light palette the 2026-09-04 11:13 UTC sweep (#19878, commit `6dd9ac68`) put on every other Worker
   page. **Interpretation note (flagging for reviewer):** because these two functions render *standalone* documents
   (a new-tab print page and the public `/r/:token` page) that never inherit the `/chat` shell's own `:root`, they
   cannot reference the shell's `var(--navy)` etc. tokens — there is no DOM tree in common. Every other place in this
   codebase that needs the canon palette in a document with no shared `:root` (e.g. `html[data-theme=light]{...}` at
   `src/worker.js:1881`, or the S5 report's own inline `:root` at `src/worker.js:1313` onward) defines the hex values
   **once**, as `--token: #hex` pairs, then references `var(--token)` everywhere else — never repeating the literal
   value. This fix follows that exact, pre-existing convention: a single new `REPORT_DOC_TOKENS` constant
   (`:root{--cream-1:#fbfaf7;--cream-2:#f5f0e8;--cream-3:#ede3d7;--ink:#1f1b16;--muted:#6e655e;--border:#ddd5c9;
   --terracotta:#9f4d32}`) is injected into both documents' `<style>` blocks, and every single property in both
   functions now reads `var(--...)` — **zero** repeated/scattered hex literals (down from 22 raw hex occurrences in
   the two functions to 7, each appearing exactly once, inside that one token-definition line, matching every other
   "token convention" file in this repo). If the reviewer's own gate greps for literal hex anywhere at all including
   inside a `:root` definition, that is a stricter bar than this repo's own established pattern applies anywhere
   else — flagging explicitly rather than silently guessing. VERIFIED (evidence below): rendered both functions in
   Node and regex-scanned the output — the only hex substrings left are the 7 tokens inside `REPORT_DOC_TOKENS`,
   each used exactly once.
2. **Renamed "Deal Rooms" → "Projects" everywhere**, per Ariel's 2026-09-04 15:40 UTC scope-update comment: sidebar
   entry, all table names, all JS function/variable names, all route paths, all user-facing copy, all code comments.
   `grep -ni "deal.room\|dealroom\|room_id\|getRoomOwned\|biddeed_deal_room"` across `src/worker.js` and the
   migration file now returns **zero** matches outside two comments that document the historical rename (kept
   deliberately, per K3 "mention it, don't delete it" — they explain *why* the code looks the way it does).
3. **S1–S5 Sticky Layers + Project FILES delivered**, scoped realistically for a single session (see "S1–S5 status"
   below for exactly what is real vs. deferred — nothing here is claimed done without evidence).

## Intent reference
No `docs/intent/19847.md` exists. Worked from the issue body + the two issue comments (scope update, JUDGE VERDICT)
per the artifact-chain rule (M6) — comments are lower precedence than the body only where they *conflict*; here they
are additive scope, so both apply.

## Scope built this session (cumulative: original C3 session + this fix pass)

1. **Migration** (`supabase/migrations/20260904b_deal_rooms_reports_19847_c3.sql`, same filename as the original PR
   — its *contents* were rewritten in this fix pass; it has **never been applied to production**, file-only, per
   the issue's explicit "NEVER apply DDL to production from the runner"): `public.biddeed_projects`,
   `public.biddeed_project_items`, `public.biddeed_project_files` (new in this fix pass), `public.biddeed_reports`,
   `public.biddeed_share_links`, storage bucket `artifacts` (private). RLS enabled, zero permissive policies,
   `anon`/`authenticated` revoked, `service_role` only.
2. **Routes** (renamed + expanded):
   - `GET/POST /chat/api/projects`, `GET/PATCH/DELETE /chat/api/projects/:id` (GET returns the S1 "since you were
     here" greeting and advances `last_viewed_at`)
   - `POST /chat/api/projects/:id/items` ("Add to Project")
   - `GET/POST /chat/api/projects/:id/files` (list / upload, versioned by filename), `PATCH`/`DELETE
     /chat/api/projects/:id/files/:fileId` (rename / delete), `GET
     /chat/api/projects/:id/files/:fileId/download` (10-min signed URL)
   - `GET/POST /chat/api/reports` (now `?project=` instead of `?room=`), `GET /chat/api/reports/:id/download`,
     `POST /chat/api/reports/:id/share` + `/share/revoke`, public `GET /r/:token`
3. **UI**: "Projects" list above "Recent chats" in the existing left drawer; a combined Files+Reports right panel
   (reusing the existing `panel-col` infrastructure) with a project banner showing the S1 greeting; a live
   "➕ Project" action on every assistant message; a "📁 New Project" entry point on every county page
   (`/chat?new_project_county=<slug>`); `?intent=` query-param pass-through for pre-filling a project's first note.

### Files touched (this fix pass, on top of the original C3 diff)
- `src/worker.js` — renamed every Deal-Room identifier/route/table reference to Projects; added
  `getProjectDetail`/`countNewCountyAuctionsSince`/`getLatestAuctionDateForCase` (S1); added
  `nextFileVersion`/`insertProjectFile`/`listProjectFiles`/`listProjectFilesForContext`/`getProjectFileOwned`/
  `renameProjectFile`/`deleteProjectFile` + 3 new routes (Project Files); wired `project_id` into the `/chat/api`
  streaming handler so a project's own conversation is created once and reused, and its files' extracted text is
  injected into the model context with a citation instruction (S4); fixed the palette regression in
  `buildConversationReportHtml`/`shareLinkPageShell`; added the county-page "New Project" hook; rewrote the left
  drawer, right panel, and client JS for the new naming + files UI.
- `supabase/migrations/20260904b_deal_rooms_reports_19847_c3.sql` — rewritten (same filename): renamed tables, added
  `biddeed_project_files`, added `case_number`/`parcel_id`/`sale_date`/`notes`/`conversation_id`/`first_touch`/
  `last_viewed_at` to `biddeed_projects`, renamed `biddeed_reports.room_id` → `project_id`. Still **not applied to
  production** — file-only, unchanged posture from the original session.
- `docs/spec/19847.md` — this file (rewritten).

## SSOT bindings used
- `public.biddeed_projects` / `_project_items` / `_project_files` / `_reports` / `_share_links` — new tables
  (migration file only, not yet live).
- `public.biddeed_chat_conversations` / `_messages` — existing C2 tables, read/linked (a project's `conversation_id`
  column points at one; nothing in C2's schema was altered).
- `public.multi_county_auctions` — **read-only** (M2 protected table), used by S1's
  `countNewCountyAuctionsSince`/`getLatestAuctionDateForCase` to compute the "since you were here" greeting from
  live data, never fabricated. VERIFIED live schema (read via the repo's own anon key, which is already hardcoded
  in `src/worker.js` for public reads): confirmed `county`, `case_number`, `auction_date`, `created_at` all exist
  on this table.
- Storage bucket `artifacts` — new, private, migration file only (not yet live); now also holds project files under
  `{project_id}/{filename}.v{n}`.
- No SSOT table named in M2 (gold_standard_*, insights, taxi_meter_*, spi_*, winnerdata.*) was touched; the one M2
  table this feature *reads* (`multi_county_auctions`) is read-only, never written.

## S1–S5 Sticky Layers status (per the scope-update comment) — honest per-item breakdown

| Layer | What was asked | What's real (VERIFIED/CONFIRMED) | What's deferred (UNKNOWN/not built) |
|---|---|---|---|
| S1 Persistent context | Remembers county/case/sale date/notes; greets with what changed since last visit | `biddeed_projects` stores county/case_number/parcel_id/sale_date/notes. `GET /chat/api/projects/:id` computes `new_auctions_count` via a real, live-schema-verified query against `multi_county_auctions` filtered by county + `created_at > last_viewed_at`, and detects sale-date drift by comparing the stored `sale_date` to the latest `auction_date` for the same case_number/county — then advances `last_viewed_at` (and updates `sale_date` if it drifted). Client renders `"<N> new <county> auctions since you were here"` / `"Sale date moved to <date>"` in a banner on project open. This is a real computed diff, not a canned string — verified by reading the actual query logic and confirming the target table's columns exist live. | Not tested against real rows (no live project has ever been opened twice, since the migration isn't applied) — the query shape is CONFIRMED correct, the live behavior is UNTESTED. |
| S2 Hooks | "Add to Project" on every county/deal/calendar/report page; `?intent=` pre-fill | "Add to Project" is live on every assistant chat message (was previously disabled with a "Coming in P2" tooltip). A "📁 New Project" link is live on every one of the 67 `/county/:slug` pages (VERIFIED: rendered `buildCountyPage('brevard', ...)` in Node and confirmed the link's href resolves to `new_project_county=brevard`), landing on `/chat?new_project_county=<slug>` which auto-creates-prompts a project with the county pre-filled. `?intent=` is read and passed through to the new project's `first_note`/`first_touch`. | Deal pages, calendar rows, and the SIGNAL$ report page do **not** yet have an "Add to Project" hook — this is a real scope cut, not an oversight: wiring into every one of those surfaces (each a separate, large template in this same file) did not fit this session's realistic budget without risking the correctness of the parts that *are* done. Flagged as a follow-up, not silently claimed complete. K2/K3 (this repo's own simplicity/surgical-change rules) argue against a rushed, unverified touch of a dozen unrelated templates in the same sitting as a rename+hex fix. |
| S3 Progressive disclosure | Report sections show real names with locked values until bought, unlocking in place | The *existing* SIGNAL$ report engine (`renderS5ReportHtml`/`s5Section`) already has its own section-gating mechanism, entirely independent of this issue. This session did not modify that engine or connect a Project's report list to it. | Not built. The Project "Reports" panel lists this issue's own conversation-derived reports (JSON/PDF/CSV), which have no gating concept — they are not SIGNAL$ reports. Connecting a project to an actual SIGNAL$ report's gated view is a real, separate feature (which report? which parcel? purchase state tracked how?) that the issue text does not fully specify — UNKNOWN, flagged rather than guessed at. |
| S4 Property-scoped chat | Project has its own chat thread; composer defaults to it; Deed cites project files | Real and wired end-to-end: opening a project sets `chatState.projectId` and, if the project already has a `conversation_id`, loads that conversation so the composer continues in the same thread; the first message sent with a `project_id` and no existing thread creates one conversation and stores it on the project (`biddeed_projects.conversation_id`), so every subsequent message in that project reuses it rather than creating a new one. The `/chat/api` POST handler now fetches the project's files (`listProjectFilesForContext`, most-recent 6, `extraction_status='ok'` only) and injects each one's extracted text into the model context with an explicit instruction to cite the filename in quotes when using a fact from it — the same pattern already proven for single-upload attachments (`upload_id` → `attachmentCtx`) in C2, just extended to "all of this project's files" instead of "one explicitly attached file." | Not tested end-to-end against a live model call (this session has no `ROUTER_PROXY_KEY`/live Anthropic proxy access) — the context-injection code path is CONFIRMED correct by reading it against the proven single-upload precedent, not independently model-tested. |
| S5 Memory | First-touch/last-seen; alerts reference project by name | `first_touch` (jsonb: source, intent, county, timestamp) is written once at project creation and never overwritten. `last_viewed_at` doubles as "last seen." Both are real, queryable columns. | No alert/notification system exists anywhere in this codebase to reference a project by name — the message-action row's own "🔔 Alert" button is *already* shipped disabled with "Coming in P3 — Alerts and Watches" (pre-existing, this session did not touch it). Claiming S5 "alerts reference the project by name" would be fabricating a subsystem that does not exist. Correctly scoped down to the two real fields; the alert-integration half is UNKNOWN/not-yet-buildable until P3 alerts exist. |

**Files (the "attachment and download abilities")**: `biddeed_project_files` supports upload (PDF/CSV/DOCX/XLSX/
images, same 8MB cap and same best-effort text extraction as C2's chat uploads), version (re-uploading the same
filename creates v2, v3, ... — never overwrites), rename, delete, and download via a 10-minute signed URL — all four
routes are live and were exercised against a running local Worker (see "Live verification"). "Download all as a
zip" was explicitly **not** built, per Ariel's "never zip" instruction in the scope-update comment.

## DoD status (original issue body's acceptance list)

| # | DoD item | Status | Evidence |
|---|----------|--------|----------|
| 1 | curl round trip for every route with a real token | VERIFIED (routing/auth layer, live) / UNTESTED (against real data) | Every renamed/new route was exercised against a real running local Worker (`wrangler dev --local`) this fix-pass session. Because the migration is correctly not applied to production, DB-backed outcomes are UNTESTED; auth-gate ordering, route dispatch, and error shapes are VERIFIED live (see "Live verification" below, includes exact curl output). |
| 2 | a downloaded file that opens | VERIFIED (mechanism) / UNTESTED (live download) | `buildConversationReportHtml`/`shareLinkPageShell` re-rendered in Node this session — well-formed HTML, zero scattered hex (see fix #1 above), identical `window.print()` PDF mechanism as the existing S5 report. Not fetched through a live signed URL (no live storage object exists — migration not applied). |
| 3 | share link 200 then 404 after revoke | VERIFIED (DB logic, isolated, from original session) / UNTESTED (live HTTP) | Unchanged from the original C3 session's isolated-Postgres verification (share-link schema and query shapes were not touched by this fix pass). Live HTTP round trip needs the migration applied first. |
| 4 | cross-user negative test (other email → 0 rows) | VERIFIED (live, isolated Postgres) | Re-ran the isolated-Postgres verification this fix pass (see "Migration verification" below) against the actual renamed schema: `bob@example.com` reading `alice@example.com`'s project by id → `0` rows; `bob@example.com` reading alice's project file → `0` rows; alice reading her own → `1` row. |

## Migration verification (disposable local Postgres, this fix-pass session — production untouched)

Ran `docker run postgres:15` (port 15433, destroyed at the end of this session), created the `pgcrypto` extension
and `anon`/`authenticated`/`service_role` (`BYPASSRLS`) roles, a stub `storage.buckets` table, applied C2's own
`20260904a_chat_persistence_19829_p1.sql` first (satisfies the `conversation_id` FK), then applied this fix pass's
rewritten `20260904b_deal_rooms_reports_19847_c3.sql`. Results:

- Both migrations applied cleanly with `psql -v ON_ERROR_STOP=1`, in order, zero errors.
- `\d public.biddeed_projects` / `\d public.biddeed_project_files` confirm the exact columns/types/defaults/FKs/
  CHECKs described above (including the new `case_number`/`parcel_id`/`sale_date`/`notes`/`conversation_id`/
  `first_touch`/`last_viewed_at` columns and the `biddeed_reports.project_id` FK with `ON DELETE SET NULL`, and
  `biddeed_project_files.project_id` FK with `ON DELETE CASCADE`).
- `SET ROLE anon; SELECT * FROM public.biddeed_projects` → `ERROR: permission denied for table biddeed_projects`
  (RLS enabled, zero policies, no grants — same default-deny pattern as every other `biddeed_*` table).
- Cross-user isolation, the exact query shapes the Worker runs (`getProjectOwned`/`getProjectFileOwned`):
  - `bob@example.com` reading `alice@example.com`'s project by id → **0 rows**.
  - `alice@example.com` reading her own project by id → **1 row**.
  - `bob@example.com` reading alice's project file → **0 rows**.
- File versioning (`nextFileVersion`): two files with the same `filename='notice.pdf'` and different `version`
  (1, 2) both persist as separate rows under the same `project_id` — confirms re-uploading a filename creates a new
  version rather than overwriting, matching `insertProjectFile`'s design.
- FK cascade: deleting a project cascade-deletes its `biddeed_project_files` rows (`ON DELETE CASCADE`, confirmed:
  0 file rows remain after the parent project delete).

## Live verification (wrangler dev --local, this fix-pass session — no production traffic or data)

Ran `npx wrangler dev --local --port 18787` against this branch's `src/worker.js` with a **dummy**
`SUPABASE_SERVICE_ROLE_KEY` (never the real key):

```
GET  /chat/api/projects                (no token)     -> 401 {"error":"Invalid or missing chat session"}
GET  /chat/api/projects                (garbage token)-> 401 {"error":"Invalid or missing chat session"}
GET  /chat/api/projects/<uuid>         (no token)     -> 401 {"error":"Invalid or missing chat session"}
POST /chat/api/projects                (no token)     -> 401 {"error":"Invalid or missing chat session"}
PATCH /chat/api/projects/abc/files/def (no token)     -> 401 {"error":"Invalid or missing chat session"}
GET  /r/does-not-exist                                -> 404 "Not found"
GET  /chat                                            -> 200, contains "Projects", 0 occurrences of "Deal Room"
GET  /county/brevard                                  -> 200, contains href with "new_project_county=brevard"
```

Also re-ran the exact `node --check` extraction the original session documented: dynamically imported the module,
called `buildChatPage('brevard','test',null)`, extracted all 3 real, fully-interpolated `<script>` blocks from the
rendered HTML (properly resolving every template-literal escape, unlike a raw source-text extraction), and ran
`node --check` on each — **all 3 pass**, including the block containing this fix pass's rewritten Projects/Files/
Reports client JS. Also confirmed `buildConversationReportHtml`/`shareLinkPageShell`'s rendered output contains only
the 7 token-definition hex values described in fix #1, nowhere else.

## Deviations from intent (material ones, with reasons)

1. **Migration not applied to production** (unchanged from original session). File-only, applied at merge.
2. **"PDF" is a self-printing, print-styled HTML document, not real PDF bytes** (unchanged from original session —
   see prior spec revision in git history for the full reasoning; `wrangler deploy --no-bundle` rules out an npm
   PDF library).
3. **CSV generation is conditional on a literal markdown-table signal, not inferred property data** (unchanged).
4. **S2 hooks are wired into chat messages and county pages only, not deal pages/calendar rows/the SIGNAL$ report
   page.** See the S1–S5 table above — a real, explicitly-flagged scope cut, not a silent gap.
5. **S3 (progressive disclosure) is not connected to the existing SIGNAL$ report engine.** See the S1–S5 table
   above — the issue text under-specifies which report/parcel/purchase-state model this would use; guessing at one
   risked shipping a plausible-looking but wrong feature, which the Honesty Protocol explicitly weighs worse than
   flagging it as UNKNOWN.
6. **S5's alert-integration half does not exist because no alert system exists anywhere in this codebase** (pre-
   existing "Coming in P3" gap, not introduced or claimed fixed by this session).
7. **Palette-fix interpretation flagged explicitly** — see fix #1 above. If the reviewer's actual gate is stricter
   than "define hex once as a token, reference `var()` everywhere else" (this repo's own established pattern), say
   so and this can be tightened further (e.g., by having the Worker inject these 7 values from a single shared
   JS-side constant object at request time instead of a CSS `:root` block, which would still need the hex to exist
   *somewhere* in the source but would not appear inside any `<style>` block at all).

## Negative tests
- `GET /chat/api/projects*` / files routes with no `X-Chat-Token`: `401` before any DB call (live, `wrangler dev`,
  this fix pass).
- `GET /chat/api/projects` with a garbage token: `401` (live).
- `GET /r/:token` for a token that was never issued: `404` (live).
- Cross-user ownership-check query shape (`id` + `owner_email` in the same WHERE): VERIFIED live against isolated
  Postgres this fix pass, for both Projects and Project Files (see "Migration verification" above).
- `anon` role against all five tables: `permission denied` (RLS enabled, zero policies, no grants — verified live
  against the isolated Postgres instance this fix pass).

## Open items / handoff
1. Apply `supabase/migrations/20260904b_deal_rooms_reports_19847_c3.sql` at merge.
2. After merge + migration: run the literal live gate from the scope-update comment — create a project from a
   county page, upload a PDF, ask Deed a question in the project chat, confirm the answer cites the file, download
   the file and a generated report (confirm both open), reopen after 10 minutes and confirm the S1 greeting appears,
   cross-user negative test against real rows, harness 0 red on `/chat`.
3. `SUPABASE_SERVICE_ROLE_KEY` must already be provisioned as a Cloudflare Worker secret (C2 handoff item, not
   re-checked this session — no `wrangler secret list` access from this runner).
4. Follow-up scope (S2 full wiring, S3 SIGNAL$ connection) — see Deviations 4–5 above; worth a dedicated follow-up
   issue with the missing details (which report model for S3) specified, rather than guessed at here.

---

## PASS 3 (2026-09-04 18:xx UTC session) — closing the CORRECTION comment's UI-gap finding

**Context.** Between this spec's last revision and this session, three more things happened on the issue thread
(all read per M6's artifact chain — comments, lower precedence than the body, but not in conflict here so they
apply as additive scope):
1. A "PASS 3" scope comment (17:15:32Z) asked for S2 hooks on every remaining surface (county/deal/calendar/
   modal/report), S3 progressive disclosure, and a persistent "Projects" sidebar entry, with a hard acceptance bar
   of **Chromium screenshots**, not curl.
2. A GHA run (33899603401, "CC GHA-only: issue 19847") landed **directly on `main`** (commit `659e2b7a`, not a PR)
   between that scope comment and this session, and itself already shipped: the report-page and both deal-page (post-
   and pre-sale) "Add to project"/"New project from this" hooks, the S3 progressive-disclosure panel
   (`renderSignalDisclosure` + `SIGNAL_REPORT_PRICE_LABEL` + `getSignalReportStatusForProject` + a new migration
   `20260904c_signal_report_paid_status_19847_pass3.sql` + a `stripe-webhook` update to set `paid_at`), and the
   county-page hook (pre-existing from the original C3 session, confirmed still present at `src/worker.js:8393`).
   That run's own GHA conclusion was reported as **failure** (comment at 17:36:30Z) — its commit message and diff
   give no indication why; this session did not diagnose that failure (out of scope — the code that landed from it
   was independently re-verified below, on its own merits, not trusted because the run "succeeded").
3. A CORRECTION comment (17:45:17Z) then said the API-only verdict from the same day was wrong: "Board row C3 is
   therefore API ✅ / UI ❌" — specifically calling out that the persistent `/chat` **Workspace sidebar** (the one
   showing "Home, Auctions, Calendar, Counties, Reports, Blog, Deed" on every page) had **no Projects entry at
   all**. This is a different UI element than the in-chat "Projects" list inside the chat drawer (which already
   existed, reachable only via a "🕘 Chats" button once already on `/chat`) — the correction's point is that nothing
   on the rest of the site (or even on `/chat` itself, unless you already knew to click that button) surfaced
   Projects as a first-class, site-wide feature.

**What this session verified was already true (re-confirmed, not re-built).** `node --check src/worker.js` — clean.
Re-read the report-page hook (`src/worker.js:1597`), both deal-page hooks (`:5201`, `:5379`), and the county-page
hook (`:8393`) — all four render a `/chat?new_project_county=&case=&source=` link, all four still present and
syntactically intact. `grep -c "deal.room\|room_id"` — zero (the C3 rename held).

**What this session built.** The one concrete gap the CORRECTION comment named: a **"📁 Projects" entry in the
persistent Workspace sidebar** (`src/worker.js`, `withPublicShell`'s `nav` array, next to Home/Auctions/Calendar/
Counties/Reports/Blog), linking to `/chat?view=projects`. On the client side, a five-line handler
(`src/worker.js`, inside the same closure as `requireIdentityThen`/`openDrawer`/`loadProjects` — the existing S2
hook IIFE) detects `?view=projects` and calls `requireIdentityThen(function(){openDrawer();loadProjects();})` —
the exact same sign-in-sheet gate every other project entry point already uses, reusing existing functions with
zero new mechanism.

**Evidence (VERIFIED, this session, live).**
- `node --check src/worker.js` — clean.
- Rendered `withPublicShell(buildChatPage('brevard','test',null), '/chat')` in Node (temp copy of the module with
  two extra `export`s added purely for this verification, never shipped) — confirms `/chat?view=projects` and the
  `Projects` label are in the emitted HTML, the `qp.get('view')==='projects'` handler string is present, and a
  regex count of `/deal.room|deal room/i` across the rendered output is `0`.
- All 5 fully-interpolated `<script>` blocks extracted from that same rendered `/chat` page pass `node --check`
  individually (same technique the prior fix-pass session used).
- **Live `wrangler dev --local` + real Chromium (Playwright) screenshots** — the acceptance bar this pass's scope
  comment explicitly raised over curl:
  - `/chat` at 1440×900 and 390×844 — "Projects" visible in the desktop Workspace sidebar and in the mobile
    hamburger drawer, both between "Reports" and "Blog", exactly where it was coded.
  - `/chat?view=projects` at 1440×900, anonymous session — the drawer auto-opens with "Projects" as the top panel
    (`#chats-drawer.open` count = 1) and the sign-in sheet renders under "Recent chats" (`.chats-signin` count = 1)
    — confirms the "signed-out users get the sign-in sheet first" requirement for this specific new entry point.
  - `/county/brevard` at 1440×900 — confirms the pre-existing "New Project" floating hook is still live (not
    modified this session, re-verified only).
  - `/report/<sample-mca-id>?key=<public-sample-key>` (the built-in, no-auth, no-billing sample report route) at
    1440×1000 — confirms the "ADD TO PROJECT" toolbar button from the prior pass's diff is still live, and that
    the "Reports" nav item correctly highlights as active on this route.
  - The deal-page (`/deal/:county/:case`) and presale deal-page hooks were **not** independently screenshotted
    this session — both require a real `multi_county_auctions`/reel row and this sandbox has no live Supabase
    access (only a dummy `SUPABASE_SERVICE_ROLE_KEY` was bound to `wrangler dev`); their hook code was re-read and
    confirmed structurally intact (see above) but the on-screen render is **UNTESTED** this session, not
    fabricated.

**Second repo: `breverdbidder/biddeed-web` (the `/radar` app), per M5.** The scope comment's remaining S2 targets —
"each calendar row and the property detail modal in /radar" — live in a separate Next.js repo this Worker proxies
to (`src/worker.js:2640-2657`, the AuctionRadar proxy branch), not in this repo. Per M5 ("for other repos: clone to
/tmp, change, commit, push from inside the clone"): cloned `breverdbidder/biddeed-web` to `/tmp/biddeed-web`,
added the same `/chat?new_project_county=&case=&source=` link to:
- `components/auctions/AuctionSidebarList.tsx` — the per-property row rendered when a calendar day is drilled
  into (the actual "calendar row"; the calendar grid itself, `AuctionCalendar.tsx`, only shows day-level aggregate
  counts, never individual properties, so there is no per-property row to hook there).
- `components/auctions/AuctionsLayout.tsx` — the property detail modal's footer.

Evidence: `npx tsc --noEmit` clean on both files (this repo requires Node ≥22; this sandbox's default Node 20
could not `npm ci` it — used the sandboxed Node 24 toolchain to install and type-check). `AuctionSidebarList` was
server-rendered with mock auction data via `react-dom/server` and the emitted HTML was regex-checked: the "Add to
project" text is present and the `href` carries the correct county/case query params. `AuctionsLayout`'s modal was
**not** independently rendered (it depends on Supabase/Clerk data-fetching effects and `next/navigation`'s router
that this sandbox has no live credentials for, and per this org's CREDENTIAL HANDLING mandate a secret is never
pulled into a chat/runner session just to stand up a local demo) — verified by type-check and code review only.
Committed on branch `claude/issue-19847-pass3-radar-hooks`, pushed, PR opened:
https://github.com/breverdbidder/biddeed-web/pull/38.

**S3 progressive disclosure and the rest of S2 (deal pages, calendar, report) beyond what's listed above** were
not touched this session — they were already delivered by the prior (build-time-failed but code-intact) GHA run
described above, and re-verifying every one of those live (which would require exercising a real Stripe purchase
and a live `multi_county_auctions` row) is out of this session's realistic scope; they are re-confirmed present in
source only, per the evidence list above.

**Homepage sidebar entry** (also named in the PASS 3 scope comment, "via #19934") is out of scope for this repo —
issue #19934 tracked that deliverable against `biddeed-web`'s homepage composer explicitly and per M5 this session
did not open a third, unrelated surface inside the `biddeed-web` clone beyond the two files named above.

### DoD status (PASS 3 scope comment's acceptance list)

| # | Acceptance item | Status | Evidence |
|---|---|---|---|
| 1 | Chromium screenshots (1440 + 390) of the control on a county page, a deal page, the calendar and the report | PARTIAL — VERIFIED for `/chat` (Projects nav, both viewports), `/county/brevard` (1440), sample `/report/:id` (1440); UNTESTED for a live deal page and for `biddeed-web`'s calendar/modal (no live data/credentials in this sandbox, not skipped by choice) | See "Evidence" above |
| 2 | curl proof that the hook creates the project with `first_touch` | UNTESTED this session — the route/handler (`createProjectThen` → `POST /chat/api/projects`) was not modified this pass, no regression risk identified, re-verifying it live requires the same real Supabase access noted above | Code re-read only |
| 3 | Sidebar entry "Projects" on the Worker `/chat` shell AND (via #19934) on the homepage | Worker `/chat` shell: VERIFIED live, this session. Homepage (#19934): out of scope for this repo/session — tracked separately | Screenshots above |
| 4 | harness 0 red on `/chat`, `/county/brevard`, `/radar` | `/chat` and `/county/brevard`: VERIFIED 200 + rendered correctly, this session, via `wrangler dev --local`. `/radar`: UNTESTED — that route is proxied to `biddeed-web`, which this sandbox cannot boot with live data (see above) | curl status codes + screenshots |
| 5 | no vendor names; tokens only | VERIFIED — no new hex literals introduced this pass (only a `<a>` tag using existing CSS classes/vars); `grep` for vendor/model names in the new lines → none | Diff review |

### Files touched this pass
- `src/worker.js` — Projects Workspace-sidebar nav entry + `?view=projects` client handler (this repo).
- `docs/spec/19847.md` — this section.
- (separate repo/PR) `breverdbidder/biddeed-web` `components/auctions/AuctionSidebarList.tsx` and
  `components/auctions/AuctionsLayout.tsx` — see PR #38 above.

### Honest residual gap
The CORRECTION comment's core complaint — "from the customer's seat nothing is visible" — is now **partially**
closed: the Projects entry point is visible and live-verified on `/chat` (desktop + mobile), and the pre-existing
county/report/deal hooks were re-confirmed intact. It is **not fully** closed: the `biddeed-web` radar/calendar/
modal hooks are shipped as source + type-check only (PR #38, not yet merged or visually verified), and a live,
credentialed end-to-end walkthrough (create a project from a real page, see it persist, reopen it, see the S1
greeting against real data) still has not happened in any session to date, because no session so far — including
this one — has had live Supabase credentials in its sandbox. That gate is explicitly listed in "Open items /
handoff" above and remains open.
