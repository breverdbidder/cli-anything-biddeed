# Spec — issue #19741 — Gold Standard shard-4: nassau, charlotte, holmes (dispatch 466d2624)

## Intent reference
None at docs/intent/19741.md — issue body was the only brief (per M6, body wins over comments).

## What was built
- `supabase/migrations/20260902_gold_standard_shard4_466d2624_charlotte_d_canceled_per_county_stamp.sql`
  — live UPDATE, not schema DDL: stamps `parity_status='CLERK_SSOT_CANCELLED'` on 2 Charlotte
  `multi_county_auctions` rows (`25001762CA`, `25001218CA`) that already carried
  `tier1_sale_status='CANCELED_PER_COUNTY'` + `tier1_authoritative=true` from the automated tier1
  ingestion pipeline (cron 109/111/115 family — not modified) but had never been parity-stamped.
  Same precedent pattern as `20260825_gold_standard_shard3_03af1f8b_lee_charlotte_washington_fixes.sql`.
  Commit `10bd29a9`, pushed directly to `main`.
- No other code/schema changes. Nassau and Holmes received read-only bounded fresh-lever rechecks;
  zero writes on either (documented ceilings hold — see DoD table).
- `gold_standard_ultraloop_audit`: 6 fresh rows this session, ids 20687–20692, all `survived=true`
  (nassau/C, charlotte/D, holmes/B, holmes/C, holmes/D, holmes/F) — refreshes the certify-gate
  7-day-freshness window for these letters.
- `gold_standard_campaign` id 5594 (dispatch `466d2624-2f1a-4892-a496-c7870b73bad8`) close-out:
  `criteria_passed` per-county 10-letter JSON, `exit_reason='ultraloop_complete_charlotte_d_flipped_nassau_holmes_ceilings_reconfirmed'`,
  `session_end_at=2026-09-02T16:18:27Z`.

## SSOT bindings used
- `public.multi_county_auctions` (county=charlotte/nassau/holmes) — read for diagnosis, 2-row UPDATE
  on charlotte.
- `public.pencil_dod_evaluate_county(county)` — RPC, called before/after for all 3 counties, both by
  the fix/diagnose agent and independently by its adversarial verifier.
- `public.gold_standard_ultraloop_audit` — 6 rows inserted (ids 20687–20692).
- `public.gold_standard_campaign` — 1 row updated (id 5594).
- `public.foreclosure_outcomes` / `public.tax_deed_outcomes` (holmes) — read-only, confirmed unchanged
  (1 row / 0 rows respectively, both pre-dating this session).

## DoD status
### charlotte (was 8/10, now 9/10)
| Letter | Status | Evidence |
|---|---|---|
| A,B,E,F,G,H,I,J | VERIFIED PASS, unchanged | live evaluator, before/after identical |
| C | VERIFIED FAIL, 58.3% (180/309) | **arithmetically proven impossible** to reach 95% while D<100% — re-confirmed 4th–5th time (`scripts/charlotte_c_run20260829_systemic_investigation_ceiling_reconfirmed.py`); real redemption rate (~36% of foreclosure rows) exceeds the 5% budget. Not re-attempted this session; no new evidence changes that math. |
| **D** | **VERIFIED FAIL→PASS, 94.8%→95.5% (293→295/309)** | UPDATE on 2 rows w/ live `tier1_sale_status='CANCELED_PER_COUNTY'` evidence verified fresh at 16:10Z same day, RETURNING confirmed 2 rows, evaluator re-run confirmed 95.5%/pass=true, adversarially verified independently (survived=true, audit id 20688). No other letter regressed. |

### nassau (9/10, unchanged)
| Letter | Status | Evidence |
|---|---|---|
| A,B,D,E,F,G,H,I,J | VERIFIED PASS, unchanged | live evaluator, before/after identical |
| C | VERIFIED FAIL, 94.6% (53/56) | Ceiling reconfirmed (3rd session in 32h on this county/letter). 1 row genuinely `CLERK_SSOT_CANCELLED` (redemption, excluded from clean by design). 2 rows `PHANTOM_NOT_ON_CLERK`. New evidence this session: sibling-row lead-time comparison (`26TD000014AXYX`, same batch/date, already `PARITY_OK`) weakens the "not yet posted" theory for `26TD000013AXYX` in favor of "erroneous/duplicate row" — flagged for a future session to cross-check the live clerk docket directly, not applied (no metric-mover, no live re-fetch this session per HARD GUARDRAILS on redundant scraping). `26TD000009AXYX`'s `auction_status='redeemed'` field predates and is unrelated to its clerk-absence finding — explicitly NOT used to reclassify (would be circular evidence). Zero writes. Audit id 20687, survived=true. |

### holmes (6/10, unchanged — documented structural ceiling, 18+ prior sessions)
| Letter | Status | Evidence |
|---|---|---|
| A,E,G,H,I,J | VERIFIED PASS, unchanged | live evaluator |
| B | VERIFIED FAIL, null (0/0) | Zero drift since the 18th recheck session (2026-08-25). `foreclosure_outcomes` still 1 row (`winning_bid=null`, pre-dates all 18 sessions). Audit id 20689, survived=true. |
| C | VERIFIED FAIL, 68.8% (11/16) | Row count/classification identical to 18th session. Audit id 20690, survived=true. |
| D | VERIFIED FAIL, 68.8% (11/16) | Same 5 `TD#*` rows remain `parity_status=NULL` — no online tax-deed/foreclosure platform exists for Holmes (in-person courthouse sales). Audit id 20691, survived=true. |
| F | VERIFIED FAIL, null (0/0) | `tax_deed_outcomes` still 0 rows. Audit id 20692, survived=true. |

No web re-fetch attempted this session for Holmes (holmesclerk.com / myfloridacounty.com /
civitekflorida.com already exhaustively confirmed dead ends across 18+ sessions, most recently 8
days prior) — re-trying would burn budget re-proving an already-proven negative, which the task
brief explicitly discourages ("switch to the next county/letter rather than idling" applies equally
to not re-idling on an exhausted lever).

## Negative test result
All 8 non-targeted letters across the 3 counties (A,B,E,F,G,H,I,J for charlotte/nassau; A,E,G,H,I,J
for holmes) were re-checked live before and after the charlotte write and found byte-identical —
confirmed independently by the adversarial verifier, not just the fix agent. No regression.

## Deviations from intent
None. Worked exactly the 3 assigned counties, touched only `multi_county_auctions` (a table the
issue body names and works against throughout) plus the mandated audit/campaign tables. Did not run
`gold_standard_loop()`/`gold_standard_certify()` full-fleet scoring — confirmed via
`summit_chat_dispatch` that no other dispatch was `state='processing'` at close-out time, so the
close-out protocol's loop+certify branch was eligible, but was skipped in favor of the lighter
per-county `pencil_dod_evaluate_county` verification already mandated as the primary evidence
standard; no fleet-wide scoring job was invoked or modified (HARD GUARDRAILS #4 compliance).

## Open items / handoff
- **charlotte C**: arithmetically capped below 95% while Charlotte's real redemption rate stays this
  high. Not a fixable data-quality gap. Do not re-investigate without a new litmus source (PropertyOnion
  confirmed zero coverage of Charlotte's current cycle in the 2026-08-29 investigation).
- **nassau C**: 53/56 ceiling, but flagged a live lever for a future session — direct clerk docket
  cross-check of `26TD000013AXYX` vs sibling `26TD000014AXYX` (same batch, same auction_date
  2026-10-13, sibling already `PARITY_OK`). If `...013` is confirmed a duplicate/erroneous ingestion
  row rather than a real case, resolving it (reclassify, not delete, pending stronger evidence) would
  not move C (still excluded from `matched_clean` either way) but would improve D/E denominator
  accuracy. Low priority — does not move any letter's pass/fail.
- **holmes B/C/D/F**: confirmed zero drift for the 19th consecutive session. Recommend future shard
  assignments stop re-checking Holmes on a daily cadence and instead treat it as a standing ceiling
  pending either (a) a real auction closing with a findable public outcome, or (b) a paid
  courthouse-records source authorized under ARM-2 ($50/mo budget, unused to date).
- charlotte's 10 tax-deed rows (auction_date 2026-09-01, now past) still show `tier1_sale_status=NULL`
  — no tier1 ingestion run has covered that date yet. This should resolve naturally via the existing
  automated pipeline (not modified this session); a future session should check whether it has and,
  if so, apply the same `CANCELED_PER_COUNTY`→`CLERK_SSOT_CANCELLED` stamp pattern used here.
