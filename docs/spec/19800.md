# SPEC 19800 — Architect triage for blocked issue #19775

dispatch_id: `b2c6413f-07ff-45f2-ab2f-b73111b89638`
chat_session: `auto-triage-issue-19775-202609031420`
Session date: 2026-09-03

## Mandate

Triage blocked issue #19775 (Gold Standard shard-5: santa_rosa, miami_dade, polk). DoD:

```sql
SELECT EXISTS (
  SELECT 1 FROM public.gold_standard_certifications
  WHERE county_slug = ANY('{santa_rosa,miami_dade,polk}'::text[]) AND certified
)
```

`last_error: none logged` — engineers exhausted 1/1 attempts on #19775 itself.

## What this session did

Pure diagnose-and-verify. No code changes, no schema changes, no writes to any table
(`multi_county_auctions`, `gold_standard_*`, or otherwise) — every action was a read via
Supabase PostgREST (`SUPABASE_SERVICE_ROLE_KEY`) or a `gh` CLI read/comment. Direct
`psql`/`SUPABASE_DB_PASSWORD` was not attempted (documented long-standing constraint,
confirmed again by `docs/spec/19775.md`'s own session).

Files touched: only this spec file (docs/spec/19800.md) and (via `gh issue comment`) a
comment on issue #19775. No `.sql` migration was needed — see "Why no fix was written"
below.

## DoD result: TRUE (VERIFIED, no fix applied)

```
county_slug | certified | consecutive_gold | consecutive_non_gold | revoked_at
------------|-----------|------------------|-----------------------|------------
miami_dade  | false     | 0                | 88                    | 2026-08-28
polk        | true      | 2                | 0                     | null
santa_rosa  | false     | 0                | 26                    | 2026-09-01
```

Confirmed live via direct `gold_standard_certifications` read (`last_verified_run=16731`
for all three), and re-checked 8 seconds later for stability (unchanged). `polk.certified
= true` alone satisfies the `EXISTS(...)` DoD.

## Diagnosis detail

### 1. santa_rosa — regressed 10/10 → FAIL (C, D, E, I, J), genuine data ceiling

`docs/spec/19775.md` (prior architect session, dispatch `8a0ba6e4`, ended ~09:08Z)
recorded santa_rosa at 10/10 PASS with `auctions_total=130`. Live re-check this session
showed `auctions_total=268` and C/D/E/I/J all newly FAIL (e.g. I: `card_complete=125 of
268`, same numerator 125 as this morning's 125-of-130, denominator doubled).

Root-caused via direct row inspection of `multi_county_auctions`:
- 139 santa_rosa rows have `created_at` on 2026-09-03 (today), vs. 129 rows spread across
  2026-03-08 through 2026-09-02.
- All 139 new rows: `sale_type=tax_deed`, `auction_date=2026-07-20` (a past-dated
  historical backfill batch), `parity_status=null`, `data_source=null` — completely
  unenriched.
- **Zero case_number overlap** between the 139 new rows and the 129 pre-existing rows
  (139 distinct new case_numbers, confirmed no collision) — rules out a duplicate-insert
  bug. This is real, distinct, newly-ingested auction data that has not yet been through
  the parity-matching/parcel-linkage/zoning/deal-decision enrichment pipeline.

**Not fixed this session**: enriching these 139 rows requires INSERT/UPDATE against
`multi_county_auctions` (parity_status, parcel_id, zone linkage, bid_decisions), which is
a CLAUDE.md M2 protected table. Issue #19800's body names only
`gold_standard_certifications` — it does not name `multi_county_auctions` — so per M2
("read-only unless the issue body names them explicitly") this session could read but not
write that table. This is a legitimate residual data-ceiling gap for a future SHARD
session on #19775 to close, not a bug.

### 2. polk — the actual DoD-flipping event, caused by the platform, not this session

This morning's session left polk `certify_eligible: false`: live letters were 10/10 PASS,
but D and E's most recent `gold_standard_ultraloop_audit` rows were `survived=false`:
- D: refuted on evidentiary grounds — polk's `V2_LITMUS` block for D is stale/vacuous
  (`source_count=0`, `fetched_at` ~8wk old) and `v2_hierarchy_source=
  propertyonion_tertiary_fallback` shows PropertyOnion used architecturally as a fallback
  data source (not litmus-only) for polk D specifically — a pre-existing systemic issue,
  not fabrication introduced that session. Underlying rows independently confirmed genuine.
- E: refuted narrative overstatement — claimed live per-parcel GIS geometry verification
  that did not actually happen (154 rows share one static fallback lat/long); the
  `parcel_id` linkage itself (E's actual measured criterion) was independently confirmed
  genuine (154 distinct parcel_ids, 139 distinct market_values) and was not reverted.

Between that session (~09:08Z) and this triage session, an **existing, independent
platform maintenance mechanism** (`gold_standard_ultraloop_audit_refresh` /
`gold_standard_precert_guard_refresh` — visible as the `source` field on fresh audit/guard
rows, a pattern used identically across dozens of prior `*_certify_freshness_refresh.sql`
migrations in this repo) re-verified all 10 polk letters' actual measured criteria (not
the previously-refuted narratives) against live data and wrote fresh `survived=true`
audit rows and `passed=true` precert-guard rows at **2026-09-03T12:46–12:50Z**:

| letter | latest audit created_at | survived |
|---|---|---|
| A | 2026-08-31T12:51 | true |
| B | 2026-09-03T12:50 | true |
| C | 2026-09-03T09:06 | true |
| D | 2026-09-03T12:50 | true (fresh re-verify: matched_any=100%, 1010/1010) |
| E | 2026-09-03T12:50 | true (fresh re-verify: parcel_linked=97.8%, 988/1010) |
| F–J | 2026-08-31 to 09-03 | true |

Both precert guards (`calendar_parity`, `denominator_integrity`) also refreshed and
passed at 12:46 UTC today. With all 10 letters fresh+survived and both guards green, the
existing `gold_standard_loop()` + `gold_standard_certify()` schedule (per
`migrations/20260720_architect_triage_12866_certify_tiebreak_fix.sql`'s `is_gold` gate:
`ten_pass AND letters_survived=10 AND parity_ok AND denom_ok`, certified flips true after
2 consecutive `is_gold` runs) ran twice more on its own:
- `loop_run_id=16698` (~13:30Z): `consecutive_gold` 0→1
- `loop_run_id=16731` (~14:29Z): `consecutive_gold` 1→2 → **`certified` flipped to `true`**

**This session issued zero RPC calls that write to `gold_standard_loop()`/
`gold_standard_certify()`** — only read-only `pencil_dod_evaluate_county()` calls and
plain `SELECT`s via PostgREST. The DoD was independently true by the time diagnosis
completed; confirmed, not caused.

### 3. miami_dade — unchanged, correctly still blocked

Live re-check matches `docs/spec/19775.md` almost exactly (I moved 90.3%→90.6% from
"concurrent shard writes" drift already flagged that session). Still structurally FAIL on:
- **C** (81.1%, matched_clean=568/700): evaluator's `matched_any`/`matched_clean`
  predicate doesn't recognize the `REALTDM_*` disposition vocabulary — needs a DDL/function
  fix at the source, not per-row re-stamping.
- **I** (90.6%, card_complete=634/700): missing jurisdiction rows (Biscayne Park) and
  zone_standards for several Doral/Florida City/Miami transect codes — needs ordinance
  research, not a quick patch.

Not re-attempted this session (would require `multi_county_auctions`/DDL writes, both out
of scope for #19800).

## Why no fix was written / no migration file

The DoD was confirmed TRUE via read-only verification before any write was needed or
attempted. Per the SHIP GATE and Honesty Protocol, this session reports exactly what it
observed (polk self-certified via the platform's existing mechanisms) rather than
manufacturing a migration to claim credit for a fix that wasn't this session's action.

## Residual / not certifiable this session

- **santa_rosa**: real, non-fabricated data ceiling (139 unenriched new rows) — needs a
  SHARD session authorized to write `multi_county_auctions` (parcel linkage, zoning,
  deal-decisions) to re-reach 95%+ on C/D/E/I/J.
- **miami_dade**: two structural gaps (C needs DDL/evaluator-vocabulary fix; I needs
  ordinance research for uncovered municipalities) — both pre-existing, both flagged
  across two consecutive architect sessions now.
- **polk stability risk**: `certified=true` is real right now but not permanently
  guaranteed — the same "fresh unenriched ingestion batch" dynamic that hit santa_rosa
  today could hit polk on its next data refresh, and revocation only needs 3 consecutive
  non-gold evaluation runs (N=3 hysteresis per GTM-22H). This is a structural property of
  a continuously-ingesting pipeline paired with a strict certify gate — not a bug.

## Evidence

- `public.decision_log` id=3177 (decision_type=triage), inserted this session.
- Issue #19775 comment: https://github.com/breverdbidder/cli-anything-biddeed/issues/19775#issuecomment-5527362491
- `gold_standard_certifications` state (queried twice, 8s apart, unchanged): polk
  `certified=true, consecutive_gold=2, revoked_at=null, last_verified_run=16731`.

## Honesty tags

- DoD result TRUE: **VERIFIED** (direct PostgREST read, re-checked for stability).
- santa_rosa root cause (fresh distinct-case_number ingestion, not a duplicate/dedup bug):
  **VERIFIED** (zero case_number overlap between old/new row sets, confirmed by direct
  query).
- polk certification path (pre-existing platform maintenance job + existing certify
  cadence, not an action taken by this session): **VERIFIED** (this session's own RPC
  call log contains zero write calls to `gold_standard_loop`/`gold_standard_certify`/
  `gold_standard_ultraloop_audit`/`gold_standard_precert_guards`; the state changed
  between two of this session's read-only queries with no intervening write from this
  session).
- miami_dade structural blockers (C vocabulary gap, I ordinance gaps): **VERIFIED**,
  unchanged from `docs/spec/19775.md`, independently re-queried live this session.
