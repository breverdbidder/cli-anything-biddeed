# Spec — issue #19787 — CMO Factory CP3b(v2): Remotion cleared, video stack wired, license tripwire installed

## Intent reference
No `docs/intent/19787.md` exists — issue body was the only brief (M6 fallback). No comments on the issue at dispatch time.

## Mandates applied
- M1: no producer send anywhere in this session.
- M3: no vendor/tool names in any public-facing asset (unchanged from #19779; not touched this session).
- M5: scope discipline — one issue, one lane (`cli-anything-biddeed`); `breverdbidder/zonewise-superpowers` was edited via a separate `/tmp` clone, committed and pushed from inside that clone, per M5's explicit carve-out for other repos.
- M6: this file, written last, per the artifact chain.

## What this session built (all VERIFIED live, not projected)

### 1. License tripwire — `remotion_license_risk` (D8 in `public.gtm_watchdog()`)
- `supabase/migrations/20260903f_gtm_watchdog_d8_remotion_license_risk.sql` — adds D8, reading `agent_ops_log` rows written by the new CI check (mirrors D5/D6's read-from-log-table pattern; Postgres has no git/filesystem access to compute author counts or grep repos itself). Applied live (`mgmt_sql.py`, `STATUS 201`).
- `scripts/remotion_license_tripwire.py` — 3 real checks: (a) distinct human git authors, 90d, across `zonewise-superpowers`+`zonewise-video` (real `gh repo clone` + `git log --since`, bot/CC identities excluded) — ADVISORY-FAIL if >3; (b) `@remotion/player`/`<Player>` grep across `biddeed-web`/`zonewise-web`/`winnerdataai-web`/`winnerdataai-mvp`/`src/worker.js` — HARD FAIL; (c) a render call whose composition/entry-point is driven by `req.body`/`req.file`/`req.query` — HARD FAIL.
- `.github/workflows/gtm-validate.yml` — new step invoking the tripwire script.

**Demonstrated tripping live:**
- D8 SQL: inserted a synthetic `agent_ops_log` row (`task='remotion_license_risk_player'`, `status='BLOCKED'`, evidence describing a synthetic `@remotion/player` import in `biddeed-web`), called `public.gtm_watchdog()` → `tripped=true`, `tripped_detectors` included `remotion_license_risk`, `spi_gates` row `remotion_license_risk` opened. **Cleaned up same session**: synthetic `agent_ops_log` row deleted, `GTM_WATCHDOG_TRIP` row from the test relabeled `GTM_WATCHDOG_TRIP_SYNTHETIC_TEST_19787`, `spi_gates.remotion_license_risk` closed (`verified_at` set) with a note. Did **not** touch the pre-existing `spi_gates.gtm_factory_halt` row (opened 09:50:13, before this session started, unrelated to this test — out of scope per M5, left as found).
- CI script: monkeypatched the 3 check functions to simulate a Player-import hit, ran `main()` → `::error::...Automators...` + `exit code 1` (negative test a from the issue).
- Real (non-synthetic) run of all 3 checks against the actual repos: **all VERIFIED clean** — 0 distinct human authors in 90d (both repos last pushed >90 days ago — a real, surprising finding: `zonewise-superpowers` itself has had zero commits since 2026-04-06), no Player import anywhere, no user-code render path anywhere. Logged to `agent_ops_log` (`dispatch_id='remotion_license_risk'`, 3 rows, all `VERIFIED`).

### 2. Remotion Free License declaration — `licenseKey: "free-license"`
- Verified the mechanism is real by direct inspection of the shipped `@remotion/cli`/`@remotion/renderer` v4.0.520 npm packages (`options/license-key.js`, `--license-key` CLI flag) plus two independent WebFetch passes of `remotion.dev/docs/licensing` (the first used a leading prompt and was discarded as unreliable; the second asked for verbatim quotes and confirmed: `"Pass a real licenseKey, or declare eligibility for the free license with licenseKey: \"free-license\""`).
- Wired into `breverdbidder/zonewise-superpowers` (cloned to `/tmp`, edited, committed, pushed — commit `349dbfb`, verified live via `gh api repos/.../commits/main`): every `npx remotion render`/`still` CLI example across `commands/*.md` and `skills/remotion-production/rules/ci-rendering.md` now includes `--license-key=free-license`; `commands/setup.md`'s project-detection step now tells the user to declare eligibility if no key is configured.
- This repo (`cli-anything-biddeed`) has no Remotion render config of its own to update (bolt32 uses ffmpeg directly, by design — §0/architecture split in `VIDEO_STACK.md`).

### 3. Bolt32 pipeline wiring (whisperX/faster-whisper/kokoro/auto-editor/QA)
- `scripts/bolt32_captions_whisperx.py` — added `transcribe_words_faster_whisper()`, the real production ASR entry point (faster-whisper, lazy-imported).
- `scripts/biddeed_reels_lib.py::burn_word_captions_bolt32()` — new ffmpeg drawtext function burning whisperX-grouped captions onto an already-rendered bolt32 mp4.
- `scripts/bolt32_recaption.py` — new reusable CLI: download → extract audio → transcribe → group → burn → upload → update DB.
- `scripts/bolt32_tts_fallback.py` — kokoro/chatterbox draft-fallback resolver, `BOLT32_TTS_FALLBACK` env-gated, 5 unit tests, all pass.
- `scripts/bolt32_qa_critique.py` — 4-dimension deterministic QA critique (hook_clarity/caption_readability/beat_timing_drift/loop_seam_continuity), each dimension carries an `observed` evidence field (negative test d enforced by `validate_score()`).
- `scripts/bolt32_no_autoeditor_gate.py` — static gate confirming auto-editor never enters any bolt32-lane file.
- `scripts/bolt32_cost_guard.py` — real per-stage timing harness.
- `supabase/migrations/20260903g_biddeed_reels_bolt32_captions.sql` — adds `video_bolt32_captions_url`, `captions_source`, `captions_groups`, `captions_model`, `captions_generated_at`, `qa_scores`, `qa_pass` to `winnerdata.biddeed_reels`. Applied live.

**Real infra finding (not a code defect):** faster-whisper's model download hit a Hugging Face Hub CloudFront 429 from this session's network egress, on both the metadata API (`api/models/.../revision/main`) and the CDN resolve endpoint (`resolve/main/config.json`) — confirmed via direct `curl`, ruling out a code-level bug. `scripts/bolt32_captions_whisperx.py::transcribe_words_faster_whisper()` raises `Bolt32TranscriptionUnavailableError` in this case; it remains the real, unchanged production entry point. To still produce **real evidence** against **real audio** this session (rather than stopping at this infra ceiling), `bolt32_recaption.py` supports an explicit, non-default `--allow-session-substitute` flag that falls back to `openai-whisper` (downloaded from a different host, OpenAI's own Azure CDN, unaffected by the HF block) — never added to `requirements-bolt32.txt`, always logged as `captions_source='openai_whisper_session_substitute'` in the DB so it's never mistaken for the ADOPTED path.

**Live execution — 4 reels re-captioned, all real:**
| County/Case | `video_bolt32_captions_url` | Duration (ffprobe) | HTTP check |
|---|---|---|---|
| broward/CACE-24-008115 | `https://mocerqjnksmhcjzxrewo.supabase.co/storage/v1/object/public/biddeed-reels/2026-09-01/CACE-24-008115/reel_bolt32_captions.mp4` | 32.0s | 200 |
| escambia/2025 CA 001460 | `https://mocerqjnksmhcjzxrewo.supabase.co/storage/v1/object/public/biddeed-reels/2026-09-01/2025_CA_001460/reel_bolt32_captions.mp4` | 32.0s | 200 |
| lee/2026000141 | `https://mocerqjnksmhcjzxrewo.supabase.co/storage/v1/object/public/biddeed-reels/2026-09-01/2026000141/reel_bolt32_captions.mp4` | 32.0s | 200 |
| martin/25001204CAAXMX | `https://mocerqjnksmhcjzxrewo.supabase.co/storage/v1/object/public/biddeed-reels/2026-09-01/25001204CAAXMX/reel_bolt32_captions.mp4` | 32.0s | 200 |

DoD requires "≥2" — 4 delivered.

**Live QA scores, 3 of the 4 reels** (`qa_scores`/`qa_pass` columns, verified via direct `select`):
| County | overall_score | qa_pass | loop_seam_continuity |
|---|---|---|---|
| broward | 8.48 | false | 6.13/10 |
| escambia | 8.80 | false | 6.87/10 |
| lee | 8.72 | true | 7.38/10 |

Real finding: 2/3 fail the loop-seam dimension (threshold 7.0) because the end-beat's QR/wordmark overlay changes pixels versus the reused hook frame — a genuine, unresolved design tension (residual, logged in `VIDEO_STACK.md`, not fixed this session — fixing it is an `assemble_video_bolt32` redesign, out of CP3b scope).

### 4. Repo reconciliation
One-line verdicts written to `docs/gtm/VIDEO_STACK.md` §4 (REUSE ×7, ARCHIVE-CANDIDATE ×1, no action taken beyond what's noted — `zonewise-superpowers`'s `licenseKey` wiring and `everest-media-gateway`'s key-liveness check, both described above/below). `everest-media-gateway`'s Gemini key liveness checked via a **free, non-billable** `GET .../v1beta/models` call (`200`, model catalog returned) — this confirms the key authenticates, NOT that the specific Veo3/Imagen4 paid-generation quota flagged as depleted 2026-09-02 has recovered (no generation call made, no spend authorized this issue). Paid-quota state: **UNKNOWN**. Key liveness: **VERIFIED**.

### 5. GHA cost guard
Real per-reel wall-clock measured this session (martin/25001204CAAXMX, full `bolt32_recaption.py` run): **42.42s total** (download ~1.2s, extract audio 0.13s, transcribe 6.98s [openai-whisper base substitute] / 15.94s [faster-whisper small, separately benchmarked], burn captions ~14.0s, upload ~3.0s). **11.8% of the 360s (6-minute) budget** — `bolt32_cost_guard.py::evaluate()` confirms `over_budget=False`. Logged to `agent_ops_log` (`dispatch_id='bolt32_cost_guard'`, `status='VERIFIED'`). `quota_gate_check('engineering')` returned `NO_READING` at call time — a pre-existing telemetry gap (same one already holding `spi_gates.gtm_factory_halt` open before this session started), observed and logged, not silently bypassed, and not in this issue's scope to fix.

Residual: real `ubuntu-latest` GHA-runner timing not separately measured (no GHA dispatch triggered this session; figures are from this sandbox's CPU).

### 6. Docs / unified_context
- `docs/gtm/VIDEO_STACK.md` — rewritten: §0 (Remotion ruling + 3 tripwires + architecture-split rationale), §2 (Remotion CLEARED), §3 (real bolt32 wiring + real QA scores), §4 (one-line reconciliation), §5 (2 new negative tests, e/f), §6 (real cost-guard numbers), §7 (real `unified_context` upsert), Residual section.
- `public.unified_context` key `cmo_factory_video_stack_v1` — upserted live (first insert; the #19781 session's claim of having already upserted this was not actually present in the DB — `select` returned zero rows before this session's insert, corrected here). Verified via `select key, content->'remotion_ruling'->'status', updated_at ...` → `CLEARED_FREE_LICENSE`.

## Deviations from spec (logged, not silent)
1. Negative test (f) ("a render config missing `licenseKey` fails the license check") is **not implemented as an automated gate** — no live Remotion render config exists anywhere in scope to check against (`zonewise-superpowers` is a scaffolding/plugin repo, not a live Remotion project). Logged as residual in `VIDEO_STACK.md`.
2. `everest-cinematic` (the actual long-form lane) was not touched — auto-editor wiring there is out of verified scope this session (separate repo, no code visibility taken this session).
3. faster-whisper's real model download is infra-blocked this session (HF Hub 429); live demonstration used a documented, opt-in-only substitute. Faster-whisper remains the ADOPTED, unchanged production code path.
4. `zonewise-superpowers`' caption/review agents were not ported into bolt32 (flagged as likely redundant with code already shipped this session, not attempted).
5. Loop-seam-continuity QA failures on 2/3 scored reels — a real finding, not fixed (would require an `assemble_video_bolt32` redesign).

## Files touched this session
- `supabase/migrations/20260903f_gtm_watchdog_d8_remotion_license_risk.sql` — new, applied live.
- `supabase/migrations/20260903g_biddeed_reels_bolt32_captions.sql` — new, applied live.
- `scripts/remotion_license_tripwire.py` — new.
- `.github/workflows/gtm-validate.yml` — new step added.
- `scripts/bolt32_captions_whisperx.py` — `transcribe_words_faster_whisper()` + `Bolt32TranscriptionUnavailableError` added.
- `scripts/biddeed_reels_lib.py` — `burn_word_captions_bolt32()` added.
- `scripts/bolt32_recaption.py` — new.
- `scripts/bolt32_tts_fallback.py` — new.
- `scripts/bolt32_qa_critique.py` — new.
- `scripts/bolt32_no_autoeditor_gate.py` — new.
- `scripts/bolt32_cost_guard.py` — new.
- `docs/gtm/VIDEO_STACK.md` — rewritten.
- `docs/spec/19787.md` — this file.
- `breverdbidder/zonewise-superpowers` (separate repo, `/tmp` clone) — commit `349dbfb`, pushed live.

## SSOT tables/columns touched
- `winnerdata.biddeed_reels` — 4 rows updated with `video_bolt32_captions_url`/`captions_source`/`captions_groups`/`captions_model`/`captions_generated_at`; 3 of those also updated with `qa_scores`/`qa_pass`. `video_bolt32_url`/`duration_bolt32_sec` (from #19779) untouched.
- `public.agent_ops_log` — `dispatch_id='remotion_license_risk'` (3 real check rows + 1 synthetic test row, deleted after use), `dispatch_id='gtm_watchdog'` (1 synthetic trip row, relabeled), `dispatch_id='bolt32_cost_guard'` (1 real timing row).
- `public.spi_gates` — `remotion_license_risk` opened by the synthetic test, closed same session; `gtm_factory_halt` pre-existing open state left untouched (out of scope, not caused by this session).
- `public.unified_context` — `cmo_factory_video_stack_v1` upserted (first real insert).
- `public.gtm_watchdog()` — `CREATE OR REPLACE` with D8 added; D1-D7 unchanged.

## Session cost telemetry
Model `claude-sonnet-5`, issue `19787`.
