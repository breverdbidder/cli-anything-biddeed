name = "ensemble-inference"
main = "index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
account_id = "83ab3f2fbbabd1a9b01a018fb4efe219"

# Secrets — set via provision-ensemble-secrets.yml workflow (workflow_dispatch)
#   SUPABASE_URL         — https://mocerqjnksmhcjzxrewo.supabase.co
#   SUPABASE_SERVICE_KEY — service role JWT
#   WORKER_AUTH_SECRET   — shared secret; same value as ENSEMBLE_WORKER_SECRET on Vercel

[[rules]]
type = "CompiledWasm"
globs = ["**/*.wasm"]
fallthrough = true
