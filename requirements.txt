# SHARD-6 Gold Standard Campaign Dependencies
httpx>=0.24.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
pypdf>=4.0.0

# BidDeed Reels v2 (issue #19752) -- scripts/biddeed_reels_lib.py generate_qr_png()
# Pillow is qrcode's own image backend -- undeclared until this session
# (2026-09-03, issue #19761): qrcode installs without it but raises
# `ModuleNotFoundError: No module named 'PIL'` the first time .make()/.save()
# actually runs, live-reproduced running scripts/biddeed_reels_pipeline_presale.py.
qrcode>=7.4.2
Pillow>=10.0.0

# Landmark Web OR adapter (issue #20050) -- also used by the pre-existing
# scripts/highlands_owner_name_lien_harvest.py, undeclared until this session.
# Landmark Web/AcclaimWeb deployments run Cloudflare/Akamai bot-management
# that 403s plain HTTP clients on the disclaimer/search POST; a real
# Chromium session passes cleanly (see scripts/or_adapters/landmark.py).
playwright>=1.40.0