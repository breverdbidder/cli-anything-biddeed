# CLIProxyAPI Gateway Configuration
# Hetzner: 87.99.129.125:8317 (localhost only — via SSH tunnel)
#
# Available models through gateway:
#   gemini-2.5-pro       → Gemini 2.5 Pro (FREE, 3-key round-robin)
#   gemini-2.5-flash     → Gemini 2.5 Flash (FREE, 3-key round-robin)
#   deepseek-v3          → DeepSeek V3.2 ($0.28/1M)
#   deepseek-r1          → DeepSeek R1 reasoner
#
# Claude stays on Max plan official CLI — NOT proxied.

CLIPROXY_BASE_URL=http://127.0.0.1:8317/v1
CLIPROXY_API_KEY=${CLIPROXY_API_KEY}

# LiteLLM / OpenAI SDK compatible aliases
OPENAI_API_BASE=http://127.0.0.1:8317/v1
OPENAI_API_KEY=${CLIPROXY_API_KEY}
