# Claude Code Plugins (sentinel-managed)

Auto-generated plugin manifests live in `.claude/plugins/`.

Run `/plugin marketplace list` inside Claude Code to see active plugins.
Each `.claude/plugins/<slug>.json` contains the install commands and priority skills.
