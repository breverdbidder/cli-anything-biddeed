session:
  repo: cli-anything-biddeed
  issue: "10981 (C/D LITMUS V2 wiring follow-up)"
  task: "Wire gold_standard_loop C/D to cd_litmus_parity_v2 (SUMMIT dispatch, brief dated Jul 9 2026)"
  loop_run: 3532
  session_started: "2026-07-10T03:40:00Z"
  session_ended: "2026-07-10T03:52:00Z"

decisions:
  - step: 1
    action: "Search repo for prior work on gold_standard_loop / cd_litmus_parity_v2 before touching anything"
    result: >
      Found the exact wiring already attempted and iterated same-day yesterday across three
      migrations (20260709_gold_standard_loop_wire_cd_litmus_v2.sql -> _scope_fix.sql ->
      _cd_v2_wire_gainonly.sql). The final gain-only version's own commit message already
      documented "exactly ONE genuine gain-only C/D flip (hillsborough D) ... not the >=3
      hoped for in the brief" -- i.e. this exact task, with this exact DoD, was already
      attempted and honestly reported as short of the guard.
    reason: "HONESTY PROTOCOL -- never assume a stale brief's premise ('loop does NOT call the v2 evaluator') is still true without checking live state first."

  - step: 2
    action: "Query live DB via pg_get_functiondef(oid) for public.gold_standard_loop (Supabase Management API, SUPABASE_ACCESS_TOKEN -- SUPABASE_DB_PASSWORD auth fails in this environment, same stale-credential issue documented by prior sessions)"
    result: "Live function body already references both cd_litmus_parity_v2 and pencil_dod_evaluate_county_v2, architecture tag 'v9_7_cd_litmus_v2_gainonly'. The mission's core premise (loop not wired) is STALE -- the wiring migration was actually applied live, not just committed as a file."
    reason: "SHIP GATE requires live-DB proof, not file-presence, before treating a migration as shipped."

  - step: 3
    action: "Hand-computed pencil_dod_evaluate_county_v2's C/D metric for all 13 counties with live (48h) cd_litmus_parity_v2 status=ok rows, cross-checked against gold_standard_county_status latest run (3531) and the 8-9-pass_count cohort"
    result: >
      Confirmed only hillsborough/D passes v2's >=95% threshold (96.0%, metric matches
      scoreboard exactly). Every other county with live v2 data (bay, broward, dixie, duval,
      escambia, hendry, highlands, okeechobee, palm_beach, pasco, polk, sarasota) sits well
      below the 95%/99% thresholds. desoto/levy/hamilton have zero usable v2 data (status=
      'unreachable' -- scrapers found nothing on the checked calendar dates).
    reason: "Verify the DoD gap is real before attempting any remediation, not just take the prior session's word for it."

  - step: 4
    action: >
      Confirmed this session runs inside cc-runner-ghonly.yml (GITHUB_ACTIONS=true,
      GITHUB_WORKFLOW_REF=.../cc-runner-ghonly.yml) with real network egress, satisfying the
      task's "cc-runner-ghonly only" constraint. Installed playwright + chromium and ran
      scripts/cd_litmus_v2_realauction_parity.py live against the real RealAuction platform
      for the 6 counties at pass_count=8 with BOTH C and D FAIL (desoto, dixie, levy,
      palm_beach, polk, sarasota) plus okeechobee (already closest to threshold at 94.3%).
    result: >
      Fresh live data written to cd_litmus_parity_v2 (ON CONFLICT DO UPDATE upsert, per the
      07-10 harvester fix already in the .py files). None crossed the 95%/99% pass threshold:
      desoto 33.3% (floridabidder unreachable, held prior value), dixie 0.0% (real 0-match
      count against dixieclerk.com), levy 90.6%, palm_beach 24.2%/37.1%, polk 60.6%/0%,
      sarasota 0%/0% (rolling 14-day window now empty), okeechobee 94.3% (deterministic,
      unchanged from 07-09 -- still 0.7pt short of D's 95% bar).
    reason: "The only honest way to test whether the DoD's >=3-flip guard is achievable is with genuinely fresh live data, not a re-run against stale rows."

  - step: 5
    action: "Ran public.gold_standard_loop() (run 3532) and public.gold_standard_certify() live, then diffed C/D status for every county at pass_count 8-9 in the pre-run baseline (3531) against 3532"
    result: >
      ZERO new C/D flips in the 8-9-pass_count cohort. hillsborough/D and duval/D remained
      PASS (pre-existing, duval's via the original inline calc not v2). gold_standard_certify
      revoked_now=0 -- no regressions, all 7 frozen gold_standard_cert_scope counties'
      C/D status unchanged, gain-only floor held as designed. 2 unrelated counties
      (marion, washington) advanced consecutive_gold accrual, unrelated to this task.
    reason: "Direct before/after diff on the actual scoreboard rows is the only credible evidence for a DoD claim, per Evidence-Before-Claims."

  - step: 6
    action: >
      Noticed 8 OTHER counties (alachua, bay, citrus, clay, franklin, martin, okeechobee/D,
      walton) also flipped C/D FAIL->PASS between run 3531 and 3532. Before attributing these
      to this session's work, checked their `detail` column text and their pre-run pass_count.
    result: >
      All 8 flips carry inline-calc detail text ("matched_clean=X of Y" / "matched_any=X of Y"),
      NOT the "v2:" / "v2_gain:" prefix the wired code emits for a v2-sourced pass -- i.e. these
      are real data-quality improvements from OTHER concurrent shard sessions (git log same
      session window: martin/bay/alachua backfills), landing in multi_county_auctions between
      my two loop runs, completely independent of the v2 wiring. Also none of the 8 were at
      pass_count 8-9 pre-flip (they were at 5-6) -- so even under the loosest reading of the
      DoD text they would not satisfy "counties that were 8-9 pass_count."
    reason: >
      NEVER-LIE / no ghost-success: a coincidental scoreboard flip in the same run is not
      evidence of causation. Misattributing another session's real fix to this task's wiring
      would be exactly the kind of false VERIFIED claim the Honesty Protocol's 3x penalty
      exists to catch.

outcome:
  wiring_requirement: "MET (pre-existing) -- gold_standard_loop live def references both cd_litmus_parity_v2 and pencil_dod_evaluate_county_v2 (v9_7_cd_litmus_v2_gainonly), confirmed via pg_get_functiondef against the live DB, not just a committed migration file."
  dod_flip_guard: "NOT MET -- 0 new C/D PASS flips in the 8-9 pass_count cohort this session (still exactly 1 total, hillsborough/D, unchanged from 2026-07-09). Root cause is a genuine live-data-coverage gap (real RealAuction match rates for near-gold counties remain 0-91% against the 95/99% bar), not a wiring defect -- reproduced this finding independently with fresh, live-harvested data rather than assuming the prior session's conclusion.
  no_regressions: "gold_standard_certify() revoked_now=0; all 7 frozen cert_scope counties' C/D held or improved, never regressed, confirming the gain-only floor is load-bearing as designed."
  no_code_change: "No SQL/function change made -- the requested wiring already exists live and correctly implements the gain-only + hierarchy + status=ok + 48h-lookback contract described in the brief. Re-shipping an unchanged CREATE OR REPLACE would be a no-op; git status stayed clean throughout."

artifacts:
  - "cd_litmus_parity_v2: 8 rows refreshed live (desoto x2, dixie x2, levy x2, palm_beach x2 -- partial run interrupted by a transient Management API timeout, completed in a second pass covering palm_beach/polk/sarasota/okeechobee, 8 more rows) -- all via the proven ON CONFLICT DO UPDATE upsert path"
  - "gold_standard_county_status: loop_run_id 3532 (670 rows, 67 counties)"
  - "gold_standard_certifications: marion, washington re-certified (unrelated to this task)"

verification:
  - check: "gold_standard_loop live def references cd_litmus_parity_v2 / pencil_dod_evaluate_county_v2"
    proof: "SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname='gold_standard_loop' -> both substrings present, architecture='v9_7_cd_litmus_v2_gainonly'"
    status: VERIFIED
  - check: ">=3 counties (8-9 pass_count) gained a new C or D PASS"
    proof: "Diff of gold_standard_county_status between loop_run_id 3531 and 3532, filtered to the pre-run 8-9 pass_count cohort -- 0 rows changed FAIL->PASS"
    status: "VERIFIED as NOT MET"
  - check: "No regressions to frozen cert_scope counties"
    proof: "gold_standard_certify() -> revoked_now=0; direct query of the 7 cert_scope counties' C/D status pre- and post-run showed no PASS->FAIL transitions"
    status: VERIFIED
