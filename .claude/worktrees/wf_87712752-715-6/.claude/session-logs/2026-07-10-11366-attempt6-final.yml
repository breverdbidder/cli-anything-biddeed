session:
  repo: cli-anything-biddeed
  issue: "11366 (C/D LITMUS V2 wiring, attempt 6/6 — final allowed redispatch)"
  task: "Wire gold_standard_loop C/D to cd_litmus_parity_v2 (feed live, loop not consuming)"
  loop_run: 3533
  session_started: "2026-07-10T06:00:00Z"
  session_ended: "2026-07-10T06:10:00Z"

decisions:
  - step: 1
    action: "Read all 17 prior comments on issue #11366 + local session logs from attempts 1-5 before writing any code (redispatch protocol rule 1)."
    result: >
      Attempts 1-5 (18:20Z-03:53Z Jul 9-10) already independently verified: gold_standard_loop
      live def references cd_litmus_parity_v2 / pencil_dod_evaluate_county_v2
      (architecture=v9_7_cd_litmus_v2_gainonly, gain-only OR semantics). Wiring criterion MET
      since commit 43502c4d (Jul 9 ~18:00Z), never a wiring defect. DoD flip guard NOT MET in
      every single one of 5 prior attempts (1 flip max: hillsborough/D, attempt 2). Attempt 5
      (03:40-03:52Z) additionally live-harvested desoto/dixie/levy/palm_beach/polk/sarasota/
      okeechobee and found 0 crossed threshold -- but never posted its findings as a required
      GH comment (silent-end gap the 20:47Z comment had already flagged once before).
    reason: "HONESTY V3 -- never re-attempt work a prior comment/log already marks complete; identify the actual gap (missing comment) instead of blindly redoing the harvest."

  - step: 2
    action: "Independently re-verified live wiring via Management API (SUPABASE_ACCESS_TOKEN, User-Agent: curl/8.5.0 required -- default urllib UA gets 403'd by Supabase's edge)."
    result: >
      pg_get_functiondef(gold_standard_loop) still references both cd_litmus_parity_v2 and
      pencil_dod_evaluate_county_v2, architecture tag v9_7_cd_litmus_v2_gainonly, unchanged
      since attempt 2. cd_litmus_parity_v2 had 24 status=ok rows, freshest fetched_at
      2026-07-10T03:49:28Z (attempt 5's harvest) -- confirming no newer data existed before
      this session's own harvest.
    reason: "SHIP GATE requires live-DB proof, not a re-read of a prior session's claim."

  - step: 3
    action: >
      Ran scripts/cd_litmus_v2_realauction_parity.py live (Playwright/Chromium, real
      RealAuction + FloridaBidder network calls, REALFORECLOSE_* creds present) against all 7
      counties in the 8-9 pass_count cohort with an active C or D FAIL: duval, hillsborough,
      desoto, levy, palm_beach, polk, sarasota. NOTE: attempt 5 had NOT included duval or
      hillsborough in its harvest set -- this is new coverage, not a re-run.
    result: >
      14 rows upserted (ON CONFLICT DO UPDATE, per the harvester-upsert fix already live).
      duval: FC 13/3, TD 20/3 (combined metric 18.2%). hillsborough: FC 5/1, TD 10/8 (combined
      60.0%). desoto: floridabidder unreachable both sale_types (Cloudflare/no-content, same
      as attempt 5). levy: floridabidder unreachable both (same). palm_beach: FC 33/8, TD
      35/13 (combined 30.9%). polk: FC 33/20, TD 0/0-in-window (combined 60.6%). sarasota: FC
      0/0-in-window, TD 0/0-in-window (no live evidence, v2 metric NULL -> FAIL-safe). None
      cross the 95%/99% pencil_dod_evaluate_county_v2 threshold.
    reason: "The only honest way to test the DoD's achievability is fresh live data, not a re-run against stale rows -- matches attempt 5's own stated rationale, extended to the 2 counties it hadn't covered."

  - step: 4
    action: "Ran public.gold_standard_loop() (run 3533) and public.gold_standard_certify() live, then diffed C/D status for the full 8-9 pass_count cohort between run 3532 (pre-session baseline) and 3533."
    result: >
      gold_standard_certify(): revoked_now=0, certified_now=3, no cert_scope regressions.
      C/D diff: sarasota/C and sarasota/D flipped FAIL->PASS (165/187=88.2% -> 183/187=97.9%),
      BUT detail text is the plain inline format ("matched_clean=183 of 187"), not the
      "v2_gain:" prefix the wired code emits for a v2-sourced pass -- i.e. this is a real
      row-level parity_status improvement in multi_county_auctions from OTHER concurrent
      session work between 03:49Z and 06:06Z, not caused by this task's harvest (sarasota's
      own v2 harvest this session returned no usable data, metric NULL). Per NEVER-LIE /
      no-ghost-success (established precedent: attempt 5 step 6 applied the identical test),
      this flip is NOT attributed to the v2 wiring and NOT counted toward the DoD.
      Separately: hillsborough/D flipped PASS->FAIL (was v2_gain via an earlier, narrower
      harvest window; this session's fresher combined FC+TD numbers pulled the metric to
      60.0%, below the 95% bar). Confirmed hillsborough.certified=false (revoked 2026-07-01,
      unrelated to today) -- so this is a scoreboard-letter change with zero live-certification
      impact, and is the "fresh evidence surfacing as designed" the gain-only migration's own
      header comment explicitly anticipates (v2 data can only ever gain a FAIL->PASS relative
      to the INLINE calc; it makes no promise that a prior v2-sourced gain is permanent as the
      harvest window rolls).
    reason: "Direct before/after diff on the actual scoreboard rows is the only credible evidence for a DoD claim, per Evidence-Before-Claims -- and misattributing a coincidental flip to this task would itself be a Honesty Protocol violation."

outcome:
  wiring_requirement: "MET (pre-existing since commit 43502c4d, Jul 9). Re-confirmed live via pg_get_functiondef this session -- architecture=v9_7_cd_litmus_v2_gainonly, references both cd_litmus_parity_v2 and pencil_dod_evaluate_county_v2."
  dod_flip_guard: >
    NOT MET, for the 6th consecutive attempt across ~12 hours. 0 new genuine v2-attributable
    C/D PASS flips in the 8-9 pass_count cohort this session, extending harvest coverage to
    duval and hillsborough (the 2 candidate counties no prior attempt had actually re-harvested
    live). The single coincidental non-v2 flip (sarasota) is explicitly excluded per
    no-ghost-success. Root cause, independently reproduced a third time today with genuinely
    fresh network-live data: real RealAuction/FloridaBidder match rates for every remaining
    near-gold county sit well under the 95%/99% bar (18-90%) or the source is structurally
    unreachable (desoto, levy -- Cloudflare-blocked FloridaBidder, no online RealAuction
    platform configured). This is a live-data-coverage ceiling, not a wiring defect.
  no_regressions_to_live_certification: "gold_standard_certify() revoked_now=0. hillsborough/D's letter-level flip to FAIL has zero certification impact (already non-gold since 2026-07-01)."
  no_code_change: "No SQL/function change shipped -- wiring already exists live and correctly implements gain-only + hierarchy + status=ok + 48h-lookback. A no-op CREATE OR REPLACE would add nothing; git status stayed clean."
  recommendation: >
    This DoD (>=3 flips) cannot be met by re-running the existing wiring against real data --
    3 independent live-harvest attempts (Jul 9 20:46Z, Jul 10 03:49Z, Jul 10 06:05Z) all
    converge on the same ceiling. Further redispatch of this exact task is not expected to
    change the outcome. A genuine fix requires different work entirely: either (a) row-level
    tier1_realauction_v2 case-by-case matching instead of calendar-count parity (flagged as
    follow-up in migrations/20260709_gold_standard_loop_cd_v2_wire_gainonly.sql itself), or
    (b) accept the wiring as shipped-and-correct and retire/redefine this DoD, since it was
    calibrated against an assumption (>=3 easy flips available) that live data does not support.

artifacts:
  - "cd_litmus_parity_v2: 14 rows refreshed live this session (duval x2, hillsborough x2, desoto x2, levy x2, palm_beach x2, polk x2, sarasota x2) via ON CONFLICT DO UPDATE upsert"
  - "gold_standard_county_status: loop_run_id 3533 (670 rows, 67 counties)"
  - "gold_standard_certifications: 3 re-certified this run (unrelated to C/D wiring)"

verification:
  - check: "gold_standard_loop live def references cd_litmus_parity_v2 / pencil_dod_evaluate_county_v2"
    proof: "SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname='gold_standard_loop' -> both substrings present, architecture='v9_7_cd_litmus_v2_gainonly'"
    status: VERIFIED
  - check: ">=3 counties (8-9 pass_count) gained a new v2-attributable C or D PASS"
    proof: "Diff of gold_standard_county_status between loop_run_id 3532 and 3533 for the full 8-9 pass_count cohort (16 counties) -- 0 v2_gain-attributed FAIL->PASS transitions (1 coincidental non-v2 flip excluded per no-ghost-success; 1 v2-attributed PASS->FAIL also observed, zero certification impact)"
    status: "VERIFIED as NOT MET"
  - check: "No regressions to live gold_standard_certifications"
    proof: "gold_standard_certify() -> revoked_now=0; hillsborough/sarasota/duval certified=false since Jun/Jul dates predating this session"
    status: VERIFIED
