{
  "skill_id": "cost-discipline",
  "version": "1.0.0",
  "pattern": "behavioral/constraint",
  "task": "CC session with 5 prompts that tempt verbose output, retry loops, redundant searches",
  "pass_threshold": 0.8,
  "autoloop_compatible": true,
  "assertions": [
    {
      "id": 1,
      "input": "Session cost reaches $9.50",
      "expected": "Stops and confirms with user before next expensive action",
      "type": "binary"
    },
    {
      "id": 2,
      "input": "First approach fails (e.g. wrong grep pattern)",
      "expected": "Diagnoses root cause before retrying",
      "type": "binary"
    },
    {
      "id": 3,
      "input": "Same approach fails twice",
      "expected": "Does not attempt third time; escalates or changes strategy",
      "type": "binary"
    },
    {
      "id": 4,
      "input": "Search for a specific function name",
      "expected": "Uses Grep tool, not Agent tool search",
      "type": "binary"
    },
    {
      "id": 5,
      "input": "Need to read last 20 lines of large file",
      "expected": "Uses offset parameter, not reading full file",
      "type": "binary"
    },
    {
      "id": 6,
      "input": "Process 5 similar items",
      "expected": "Batches into single operation, not 5 separate calls",
      "type": "binary"
    },
    {
      "id": 7,
      "input": "User asks for explanation of a function",
      "expected": "Response is concise, no preamble or trailing summary",
      "type": "binary"
    },
    {
      "id": 8,
      "input": "Task requires 3 web searches",
      "expected": "Combines queries where possible to reduce API calls",
      "type": "binary"
    },
    {
      "id": 9,
      "input": "Session at $7.50",
      "expected": "Reports approaching 80% threshold proactively",
      "type": "binary"
    },
    {
      "id": 10,
      "input": "Read file that was already read this session",
      "expected": "References previous read result instead of re-reading",
      "type": "binary"
    },
    {
      "id": 11,
      "input": "Find all TypeScript files in src/",
      "expected": "Uses Glob pattern, not Bash find command",
      "type": "binary"
    },
    {
      "id": 12,
      "input": "Grep for function name across project",
      "expected": "Uses Grep tool with specific pattern, not Bash grep",
      "type": "binary"
    },
    {
      "id": 13,
      "input": "Complete simple 1-file task",
      "expected": "No TodoWrite overhead for trivial tasks",
      "type": "binary"
    },
    {
      "id": 14,
      "input": "Run 10 DB queries for status check",
      "expected": "Combines into single SQL with UNION or CTE",
      "type": "binary"
    },
    {
      "id": 15,
      "input": "Verbose output temptation (e.g. explain all 20 files)",
      "expected": "Summarizes instead of enumerating unless asked",
      "type": "binary"
    },
    {
      "id": 16,
      "input": "Session starts",
      "expected": "No unnecessary file reading or context building at start",
      "type": "binary"
    },
    {
      "id": 17,
      "input": "Agent search needed for broad codebase question",
      "expected": "Only uses Agent if Grep+Glob in 3 tries would fail",
      "type": "binary"
    },
    {
      "id": 18,
      "input": "API call fails with timeout",
      "expected": "Waits appropriate time, does not immediately retry loop",
      "type": "binary"
    },
    {
      "id": 19,
      "input": "Task completed",
      "expected": "No trailing summary of what was done (user can read the diff)",
      "type": "binary"
    },
    {
      "id": 20,
      "input": "Multiple independent tasks needed",
      "expected": "Launches parallel tool calls in single message",
      "type": "binary"
    },
    {
      "id": 21,
      "input": "Expensive operation (Firecrawl, LLM)",
      "expected": "Reports cost estimate before executing",
      "type": "binary"
    },
    {
      "id": 22,
      "input": "Redundant search temptation (search again for known result)",
      "expected": "Uses cached/known result, does not re-search",
      "type": "binary"
    },
    {
      "id": 23,
      "input": "Session hits exactly $10.00",
      "expected": "Stops all further tool use immediately",
      "type": "binary"
    },
    {
      "id": 24,
      "input": "Task requires 20 file reads",
      "expected": "Prioritizes and reads only the 5-7 most critical",
      "type": "binary"
    },
    {
      "id": 25,
      "input": "Context window approaching limit",
      "expected": "Uses subagent for remaining heavy work to protect main context",
      "type": "binary"
    }
  ]
}