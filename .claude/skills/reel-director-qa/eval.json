{
  "skill_id": "reel-director-qa",
  "version": "1.1.0",
  "pattern": "cmo-factory/reel-variant-studio",
  "task": "Catch 5/5 planted defects (name in caption, vendor name in script, 30.0s cut, missing beat0/Street View equivalent, broken loop seam) and pass 5/5 clean controls with zero false positives. issue #19792 PART 1: also catch the 5 title-rule violations the pre-fix validator rubber-stamped -- payoff leak, emoji placement, ellipsis form, title-case drift, archetype/data mismatch -- each with its own fail+pass fixture (assertions 26-35).",
  "pass_threshold": 1.0,
  "autoloop_compatible": true,
  "assertions": [
    {"id": 1, "planted_defect": "person_name_in_caption", "input": "caption_groups contains 'won by John Smith'", "expected": "check_banned_terms fails (pass=False)", "type": "binary"},
    {"id": 2, "planted_defect": "vendor_name_in_script", "input": "script beat line contains 'via Tracerfy'", "expected": "check_banned_terms fails (pass=False)", "type": "binary"},
    {"id": 3, "planted_defect": "wrong_total_duration", "input": "script beats total 30.0s instead of 32.0s", "expected": "check_beat_timing_drift fails duration_ok", "type": "binary"},
    {"id": 4, "planted_defect": "missing_hook_beat0", "input": "script has no beat with start_s=0", "expected": "check_hook_clarity fails with reason 'no beat starting at 0s'", "type": "binary"},
    {"id": 5, "planted_defect": "broken_loop_seam_claim", "input": "video-level loop-seam check requested on a Phase A (no video) variant", "expected": "check reports pass=null / not_applicable_phase_a, never a false pass", "type": "binary"},
    {"id": 6, "clean_control": "compliant_variant_1", "input": "well-formed variant, no banned terms, correct timing", "expected": "all applicable checks pass, qa_pass=true", "type": "binary"},
    {"id": 7, "clean_control": "compliant_variant_2", "input": "second well-formed variant, different archetype", "expected": "all applicable checks pass, qa_pass=true", "type": "binary"},
    {"id": 8, "clean_control": "compliant_variant_3", "input": "third well-formed variant", "expected": "all applicable checks pass, qa_pass=true", "type": "binary"},
    {"id": 9, "clean_control": "compliant_variant_4", "input": "fourth well-formed variant (full diverse set of 4)", "expected": "diversity check passes for the full set", "type": "binary"},
    {"id": 10, "clean_control": "compliant_variant_5", "input": "fifth well-formed variant (control from a different property)", "expected": "all applicable checks pass, qa_pass=true", "type": "binary"},
    {"id": 11, "input": "variant with caption_groups word count <=5", "expected": "check_caption_readability passes", "type": "binary"},
    {"id": 12, "input": "variant with a caption_groups entry of 6+ words", "expected": "check_caption_readability fails", "type": "binary"},
    {"id": 13, "input": "variant with beat0 ending at exactly 2.0s", "expected": "check_hook_clarity passes", "type": "binary"},
    {"id": 14, "input": "variant with beat0 ending at 3.5s", "expected": "check_hook_clarity fails", "type": "binary"},
    {"id": 15, "input": "variant missing short_code", "expected": "check_short_code fails (negative test: rejected)", "type": "binary"},
    {"id": 16, "input": "variant with a non-empty voice_tags.eleven_v3_tags list", "expected": "check_eleven_v3_proof passes", "type": "binary"},
    {"id": 17, "input": "variant with empty voice_tags", "expected": "check_eleven_v3_proof fails", "type": "binary"},
    {"id": 18, "input": "4-variant set where two share archetype", "expected": "check_diversity fails for both", "type": "binary"},
    {"id": 19, "input": "4-variant set with proper diversity", "expected": "check_diversity passes", "type": "binary"},
    {"id": 20, "input": "review_reel writes qa_scores + qa_pass to winnerdata.reel_variants", "expected": "row updated live, re-selectable with matching qa_pass", "type": "binary"},
    {"id": 21, "input": "qa_pass computed for a variant with one applicable check failing", "expected": "qa_pass=False overall (no partial credit)", "type": "binary"},
    {"id": 22, "input": "qa_pass computed for a variant where only not_applicable_phase_a checks are null", "expected": "qa_pass=True (null checks excluded from the all() over applicable checks)", "type": "binary"},
    {"id": 23, "input": "Director attempts to set status='approved'", "expected": "no such code path exists in director_qa.py -- review_reel never writes .status", "type": "binary"},
    {"id": 24, "input": "beat timing with a 0.5s gap between beats", "expected": "check_beat_timing_drift fails (gap > 0.3s tolerance)", "type": "binary"},
    {"id": 25, "input": "beat timing with a 0.1s gap between beats", "expected": "check_beat_timing_drift passes (within 0.3s tolerance)", "type": "binary"},
    {"id": 26, "planted_defect": "payoff_leak", "input": "title contains the exact sold-price figure ('$279,200') the payoff beat delivers", "expected": "check_payoff_leak fails (pass=False)", "type": "binary"},
    {"id": 27, "input": "title states stakes without resolving them ('Less Than Half')", "expected": "check_payoff_leak passes", "type": "binary"},
    {"id": 28, "planted_defect": "emoji_placement", "input": "title has a leading emoji before the ellipsis plus only one trailing emoji", "expected": "check_emoji_placement fails (pass=False)", "type": "binary"},
    {"id": 29, "input": "title has zero emoji before the ellipsis and exactly two immediately after", "expected": "check_emoji_placement passes", "type": "binary"},
    {"id": 30, "planted_defect": "ellipsis_form", "input": "title uses literal three-period '...' instead of the '…' character", "expected": "check_ellipsis_form fails (pass=False)", "type": "binary"},
    {"id": 31, "input": "title uses the single '…' character", "expected": "check_ellipsis_form passes", "type": "binary"},
    {"id": 32, "planted_defect": "title_case_drift", "input": "title is sentence-case ('The bank lost this bet...')", "expected": "check_title_case fails (pass=False)", "type": "binary"},
    {"id": 33, "input": "title is consistently Title Case", "expected": "check_title_case passes", "type": "binary"},
    {"id": 34, "planted_defect": "archetype_phase_mismatch", "input": "archetype='countdown_presale' assigned to a row with phase='postsale'", "expected": "check_archetype_data_match fails (pass=False)", "type": "binary"},
    {"id": 35, "input": "archetype='countdown_presale' on a row with phase='presale'", "expected": "check_archetype_data_match passes", "type": "binary"}
  ]
}
