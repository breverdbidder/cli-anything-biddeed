{
  "skill_id": "reel-animator",
  "version": "1.0.0",
  "pattern": "cmo-factory/reel-variant-studio",
  "task": "Render 4 elements x 3 properties under the 90s/element budget with zero frame drops (ffprobe duration matches spec)",
  "pass_threshold": 1.0,
  "autoloop_compatible": true,
  "assertions": [
    {"id": 1, "input": "render kinetic_hook element, property 1", "expected": "ffprobe duration == 2.0s", "type": "binary"},
    {"id": 2, "input": "render parcel_outline_drawon element, property 1", "expected": "ffprobe duration == 6.0s", "type": "binary"},
    {"id": 3, "input": "render price_bar_race element, property 1", "expected": "ffprobe duration == 8.0s", "type": "binary"},
    {"id": 4, "input": "render loop_seam_morph element, property 1", "expected": "ffprobe duration == 1.0s", "type": "binary"},
    {"id": 5, "input": "render all 4 elements, property 2", "expected": "all 4 durations correct", "type": "binary"},
    {"id": 6, "input": "render all 4 elements, property 3", "expected": "all 4 durations correct", "type": "binary"},
    {"id": 7, "input": "any render across the 12 (4x3)", "expected": "budget_ok == true for every one (no timeout past 90s)", "type": "binary"},
    {"id": 8, "input": "any render across the 12 (4x3)", "expected": "uploaded == true with a storage url for every one", "type": "binary"},
    {"id": 9, "input": "render with revideo project directory absent", "expected": "render_element_revideo returns ok=False with an explicit reason, does not hang past budget", "type": "binary"},
    {"id": 10, "input": "primary engine (revideo) unavailable", "expected": "falls through to ffmpeg_kinetic_fallback and reports that engine name honestly, not 'revideo'", "type": "binary"},
    {"id": 11, "input": "ffmpeg render exceeds 90s (simulated via a 0.01s budget override)", "expected": "falls back to render_static_fallback", "type": "binary"},
    {"id": 12, "input": "two calls with the same seed", "expected": "produce byte-identical or duration-identical deterministic output (same fontsize jitter)", "type": "binary"},
    {"id": 13, "input": "kinetic_hook ffmpeg filter", "expected": "uses BRAND_AMBER (#F59E0B) text color per DESIGN.md tokens", "type": "binary"},
    {"id": 14, "input": "any element background", "expected": "uses BRAND_BG (#020617) or BRAND_NAVY (#1E3A5F) per DESIGN.md tokens", "type": "binary"},
    {"id": 15, "input": "font resolution", "expected": "calls biddeed_reels_lib._ensure_font(), not a hardcoded/reimplemented font path", "type": "binary"},
    {"id": 16, "input": "storage upload", "expected": "calls biddeed_reels_lib.storage_upload(), not a reimplemented upload", "type": "binary"},
    {"id": 17, "input": "render_element for an unknown element kind", "expected": "returns ok=False with 'unknown element kind' reason, does not crash", "type": "binary"},
    {"id": 18, "input": "static fallback render", "expected": "completes within 30s and produces a valid mp4 (ffprobe succeeds)", "type": "binary"},
    {"id": 19, "input": "loop_seam_morph render", "expected": "uses ffmpeg xfade filter for the crossfade, not a static image", "type": "binary"},
    {"id": 20, "input": "price_bar_race render", "expected": "contains multiple time-sliced drawtext filters (count-up steps), not one static drawtext", "type": "binary"},
    {"id": 21, "input": "engine field on every reported render", "expected": "is one of revideo, ffmpeg_kinetic_fallback, static_fallback -- never a fabricated/unknown value", "type": "binary"},
    {"id": 22, "input": "seconds field on every reported render", "expected": "is a positive float, wall-clock measured", "type": "binary"},
    {"id": 23, "input": "output key path for a render", "expected": "matches reel-variants/animator-samples/{property_key}/{element_key}.mp4", "type": "binary"},
    {"id": 24, "input": "revideo scaffold CLI behavior in this sandbox", "expected": "documented honestly in animator.py's module docstring as a real, observed limitation, not silently ignored", "type": "binary"},
    {"id": 25, "input": "claim '4 elements rendered under budget with zero frame drops' for the eval harness itself", "expected": "backed by an actual skill_eval_results row per element, not a summary-only claim", "type": "binary"}
  ]
}
